---
module: MySQL
tags: [MySQL, MOC]
difficulty: hard
last_reviewed: 2026-05-09
---

# MySQL 篇 MOC (Map of Content)

这里是 MySQL 数据库核心知识的索引地图。数据库底层原理、索引组织结构、事务并发调优永远是所有后端开发面试和工程优化的核心。

由于 MySQL 体系不仅包含基础 SQL，还涉及及其庞大的底层结构，我们基于**主题聚类（Concept-Cluster）**将其内容做了系统的拆分，帮助你在数据库设计、问题排查和面试上拔得头筹。

---

## 🗺️ 知识体系导航

建议按照以下顺序进行系统学习和复习：

 基础使用与整体架构
从 SQL 多表外连接（Left Join / Inner Join）到三大范式，再到 MySQL 服务端是如何组织一条查询语句或更新语句流转的（连接器 -> 分析器 -> 优化器 -> 执行器）。
👉 **[[MySQL 基础与架构]]**

 核心重灾区：底层引擎与日志系统
**底层设计的第一大考察点。**
死磕 InnoDB 中极为经典的 Buffer Pool，以及 MySQL 最核心的两大基石日志体系：二进制日志（Binlog）与重做日志（Redo log），并理解它为何需要“两阶段提交”来保证崩溃恢复时的数据一致性。
👉 **[[引擎与日志]]**

 👑 核心重灾区：索引机制详解
**数据库调优的灵魂与面试命中率最高的话题。**
彻底理解 B+树 为什么比普通二叉树以及 Hash 表更适合作为数据库索引；深度拆解聚簇索引与非聚簇索引带来的“回表”动作；掌握联合索引（最左前缀匹配）、覆盖索引、索引下推，以及通过 `explain` 分析 SQL 走慢的原因。
👉 **[[索引机制详解]]**

 核心重灾区：锁机制
"按索引粒度看锁"——表锁、行锁、记录锁、间隙锁、临键锁、意向锁。理解锁加在哪里，才能搞懂死锁怎么产生、怎么排查。
👉 **[[锁机制]]**

 核心重灾区：事务与 MVCC
ACID 的微观实现之道。理解四大隔离级别背后的原理；详细理解 MVCC（ReadView + Undo Log 版本链）是如何让"读"不加锁、却又能看到一致快照的。
👉 **[[事务与MVCC]]**

 高可用架构与分库分表
从单机极限到分布式架构。主从复制（读写分离）、延迟如何处理？以及当单表达到千万级数据时，垂直与水平分库分表（Sharding）的拆分策略及扩容演进方案。
👉 **[[高可用与分库分表]]**

 SQL 优化与生产实战
各种极限场景的实战应对：百万级大表热加字段？深分页问题怎么办？CPU 狂飙问题如何排查与降级定位。
👉 **[[优化与实战]]**

---

> **学习笔记提示**：
> MySQL 的学习关键是**理清各组件在 I/O 落盘与内存间的权衡**，每一个机制——从 B+ 树变粗变矮，到 Redo Log 的顺序写覆盖随机写，都是典型的空间换时间、顺序写换随机写的硬件底层博弈法则。

## 相关链接

- [[_MOC]] — JDBC、连接池、`@Transactional` 与 MySQL 的衔接
- [[_MOC]] — MVCC 的版本可见性与 JMM 的可见性思路同源
- [[_MOC]] — 缓存与数据库一致性、读写分离
- [[_MOC]] — Spring 事务传播行为底层依赖 MySQL 隔离级别


---

## 面试考点

### 架构与 SQL 基础

- MySQL 的执行流程？一条 SQL 是怎么执行的？ · [[MySQL/MySQL 基础与架构#一条查询语句是如何执行的？|客户端到存储引擎的完整链路]]
- 内连接、左连接、右连接有什么区别？
- 数据库的三大范式是什么？
- varchar 和 char 的区别？
- 记录货币用什么类型比较好？
- DATETIME 和 TIMESTAMP 有什么区别？
- in 和 exists 的区别？
- count(1)、count(*) 与 count(列名) 的区别？
- drop、delete 与 truncate 的区别？

### 索引

- InnoDB 引擎的索引结构是什么？为什么不用红黑树或 B 树？
- 一棵 B+ 树能存多少数据？
- 什么是聚簇索引和非聚簇索引？
- 什么是回表？怎么避免？
- 联合索引了解吗？最左前缀原则？
- 覆盖索引了解吗？
- 索引失效的常见场景有哪些？
- 索引是不是建的越多越好？
- 什么样的字段适合加索引？

### 事务与 MVCC

- MySQL 事务的四大特性是什么？
- ACID 靠什么保证的？
- 事务的隔离级别有哪些？
- 事务的隔离级别是如何实现的？
- 什么是幻读？如何避免？
- 什么是当前读和快照读？
- MVCC 了解吗？
- 什么是版本链？
- ReadView 是什么？如何判断版本可见性？
- 可重复读和读已提交在 ReadView 上的区别？
- MVCC 的版本数据存储在哪里？
- Undo Log 怎么知道前一个版本是什么、在哪？

### 锁

- MySQL 中有哪几种锁？
- 说说 MySQL 的行锁？
- 记录锁、间隙锁、临键锁的区别？
- 临键锁的退化规律是什么？
- 意向锁是什么？
- MySQL 的乐观锁和悲观锁了解吗？
- 如何通过悲观锁和乐观锁解决库存超卖问题？
- 死锁是怎么产生的？
- InnoDB 是如何检测和处理死锁的？
- 如何预防死锁？
- 哪些 ALTER 操作会导致锁表？怎么避免？

### 引擎与日志

- InnoDB 和 MyISAM 主要有什么区别？
- InnoDB 的 Buffer Pool 了解吗？
- InnoDB 对 LRU 算法的优化了解吗？
- MySQL 日志文件有哪些？
- 请重点说说 binlog？
- 有了 binlog 为什么还要 undo log 和 redo log？
- 说说 redo log 的工作机制？
- 什么是 WAL？
- innodb_flush_log_at_trx_commit 参数了解吗？
- binlog 和 redo log 有什么区别？
- 为什么要两阶段提交？
- LSN 了解吗？

### 高可用与分库分表

- MySQL 数据库读写分离了解吗？
- 主从复制原理了解吗？
- 主从同步延迟怎么处理？
- 半同步复制了解吗？
- 你们是怎么分库的？
- 水平分库分表的分片策略有哪几种？
- 不停机扩容怎么实现？
- 分库分表会带来什么问题？
- 雪花算法具体是怎么实现的？时钟回拨怎么解决？

### 优化与实战

- 怎么分析慢查询？
- 怎么避免慢查询？
- 什么是深分页？offset 太大会有什么问题？
- 书签分页（游标分页）是为了解决什么问题？
- 百万级别以上的数据如何删除？
- 千万级大表如何添加字段？
- MySQL 导致 CPU 飙升的话，要怎么处理？
