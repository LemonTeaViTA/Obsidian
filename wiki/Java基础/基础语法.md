## 基础语法
### Java 有哪些基本数据类型？各占用多少字节？

**答案：**

| 类型 | 关键字 | 字节 | 取值范围 |
|------|--------|------|---------|
| 字节型 | byte | 1 | -128 ~ 127 |
| 短整型 | short | 2 | -32768 ~ 32767 |
| 整型 | int | 4 | -2³¹ ~ 2³¹-1 |
| 长整型 | long | 8 | -2⁶³ ~ 2⁶³-1 |
| 单精度 | float | 4 | ±3.4×10³⁸ |
| 双精度 | double | 8 | ±1.7×10³⁰⁸ |
| 字符型 | char | 2 | \u0000 ~ \uffff |
| 布尔型 | boolean | 1/4* | true/false |

*boolean：数组中 1 字节，单独使用时未定义

---

### & 和 && 的区别？

**答案：**

| 运算符 | 名称 | 特点 | 使用场景 |
|--------|------|------|---------|
| & | 逻辑与/按位与 | 两边都执行 | 位运算 |
| && | 短路与 | 左边 false 时右边不执行 | 常规判断（推荐） |

**示例：**
```java
// 短路保护
if (str != null && str.length() > 0) {
    // 安全：如果 str 为 null，不会执行 length()
}
```

---

### final、finally、finalize 的区别？

**答案：**

| 关键字 | 作用 | 使用场景 |
|--------|------|---------|
| final | 修饰符，表示不可变 | 修饰类（不可继承）、方法（不可重写）、变量（不可修改） |
| finally | 异常处理的一部分 | try-catch-finally，最终会执行的代码块 |
| finalize() | Object 类的方法 | GC 回收前调用（已废弃） |

---

### == 和 equals 的区别？

**答案：**

| 对比项 | == | equals() |
|--------|----|---------|
| 本质 | 运算符 | Object 类的方法 |
| 基本数据类型 | 比较值 | 不能使用 |
| 引用数据类型 | 比较地址 | 默认比较地址，可重写比较值 |

**示例：**
```java
String s1 = new String("abc");
String s2 = new String("abc");
s1 == s2;      // false（地址不同）
s1.equals(s2); // true（值相同）
```

---

### switch 语句能否用在 byte/long/String 类型上？

**答案：**

- Java 5 之前：`switch` 只支持 `byte/short/char/int`。
- Java 5 起：支持 `enum`。
- Java 7 起：支持 `String`。
- `long` 在所有版本中都不支持用于 `switch` 表达式。

---

### break、continue、return 的区别及作用？

**答案：**

- `break`：跳出当前整个循环或 `switch`。
- `continue`：结束本次循环，进入下一次循环。
- `return`：结束当前方法并返回。

---

### 用效率最高的方法计算 2 乘以 8？

**答案：**

使用左移运算：`2 << 3`。

左移 3 位等价于乘以 $2^3$，结果为 16。

---

### 说说自增自减运算？

**答案：**

- 前缀：`++a` / `--a`，先自增(减)再参与表达式。
- 后缀：`a++` / `a--`，先参与表达式再自增(减)。

示例：
```java
int a = 1;
int b = ++a; // a=2, b=2
int c = a++; // a=3, c=2
```

经典坑：
```java
int i = 1;
i = i++;
System.out.println(i); // 1
```
原因是后缀自增会先返回旧值参与赋值，最终把旧值写回。

---

### float 是怎么表示小数的？（补充）

**答案：**

`float` 按 IEEE 754 单精度浮点数表示，32 位由三部分组成：

- 符号位（Sign）
- 指数位（Exponent）
- 尾数位（Mantissa）

通用形式是：
$$
V = (-1)^S \times M \times 2^E
$$

因为二进制浮点是近似表示，小数运算会有精度误差。

---

### 讲一下数据准确性高是怎么保证的？（补充）

**答案：**

金融计算不要用 `float/double`，常见两种方案：

1. 使用 `BigDecimal`（推荐）。
2. 把金额转成最小货币单位（如分）用整数计算。

示例：
```java
BigDecimal a = new BigDecimal("0.1");
BigDecimal b = new BigDecimal("0.2");
System.out.println(a.add(b)); // 0.3
```

---
