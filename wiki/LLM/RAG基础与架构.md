---
module: LLM
tags: [RAG, 文档解析, 分片, 架构, 离线管道, 命题化切割, 句子窗口检索]
difficulty: hard
last_reviewed: 2026-05-18
---

# RAG 基础与架构

## 一、RAG 是什么

### RAG 的诞生背景

大语言模型在落地应用中存在三个固有缺陷：

1. **知识截止**：模型训练数据有截止时间，无法获取实时更新的行业信息与业务数据。
2. **事实幻觉**：模型生成内容易出现事实错误，在专业领域场景中输出错误信息，引发合规风险与业务损失。
3. **垂直领域理解不足**：通用大模型对垂直行业的专业术语、业务逻辑理解不足，无法满足企业级场景的精准问答需求。

RAG（Retrieval-Augmented Generation，检索增强生成）通过"检索-增强-生成"的核心架构，打破了模型固有知识的限制——将外部知识库作为事实依据，让模型生成的每一条结论都有可追溯的来源支撑，从根源上降低幻觉风险，同时支持知识库的动态更新，适配企业级业务的实时性需求。

### RAG 的核心价值

核心流程：用户提问 → 从知识库检索相关 chunk → 把 chunk 塞进 prompt → LLM 基于证据生成答案。

**解决的四个核心问题：**
- 知识截止：训练数据有时效，RAG 外挂动态知识库
- 事实幻觉：基于检索到的真实文档生成，答案可追溯
- 专业领域：垂直知识库比通用模型更精准
- 可控性：知识库独立管理，随时更新，无需重训模型

**RAG vs 微调：** 微调把知识烧进权重，更新成本高；RAG 把知识放在外部，随时可改。企业知识库首选 RAG。

> [!tip] RAG 不是万能的
> RAG 适合"需要事实依据的问答"，不适合"需要推理或创造的任务"。比如写诗、做数学题、设计架构方案——这些场景 RAG 检索到的文档反而会干扰模型的自由推理。选型时先问：**这个任务的答案是"在某个文档里"还是"需要模型思考出来"？**

---

## 二、系统架构：离线 + 在线两阶段

### 离线阶段（知识库构建）

```
原始文档 → 文档解析(§三) → 文本清洗(§四) → 分块Chunking(§五) → Embedding向量化 → 索引构建 → 存储
```

五个模块缺一不可，任何一步出问题都会导致后续全崩。

> [!warning] 分块（Chunking）是 RAG 质量的最大杠杆
> 实践中 80% 的 RAG 质量问题可以追溯到分块策略不当：chunk 太大导致噪声多、相关性差；chunk 太小导致上下文缺失、答案碎片化。优先优化分块，收益远大于换 Embedding 模型或调检索参数。

### 在线阶段（查询响应）

```
用户query → 查询理解与路由 → query向量化 → 召回(Recall) → 重排(Rerank) → Prompt拼接 → LLM生成
```

关键点：Embedding 模型在两个阶段必须一致，文档和 query 要在同一语义空间里比较。

### 管道步骤导航

| 步骤 | 所属阶段 | 详细内容位置 |
|------|---------|-------------|
| 文档解析 | 离线 | 本文 §三 |
| 文本清洗 | 离线 | 本文 §四 |
| 分块 Chunking | 离线 | 本文 §五 |
| Embedding 向量化 | 离线 | [[RAG向量与Embedding#五、Embedding 模型选型]] |
| 索引构建与存储 | 离线 | [[RAG向量与Embedding#三、向量数据库选型]] |
| 查询理解与路由 | 在线 | [[RAG检索策略#一、查询理解与意图识别]] |
| 召回（多路检索） | 在线 | [[RAG检索策略#二、检索优化四层框架]] |
| 重排 Rerank | 在线 | [[RAG检索策略#2.4 重排序（Rerank）]] |
| Prompt 拼接与生成 | 在线 | 本文 §六 |
| 高级模式 | 进阶 | [[RAG高级技术]] |

---

## 三、文档解析

文档解析是离线管道的第一步，目标是将各种格式的原始文档转化为干净的结构化文本。解析质量直接决定了知识库的上限——解析丢失的信息，后续任何优化都无法弥补。

### 3.1 RAG 知识库常见文件类型与核心难点

| 文件类型 | 典型来源 | 核心难点 |
|---------|---------|---------|
| **PDF** | 合同、论文、财报、技术手册 | 多栏排版、扫描件、嵌套表格、公式、图文混排 |
| **Word (docx)** | 内部文档、制度规范 | 嵌入对象（OLE）、修订痕迹、复杂样式继承 |
| **Excel (xlsx)** | 数据报表、配置表 | 多 Sheet、合并单元格、公式依赖、非表格化布局 |
| **PPT (pptx)** | 培训材料、方案汇报 | 信息在图片里、动画顺序、备注与正文分离 |
| **HTML/网页** | 在线文档、帮助中心 | 导航/广告噪声、动态渲染内容、结构语义丢失 |
| **扫描件/图片** | 历史档案、手写笔记 | OCR 准确率、版面还原、表格结构丢失 |
| **音视频** | 会议录音、培训视频 | ASR 转写准确率、说话人分离、时间戳对齐 |

### 3.2 PDF 解析

PDF 是 RAG 系统中最复杂也最常见的文件类型。难点不在于"提取文字"，而在于"还原文档的逻辑结构"。

#### 问题一：多栏排版阅读顺序错乱

传统工具（PyPDF2、pdfplumber）按物理坐标从上往下逐行读取，不理解"栏"的概念。双栏 PDF 会把左栏第一行和右栏第一行拼在一起，三栏更混乱。

**解决方案：版面分析（Layout Analysis）**

先用目标检测模型识别页面的物理布局区域（标题、正文、表格、图片、页眉页脚、侧边栏），再按逻辑阅读顺序重组内容。

技术实现路线：
- **Pipeline 方案**：版面检测模型（如 DocLayNet 训练的 RT-DETR）识别区域类型和坐标 → 阅读顺序排序算法（基于区域位置关系推断从左到右、从上到下的逻辑顺序）→ 按排序结果拼接文本。代表工具：Docling、MinerU
- **端到端方案**：直接用视觉语言模型将页面图像转为结构化输出，跳过显式的区域检测步骤。代表模型：Dolphin-v2（字节跳动，3B 参数，识别 21 种内容类型）、SmolDocling（IBM，超紧凑 VLM）

#### 问题二：表格结构丢失

PDF 中的表格没有真正的"单元格"概念——只是一堆文字和线条的坐标集合。传统提取后所有数据串成一行，行列关系完全丢失。跨页表格更难处理。

**解决方案：表格结构识别（Table Structure Recognition, TSR）**

- **TableFormer（IBM）**：专门的表格结构识别模型，输入表格区域图像，输出单元格的行列坐标和内容。Docling 内置此模型
- **表格转 Markdown/HTML**：识别出单元格后，按行列关系输出为 Markdown 表格或 HTML `<table>` 格式，LLM 能直接理解
- **跨页表格处理**：检测表格是否跨页（下一页开头无表头但有数据行），自动拼接并补全表头
- **LLM 辅助描述**：对复杂表格，用 LLM 生成一段自然语言描述（"该表展示了 2023 年 Q1-Q4 各产品线的营收对比"），作为额外的检索入口

#### 问题三：扫描件 PDF（图片型）

扫描件 PDF 每页就是一张图片，没有可提取的文字层。必须走 OCR 流程。

**解决方案：OCR + 版面分析联合**

- **PaddleOCR + 区域检测**：先检测区域类型（文字/表格/代码/图片），再对不同区域分别处理——文字区域做文本 OCR，表格区域做表格结构识别，代码区域保持缩进格式
- **TurboOCR**：GPU 加速的 OCR 推理引擎，C++/CUDA/TensorRT 重写整个推理链路，将 PaddleOCR 速度提升 50 倍（268 img/s vs 5 img/s）。底层 PP-OCRv5 + TensorRT FP16 + 内嵌 Wuffs PNG 解码器 + PDFium 渲染引擎。支持安全/快速/平衡/流式四种模式，Docker 一行部署
- **VLM 直接理解**：用视觉语言模型（GPT-4o、Qwen-VL）直接"看"扫描件图片生成文本描述，跳过传统 OCR。适合图表、流程图等 OCR 难以处理的内容

> TurboOCR 项目：https://github.com/aiptimizer/TurboOCR

#### 问题四：公式识别

学术论文和技术文档中大量数学公式，传统 OCR 完全无法处理。

**解决方案：**
- **专用公式识别模型**：如 Pix2Tex、Nougat（Meta），输入公式图像，输出 LaTeX 代码
- **MinerU 内置公式识别**：自动检测公式区域，调用公式识别模型转为 LaTeX，嵌入到输出的 Markdown 中
- **Dolphin-v2**：端到端处理，原生支持行内公式和独立公式块的识别与转写

#### 前沿方向：跳过 OCR 的视觉检索（ColPali / VisionRAG）

传统 RAG 必须先把文档转成文本再做 Embedding。ColPali 提出了完全不同的思路：**直接对文档页面图像做 Embedding，跳过 OCR 和文本提取**。

- **ColPali**：基于 PaliGemma 视觉语言模型，将每个 PDF 页面视为图像，生成 patch 级别的视觉 Embedding。检索时用 late interaction 机制计算 query 与页面 patch 的细粒度相似度。保留了版面、表格、图表的空间信息，OCR 错误不再是问题
- **VisionRAG**：在 ColPali 基础上构建完整的 OCR-free 检索系统。三层金字塔索引（页面级 → 区域级 → patch 级），支持任意 VLM 作为生成器。模型无关，可插拔不同的视觉编码器

适用场景：扫描件多、版面复杂、OCR 错误率高的场景。缺点：需要 GPU 做视觉编码，存储成本比纯文本向量高。

> ColPali 论文：https://arxiv.org/abs/2407.01449
> VisionRAG 论文：https://arxiv.org/abs/2511.21121

### 3.3 Word (docx) 解析

#### 核心难点

- **嵌入对象（OLE）**：Word 中嵌入的 Excel 表格、Visio 图、其他文档，python-docx 无法直接提取内容
- **复杂样式继承**：标题层级通过样式定义，但用户可能手动加粗代替标题样式，导致层级识别失败
- **修订痕迹与批注**：包含多个版本的内容，需要决定保留哪个版本
- **文本��与形状**：浮动文本框中的内容不在主文档流中，容易遗漏

#### 解决方案

- **python-docx**：提取正文段落和层级标题，按样式名称（Heading 1/2/3）识别文档结构
- **嵌入对象处理**：检测 OLE 对象类型，Excel 嵌入用 openpyxl 解析，图片嵌入用 OCR/VLM 处理
- **MarkItDown**：微软官方方案，直接将 docx 转为 Markdown，保留标题层级、列表、表格结构
- **Docling**：IBM 开源，对 docx 做完整的结构化解析，输出带层级信息的 JSON 文档模型

### 3.4 Excel (xlsx) 解析

#### 核心难点

- **多 Sheet 结构**：一个文件包含多个工作表，Sheet 之间可能有交叉引用
- **合并单元格**：合并后的单元格在解析时只有左上角有值，其余为空，破坏行列对齐
- **公式与计算值**：单元格存的是公式（`=SUM(A1:A10)`），需要决定保留公式还是计算结果
- **非表格化布局**：用户把 Excel 当画布用——标题在 A1，数据从 C5 开始，中间有空行空列做分隔
- **语义缺失**：纯数字列（如 `12345, 67890`）没有上下文，Embedding 模型无法理解含义

#### 解决方案

- **Sheet 级别处理**：每个 Sheet 独立解析，Sheet 名称作为元数据标签
- **合并单元格展开**：检测合并区域，将合并值填充到所有被合并的单元格中
- **表头识别与绑定**：识别第一行（或冻结行）为表头，将每个数据行转为"表头: 值"的键值对格式，让 Embedding 模型能理解语义
- **转为自然语言描述**：对关键数据表，用 LLM 生成摘要描述（"该表记录了 2023 年各部门的预算分配，总预算 500 万"），作为额外检索入口
- **NL2SQL 路由**：对于需要精确数值查询的场景（"Q3 营收是多少"），不走向量检索，而是将 Excel 导入数据库，用 NL2SQL 做结构化查询

### 3.5 PPT (pptx) 解析

#### 核心难点

- **信息在图片里**：关键内容做成图片（流程图、架构图、数据图表），python-pptx 只能提取文本框文字
- **动画与构建顺序**：内容按动画步骤逐步展示，但解析时全部平铺，丢失逻辑顺序
- **备注页**：演讲者备注中往往有比正文更详细的解释，但容易被忽略
- **母版与模板**：页眉页脚、Logo 等模板内容重复出现，造成噪声

#### 解决方案

- **图片元素 OCR/VLM**：提取 PPT 中的图片元素，用 OCR 识别文字，或用 VLM（GPT-4o、Qwen-VL）生成图片内容的自然语言描述
- **备注合并**：将每页的备注内容与正文合并，备注往往是正文的详细解释，对检索很有价值
- **模板去噪**：识别跨页重复出现的文本（Logo、页码、公司名），标记为模板内容过滤掉
- **幻灯片级分块**：每页幻灯片作为一个独立的语义单元，标题作为 section_path 元数据

### 3.6 HTML / 网页解析

#### 核心难点

- **噪声内容多**：导航栏、侧边栏、广告、Cookie 提示、页脚链接等占页面 70%+ 的内容与正文无关
- **动态渲染**：SPA 应用的内容通过 JavaScript 动态加载，静态 HTML 抓取拿不到
- **结构语义丢失**：传统方案将 HTML 转为纯文本，丢失了标题层级、列表结构、表格格式等有价值的结构信息

#### 解决方案

- **正文提取算法**：Readability.js（Mozilla）/ Trafilatura（Python）通过文本密度分析和 DOM 树结构识别正文区域，过滤导航和广告。Trafilatura 对多语言支持好，提取准确率高
- **HtmlRAG（WWW 2025）**：研究发现 HTML 比纯文本更适合作为 RAG 的知识格式——HTML 标签保留了标题层级（`<h1>`-`<h6>`）、表格结构（`<table>`）、列表关系（`<ul>`/`<ol>`）等语义信息。方案：对原始 HTML 做两步清洗（块级标签清理 + 基于文本的精简），保留结构标签但去除噪声，直接将清洗后的 HTML 喂给 LLM
- **Markdown 转换**：用 Defuddle、site2rag 等工具将网页转为 Markdown，保留结构的同时大幅减少 token 消耗
- **动态内容处理**：对 SPA 应用使用 Playwright/Puppeteer 做浏览器渲染后再提取

> HtmlRAG 项目：https://github.com/plageon/HtmlRAG

### 3.7 扫描件与图片文档

#### 核心难点

- **OCR 准确率**：手写体、模糊扫描、倾斜拍照、复杂背景都会严重影响识别准确率
- **版面还原**：扫描件的多栏、表格、图文混排需要先做版面分析再分区域 OCR
- **图表理解**：柱状图、饼图、流程图等图表内容，OCR 只能识别其中的文字标签，无法理解图表含义

#### 解决方案

- **传统 Pipeline**：图像预处理（去噪、纠偏、二值化）→ 版面分析（区域检测）→ 分区域 OCR → 后处理（拼接、格式化）
- **Dolphin-v2（字节跳动，ACL 2025）**：3B 参数的端到端文档解析模型，直接将文档图像转为结构化输出。两阶段方法：第一阶段生成阅读顺序的版面元素序列（识别 21 种内容类型：文本、标题、表格、公式、代码、图片说明等）；第二阶段用异构锚点提示（Heterogeneous Anchor Prompting）对每个元素做精细识别。支持数字文档和拍照文档，MIT 开源
- **VLM 直接理解**：对于图表、流程图等 OCR 无法处理的内容，直接用视觉语言模型"看图说话"，生成自然语言描述作为检索内容

> Dolphin-v2：https://huggingface.co/ByteDance/Dolphin-v2
> Dolphin 论文：https://arxiv.org/abs/2505.14059

### 3.8 音视频文档

#### 核心难点

- **ASR 转写准确率**：专业术语、方言、多人对话场景下错误率高
- **说话人分离**：多人会议需要区分谁说了什么
- **时间戳对齐**：需要知道某段内容对应视频的哪个时间点，便于溯源

#### 解决方案

- **Whisper（OpenAI）**：开源 ASR 模型，支持多语言转写和时间戳输出
- **说话人分离（Speaker Diarization）**：pyannote.audio 等工具识别不同说话人，输出带说话人标签的转写文本
- **语义分段**：ASR 输出的长文本按语义主题分段（而非按固定时长），每段保留起止时间戳作为元数据
- **视频关键帧提取**：对视频按场景变化提取关键帧，用 VLM 描述画面内容，与对应时间段的语音转写合并

### 3.9 文档解析工具全景

#### Pipeline 方案（模块化组合）

**Docling（IBM，Linux Foundation AI）：** 开源文档转换工具包，模块化架构。核心组件：DocLayNet 版面分析模型（基于 RT-DETR/DFINE 目标检测）+ TableFormer 表格结构识别 + OCR 引擎。输出统一的文档模型（DoclingDocument），保留完整的层级结构和元素类型。支持 PDF、docx、pptx、HTML、图片。可在普通 CPU 上运行。

> Docling 项目：https://github.com/DS4SD/docling
> 论文：https://arxiv.org/abs/2501.17887

**MinerU 2.5-Pro（上海 AI 实验室）：** 核心洞察是文档解析的性能瓶颈不在模型架构，而在训练数据质量。1.2B 参数小模型仅靠数据工程登顶文档解析榜首，超越 235B 参数大模型。数据引擎三大组件：多样性与难度感知采样 → 跨模型一致性验证（K 个异构 SOTA 模型同时解析量化难度）→ 评判与精炼流水线（渲染对比定位错误迭代校正）。三阶段训练：大规模预训练 → 困难样本差异化微调 → GRPO 强化学习对齐。

> MinerU 项目：https://github.com/opendatalab/MinerU
> 论文：https://arxiv.org/pdf/2604.04771

#### 端到端方案（单模型通吃）

**Dolphin-v2（字节跳动）：** 3B 参数通用文档解析模型，MIT 开源。两阶段异构锚点提示架构，原生支持 21 种内容类型。数字文档和拍照文档通吃，像素级精度。

**SmolDocling（IBM）：** 超紧凑视觉语言模型，将页面图像转为 DocTags 序列（一种专为文档设计的标记语言）。视觉编码器 + 投影层 + 语言模型的端到端架构。

#### 格式统一方案（广覆盖）

**MarkItDown（微软）：** 一行代码把十几种格式统一转成 Markdown。支持 PDF/Word/Excel/PPT/图片/音频/HTML/CSV/JSON/XML/ZIP。设计哲学是"为 LLM 设计"——Markdown 是主流 LLM 的"母语"，Token 效率比 PDF 高约 30%。可选依赖组按需安装，支持替换本地 VLM 做 OCR，提供 MCP Server 集成。

> MarkItDown 项目：https://github.com/microsoft/markitdown

#### 多模态方案（图文混合）

**RAG-Anything（港大 HKUDS）：** 解决传统 RAG 只能处理纯文字的问题。MinerU 文档解析还原版式结构 → 双图谱架构（跨模态关系图谱 + 文字语义图谱）→ VLM 视觉语言模型处理图片内容。三种查询模式：Pure Text / VLM Enhanced / Hybrid。

> RAG-Anything 项目：https://github.com/HKUDS/RAG-Anything

#### 工具选型决策

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 一般文档快速处理 | MarkItDown | 格式覆盖广，一行代码搞定 |
| 复杂 PDF（多栏、公式、嵌套表格） | MinerU / Docling | 版面分析深度强 |
| 扫描件/拍照文档 | Dolphin-v2 + TurboOCR | 端到端识别，速度快 |
| 图文混排、需要理解图表 | RAG-Anything | 多模态能力 |
| 版面极复杂 + OCR 错误多 | ColPali/VisionRAG | 跳过 OCR，直接视觉检索 |
| 网页内容 | Trafilatura + HtmlRAG | 正文提取 + 保留结构语义 |

### 3.10 结构感知解析与创新架构

#### BookRAG：文档层级树 + 知识图谱（港中文）

传统文档解析只提取文字内容，丢失了文档的原生结构。BookRAG 的思路：把文档还原成和目录一模一样的层级树——根节点是文档标题，一级节点是各章，二级节点是各小节，叶子节点是段落/表格/图片/公式。再从树节点中提取实体建成知识图谱，通过**图-树链接（GT-Link）**把每个实体锚定回它所在的章节节点。

检索时通过实体/关键词直接定位到对应的章节子树，把检索范围从"全文几万块"缩到"某一小节几十块"。

> 论文：https://arxiv.org/pdf/2512.03413

#### ChromaFs：虚拟文件系统（Mintlify）

**解决的问题：** 传统 RAG 只能"搜"不能"逛"——AI 只能拿到碎片化的 top-K 片段，无法浏览目录、查看完整文档。

**核心方案：** 用向量数据库模拟 UNIX 文件系统交互逻辑。文档结构按"页面=文件、章节=目录"构建 JSON 目录树存入 Chroma，`ls`/`cd`/`find` 在内存执行（毫秒级），`cat` 时从 Chroma 拉取分块拼接完整文档，`grep` 用双层优化（Chroma 粗过滤 + Redis 缓存精确匹配）。所有写操作抛只读错误保证无状态。

> 项目：https://www.mintlify.com/blog/how-we-built-a-virtual-filesystem-for-our-assistant

---
## 四、文本清洗

文本清洗是解析后、分块前的关键步骤，目标是消除噪声、统一格式，确保进入分块阶段的文本是干净且语义连贯的。

### 噪声去除

- **页眉页脚过滤**：跨页重复出现的文本（出现 ≥2 次）自动识别为页眉页脚并删除
- **导航与广告剥离**：HTML 文档去除导航栏、侧边栏、广告区块、Cookie 提示等非正文内容
- **水印与装饰文本**：PDF 中的水印文字、页码、版权声明等非内容性文本过滤
- **短片段过滤**：长度过短的无效文本片段（如 < 50 字符）直接丢弃

### 编码与格式规范化

- **字符编码统一**：所有文本转换为 UTF-8，处理 BOM 标记、Latin-1 混入等编码问题
- **空白符规范化**：多余空格、制表符、连续换行符压缩为单个；全角/半角空格统一
- **标点符号统一**：中英文标点变体统一（如中文逗号 `，` 和英文逗号 `,` 根据上下文语言统一）
- **特殊字符处理**：不可见控制字符（零宽空格、软连字符等）删除；Unicode 变体字符规范化（如全角数字转半角）

### 重复内容检测

大规模文档集合中，相同或高度相似的内容会造成检索冗余和 token 浪费。

- **精确去重**：基于文本哈希（MD5/SHA256）检测完全相同的段落
- **近似去重**：MinHash + LSH（局部敏感哈希）计算文本指纹，识别高度相似但不完全相同的段落（如不同版本的同一文档）
- **跨文档去重**：在文档集合层面标记重复内容，保留最新或最完整的版本

### 语言检测与分段

- **语言识别**：对多语言混合文档，检测每个段落的语言，以便后续使用对应的分词器和 Embedding 模型
- **逻辑段落识别**：基于空行、缩进、标题标记等信号识别逻辑段落边界，为后续分块提供自然切分点
- **句子分割**：中文按句号/问号/感叹号分句，英文用 spaCy 或 NLTK 的句子分割器，为句子级分块做准备

### 内容增强（可选）

- **缩写展开**：将领域缩写展开为全称（如 "RAG" → "RAG（Retrieval-Augmented Generation）"），提升检索时的语义匹配度
- **停用词策略**：RAG 场景中仅移除停用词可将 API 成本降低约 30%，同时保持语义质量。但过度的词干提取可能损害语义，需谨慎
- **元数据提取**：为每段文本提取对应元数据——文档唯一 ID、文档类型、所属章节标题、更新时间、访问权限等级，用于后续检索过滤和来源追溯

---

## 五、分块策略（Chunking）

> 切分不只是"设置一下 chunk_size"，它是需要分析文档特点、设计处理策略的工程模块。切错了，后面做再多优化，全是在地基不稳的楼上加砖。

### 5.1 为什么分块是最大杠杆

固定长度切分是最被低估的灾难。实测只改切分方式，不换 Embedding 模型，不调检索策略，Recall@5 就能从 0.67 提升到 0.91。三代方案演进：

- **V1 固定 512 token**：在句子中间截断，语义残缺
- **V2 句子级切分**：按句号/问号等自然边界切，但不理解文档层级结构，表格和列表处理差
- **V3 语义感知切分**：层级识别 + 递归细切 + 特殊元素处理，四项核心改进（见下）

### 5.2 V3 核心设计：四项改进

**1. 层级结构识别**

多策略融合识别章节标题——数字编号（`1.1`、`1.1.1`）、中文编号（`第X章/条`、`（X）`）、混合编号（`a)`、`i.`）。切分原则：同一章节内容尽量放在同一个 chunk，跨章节绝不合并。

**2. 超长章节递归细切**

- 普通章节 max_size = 1024 token
- 关键条款（责任/免责/费率）max_size = 1536 token——这类内容任何截断都会导致语义丢失
- 超长则先按子小节边界切，没有子小节则按句子边界切

**3. 表格专项处理**

- 小表格（≤300 token）：整体作为一个 chunk
- 大表格（跨页/超 300 token）：按行切分，但**每个 chunk 都复制表头**
- 不复制表头的后果：LLM 收到没有表头的数据行，不知道字段含义，只能胡编

**4. 列表项前导句合并**

问题：`"以下情况不在承保范围：（1）核辐射（2）战争"` 如果把 `（1）核辐射` 单独成 chunk，向量里没有"不在承保范围"这个关键语义，LLM 会误判为承保。

方案：识别列表结构，把前导句和所有列表项合并为一个 chunk；超长则至少保留前导句到每个 chunk。

### 5.3 Overlap 策略

Overlap 解决的是"关键信息恰好落在两个 chunk 边界处"的问题。

**100 token 是性价比最优点**：召回率提升显著，存储只增加 10%，再往上收益递减。

进一步优化：**基于句子边界的智能 overlap**——固定 overlap 可能恰好切在句子中间，智能 overlap 在确定重叠区域后向后扫描到最近句子结尾才截断，避免了 87% 的"句子在 overlap 区域被截断"问题。

### 5.4 chunk_size 确定原则

chunk_size 不是通用参数，要和文档类型挂钩：
- 通用中文文档：平均句子 20-30 字，512 token ≈ 2-3 段，够用
- 保险条款：每句平均 40-50 字，512 token 经常只有一段话的开头和中间，需要 1024-1536

三个约束要同时满足：
1. **检索精度**：chunk 越长语义越分散，embedding 区分度下降
2. **语义完整性**：太小容易切断完整语句，太大包含多个主题
3. **token 预算**：LLM 上下文窗口有限，512 token × 5 个 chunk + prompt ≈ 4000 token，给生成留足空间

### 5.5 Chunk 元数据设计

元数据是检索过滤和答案溯源的基础，离线阶段不打标签，在线阶段想过滤都没法过滤。

```python
@dataclass
class ChunkMetadata:
    doc_id: str          # 文档ID
    chunk_id: str        # chunk唯一ID
    section_path: str    # 层级路径："第3条 保险责任 > 3.2 责任免除"
    chunk_type: str      # "text" | "table" | "list"
    is_key_clause: bool  # 是否关键条款（责任/免责/费率）
    prev_chunk_id: str   # 前一个chunk的ID（上下文扩展用）
    next_chunk_id: str   # 后一个chunk的ID
    token_count: int
    page_range: str      # 来源页码范围
```

**各字段用途：**
- `section_path`：答案溯源，"根据第3条保险责任 > 3.2 责任免除 > (2)，核辐射不在保障范围"
- `is_key_clause`：检索时给关键条款加权，关键词匹配（"责任""免责""费率""赔付"）+ 章节标题判断
- `prev/next_chunk_id`：检索到半截信息时自动拉取相邻 chunk 补全上下文

### 5.6 前沿分块技术

#### 语义切分（Semantic Chunking）

用 Embedding 模型计算相邻句子的向量相似度，相似度突变处（语义跳跃点）作为切分边界。理论上比规则方法更精准，但计算成本高——一份文档需要 30-40 秒（规则方案 2-3 秒），大规模场景暂不实用。

#### 父子分块（Small-to-Big / Hierarchical RAG）

创建父-子分块关系，解决"小分块匹配精确但缺乏上下文，大分块上下文充足但匹配不准"的矛盾：

- 父分块：2000 字符的大块，存内容和元数据
- 子分块：500 字符的小块，存向量和 `parent_id` 引用
- 检索时：用查询向量匹配子分块向量 → 通过 `parent_id` 拉取父分块返回给 LLM

适用场景：文档具有清晰的层次结构（技术手册、法律文件、研究论文）。

#### Late Chunking（2024 年底）

先用长文本 Embedding 模型对整个文档编码，获取每个 token 在完整上下文下的向量表示，再在向量层面做切分。好处是每个 token 的向量里都带有全文上下文信息，不存在"切断了就丢失前文语义"的问题。缺点：对模型上下文长度有要求，多文档批量处理时内存占用是传统方案的 8-10 倍，目前处于研究阶段。

#### 金山办公判别式分块框架（2026）

论文：《Toward General Semantic Chunking: A Discriminative Framework for Ultra-Long Documents》

核心思路：跳出"生成式分块"，将主题分割转化为"相邻句子间是否存在主题边界"的二分类问题。推理速度比生成式模型快两个数量级。

**架构五阶段：**
1. **句子级细粒度建模**：将文档拆分为句子级基础文本块
2. **token 级语义编码**：用 Qwen3-0.6B 作为骨干网络，对全文本做 token 级编码
3. **块级语义聚合**：通过注意力池化聚合得到每个句子的块级语义表示
4. **跨块上下文建模**：轻量 Transformer 编码器建模跨句子的上下文依赖关系
5. **边界判别预测**：MLP 分类头直接输出相邻句子间的主题边界概率

**超长文档处理：** 重叠滑动窗口策略——超过 13k token 的文档按固定长度拆分窗口，相邻窗口保留 10% 重叠区域，重叠区域的边界概率取多窗口预测均值，支持任意长度文档。

**启发式后处理三阶段：**
1. 按预设阈值做初始分块，生成高置信度的粗粒度语义块
2. 超长块递归选取块内边界概率最高的位置二次拆分，直到满足长度约束
3. 超短块对比左右边界语义连续性，自动合并到语义更连贯的一侧

**VFSC 超长块检索适配：** 分块后的超长块超出嵌入模型上下文窗口，截断会导致语义丢失。方案：用一个融合向量 + 一个标量因子，严格还原超长块所有子段的平均余弦相似度，存储复杂度从 O(N) 降至 O(1)，零语义损失。

> 论文：https://arxiv.org/pdf/2602.23370

#### 句子窗口检索（Sentence Window Retrieval）

存储时以单句为粒度各自向量化，检索命中某句后，动态扩展返回前后 N 句形成上下文窗口给 LLM。

```
存储：[句1向量] [句2向量] [句3向量] ... （每句独立）
检索：命中句3 → 返回 [句1, 句2, 句3, 句4, 句5]（窗口=2）
```

与父子分块的区别：父子分块的父块是预先存储的固定大小块；句子窗口是检索时动态扩展，更灵活但需要在检索后实时拼接。

适用场景：文档句子密度高、每句信息独立性强（如法律条款、技术规范）。代价：存储量大（每句都要向量化）。

#### 命题化切割（Proposition-based Chunking）

来自论文 Dense X Retrieval（arXiv:2312.06648，EMNLP 2024，Chen et al.）。用 LLM 将文档分解为**命题**（Proposition）：每个命题是一个原子性的、自包含的事实陈述，不依赖上下文即可理解。

```
原文："苹果公司在 2023 年第四季度营收增长 40%，主要受 iPhone 15 系列驱动。"

命题化后：
- "苹果公司 2023 年第四季度营收增长 40%"
- "iPhone 15 系列是苹果公司 2023 年第四季度营收增长的主要驱动力"
```

每个命题语义密度最高，检索时精准匹配。代价：LLM 调用成本高，处理速度慢，适合高价值文档（财报、合同、技术规范）。

#### 六方案对比

| 方案 | 核心思路 | 适合场景 | 代价 |
|------|---------|---------|------|
| Overlap 重叠切割 | 相邻 chunk 保留 10-20% 重叠 | 兜底方案，所有场景 | 存储略增，不彻底解决语义截断 |
| 语义边界切割 | NLP 工具识别句子边界，以句为单位填充 | 通用场景，质量要求中等 | 需 NLP 工具，速度慢于规则 |
| 句子窗口检索 | 单句向量化，命中后动态扩展窗口 | 句子密度高的文档 | 存储量大 |
| 父子分块 | 小 chunk 检索，大 chunk 返回 | 有层次结构的文档 | 需维护父子关系 |
| 命题化切割 | LLM 分解为独立命题 | 高价值文档，精度要求极高 | LLM 成本高，速度慢 |
| Contextual Retrieval | 向量化前为每个 chunk 生成上下文描述 | 结构化文档，精确匹配 | LLM 成本（Prompt Caching 可降 80-90%） |

**工程组合推荐：** Overlap 兜底 + 语义边界保证质量 + 高价值场景加父子分块或 Contextual Retrieval。

---

## 六、Prompt 拼接与生成

检索和重排完成后，需要将检索到的 chunks 组装成 prompt 送入 LLM 生成最终答案。这一步看似简单，但设计不当会导致"检索到了正确内容，LLM 却给出错误答案"。

### 上下文窗口预算管理

LLM 的上下文窗口是有限资源，需要合理分配：

```
可用 chunk 预算 = 总窗口大小 - 系统提示 - 用户查询 - 预留生成空间
```

根据预算动态调整检索的 chunk 数量和长度。自适应压缩方法（ACC-RAG）根据查询复杂度动态调整压缩率——简单查询压缩率低（保留更多细节），复杂查询压缩率高（覆盖更多来源）。

### Lost in the Middle 问题

研究表明，LLM 对 prompt 开头和结尾的内容关注度更高，中间部分容易被忽视。

**解决方案：**
- 将最相关的 chunk 放在 prompt 的开头或结尾位置
- 使用重排序模型确保 chunk 按相关性严格排序
- 对于多个高相关 chunk，采用交错排列（最相关→次相关→...→第二相关）

### 去重与冗余处理

当多个 chunks 高度相似或包含重复信息时，应检测并移除冗余内容，保留最有信息量的版本。避免相似信息占用宝贵的 token 预算。

### 来源引用与元数据保留

在组织 chunks 时保留文档来源、页码、时间戳等元数据，在最终答案中格式化引用（如 `[来源1]`、`[页码X]`），便于用户验证信息真实性和追溯。

### Prompt 模板设计

构建标准化的 prompt 模板，结构为：

```
系统角色定义（你是XX领域的专家助手...）
→ 检索上下文（按优先级排序的 chunks，用分隔符划分）
→ 用户查询
→ 输出格式要求（引用来源、不确定时说明、不编造）
```

关键原则：
- 明确指示 LLM 只基于提供的上下文回答，不使用自身知识
- 要求标注答案来源（哪个文档、哪个章节）
- 对于无法回答的问题，要求明确说"根据现有资料无法回答"而非编造

### 上下文压缩

当检索内容超过预算时的处理策略：
- **提取式压缩**：从每个 chunk 中提取与查询最相关的关键句子
- **摘要式压缩**：用 LLM 对 chunk 生成针对当前查询的摘要
- **渐进式加载**：先用 top-3 chunk 生成初步答案，如果置信度不够再追加更多 chunk

**多文档压缩两种模式：**

| 模式 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| Map-Reduce | 并行对每个 chunk 独立摘要，再汇总 | 速度快（并行），适合大量文档 | 汇总时可能丢失跨 chunk 的关联信息 |
| Refine（串行迭代） | 先摘要第一个 chunk，再逐步将后续 chunk 融入精炼 | 能捕捉跨 chunk 的信息关联 | 串行执行，延迟高；早期 chunk 影响权重更大 |

选择依据：文档数量多且独立性强 → Map-Reduce；文档间有强关联（如同一事件的多篇报道）→ Refine。

---

## 七、相关链接

- [[RAG检索策略]] — 在线阶段：查询理解、四层检索优化、混合检索、重排
- [[RAG向量与Embedding]] — 向量数据库选型、Embedding 模型、索引算法
- [[RAG高级技术]] — Agentic RAG、Self-RAG、GraphRAG、工程实践
- [[LLM基础与训练]] — Transformer 与 Embedding 原理基础
- [[Agent核心概念]] — Agent 架构中的 RAG 定位
