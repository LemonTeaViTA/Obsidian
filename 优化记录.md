# 知识库优化记录

> ==格式规范==：每条 2-3 行，说明改了什么 + 为什么。大型审计报告存 `wiki审计/历史审计/`，此处只留摘要+链接。
> ==归档策略==：每月初归档上月内容到 `优化记录_归档/{YYYY-MM}.md`。

## 2026-08-04

- `projects/ai-agent-platform-learning/` — 将 ai24、ai-program-factory、code-agent 快进到远端最新 `master` 并全量重建 CodeGraph；按新源码修正拓扑只读 API 未合入主干、产物历史入口未接线两处边界，补充项目 ID / 项目树筛选、领域技术方案、`reconcile-approval-items`、Web Validation/Figma 资产交接和 `files_gate` 自动执行降级。**原因**：三个仓库继续演进后，原审计混入开发分支能力，并漏记主干新增的审批、筛选和节点级视觉验证链路。

## 2026-08-03

- `projects/ai-agent-platform-learning/` — 核验并重建 ai24、ai-program-factory 的 CodeGraph，确认 code-agent 索引已同步；新增 2026-08 源码审计及平台管理、Agent 执行、前端展示三个当前实现专题，并修正根导航、审批接口、实时通信、Hook 白名单与历史审批模型提示。**原因**：三个业务仓库在 2026-07-23 后新增审批角色明细、个人工作台、产物版本、最新模板、Neo4j 拓扑、Kimi CCR 和 `files_gate`，原学习入口已与源码明显漂移。

## 2026-07-22

- `思考记录.md`、`wiki/Git.md` — 记录“治理约定应固化为仓库约束”与“AI 并行开发需要分支 + worktree + checkpoint”两条 ADR，并补充可执行的 Git 并行工作流。**原因**：将本次治理经验和用户在 AI 开发中的误区纠正沉淀为可复用决策。
- `.gitattributes`、`.editorconfig`、`.gitignore` — 将 276 个仅含 CRLF 行尾差异的文件安全归一化为 LF，并增加 Git / 编辑器约束与 raws 跟踪例外。**原因**：行尾污染制造 8.6 万行伪 diff，并直接破坏 Shell 脚本执行。
- `wiki审计/check.sh` — 兼容 macOS Bash 3.2，移除关联数组和 GNU `grep -P` 依赖，修正 Callout 误报、缺目录报错和链接扫描范围。**原因**：原脚本在本机无法运行，且会漏报 U+FFFD、误报链接与 Callout。
- `raws/WORKSPACE.md` — 恢复 raws 单一操作契约及 inbox/staged/processed 目录骨架，仅跟踪规则文件和空目录。**原因**：README 与方法论均依赖该文件，但目录此前被整体忽略且实际缺失。
- `AGENTS.md`、`README.md`、`wiki审计/README.md` — 修正 `projects/` 迁移后的旧路径、删除不存在的 PaiFlow 目录声明，并同步目录与审计说明。**原因**：治理文档与实际目录漂移会让后续 Agent 应用错误规则。
- `projects/CluadeCode/`、`projects/SageCLI/` — 为 32 篇项目文档补齐 `module` / `type` / `tags` / `last_reviewed`，并修复活跃项目文档中的 U+FFFD。**原因**：两个项目目录未遵守 project frontmatter 最小约定，部分正文已出现可读性损坏。
- `wiki/Agent/`、`wiki/Meta/管理方法论.md`、`wiki/文档解析/文本清洗.md`、`wiki/并发编程/线程基础与生命周期.md` — 修复 13 处 U+FFFD，更新受影响 wiki 的 `last_reviewed`；文本清洗示例改用 `\uFFFD` 转义。**原因**：真实乱码与教学用替换字符混在一起，使编码审计长期无法可靠清零。
- `wiki/RAG/_MOC.md`、`wiki/计算机网络/_MOC.md` — 补齐 `difficulty` 并更新复核日期；`career/简历/` 同步修复 3 处活跃内容乱码。**原因**：消除 frontmatter 缺口并保证求职材料可读。

## 2026-06-25

- 知识库结构重构：新建 `projects/`（Baize/CluadeCode/SageCLI 移出 wiki）、`career/`（简历移出 wiki）；治理文档从6份压缩到3份（删 QUALITY_CHECKLIST/RAW_WORKFLOW_GUIDE，管理方法论移入 wiki/Meta/）。**原因**：wiki 专注技术知识，项目文档和求职材料分离，降低维护复杂度。
- 面试题目.md 废弃，352道考点分散到各模块 `_MOC.md` 的 `## 面试考点` 节。**原因**：LLM 提问替代了人工勾选，全局索引维护成本高于价值。
- 补充 `wiki/操作系统/` 进程通信/内核态/页面置换三节；补充 `wiki/计算机网络/` TCP可靠传输/HTTP进阶/DNS/WebSocket 四节。**原因**：两模块对比 Agent/RAG 严重欠缺，且是 Java 后端面试基本盘。
- 并发编程4个超600行文件（锁/线程池/并发工具类/线程通信）顶部加速览 callout。**原因**：知识串联不宜拆分，但需要快速定位入口。
- 优化记录历史大条目（05-25/05-28/05-20）存档到 `wiki审计/历史审计/`，本文件压缩至275行。**原因**：信噪比过低，日常查询时被淹没。


## 2026-06-12

- `wiki/Agent/Plan-and-Execute 模式.md:53` — 修复"数据处理流水线"中"理"字 U+FFFD 乱码。**原因**：写入时字符损坏，用户阅读时发现。

## 2026-06-03

- ==整理 Clippings 简历相关 3 篇 raw → 新建 `wiki/简历/` 两文档==——按 Diátaxis 拆为 How-to + Reference 两类：
  - **新建 [[简历写法指南]]**（How-to）—— 提炼《我不能保证你去大厂》的写法规范：专业技能（大小写规范 + 主动带出核心八股 + 一页精简版）、项目经历黄金公式（技术栈+解决问题+业务+量化数据）+ 四注意事项、实习经历本质。**原因**：这是"怎么写简历"的方法论，属事实性知识入 wiki，独立成 How-to 文档。
  - **新建 [[RAG项目简历范例]]**（Reference）—— 从派聪明 RAG 简历提取真实 bullet，按 10 个技术方向归类（流式对话/双引擎检索/文件分片/Kafka/Agent体系/上下文/权限/多模型路由/测试/部署）+ AI技能模板 7 条 + 量化指标参考表。**原因**：照着抄的范例库属 Reference，与写法指南分开避免 Diátaxis 混类；对齐 Java + Agent 求职方向。
  - **丢弃内容**：星球/社群链接、"简历派"网站宣传、面渣逆袭 PDF 营销、座右铭、《精选简历》894 行里的全部图片链接、运维方向技能（与求职方向不符）。**标题党纠正**：派聪明那篇标题称"附 20 道面试题"，但全文无面试题，未编造。
  - **原文归档**：3 篇 Clippings 已移至 `raws/processed/`。**来源保留**：两文档末尾均标注 `> 数据来源` 链接（派聪明社区 / 二哥编程星球）。

## 2026-06-01

- ==wiki 全量审计后 P0+P1 修复==（12 个领域单元、共改 ~50 个文件、新建 5 个文件、~85 条 P0/P1/P2 修复项）——按"领域报告→领域专家+知识管理专家域内真实修复"双层流水线执行，铁律：只改本文件夹、不碰 MOC/索引、不碰 `面试题目.md`、跨文件夹坏链只记录交阶段2。修复日志全部留底 `wiki审计/_修复日志/<unit>.md`，机械验收见 `wiki审计/修复后验收报告.md`，本轮汇总见 `wiki审计/修复总报告.md`。

  - **[[面向对象]] → 拆 3 篇** —— 原 930 行红旗，按主题切为 [[面向对象核心]]（335 行 三大特性/重载重写/抽象类与接口）、[[类成员与关键字]]（385 行 访问控制/this/static/final/内部类）、[[对象语义]]（268 行 ==/equals/值传递/深浅拷贝/对象创建）。三篇各加 `[!info] 速览` callout + 三向互链 + SSoT 回链至 [[包装类与 Object]] Integer 缓存与 equals-hashCode 单点。**原因**：单文档 930 行 + 10 个 H2 同时触发"行数 > 600"与"H2 > 8"双红灯，且主题强可分。
  - **[[Function Calling]] 拆 + [[Harness Engineering]] 合** —— [[Function Calling]] 从 650 → 483 行 / 9 H2 → 8 H2，多模态三节拆出新建 [[Function Calling 多模态]]（195 行），§五+§六 合并；[[Harness Engineering]] 12 H2 → 8 H2，消灭非法 `## 八-A` 编号，尾部 5 节合并为 §七，§一-§六 保持锚点稳定。**原因**：>600 行 + 顶层小节超阈值 + 非法编号"八-A"违反 OBSIDIAN_STYLE。
  - **[[RAG检索策略]] → 拆 [[RAG查询理解]]** —— 主文件 904 → 596 行；§一查询理解（~300 行）独立为 [[RAG查询理解]]（324 行，6 个 H2）。同步消除 §2.1 与 §5.1 父子分块 SSoT 重复（§2.1 改 callout 指 §5.1）；修正"L5 命名"与五处标题内嵌高亮。**原因**：>600 行红旗 + 域内 SSoT 违规 + 标题不应内嵌 `==高亮==`。
  - **[[计算机网络]] 章节重排 + 拆 [[Agent传输方式]]** —— 顶层编号错乱"一→四→二→三→五"重排为"一→二→三→四→五"；第 5 章 Agent 传输方式（stdio/Streamable HTTP/WebSocket）整章拆出为 [[Agent传输方式]]（268 行，独立 Diátaxis）；TLS 握手轮次（TLS 1.2=2-RTT / TLS 1.3=1-RTT）事实订正；高亮密度降级；不雅举例改为中性。主文件 564 → 310 行。**原因**：编号错乱破坏目录可读性 + 第 5 章 Diátaxis 类型与前 4 章不同（Reference vs Explanation 混排）。
  - **[[Redis]] 域 SSoT 收敛 → 新建 [[Redis 实战场景]]** —— 把秒杀 / 限流（计数器/滑动窗口/令牌桶 Lua）/SCAN 海量 key / 客户端断连检测四块实战 How-to 全部收敛到新文件 [[Redis 实战场景]]（167 行）单点；[[底层数据结构与实战]] 删 120 行实战节、[[Redis 进阶功能]] 删滑动窗口限流 Lua、[[Redis 基础]] 删令牌桶 Lua + make/docker 部署命令，三处统一改为 callout 指向 SSoT。同步补 Redis Stream（消费组/XCLAIM/PEL）+ Client-side caching（CLIENT TRACKING/BCAST）+ SDS 3.2+ 五种 sdshdr + intset encoding 字节缓冲说明。**原因**：限流 Lua 在 3 个文件重复（SSoT 红线）+ [[Redis 基础]] Diátaxis 混（Explanation/Reference/How-to 三类混排）+ 知识漏 Stream 与 Client-side caching。
  - **[[算法]] 域 P0 代码 bug + KMP 补齐** —— [[树]] `maxPathSum` `self.max_sum` 裸写类体（`NameError`/`IndentationError`）→ 移入方法首行；[[图]] `rawnge(len(grid))` → `range`；[[数组]] L37/[[字符串]] KMP 注释 U+FFFD 乱码各 1 处。同步在 [[字符串]] 新增 `## 七、字符串匹配：KMP`（含 strStr + 重复子字符串两题完整解，单测全过）；[[链表]] frontmatter `easy` → `medium` + 「合并 K 个升序链表」域内 SSoT 收敛到 [[堆]]（链表侧改 callout）；[[堆]] 非法 `[!success]` → `[!tip]`；[[数组]]/[[链表]]/[[树]] 长文加 `[!tip] 本页速览`。**原因**：报告承诺 KMP 但文中通篇没有（双向同步破损）；P0 代码题解直接 `NameError` 是硬错误；多解法跨题 SSoT 防漂移。
  - **[[JVM]] 域知识订正 + 解释执行小节迁移** —— 4 条 P1 事实订正：8 字节对齐 ≠ 缓存行 64B（补伪共享/`@Contended`）；jaotc/AOT JDK 17 已移除（改指 GraalVM Native Image / AppCDS / Leyden）；Graal JIT/JVMCI 主线已移除（仅 GraalVM 发行版）；JDK 9+ PlatformClassLoader 取代 ExtClassLoader（无 rt.jar/无 sun.misc.Launcher）。"动态年龄判定"原文方向反了已订正；`-XX:+UseParalledlOldGC` 改正。"解释执行与编译执行"小节从 [[JVM 类加载机制]] 整段迁到 [[JVM 概述]]（话题归位）。[[JVM 内存管理]]/[[JVM 垃圾收集]] 加顶部速览 callout。**原因**：过时知识会让面试作答失分，"解释执行"小节属执行引擎而非类加载（Bounded Context 越界）。
  - **[[MySQL]] 域 P0 乱码 + 6 条事实订正** —— [[锁机制]] L36 SQL 注释 3 个 U+FFFD（"备"字损坏）字节级修复；[[索引机制详解]] B+树空间复杂度 `O(logN)` → `O(N)`（澄清树高 `log_m N` 是 I/O 次数不是空间）；[[引擎与日志]] Buffer Pool 默认值 128MB（原文 1-4G/25% 实为 AWS RDS 自动调参规则）；[[MySQL 基础与架构]] DATETIME 5 字节（5.6.4+，原文 8 字节过时）+ VARCHAR 65535 字节（原文"字符"概念混淆）；[[事务与MVCC]] RU 隔离级别加锁表述精修（区分 MVCC 与行锁）；Online DDL 域内去重（[[高可用与分库分表]] 千万级加字段节改 callout 指 [[锁机制#DDL 与锁]] 单点）。两篇 >400 行加顶部速览。**原因**：B+树空间复杂度 / Buffer Pool 默认值 / DATETIME 字节 / VARCHAR 字节均为面试高频硬错点。
  - **[[Java基础]] 域** —— [[String]] L43 U+FFFD（"等价于"）+ 注解篇 8 处代码块中文弯引号 → 半角；[[基础语法]] Integer 缓存 callout 回链 [[包装类与 Object]] 单点；[[Java 概述]] LTS 措辞精修（OpenJDK 仍可免费商用）；[[类成员与关键字]] static 变量存储位置补 JDK 7+ 在堆中 Class 对象（原文"方法区"已过时）。**原因**：编码乱码 / 代码块标点 / 域内 SSoT 三类机械问题，配套拆分一并清理。
  - **[[Spring]] 域 4 条事实订正** —— [[AOP 与动态代理]] JDK 动态代理 / CGLIB 示例 `return null` → `return returnValue`（非 void 方法丢失返回值）；[[Spring 基础与 IoC]] "Spring ORM 支持 MyBatis-Plus" 改为 Hibernate/JPA（MyBatis 走 mybatis-spring 第三方）+ Bean 生命周期补 BeanPostProcessor before/after 双回调 + AOP 在 `postProcessAfterInitialization` 生成；[[Spring 事务]] protected 方法事务为何不生效根因订正（`AnnotationTransactionAttributeSource.publicMethodsOnly=true`）；[[Spring Boot 与微服务]] 自定义 Starter 改 Boot 3.x 的 `AutoConfiguration.imports`（`spring.factories` 2.7 起废弃、3.0 移除）。**原因**：会误导面试作答的事实性错误优先级高。
  - **[[操作系统]] 域知识补齐** —— 补 TLB PCID/ASID（纠正"进程切换必然全刷 TLB"）+ 次缺页(minor) vs 主缺页(major) 对比 + ET 模式必须 `O_NONBLOCK` + CFS → EEVDF（Linux 6.6+ 默认） + 协程栈大小订正（pthread 8MB / HotSpot 1MB / goroutine 2KB 自动扩展）。代码块加 `text` 语言标签。**原因**：报告点名的高频追问点 + 调度器/栈大小事实性更新。
  - **文档解析 / 数据格式 / RAG 周边** —— [[文档解析综述]] 域 31 处裸 ```` ``` ```` 围栏补 `text` 语言标签；[[OCR]] CTC 示例用 `[blank]` 显式标记（原 `-` 兼作分隔符与 blank 易混）；[[VLM文档解析]] 补 SigLIP 是 PaliGemma 内部视觉编码器衔接说明；[[文本清洗]] 不可见字符正则改显式 Unicode 转义 + 注释。[[XML]] 与 [[OOXML]] SSoT 去重（删 [[XML]] §五重复代码块）；[[PDF]] 扫描版栅格编码（CCITT G4/JBIG2/JPEG2000）订正 + OCR 编码 callout；[[OOXML]] `r:` Office Relationships 命名空间严谨化。[[RAG向量与Embedding]] causal-mask 收紧为两条路线（去 mask vs last-token pooling） + 删 KaLM 12B 存疑硬数字 + 代码 embedding 模型名核实（Voyage-code-3/CodeXEmbed/Jina v2-base-code 替换未确认的 Qwen2.5-Coder-Embedding/CodeGemma-Embedding）。**原因**：代码块标签、SSoT、过度概括与不可核验硬数字属机械级清理与领域事实订正。
  - **机械验收清零** —— 全 wiki U+FFFD 乱码 100% 清零（文本清洗教学样例改用 `U+FFFD` 转义表示）；代码块中文弯引号 19 处全清；wikilink NO_FILE 从 60 降到 6（剩 Hermes 框架/ColPali/`[[主文档]]` 等需人工决策的占位）；MOC 中目录链接 `[[wiki/X/]]` 4 处改为 `[[Agent索引_MOC|Agent 体系]]` / `[[RAG基础与架构|RAG 体系]]` 等合法 wikilink。**原因**：机械级噪声不应再拖到后续 sync agent。

- ==Phase 2 后续修复==（暂存整合 / MOC 统一与新建 / 全局改链）——完成 Phase 1（21 领域文件夹内 P0+P1 修复）后的三个收尾阶段，铁律：不重做 Phase 1 已完成工作、追加而非覆盖现有记录。修复日志见 `wiki审计/_修复日志/_阶段2*.md`，验收见 `wiki审计/Phase2验收报告.md`，追加至本文件与 `wiki审计/修复总报告.md`。

  - **阶段 2A：暂存内容整合** —— `wiki审计/_迁移暂存/` 3 个文件并入正式 wiki：
    - `方法论-AI辅助知识管理.md`（46 行）→ [[wiki-管理方法论]] 新增 §九"AI 辅助知识管理的底层架构"（三层架构 raw/wiki/schema + 两个大脑分离 + 知识代谢循环）
    - `RAG-FTS5与分块教程.md`（362 行）→ 新建 [[全文检索与分块教程]]（独立 Tutorial，FTS5 机制 + 滑动窗口 + trigram 分词 + 倒排索引流程 + 三大误区澄清）
    - `RAG-延迟优化策略.md`（34 行）→ [[RAG检索策略]] 新增 §六"生产环境检索延迟优化"（并行/缓存/两阶段/超时降级/动态 Top-K/模型降级 六策略 + 语义缓存）
    - 暂存目录已清空，所有目标文件 < 800 行（最大 631 行）。**原因**：Phase 1 剥离的跨域内容现归位到对应 wiki 章节或独立文件。

  - **阶段 2B：MOC 统一与新建** —— 更新 6 个现有 MOC（Agent/算法/Java基础/Redis/并发/LLM），新建 4 个 MOC（文档解析/数据格式/操作系统/PaiFlow），处理孤儿页 [[Git]]（挂入 Agent MOC 相关链接）：
    - 新增文件登记：[[Agent 工程实践]] / [[MCP Server 生态]] / [[MCP 安全模型]] / [[Plan 模式家族对比]] / [[动态规划-背包]] / [[动态规划-多维]] / [[类成员与关键字]] / [[对象语义]] / [[Redis 实战场景]] / [[线程通信与同步]] / [[Function Calling 多模态]]
    - 新建 MOC 覆盖：文档解析 6 文件（版面/OCR/TSR/公式/VLM/清洗）、数据格式 4 文件（XML/DOM/OOXML/PDF）、操作系统 1 文件、PaiFlow 1 文件
    - frontmatter `last_reviewed` 统一更新为 2026-06-01
    - 当前 14 个核心域均有 MOC，覆盖 131 个内容文件。**原因**：Phase 1 拆分/新建的文件需纳入对应 MOC；4 个域达到建 MOC 门槛。

  - **阶段 2C：全局改链修复** —— 修复跨域死链与锚点漂移（修复 12 个文件 / 110+ 条链接）：
    - [[面试题目]] 80+ 条死链修复：路径前缀统一（50+ 条 `[[LLM/xxx]]` → `[[Agent/xxx]]` / `[[RAG/xxx]]`）+ Agent 核心概念拆分后锚点重指（12 条指向 [[ReAct 与 Harness 实现]] / [[Function Calling]] / [[MCP 协议]] / [[Agent Memory 系统]] / [[Plan-and-Execute 实现]] / [[Agent 可靠性设计]] / [[Agent 安全模型]]）+ Baize 项目锚点调整（7 条）+ 删除不可解析链接 3 条（Hermes 框架 / 框架选型 → 纯文本）
    - 跨域文件 15+ 条修复：MySQL 引用 4 条（`[[MySQL 锁与事务机制]]` → `[[MySQL/事务与MVCC]]` 等）、Agent 引用 3 条、其它域 8 条
    - 锚点漂移 10+ 处修复：Agent 工程实践 CLI 设计节重编号、Multi-Agent 架构独立、算法 MOC 锚点对齐、HashMap 锚点调整
    - wikilink NO_FILE 从 60 → 6（剩占位符），全局改链清单 ~50 条记录全部执行完毕
    - [[面试题目]] 健康度：修复前 76+ 条死链 → 修复后 0 条关键死链，425 条题目中 366 条带链接（86%）全部可达。**原因**：Phase 1 拆分/重组后的跨文件夹引用未同步；Agent 核心概念拆分为 7 个独立文件后 12 条锚点需语义判断重指。

  - **验收红旗清单**（Phase2验收报告标记）：
    - 🚩 P0：[[面试题目]] 仍有 14/20 抽样锚点失效（70%），根因链接格式缺目录前缀（`[[索引机制详解#...]]` 应为 `[[MySQL/索引机制详解#...]]`），需批量添加目录前缀 + 锚点验证（预计 30-60 分钟）
    - 🚩 P1：4 个 LLM/Agent 路径错误（`[[LLM/Agent 核心概念]]` → `[[Agent/Agent 核心概念]]`，出现在 PaiFlow/Baize 文档）
    - ✅ UTF-8 乱码已清零（1 处 `数据格式_MOC.md:25` 已修复）
    - ✅ 超长文件仅剩 1 个（[[线程基础与ThreadLocal]] 842 行，轻微超标 5%）
    - ✅ MOC 体系完整（14 个域，覆盖率 100%）
    - ✅ 暂存目录已清空
    - ✅ Wikilink 成功率 94.4%（10/179 坏链，其中 6 个为术语占位符/误识别）

## 2026-05-28

- 第十八-二十一期内容补入 Agent 工程实践/可靠性/安全模型，HITL 三档授权。详细记录：[[历史审计/2026-05-28-Agent工程实践补充]]

## 2026-05-27

- `wiki/LLM/Agent 工程实践.md` ==§二 Multi-Agent 架构 新增两个子节==——回应用户引用的"第五期：Multi-Agent 协作 + 角色分工"功能（三角色 Planner/Worker/Reviewer + 主从 Orchestrator + 审查不通过反馈重试 + 冲突自动解决）。盘点确认 wiki 大部分概念分散覆盖在 4 个文档中，==但"三角色经典模式"集中讲解和"冲突仲裁"两个点是缺的==：

  - **新增子节"==经典三角色组合：Planner + Worker + Reviewer=="**：
    - 三角色职责定义对照表（==每个角色对应已有 wiki 模式==）
    - ==关键认知：三角色 = Plan-and-Execute + Reflection 的产品化封装==——拆开看每个都是已有概念的组合
    - Reviewer 反馈 → Worker 重试的完整协议代码（feedback_history 结构化反馈 + 重试上限固定 + 失败带上下文上报）
    - Reviewer 实现两种选择（同模型 Self-Reflection vs 独立 Critic Model）+ ==生产推荐"Worker 用 Sonnet, Reviewer 用 Opus"==

  - **新增子节"==多 Agent 冲突仲裁=="**：
    - 四种仲裁策略对照表（多数投票 / 权威优先 / Meta-Agent 仲裁 / 辩论收敛）
    - 冲突类型与对应策略映射（事实冲突 / 推理冲突 / 风格冲突 / 评估冲突）
    - 工程要点（默认多数投票 + Meta-Agent 兜底 + 仲裁过程审计 + 辩论上限 3 轮）
    - ==与单 Agent 失败处理的区别==——单 Agent 失败 vs 多 Agent 仲裁是两个互补维度

  ==关键洞察==（值得面试时讲）：
  - ==三角色不是新概念== —— 是 Plan-and-Execute（Planner+Worker）+ Reflection（Reviewer）的产品化
  - CrewAI / AutoGen / Anthropic Multi-Agent Research 都是这套
  - ==仲裁与重试的边界==：单 Agent 内部出错走 L1-L4 决策，多 Agent 结论冲突走仲裁

  Agent 工程实践.md 从 665 → 773 行（新增 ~108 行）。

  **原因**：用户引用的第五期功能描述==每条都能映射到已有 wiki==，==但"Planner+Worker+Reviewer 三角色经典模式"和"多 Agent 冲突仲裁"==没有专门讲解，分散覆盖让读者难以系统理解。本次补全把"三角色 = Plan-and-Execute + Reflection 组合"这个==关键认知顶到首位==，把"==四种仲裁策略=="作为单 Agent 失败处理的互补维度——读者读完能讲清"==CrewAI 的 Multi-Agent 不是黑魔法,是已有模式的组合=="，这是 Agent 工程师面试时区分概念党与判断派的关键

## 2026-05-25

- RAG基础与架构.md 拆分重构（1165→217行），新建文档解析综述/分块策略/分块策略前沿。详细记录：[[历史审计/2026-05-25-RAG基础与架构大重构]]

## 2026-05-22

- 重新划分分块策略（离线）与检索时上下文扩展（在线）的章节归属：
  - `wiki/LLM/RAG基础与架构.md` §5.2 L5 ==重命名==："检索时增强"→"chunk 内容增强"，仅保留 Contextual Retrieval 和 Late Chunking（纯离线策略）；Overlap 并入 L1（作为标配，不再单列）；==删除==父子分块、句子窗口检索（这两个本质是在线检索逻辑）
  - §5.5 选型决策树相应调整：父子分块/句子窗口移到"在线检索增强"列，wikilink 指向 RAG检索策略.md
  - §5.2 横向对比表更新：去掉父子分块、句子窗口、Overlap 单独行，增加"配套在线检索策略见 [[RAG检索策略#五、检索时的上下文扩展]]"提示
  - `wiki/LLM/RAG检索策略.md` ==新增 §五"检索时的上下文扩展"==：
    - 5.1 父子分块（Small-to-Big）：离线父子结构 + 在线检索逻辑（query→子块→parent_id→父块）+ 代码示例 + 关键参数
    - 5.2 句子窗口检索：单句向量化 + 动态窗口扩展 + 代码示例 + 关键参数
    - 5.3 父子分块 vs 句子窗口对比 + 选型建议
    - 5.4 与离线分块策略的关系（离线 chunk 形态 ↔ 在线检索逻辑配套表）
  **原因**：用户指出"L5 检索时增强"的命名和内容混乱——5 个方法里只有 2 个真正是"检索时"的（父子分块、句子窗口），其他 3 个（Overlap/Contextual Retrieval/Late Chunking）都是纯离线；分类按"离线/在线"边界严格切分后，每个章节只讲一个阶段的事，逻辑清晰
  **关联**：RAG检索策略.md frontmatter 加 ==父子分块、句子窗口== 标签；last_reviewed 更新为 2026-05-22；顺手修复 RAG检索策略.md L326 的旧乱码并恢复为"原理"

- `wiki/LLM/RAG基础与架构.md` 第四章"文本清洗"扩写 — 从仅列"做什么"扩展为讲"怎么做"+"为什么"：
  - **4.1 噪声去除**：页眉页脚识别（跨页文本比对+位置过滤+版面分析模型三种方法）、PDF 水印识别（颜色/旋转/关键词/版面模型四种方法）、HTML 导航剥离（DOM选择器+文本密度算法）
  - **4.2 编码与格式规范化**：字符编码（BOM/chardet/替换字符）、空白符规范化（正则）、标点符号上下文判断、Unicode NFKC 规范化
  - **4.3 重复内容检测**：==关键决策"在 chunk 前做、文档/段落粒度做"==；精确去重（哈希）；==MinHash + LSH 算法原理==（k 个哈希函数取最小值得签名向量、LSH 分桶避免 O(n²)）；保留策略（最新/最完整/最权威）
  - **4.4 语言检测与分段**：fastText/langdetect/langid 工具对比；为什么要做语言检测（分词器/Embedding/分句规则）；混合语言按段落检测；逻辑段落识别的多信号判断；中英文句子分割（spaCy 处理缩写/引号边缘情况）
  - **4.5 内容增强**：缩写展开（首次出现替换避免膨胀）、停用词策略（向量检索不要移除停用词、不做词干提取）、元数据提取
  **原因**：用户在阅读第四章时疑问"页眉页脚怎么判断、水印怎么识别、重复检测在 chunk 前还是后、语言检测和分句怎么做"——原文只列了"做什么"没讲"怎么做"，是知识库的明显空白
  **关联**：last_reviewed 更新为 2026-05-22；引用 [[版面分析]]、[[DOM]]、[[RAG向量与Embedding#选型决策树]]、[[RAG高级技术#知识冲突处理]]

## 2026-05-20

- 检索策略 §一 从传统意图分类重写为现代 LLM 路由模式。详细记录：[[历史审计/2026-05-20-RAG检索策略重写]]

## 2026-05-19

- `wiki/LLM/RAG基础与架构.md` Section 五"分块策略"重构 — 去项目化（删除 V1/V2/V3 版本号、保险条款举例、`is_key_clause` 等项目字段）、去具体指标数字，改为通用知识叙事
  **重构后结构**：5.1 核心矛盾 → 5.2 五层方法学全景（L1规则/L2结构感知/L3语义/L4命题级/L5检索时增强）→ 5.3 当前SOTA（Contextual Retrieval/Late Chunking/金山判别式/命题化）→ 5.4 通用工程参数（chunk_size/Overlap/元数据模板）→ 5.5 选型决策树
  **原因**：原章节读起来像保险项目的实施方案，而非通用知识；用户希望按"有哪些方法 → 最新方法 → 各自优势"组织

- `wiki/LLM/RAG基础与架构.md` Section 三"文档解析"重构 — 从"按文件类型分章"改为"内容元素 × 容器格式"两个正交维度
  **重构前问题**：3.2 PDF / 3.3 Word / 3.4 Excel / 3.5 PPT / 3.7 扫描件 各自重复讨论表格、图片、公式、OCR——同一个内容元素在每种格式下重复讲一遍
  **重构后结构**：
    - 3.1 两个正交维度（容器格式 vs 内容元素）
    - 3.2 格式光谱：按结构化程度排序（XML原生 → DOM → 坐标 → 像素 → 时序）
    - 3.3 内容元素的通用解析方法（核心章节）：表格TSR / 图片图表 / 公式 / 多栏版面 / OCR
    - 3.4 各格式的独有特点（去重后只剩独有难点：PDF坐标还原 / Word的OLE和样式 / Excel合并单元格和NL2SQL / PPT动画和备注 / HTML噪声 / 音视频时序）
    - 3.5 跳过解析（视觉检索）/ 3.6 工具全景 / 3.7 创新架构（保留原内容）
  **原因**：用户指出 PDF 是其他类型文件的"整合版"，文件类型和内容元素本就是正交的两个维度，按文件类型组织导致大量重复

## 2026-05-18

- `wiki/LLM/AI编程工具.md` — 新增 Section 六"Claude Code 概念体系"（Commands/Skills/Rules/Hooks/Subagents/Plugins 六层概念）；新增 Section 七"AI 辅助知识管理方法论"（三层架构/两个大脑分离/知识代谢循环）
  **来源**：Claude Code常用命令指北、Claude Code+Obsidian指南（方法论部分）

- `wiki/LLM/RAG检索策略.md` — 新增 Section 四"多轮对话检索去重"（VimRAG/GGPO 模型层方案 + Milvus `expr not in`/`group_by_field` 工程方案 + Redis 生产细节）；Section 2.4 新增"Embedding + Rerank 搭配原则"（同系列配对原则 + 四组经典搭配）；新增 ColBERT Late Interaction 架构说明（arXiv:2004.12832）
  **来源**：多轮对话重复召回文章（VimRAG arXiv:2602.12735）、BGE/GTE/Rerank选型文章

- `wiki/LLM/RAG高级技术.md` — Section 三新增"RAG-Gym：过程监督与策略优化"（ReSearch 架构 + 三维优化框架 + 实验数据 41%→60% F1，arXiv:2502.13957）；Section 七扩展为完整"多模态 RAG"章节（CLIP/Caption/ColPali 三方案对比 + MultiVectorRetriever 模式 + 表格处理 + 六个生产踩坑，ColPali arXiv:2407.01449）；原 Section 七改为 Section 八；新增"知识冲突处理"子节；Contextual Retrieval 补充 Prompt Caching 具体数据（成本降低 80-90%，失败率降低 49%/67%）
  **来源**：RAG-Gym文章、多模态RAG文章、万字面试题库

- `wiki/LLM/RAG向量与Embedding.md` — Section 五新增"选型决策树"（语种→上下文长度→部署资源三维决策）+ BGE-M3 vs GTE-multilingual-base 详细对比表 + MTEB 阅读方法；Section 六微调阈值补充（Recall@5 < 0.7 需微调 + 500-1000 对数据量指导 + 保险领域实测案例 MRR 0.58→0.82）
  **来源**：BGE/GTE/Rerank选型文章（BGE-M3 arXiv:2402.03216）

- `wiki/LLM/RAG基础与架构.md` — Section 5.6 新增"句子窗口检索"（单句向量化+动态窗口扩展）、"命题化切割"（Dense X Retrieval arXiv:2312.06648，EMNLP 2024）、六方案对比表；Section 六"上下文压缩"新增 Map-Reduce vs Refine 对比
  **来源**：文档切割6方案文章、万字面试题库

- `raws/inbox/Clippings/` 8 篇文档全部移入 `raws/processed/`（6 篇 RAG 文章已整合，2 篇 Claude Code 工具文章已提取方法论）



- `wiki/Baize项目/` 11 个文件整理 — 全部加 frontmatter（module: Baize项目）；修复 6 处断链（`[[检索召回与优化]]` 等旧路径 → LLM/ 下的新文件）；全部加 `## 相关链接`；6 个文件加 callout（Bitmap 追踪、WebSocket vs SSE、JWT 代价、分块表独立、动态上下文、Embedding 选型）
  **原因**：项目文件缺少 OBSIDIAN_STYLE 规范的基础结构

- `wiki/PaiFlow/PaiFlow工作流平台.md` — 加 frontmatter + `## 相关链接`
  **原因**：同上

- `wiki/面试题目.md` — 项目部分（Baize ~80 题）从 Agent与LLM 之前移到文件末尾；删除重复 `## 项目（Baize）` header；新增 `## 项目经验` 总标题；新增 `### PaiFlow（工作流平台）` + 8 题；总计 425 题
  **原因**：用户要求项目放最后，PaiFlow 需要面试题入口

- 全量审计修复 — `wiki/集合框架/集合框架索引_MOC.md` frontmatter 标准化（旧 aliases 格式 → 标准 4 字段）；3 个文件补全 `## 相关链接`（并发编程MOC、集合框架MOC、算法/数组.md）；11 个核心内容文件各加 1 个 callout（锁升级不可逆、线程池参数、happens-before、工作窃取、CountDownLatch vs CyclicBarrier、ArrayList 真实性能、HashSet 本质、JDK/JRE/JVM、双亲委派打破、EXPLAIN 三字段、雪花时钟回拨）
  **原因**：收尾审计，按 OBSIDIAN_STYLE 和 QUALITY_CHECKLIST 标准补齐遗漏

- `wiki/LLM/LLM基础与训练.md` P2 内容补充 — 新增 QLoRA（NF4/Double Quantization + 微调工具对比表）、Flash Attention（三代演进）、推理部署框架（vLLM/SGLang/TensorRT-LLM/Ollama/Triton 对比 + SGLang RadixAttention 详解）
  **原因**：LLM 工程师面试必备知识点

- `wiki/LLM/RAG向量与Embedding.md` P2 内容补充 — 向量数据库对比表扩充至 7 个（+Qdrant/Chroma）；新增生产级 RAG 检索优化（延迟优化 4 策略 + 并发优化）
  **原因**：向量数据库选型是 RAG 面试高频题

## 2026-05-09

- `wiki/LLM/` 全模块重组 — 8 个文件去除数字前缀（`01_LLM基础与训练.md` → `LLM基础与训练.md`）；`wiki/RAG/` 合并进 `wiki/LLM/`（2 文件迁入，占位文件删除，空目录删除）；`检索召回与优化.md`（1923 行）拆分为 `RAG检索策略.md`（871 行）+ `RAG向量与Embedding.md`（385 行）+ `RAG高级技术.md`（697 行）；新建 `LLM索引_MOC.md`；全 wiki 78 处 wikilink 更新（RAG/ 路径 → LLM/，数字前缀 → 简名）
  **原因**：RAG 与 LLM 是同一知识体系，分两个文件夹增加导航成本；数字前缀阻碍 wikilink 可读性；1923 行的单文件违反可维护性原则

- `wiki/面试题目.md` — 完整重写（541 行，410 题），重排模块顺序为 Java基础→集合框架→并发编程→JVM→MySQL→Redis→Spring→网络→OS→项目→Agent，MySQL 从 ~10 题扩充至 59 题（6 子分类），修复所有断链（volatile、锁与事务拆分后的旧链接）
  **原因**：原文件模块顺序与学习路径不匹配，MySQL 题目严重不足，多处 wikilink 指向已拆分/迁移的旧文件

- `wiki/MySQL/` 模块重组 — `锁与事务机制.md` 拆分为 `锁机制.md`（398 行）+ `事务与MVCC.md`（247 行）；`引擎与日志.md` 修正 binlog"物理日志"→"逻辑日志"并加 warning callout，删除 innodb_flush_log_at_trx_commit 重复段落；`MySQL 基础与架构.md` 加 3 个 callout；`索引机制详解.md` 加阿里手册 callout；7 个文件更新旧 wikilink
  **原因**：锁与事务合一文件过长且主题耦合；binlog 分类错误是技术硬伤

- `wiki/Redis/` 全 6 个内容文件 — 各加 1-2 个 callout（多线程、淘汰策略、SETNX 三大坑、everysec、脑裂、跳表 vs 红黑树）；`Redis 基础.md` DRY 去重 ~30 行哨兵/集群部署配置改为 wikilink；frontmatter 标准化
  **原因**：callout 密度为 0，缺少面试关键提醒；部署配置与高可用文件重复

- `wiki/Spring/` 全 5 个内容文件 — 各加 1-2 个 callout（三级缓存局限、@Resource、内部调用失效、CGLIB 默认、spring.factories 迁移、HttpMessageConverter）；frontmatter 标准化
  **原因**：callout 密度为 0，Spring 面试易踩坑点缺少醒目提示

- `wiki/算法/` 14 个文件 — 全部加 frontmatter + 相关链接；`算法_MOC.md` 完整重写（20+ 模式识别信号表、4 阶段学习路径）；`位运算.md` 从 63→240 行、`排序.md` 从 96→350 行；`数组.md`、`动态规划.md` 加模式识别信号表
  **原因**：算法文件缺少 frontmatter 和交叉链接；核心算法（位运算、排序）内容过少；缺少模式识别方法论

- `wiki/并发编程/` 审计修复 — `锁.md` 移除断链 `[[锁与事务机制|乐观锁]]`，volatile 章节精简为一句话 + wikilink；`线程基础与ThreadLocal.md` 新增完整 volatile 章节（60+ 行）；`Java 内存模型.md` 删除 2 条无效 markdown 链接；`并发编程索引_MOC.md` frontmatter 标准化
  **原因**：volatile 归属线程安全（可见性/有序性）更合理；断链和无效链接影响导航

- `wiki/集合框架/` 审计修复 — `集合框架概述.md` 修复 `U+FFFD` 损坏字符；`Set体系与HashSet.md` 去重 ~45 行 putVal 源码改为 3 条 bullet + tip callout；MOC frontmatter 标准化
  **原因**：putVal 源码与 HashMap 文件重复（DRY 违反）

## 2026-05-08

- `OBSIDIAN_STYLE.md` — 新增第 11 节"内容组织"（先概念后应用、单一信息源 DRY、就近原则），并在快速自检清单中补充对应检查项
  **原因**：JVM 文件审计发现内容组织问题，需将原则固化为规范

- `wiki/JVM/JVM 垃圾收集.md` — 重组章节顺序：GC 基础 → 算法 → GC 分类 → 并发 GC 通用机制（三色标记/漏标/写屏障/卡表 RSet/Safepoint）→ 收集器；合并重复的三色标记讲解
  **原因**：原顺序违反"先概念后应用"——收集器章节引用了尚未介绍的三色标记等机制

- `wiki/JVM/JVM JIT与字节码.md` — 调整章节顺序：JIT 基础 → 字节码基础 → JIT 核心优化；将字节码基础移至优化章节之前
  **原因**：invoke 指令等字节码概念在逃逸分析等优化章节中被引用，需先介绍

- `wiki/JVM/JVM 内存管理.md` — 删除 GC 收集器对照表，改为一句话 + wikilink；逃逸分析详解替换为概括 + 链接到 JIT 文件
  **原因**：DRY 去重，详解保留在各自归属文件，避免两处维护

- `wiki/JVM/JVM 类加载机制.md` — Class 文件结构和 invoke 指令详解替换为概括 + 链接到 JIT 文件
  **原因**：DRY 去重，详解归属 JIT 文件

- `AGENTS.md`、`QUALITY_CHECKLIST.md`、`RAW_WORKFLOW_GUIDE.md` — 按业界标准重写：AGENTS.md 改为任务触发式操作指令（Boundaries 三层结构 + 完成标准）；QUALITY_CHECKLIST.md 压缩至 149 行，新增可发现性检查；RAW_WORKFLOW_GUIDE.md 删除与 AGENTS.md 重复内容
  **原因**：原文件写成人类文档而非 agent 操作手册，业界研究显示散文原则会被 agent 忽略

## 2026-05-07

- `wiki/Java基础/` 全部 7 个文件 — 从浅层 bullet 扩充为面试深度内容，补充代码示例、对比表、callout、高亮、wikilink
  **原因**：原内容过于简略，缺乏代码示例和底层原理，不足以应对面试追问

- `wiki/并发编程/线程基础与ThreadLocal.md` — 新增 Condition API 完整讲解（生产者-消费者示例、vs wait/notify 对比表）、CompletableFuture 扩充（链式调用、组合多任务、异常处理）
  **原因**：Condition 和 CompletableFuture 是高频面试题，原文件缺失

- `wiki/集合框架/集合框架概述.md` — 新增 ArrayDeque vs LinkedList 对比表+代码、PriorityQueue 小顶堆/大顶堆+Top-K 应用
  **原因**：队列相关题目在面试中高频出现，原文件只有概述

- `wiki/集合框架/HashMap核心原理.md` — 扩充 LinkedHashMap（两种排序模式+LRU 缓存实现）、TreeMap（自定义 Comparator+范围查询 API+对比表）
  **原因**：LinkedHashMap LRU 和 TreeMap 是面试高频题，原文件缺失

- `wiki/JVM/JVM JIT与字节码.md` — 新建文件（350 行），覆盖分层编译、方法内联、逃逸分析三大优化、OSR、Class 文件结构、5 种 invoke 指令族、invokedynamic 与 Lambda 原理、CodeCache、JVM 预热
  **原因**：JIT 是 JVM 性能调优的核心，原知识库完全缺失

- `wiki/JVM/JVM 垃圾收集.md` — 补充三色标记漏标充要条件、SATB vs Incremental Update、写屏障、卡表/RSet、Safepoint、ZGC 染色指针
  **原因**：并发 GC 核心机制是面试区分度题目，原文件缺失

- `wiki/JVM/JVM 内存管理.md` — 补充直接内存、Mark Word 64 位位分布表、OOM 家族 7 种完整表，修正 ParNew 命名错误
  **原因**：OOM 分类和 Mark Word 结构是面试高频题，原文件缺失

- `wiki/JVM/JVM 调优.md` — 补充 GC 日志逐行解读、三个典型故障案例（CMS Concurrent Mode Failure、G1 Humongous、Metaspace OOM）
  **原因**：GC 日志解读和故障排查是实战面试必考，原文件缺失

- `wiki/JVM/JVM 类加载机制.md` — 补充 Class 文件结构、五大 invoke 指令族
  **原因**：字节码层面知识是 JVM 深度题目的基础

- `wiki/LLM/06_Agent工程实践.md` — 新增 Agent 可靠性设计（死循环检测、Fallback、错误累积防护、Prompt Injection 防御）和幻觉缓解策略
  **原因**：Agent 岗位面试的区分度题目，原文件缺失

- `wiki/LLM/01_LLM基础与训练.md` — 补充 Self-Attention 计算细节（√dk 解释）、Decoder-only 架构、LoRA 微调原理
  **原因**：LLM 底层理解是 Agent 岗位的加分项

- `wiki/面试题目.md` — JVM 板块从 16 题扩充为 37 题（5 个子分类），并发编程新增 Condition，集合框架新增 LinkedHashMap LRU/ArrayDeque/PriorityQueue
  **原因**：与 wiki 内容同步，确保面试题目索引完整

- `OBSIDIAN_STYLE.md` — 新建，定义 wiki 的 Obsidian 格式规范（高亮/callout/frontmatter/wikilink），区分业界共识和本 vault 自定义规则
  **原因**：之前无成文规范，AI 协作时风格不一致

- `QUALITY_CHECKLIST.md` — 新增 Obsidian 风格规范检查节，修正旧文件路径，删除重复 callout 条目，更新自动化工具脚本
  **原因**：与 OBSIDIAN_STYLE.md 保持同步，修正过时路径

- `AGENTS.md` — 更新目录结构（加入 Java基础/、JVM/、OBSIDIAN_STYLE.md），更新题目数量（230 → 325），新增 OBSIDIAN_STYLE.md 引用
  **原因**：目录结构和题目数量已过时
