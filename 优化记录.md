# 知识库优化记录

> ==当前活跃记录==：仅保留 ==2026-05== 整月。==老的归档==：
> - [[优化记录_归档/2026-04]] — 2026-04-13 ~ 2026-04-30
>
> ==归档策略==：每月初归档上月内容到 `优化记录_归档/{YYYY-MM}.md`。==流水账文档不受 600 行限制==——见 [[wiki-管理方法论#3.2 流水账文档的特殊规则]]。

## 2026-05-28

- ==第十八-二十一期 + 第六期 HITL 增强，拆 5 处补到 3 个文档==——回应用户对多个期次的"看看这几个部分的内容如何"。5 个知识点按重要性依次补完：

  - **[[Agent 工程实践]] §八-B 末尾**新增"==为什么 Coding Agent 不做沙箱：安全模型的正确认知=="(~80 行):
    - ==本地 CLI Agent 安全模型== = HITL + PathGuard + CommandGuard + AuditLog（不是沙箱）
    - ==4 条不做沙箱的理由==（削弱能力 / 虚假安全感 / 体验差 / 用户已授权）
    - ==真正的 Agent 沙箱是 microVM 级==（Devin/Modal/Anthropic Computer Use 用 Firecracker/gVisor）+ 产品对照表
    - ==CommandGuard 黑名单==（sudo rm -rf / mkfs / dd of=/dev / fork bomb / curl|sh / find / / chmod 777 / / shutdown）+ Python 实现 + `outcome=deny, approver=policy` 审计
    - ==PathGuard 三种拦截==（绝对路径外逃 / `..` 穿越 / 符号链接逃逸）+ Python 实现
    - 安全模型四层流程图（CommandGuard → PathGuard → HITL → 执行+AuditLog）

  - **[[Harness Engineering]] §八 后**新增"==§八-A Prompt 分层架构：System Prompt 的工程化管理=="(~70 行):
    - ==为什么需要分层==（多角色共享 base / 用户项目级定制 / 动态上下文注入 / 审计变更）
    - ==组装顺序==（base → personality → mode → approval → project_context → skills → context_mgmt → handoff）+ ==越靠后越动态==（稳定层进 cache，变化层不进）
    - ==三层文件覆盖==（内置 < 用户级 < 项目级，整文件替换）+ 与 [[Agent Skills 体系]] 三层加载同构
    - ==PromptAssembler 实现==（三层查找 + 动态变量替换）
    - 4 条关键设计原则（base 必须含 `## Language` / 动态内容靠后 / 整文件替换 / 变更审计）

  - **[[Agent 工程实践]] §八-B 回滚机制**扩写"==Side-History Git Snapshot=="(~60 行):
    - ==独立 git 仓库不写用户 `.git`==（不污染项目历史 / 不破坏 index 状态 / 可随时清理）
    - ==三种快照类型==（pre-turn / post-turn / pre-restore）+ 用途对照
    - Python 实现（`create_snapshot` + `restore_snapshot`）
    - ==与 LangGraph checkpoint 的对比==（checkpoint 存 Agent 状态，side-history 存文件系统状态，两者互补）
    - `revert_turn` 纳入 HITL 危险工具链（恢复文件是不可逆操作）

  - **[[Function Calling]] §三 image 章节末尾**新增"==用户输入图片 + 历史裁剪=="(~60 行):
    - ==用户输入图片 vs 工具返回图片==（消息角色 / 触发方式 / 典型场景对比）
    - Anthropic user content 格式示例
    - ==图片预处理三步==（压缩缩放 / alpha 铺白底 / 注入元信息）
    - ==本轮图片优先原则==（不能用历史旧截图替代当前图片）
    - ==历史 image payload 裁剪==（新 turn 省略历史图片只保留文本元信息）+ Python `prune_image_history` 实现
    - `reasoning_content` 也不回传进历史（只写日志）

  - **[[Agent 工程实践]] §七 前**新增"==§七-A 异步后台任务与 Runtime API=="(~70 行):
    - ==Durable Task Queue==（SQLite 持久化 + 任务生命周期 enqueued→running→completed/failed/canceled）+ SQL schema
    - ==Worker Pool==（默认 2 个后台 worker，共享 ToolRegistry/MCP/Skills，各自独立 Memory/HITL）
    - CLI 闭环（`/task` / `/task add` / `/task cancel` / `/task log`）
    - ==Runtime API==（`POST /v1/threads` / `POST /v1/threads/{id}/turns` / `GET /v1/threads/{id}/events`）
    - ==与 OpenAI Assistants API 同构==（Thread/Run/SSE events 对应 Thread/Turn/SSE events）
    - 安全：强制 API Key + 不暴露公网
    - 典型场景（CI/CD 集成 / IDE 插件 / 脚本自动化）

  ==关键认知==（面试题区分度高）:
  - ==本地 CLI Agent 不做沙箱不是偷懒==——沙箱削弱能力 + 虚假安全感 + 体验差；真正隔离用 microVM（Devin/Modal）
  - ==CommandGuard 是 HITL 之前的快速拒绝==——明显危险命令不弹窗直接拒绝，减少 HITL 骚扰
  - ==Prompt 分层架构的组装顺序决定 cache 效率==——稳定层靠前进 cache，动态层靠后每轮变
  - ==Side-History Git 独立仓库==——不污染用户 .git，与 LangGraph checkpoint 互补（状态 vs 文件系统）
  - ==历史 image payload 必须裁剪==——旧截图反复进 context 浪费 token，新 turn 只保留文本元信息
  - ==Durable Task Queue + Runtime API 是 Agent 服务化的关键==——SQLite 持久化 + OpenAI Assistants API 同构设计

  **原因**：用户的 4 个期次 + 1 个补充期次描述了 PaiCli 的多个功能，拆开看有 5 个通用工程模式 wiki 完全空白（沙箱认知 / Prompt 分层 / Side-History / 用户图片输入 / Durable Task Queue）。这些都是 Claude Code / Cursor / Devin / OpenAI Assistants 都有的机制，wiki 0 覆盖是重大缺口。

- ==新建 `wiki/LLM/LSP 与代码诊断.md`==(566 行)——回应用户对"第十七期：LSP 诊断注入"的"这个部分的内容有吗"。判断:6 个 PaiCli 功能聚焦在==两个 wiki 完全空白的话题==(LSP 协议本身 + Post-Edit 诊断回注模式),独立成文:

  - **§一 LSP 是什么 + 为什么会出现**:M×N 困境 → LSP M+N 解法 + Microsoft 2016 提出 + 与 [[MCP 协议]] 同源类比(都是 JSON-RPC 解决 M×N)
  - **§二 LSP 协议核心**:通信机制(JSON-RPC over stdio)+ ==核心方法对照表==(initialize / textDocument/didOpen / textDocument/didSave / ==publishDiagnostics 主动推送== / definition / references / hover / completion / codeAction / rename / workspace/symbol)+ 一次诊断的完整流程(7 步时序)+ 最简 LSP Client Python 代码
  - **§三 主流 LSP Server 生态**:9 个语言 server 对照表(JDT LS / rust-analyzer / pyright / gopls / typescript-language-server / clangd / kotlin-language-server / omnisharp / sourcekit-lsp)+ 2026 趋势(AI 友好化 batch API)
  - **§四 LSP vs MCP:JSON-RPC 双胞胎**:6 维对照表(提出时间 / 连接对 / 协议基础 / 核心方法 / 推送机制 / 生态成熟度)+ ==MCP 明显借鉴 LSP 设计==的认知 + ==LSP 服务通过 MCP 暴露给 LLM== 的桥接模式(langserver-mcp 等)+ 内置 vs MCP 桥接的选型
  - **§五 Coding Agent 的诊断回注模式**:==Post-Edit 触发时序==(write_file → 异步触发 → pending 队列 → 下一轮注入)+ 关键设计 4 点(异步不阻塞 / 缓存 pending / 下一轮注入 / 格式化为人类可读)+ ==三种推理模式都注入==(ReAct 每轮 / PE 每 Step / Multi-Agent Worker+Reviewer)+ 异步 vs 同步等待对比 + 与 [[Reflection 实现#3.3 外部验证器]] 的落地关系
  - **§六 诊断格式化与容量管理**:`[error] file:line:col - message` 标准格式 + ==默认上限 20 条==的理由(token 预算 / 注意力稀释 / cache 稳定性)+ 优先级排序代码(severity > 当前文件 > 行号 > 重复抑制)+ ==注入后清空 pending 队列==
  - **§七 渐进式实现策略**:MVP(JavaParser / tree-sitter,覆盖 60% 语法错)→ v1(1-2 核心 LSP)→ v2(全语言)+ JavaParser 代码示例 + MVP 局限(只查语法,不查类型/跨文件)+ 升级工程成本
  - **§八 与 Reflection / Generator-Evaluator 的关系**:诊断回注 = 外部验证器具体落地 + Generator-Evaluator 架构具象 + ==LSP vs LLM-as-Judge vs 自我反思==三方对比(确定性 / 成本 / 速度)
  - **§九 关键认知与面试要点**:5 条认知 + 高质量答案模板

  ==同时更新==:
  - [[Reflection 实现]] §3.3 外部验证器末尾加 callout 指向 [[LSP 与代码诊断#五]]
  - [[Coding Agent 工具集]] §2.2 代码搜索类末尾加一行 LSP 协议说明 + 诊断回注链接
  - [[LLM索引_MOC]] Stage 3 加第 17 项(原 17-22 顺延为 18-23,Stage 4 顺延为 24-25)

  ==关键认知==(面试题区分度高):
  - ==LSP 与 MCP 是 JSON-RPC 双胞胎==——MCP 明显借鉴 LSP 设计(capability 协商 / notifications),MCP 是 LSP 思想在 Agent 时代的演化
  - ==Coding Agent ≠ IDE,诊断处理方式根本不同==——IDE 给人看,Agent 把诊断回注 LLM 让它自己修
  - ==Post-Edit 诊断必须异步不阻塞==——阻塞等 LSP 分析(1-5s)体验劝退,异步缓存 pending 队列是生产标配
  - ==诊断容量管理是 prompt 工程==——默认 20 条 + 优先级排序,避免 1000 条 flood prompt
  - ==渐进式实现==——MVP 轻量解析器先上,验证模式有价值再接完整 LSP
  - ==LSP 诊断比 LLM-as-Judge 更准更便宜更快==——确定性工具验证是 Reflection 外部验证器的最佳落地

  **原因**:用户的第十七期描述==聚焦在两个 wiki 完全空白的话题==——LSP 协议本身(wiki 仅在 Coding Agent 工具集提了一句)和 Post-Edit 诊断回注模式(Reflection §3.3 只讲了思想没讲落地)。LSP 是 Coding Agent 调用语言能力的核心协议,与 MCP 同源同设计,Cursor / Claude Code / Cline / Aider 都做诊断回注,wiki 0 覆盖是重大缺口。独立成文是因为 LSP 是完整协议主题(与 [[MCP 协议]] / [[Function Calling]] 同级),体量足够(566 行)。

- ==新建 `wiki/LLM/Coding Agent TUI 设计.md`==(550 行)——回应用户对"第十六期：TUI 产品化"的功能盘点。判断:用户提到 v16.1 的双形态 Renderer + inline/lanterna/plain 三实现,==这是 wiki 完全空白的"Coding Agent 的门面"领域==,与现有 [[Agent 工程实践#三、CLI 设计]]==正交但不同==——那个讲"被 AI 调用的 CLI",这个讲"AI 给用户用的 TUI",混在一起会乱:

  - **§一 Coding Agent 的产品形态光谱**:5 种形态(非交互 CLI / inline 流式 TUI / 全屏 TUI / IDE 插件 / 桌面应用)+ 与 [[AI 编程工具]] 的承接关系(本文是终端形态内部的细分)
  - **§二 TUI 三种渲染形态**:
    - inline 流式 TUI(主流,Claude Code / Qoder / Aider 风格)——主屏直出 + 流式 token + 底部 dock + 折叠块 + 行内 diff
    - lanterna 全屏 TUI(三栏 / 模态弹窗)
    - plain 兜底(CI / 单测 / 不支持 ANSI 终端)
    - 三形态对比表(默认推荐 / 终端兼容 / 流式体验 / CI 友好 / 学习曲线 / 实现复杂度)
  - **§三 Renderer 与 Agent 核心解耦**:==关注点分离架构图==(三种 Renderer 共享 Agent / ToolRegistry / MemoryManager / MCP / Skills / HITL 核心)+ Renderer 接口长什么样的 Java 接口示例 + ==共享什么不共享什么==的对照表 + ==测试友好性==(plain Renderer 让单测能跑完整 Agent 循环)
  - **§四 流式渲染的工程细节**:
    - LLM token 流式拼接(代码块缓冲 / markdown 表格缓冲 / 状态栏冲突 / 输入提示符冲突)+ Python 实现示例
    - 工具调用块的折叠展开(`Read 3 files (ctrl+o to expand)`)+ 实现要点
    - 行内 git diff(Myers diff + ANSI 着色 + 行号 + 大 diff 折叠)
    - ==状态栏更新与正文不冲突==(JLine `Status` API 工作原理 + 4 个常见坑)
  - **§五 交互式 HITL 的 UI 设计**:
    - inline 单字符 prompt(`[y/n/a/s/m]` 5 个含义 + 风险等级)
    - lanterna 模态弹窗
    - plain 逐行 prompt(==非交互场景默认拒绝==避免 CI 卡死)
    - 三种 UI 的设计共识(关键信息显眼 / 选项快捷化 / 默认安全 / 高危显示风险)
  - **§六 运行时切换推理模式**:`/plan` / `/team` / `/cancel` 命令 + UX 要点(当前模式可见 / 切换不丢上下文 / 优雅终止 / 模式专属 UI)+ ==`/cancel` 协作式取消实现==(检查点密集 + 输出当前进度 + 清理资源)
  - **§七 终端兼容性与降级**:5 个环境变量降级开关(`NO_COLOR` / `_NO_STATUSBAR` / `_RENDERER` / `TERM=dumb`)+ 自动检测降级代码(非 TTY / CI / dumb terminal → plain)+ ==配置演进 deprecated 处理==(不删旧开关 / stderr 警告 / 文档明确生命周期)
  - **§八 主流产品 TUI 风格对比**:Claude Code / Codex CLI / Qoder / Aider / Goose / Cline / Cursor terminal / PaiCli 8 个产品对照表

  ==同时更新==:
  - [[LLM索引_MOC]] 第 5 节"AI 产品形态"导航加入新文档 + Stage 4 学习路径加第 24 项
  - [[AI 编程工具]] §2.1 形态对比表后追加 callout 指向 [[Coding Agent TUI 设计]]

  ==关键认知==(面试题区分度高):
  - ==两种 CLI 不是一回事==——被 AI 调用的工具型 CLI(JSON 输出) vs 给用户用的交互式 TUI(流式 + 折叠 + 状态栏),不要混
  - ==Renderer 抽象是生产 Coding Agent 的核心==——三形态共享 Agent / Tool / Memory / MCP / Skills / HITL 核心,只是 UI 呈现不同
  - ==HITL 决策在核心层,UI 在 Renderer 层==——同一个决策,inline 单字符,lanterna 模态,plain 回车,==决策结果一致==
  - ==永远不要假设支持高级特性==——降级机制(NO_COLOR / 非 TTY / CI 检测)是 TUI 工程核心
  - ==协作式取消优于强杀==——`/cancel` 设 flag 而非杀线程,在 checkpoint 退出 + 输出当前进度
  - ==inline 流式是事实标准==——Claude Code / Qoder / Aider / Goose / PaiCli 默认都这个形态

  **原因**:用户的第十六期描述==大量是 PaiCli 实现细节==(JLine Status / lanterna 三栏 / `PAICLI_RENDERER` 等),但拆开看 ==Renderer 抽象 / 三形态对比 / 流式渲染细节 / HITL UI 设计 / 终端降级 / 协作式取消== 都是 ==生产级交互式 Coding Agent 通用工程经验==。Claude Code / Qoder / Cursor / Aider 都有同等机制,wiki 完全空白,这是个生态级缺口。独立成文是因为内容体量足够(~550 行)且与现有 [[Agent 工程实践#三、CLI 设计]] 是正交概念——混在一起会让两类 CLI 概念混乱。

- ==第十五期 Skill 系统在 [[Agent Skills 体系]] 增量补 5 处==——回应用户的"这一部分的内容呢"。8 个 PaiCli 功能==聚焦度极高,全在 Skills 一个文档==。174 → 401 行(新增 ~227 行):

  - **§二 渐进式披露 末尾**新增"==索引段预算=="(~25 行):
    - ==启用 skill 上限~20 个==(超过选择困难)、==索引段 ≤ 4KB==(避免吃掉 system prompt 预算)、==单 skill 索引 ≤ 200 字符==
    - 索引段示例 + ==prompt cache 友好==(索引稳定不变和 system prompt 一起进缓存,呼应 [[长上下文工程#prompt-caching]])
    - 上限超出的==正确做法==(按使用频率/最近调用排序取前 N,不是粗暴截断)

  - **§四 SKILL.md 加载机制 末尾**新增"==专用 load_skill vs 通用 view=="(~30 行):
    - 6 维对照表(LLM 输入 / 路径错误率 / 三层位置查找 / 内容截断 / 注入位置 / 缓存友好)
    - ==专用工具核心价值==:把"如何加载"复杂性从 LLM 转移到 Host,LLM 只做语义判断
    - ==SkillContextBuffer 设计==:不直接拼到 tool message,而是缓存到下一轮 user message 前置——既不污染 tool result,又保证"指导位置"

  - **§九 安全机制 末尾**新增"==与 HITL 的协同:Skill 不引入新审批维度=="(~30 行):
    - ==关键设计原则==:Skill 是"使用说明书"不是"权限提升",Skill 内调用工具走该工具既有 HITL 规则
    - 4 条不引入 Skill 维度的理由(避免授权矩阵爆炸 / Skill 是行为模板不是身份 / 审批本质是工具 / 简化心智模型)
    - ==反面教材==:某些产品给 Skill 单独"信任"开关导致预期与实际不符
    - 与 [[Agent 工程实践#hitl-工具读写粒度细化]] 的关系:HITL N 维矩阵 vs Skill 指令模板层正交

  - **§十二 目录位置 改写**==两层 → 三层加载位置 + 覆盖原则==(~25 行):
    - ==新增内置层==(jar 内 / 安装目录 = 产品出厂默认 skill)+ 用户级 + 项目级
    - ==同名 skill 整体覆盖==的理由(字段级合并语义模糊,行为不可预测)+ 派生模式(复制目录再改)
    - 与 [[长上下文工程]] CLAUDE.md 项目级/用户级覆盖思路一致

  - **新增 §十四 "==典型案例:web-access skill=="**(~110 行):
    - ==目录结构==(SKILL.md + 6 个站点经验 + cdp-cheatsheet)
    - ==SKILL.md 内容结构==(浏览哲学四步法 / 工具选择表 / 浏览器优先级 / Jina 兜底)
    - references 子文档示例(mp.weixin.qq.com 经验:防爬墙特征 + 推荐流程 + 已知坑)
    - ==决策手册型 vs 流程型 skill== 对照(web-access / code-review 是手册型,ppt-generation / deploy 是流程型)
    - 这个 skill 体现的 4 个关键设计点(渐进式披露 / 排除条件 / 复用宿主工具 / 沉淀机制)

  - last_reviewed 更新 2026-05-09 → 2026-05-28

  ==关键认知==(面试题区分度高):
  - ==索引段是 prompt cache 友好的==——稳定不变,跟 system prompt 一起进缓存,真实正文懒加载
  - ==专用 load_skill 把复杂性从 LLM 转移到 Host==——LLM 只做语义判断,生产 Coding Agent 都用专用工具
  - ==SkillContextBuffer 注入到下一轮 user 消息前==——而不是 tool result,避免污染又保证指导位置
  - ==Skill 不引入新审批维度==——避免授权矩阵爆炸,Skill 是行为模板不是身份
  - ==同名 skill 整体覆盖不字段级合并==——行为可预测优先于灵活性
  - ==决策手册型 skill== vs ==流程型 skill== 是 Skill 设计的两大分支

  **原因**:用户的第十五期描述==聚焦度极高==,8 个功能基本都在"Skill 系统"这一个领域,前几期"分散在 3-4 个文档"的模式不适用。这次拆开看 wiki 现有 [[Agent Skills 体系]] 在 ==索引预算 / 专用工具 vs view / Skill 与 HITL / 三层加载 / 典型案例== 5 个点都欠覆盖——尤其==索引段大小限制 / SkillContextBuffer / Skill 与 HITL 不引入维度==是生产实践细节,Anthropic / Cursor / Claude Code 都做但很少有人系统讲。本次集中补 [[Agent Skills 体系]],让这个文档==从"概念入门级"升级到"生产级实现=="。

- ==第十四期 CDP 会话复用 + 登录态访问拆 3 处补到 3 个文档==——回应用户的"这又是哪些方面的知识点"。8 个 PaiCli 功能聚焦在==浏览器 MCP 进阶模式==,5 个核心知识点 wiki 完全空白:

  - **[[MCP 协议]] §5.4 浏览器自动化**==大幅扩写==(~150 行):
    - 新增 chrome-devtools-mcp 到 server 列表(Google 官方 2025,2026 事实标准)
    - **==底层协议:Chrome DevTools Protocol (CDP)=="子节**——是所有浏览器 MCP 的共同底层(puppeteer/playwright/chrome-devtools-mcp 都是 CDP 客户端封装)+ macOS/Linux/Windows 启动命令 + 探活 endpoint(`127.0.0.1:9222/json/version`)+ MCP↔CDP 关系图
    - **"==isolated vs shared:两种工作模式=="子节**——核心设计选择对照表(profile / 登录态 / 适用 / 安全风险)+ ==isolated 默认的 fail-safe 理由==+ shared 切换流程(用户开 `chrome://inspect/#remote-debugging` → PaiCli 探活 → 切换 + 清空"全部放行")
    - **"==登录态访问的三种方案=="子节**——CDP 复用(主流) / Cookie injection / OAuth + token 持久化对比表
    - **"==Tab 所有权追踪:共享浏览器时的隔离原则=="子节**——`AGENT_OWNED_TARGETS` 标记代码 + ==不可证明则拒绝==的 fail-safe 原则 + 通用模式扩展(Multi-Agent 共享资源)

  - **[[Coding Agent 工具集]] §2.5 联网类**追加"==浏览器模式自适应=="(~25 行):
    - Agent 遇登录页/权限不足时主动调 `browser_connect` 切 shared 的典型流程(GitHub PR 例子)
    - 关键设计原则:==公开页面不提前切==(避免身份暴露)+ ==只在 Agent 判断需要时切==+ 切换清空"全部放行"

  - **[[Agent 工程实践]] HITL 章节**新增 3 个子节(~120 行):
    - **"==HITL 工具读/写粒度细化=="**:==粒度 2:读型 vs 改写型工具==(take_snapshot 仍 Always Allow / click·fill_form·evaluate_script 回退单步)+ ==敏感页面正则匹配==(`/checkout`、`/payment`、`/admin`、`/transfer` 等)+ `needs_step_approval` 决策代码 + ==HITL 是 N 维矩阵==(粒度/读写/敏感度/可逆性)
    - **"==模式切换 = 会话边界=="**:==上一节"会话级失效"原则的扩展==——浏览器模式切换 / MCP server 重启 / 工作目录切 / 模型切 / `/permissions reset` 都视为会话边界,==身份/权限/信任前提任何一个变化都重置授权==(保守 fail-safe)
    - **"==审计日志 schema 演进与向后兼容=="**:5 条兼容原则(只加不删 / 不改类型 / 新字段可选 / schema 版本号显式 / 枚举只增不减)+ 读取端 `.get()` 兼容代码 + ==踩过的坑案例==(args 从 dict 改 base64 string 导致半年合规数据丢失)+ ==审计日志比 API 演进更严格==(写出去就改不了)

  ==关键认知==(面试题区分度高):
  - ==chrome-devtools-mcp / puppeteer / playwright 共同底层都是 CDP==——本质都是 CDP 客户端的 MCP 封装
  - ==isolated 默认是 fail-safe==——除非用户主动切 shared,Agent 不会拥有用户身份
  - ==登录态主流方案是 CDP 复用==——零迁移成本 + 零站点适配,合规要求高才上 OAuth
  - ==Tab 所有权"不可证明则拒绝"==——是 Multi-Agent 共享资源的通用模式
  - ==HITL 是 N 维矩阵不是单维开关==——粒度 / 读写 / 敏感度 / 可逆性叠加判定
  - ==模式切换=会话边界==——身份/权限范围变就必须重置授权,避免默默升权
  - ==审计日志 schema 比 API 更严格==——写出去就改不了,只能加字段不能改字段

  **原因**:用户的第十四期描述==看似都是 PaiCli 浏览器命令==(`/browser` 命令组、isolated/shared 切换、敏感页面规则),但拆开看 ==CDP 协议层 / isolated-shared 模式 / 登录态访问方案 / Tab 所有权 / HITL 读写粒度 / 模式=会话边界 / 审计 schema 演进== 都是 ==生产级 Agent 通用工程经验==。Cursor / Claude Code / Cline 都有同等机制,wiki 0 覆盖,这是重大缺口。本次拆 3 处补到 3 个文档,==避免新建一个"浏览器模式专题"==(因为内容是已有章节的深化,不是新独立主题)。

- ==第十三期 Chrome DevTools MCP 拆 4 处补到 4 个文档==——回应用户的"这个是哪个部分的内容呢有相关的知识吗"。8 个 PaiCli 功能拆开看分散在 ==4 个领域==,不独立成文,补到对应 wiki:

  - **[[Coding Agent 工具集]] §2.5 联网类**新增"==fetch_url 局限 + 分级 fallback=="(~25 行):
    - fetch_url 处理不了的三类页面(SPA / 防爬墙 / 需交互)
    - ==分级 fallback 流程==(轻量 fetch_url → 失败 → 重量浏览器 MCP)
    - 成本对比表(启动/延迟/内存/输出)+ ==失败站点黑名单==(微信公众号 / 知乎专栏 / 推特 / 小红书命中后直接走浏览器 MCP)
    - 链接到 chrome-devtools-mcp 和 image tool result

  - **[[Function Calling]] §三 四种消息角色**新增"==Image Tool Result:多模态返回=="(~80 行):
    - 三家厂商 image content 格式(==Anthropic content blocks 数组== / ==OpenAI image_url + data:image/png;base64== / Qwen-VL 兼容 OpenAI)
    - ==text fallback 必须保留==的 4 个理由(模型不支持 / 审计日志 / 成本敏感 / 上下文压缩)
    - 生产实现 `normalize_tool_result` 函数(根据 LLM 能力动态选 image / text)
    - 典型 Browser MCP 工具返回结构(image + text + snapshot 三段式)
    - ==Token 计费陷阱==(1024×1024 截图 ~1500 tokens,5-10 张截图就几万 token)+ DOM snapshot 优先策略

  - **[[MCP 协议]] §3.5 新增"==Server 生命周期与异步启动=="**(~110 行):
    - ==initialize 握手 3 步流程==(initialize → 协商 capabilities → notifications/initialized 完成)
    - ==错误做法 vs 异步并发启动==代码对比(同步串行 5 个 server × 2s = 10s 劝退 vs 异步 + 首屏不阻塞)
    - ==两层超时设计==(CLI 首屏 8s vs Server initialize 60s,慢 server 不被错杀)
    - ==Server 状态机==(init → starting → ready / failed)+ `/mcp` 命令显示状态
    - ==健康检查 / 自动重启策略==(心跳 / 指数退避 1s→5s→30s→5min / 5 分钟 5 次上限进 failed / 手动重启重置计数器)

  - **[[Agent 工程实践]] HITL 章节**新增"==HITL 三档授权粒度=="(~60 行):
    - ==三档对照表==(单次确认默认 / Server-工具级"全部放行" / 全局信任模式)+ 失效时机
    - 典型使用流(用户选 [A] 全部放行 → chrome-devtools 后续 N 次调用直接执行)
    - ==5 条设计原则==(粒度可配 / 高危操作绕过 / 会话级失效 / 显式撤销 `/permissions reset` / 审计仍要记录)
    - 安全权衡表(单次 vs Always Allow 的安全/体验/失控代价对比)
    - ==与工具白名单的互补关系==(白名单声明式开发者定 vs HITL 运行时用户定)

  ==关键认知==(面试题区分度高):
  - ==fetch_url 的局限==——主流 Coding Agent 都内置失败站点黑名单 + 分级 fallback
  - ==tool_result 不止 text==——image 类型是 Browser/Computer Use 闭环的关键协议,但 ==text fallback 必须永远保留==
  - ==MCP 异步启动是必做==——同步串行启动 5 个 server 等 10s 用户劝退;两层超时(8s/60s)兼顾体验和容错
  - ==HITL 不是"每次问"==——三档粒度按风险分级,==Always Allow 下高危操作仍单独确认==

  **原因**:用户的第十三期描述==看似都是 PaiCli 产品功能==,但拆开看有 4 个是 ==生产 Coding Agent 通用工程经验==(Cursor / Claude Code 都有同等机制):分级 fallback / image tool result / MCP 异步启动 / HITL 三档粒度。这次拆 4 处补到 4 个文档,==避免新建一个"chrome-devtools 专题"文档(因为本质是分散经验,不是单一主题)==。

- ==新建 `wiki/LLM/长上下文工程.md`==(518 行)——回应用户对"第十二期：长上下文工程"的功能盘点。判断：用户提到 8 个 PaiCli 产品功能，==其中 2 个核心话题 wiki 完全空白==(grep "prompt cache" 全 wiki 0 条结果)，必须独立成文：

  - **§一 Prompt Caching 完整篇**：三家厂商对比表（Anthropic 手动 cache_control / DeepSeek 自动 prefix / OpenAI 自动 / Gemini CachedContent / Qwen-GLM-Kimi 类 DeepSeek）+ ==Anthropic `cache_control` 4 条规则==（最多 4 个缓存点 / 前缀必须完全一致 / 顺序敏感 / 最小 1024 token）+ ==缓存命中率经济学==（一次 20 轮会话 ¥1.8 → ¥0.28 省 84%）+ ==分层缓存策略==（system/tools 最稳 → CLAUDE.md → 历史摘要 → 当前消息）+ 三个常见失效坑（时间戳进 prompt / tools 顺序变 / 消息合并不一致）

  - **§二 模型能力声明与动态预算**：`LlmClient` 接口 5 个能力声明（maxContextWindow / supportsPromptCaching / promptCacheMode / recommendedOutputTokens / supportsParallelToolCalls）+ ==2026 主流模型 window 对照表==（Claude 200K-1M / GPT-5 200K / DeepSeek V4 1M / Gemini 1M+ / GLM-5.1 200K / StepFun 256K / Kimi K2.6 256K / Qwen3-Max 256K）+ ==80% Rule 动态预算==（budget = 0.8 × window - output - safety,允许系统属性覆盖）

  - **§三 Context Mode 切换策略**：==short/balanced/long 三档==(历史压缩 / RAG topK / Resources 索引联动) + 模式驱动的 5 维下游策略矩阵 + ==自动 vs 手动选择==(window<50k 强制 short / window≥500k 命中关键词建议 long)

  - **§四 Long Context 下的 RAG topK 自适应**:Window vs topK 对照（32K→3-5 / 200K→10 / 1M→20-50）+ ==`top_k=None` 默认参数让自适应不被覆盖==的踩坑 + ==实时 grep 优先 + RAG 是补充==的 Coding Agent 主路径

  - **§五 MCP Resources 在 Long Context 下的索引注入**:long mode 下==只注入 URI + description 不注入正文==(token-friendly,50 个 resources × 10 token = 500 token)+ short/balanced 不注入的理由(window 紧 + cache 命中率下降 + 大部分任务用不到)

  - **§六 Token 与成本可观测性**:5 个必显指标(total / cached / hit rate / output / 成本)+ ==状态栏 vs 正文输出区==(底部状态栏不污染正文)+ ==`/context` 命令==暴露 Harness 内部完整状态

  - **§七 关键认知与面试要点**:5 条认知 + 一段高质量答案模板("==你们 Agent 怎么处理长上下文?==")

  ==关键洞察==(面试题区分度高):
  - ==Prompt Caching 是省钱第一杠杆==——Coding Agent 不开 cache 字面意义烧钱(命中率 85%+ 省 80%+)
  - ==模型能力 self-aware 是多模型 Agent 的基础==——Harness 不能写死参数
  - ==Context Mode 是"换一种代价"==——long 不是更高级,80% 任务 balanced 够用
  - ==Long Context 下 RAG topK 必须自适应==——topK=5 跑 1M 模型召回率太低,topK=50 跑 32k 挤爆 prompt
  - ==MCP Resources 索引注入只塞 URI + 描述不塞正文==——token-friendly 优化

  ==同时更新==:
  - `LLM索引_MOC.md` Stage 3 学习路径加入第 22 项(原 22→23 顺延)
  - 第二节"工程框架"导航补"长上下文工程"条目

  **原因**:用户的第十二期描述==大量是 PaiCli 产品自定义==(LlmClient 接口 / `/context` 命令 / 状态栏布局),但拆开看==Prompt Caching、动态预算、Context Mode 切换、topK 自适应、Resources 索引注入==都是 ==2025-2026 生产级 Agent 通用工程方法论==。Prompt Caching 在 wiki 0 覆盖却是 Coding Agent 成本第一杠杆——不补就是 wiki 重大缺口。这次补完后 wiki 才==真正覆盖了"模型选型 → 路由 → 长上下文工程"完整成本/能力优化链路==。

- ==`wiki/LLM/MCP 协议.md` 大补 §四 Host 集成层 + §6.3 凭证脱敏 + §二 transport 升级到 Streamable HTTP==——回应用户对"第十期：MCP 协议核心"的功能盘点。==703 行（新增 ~150 行）==。判断：用户提到的是 PaiCli 产品功能，但拆开看==有 6 点是 MCP 协议本身的工程经验==（不是产品自定义）；产品层（`@`引用 / `/cancel` / `/mcp` 命令）放到 PaiCli 项目目录，不进 MCP 协议 wiki：

  - **§二 transport 章节升级**：==旧 SSE 改为 Streamable HTTP==（2025 MCP 规范变更，单 endpoint POST + chunked，取代 2024 双 endpoint SSE），加 client 代码示例 + Server 配置 `command` vs `url` 区分

  - **§四 全新章节"Host 集成层：MCP → LLM tools 的转换与维护"** ~120 行：
    - ==4.1 工具命名空间== `mcp__{server}__{tool}` 前缀（避免命名冲突 + LLM 看出工具来源 + 审计追踪）
    - ==4.2 Schema 清洗==——三大坑（`$ref` Anthropic 直接 400 / 深嵌 `anyOf` / 长 description）+ ~20 行 `clean_schema` 实现
    - ==4.3 Resources 暴露为虚拟工具==——`list_resources` + `read_resource` 两个虚拟工具（解决 LLM 只懂 tool_calls 不懂 Resource 协议的工程妥协）
    - ==4.4 notifications 处理==——`tools/list_changed` / `resources/list_changed` / `resources/updated` 三种 notification 的实现（实现"运行中加挂工具不重启 Host"）

  - **§6.3 审计日志凭证脱敏**：~50 行 `redact_for_audit` 实现，==key 名集合屏蔽==（token/key/password/secret/authorization）+ ==value 模式正则==（Bearer xxx / sk-... / ghp_... / xox*-...）+ 嵌套 dict 递归 + 真实代价说明（log 一旦落 ELK 就要批量轮换）

  - 后续章节 §五-§十 全部顺延 1（原 §四 → §五 / 原 §九 → §十）

  ==关键认知==（面试题区分度高）：
  - ==Streamable HTTP 取代 SSE== —— 2025 协议本身演进，wiki 不更新就过时
  - ==`mcp__` 前缀 / Schema 清洗 / Resources 虚拟化== 都是 ==Host 端必踩坑==——讲清这些区分"用过 MCP"和"实现过 MCP 集成"
  - ==notifications 让 MCP 工具能热更==——这是 Cursor / Claude Code 体验流畅的隐藏机制
  - ==Anthropic 不支持 `$ref`==——这是早期 GitHub MCP server 集成失败的真实坑

  **原因**：用户的第十期描述==大量是 PaiCli 产品自定义功能==（命名空间 / HITL / 默认配置等已在 wiki 其他位置覆盖），但==其中 6 个点是 MCP 协议本身的工程经验==——把它们抽出来落地到 MCP 协议 wiki，让"实现 MCP Host"的人有完整参考。这次补完后 MCP 协议 wiki 才==真正覆盖了从协议→Host 集成→生态→安全的完整生产视角==，前面只讲了"是什么"和"用什么"，==缺了最值钱的"集成时怎么做"==。

- ==`wiki/LLM/Coding Agent 工具集.md` 补充 §4.8 联网工具的 SSRF 防御==——回应用户对"第九期：联网能力 + Web 工具"的延伸提问，补全 ==Agent 时代联网安全==这个生产必踩点：

  - **三种典型攻击场景**对照表（==`http://169.254.169.254/` 偷云 IAM 凭证== / `file:///etc/passwd` 读敏感文件 / `http://localhost:6379/` 攻击内网服务）+ 攻击链梳理（==Prompt Injection 控制 LLM 输出 URL==→ Agent 用自己的网络身份发请求）
  - **五道防线完整实现代码**（~30 行 Python `safe_fetch`）：协议白名单 / DNS 解析后查 IP（防 ==DNS rebinding==）/ 重定向手动追 / 大小限制 5MB / 频率限制
  - **Agent 场景特殊考量**：URL 来源标记（LLM 输出的 URL 比用户输入多一道检查）/ ==network namespace 沙箱网络==（默认禁内网，纵深防御）/ 凭证隔离（Agent 不持有云元数据访问权限）/ 审计日志
  - 在 §2.5 联网类==开篇加一行安全提示==指向新章节，避免读者读完普通用法就跑

  ==关键认知==（面试题区分度高）：
  - ==Agent 时代 SSRF ≠ 传统 SSRF==——LLM 输出的 URL 可被 Prompt Injection 控制，新攻击面
  - ==DNS rebinding 是隐藏雷点==——只查域名白名单不够,必须解析后再查 IP
  - ==重定向不能让 requests 自动跟==——第一跳合法，第二跳指向内网就完蛋

  **原因**：用户的第九期功能描述涵盖了 web_search/fetch_url 的功能/选型/限流，==但 SSRF 这个 Agent 上线前必过的安全审计项没专门讲==。Anthropic Claude/OpenAI Browse 都内置了这套防护，==是"懂 LLM"和"懂 LLM 安全"的分水岭面试题==。

## 2026-05-27

- `wiki/LLM/Agent 工程实践.md` ==§二 Multi-Agent 架构 新增两个子节==——回应用户引用的"第五期：Multi-Agent 协作 + 角色分工"功能（三角色 Planner/Worker/Reviewer + 主从 Orchestrator + 审查不通过反馈重试 + 冲突自动解决）。盘点确认 wiki 大部分概念分散覆盖在 4 个文档中，==但"三角色经典模式"集中讲解和"冲突仲裁"两个点是缺的==：

  - **新增子节"==经典三角色组合：Planner + Worker + Reviewer=="**：
    - 三角色职责定义对照表（==每个角色对应已有 wiki 模式==）
    - ==关键认知：三角色 = Plan-and-Execute + Reflection 的产品化封装==——拆开看每个都是已有概念的组合
    - Reviewer 反馈 → Worker 重试的完整协议代码（feedback_history 结构化反馈 + 重试上限固定 + 失败带上下文上报）
    - Reviewer 实现两种选择（同模型 Self-Reflection vs 独立 Critic Model）+ ==生产推荐"Worker 用 Sonnet, Reviewer 用 Opus"==

  - **新增子节"==多 Agent 冲突仲裁=="**：
    - 四种仲裁策略对照表（多数投票 / 权威优先 / Meta-Agent 仲裁 / 辩论收敛）
    - 冲突类型与对应策略映射（事实冲突 / 推理冲突 / 风格冲突 / 评估冲突）
    - 工程要点（默认多数投票 + Meta-Agent 兜底 + 仲裁过程审计 + 辩论上限 3 轮）
    - ==与单 Agent 失败处理的区别==——单 Agent 失败 vs 多 Agent 仲裁是两个互补维度

  ==关键洞察==（值得面试时讲）：
  - ==三角色不是新概念== —— 是 Plan-and-Execute（Planner+Worker）+ Reflection（Reviewer）的产品化
  - CrewAI / AutoGen / Anthropic Multi-Agent Research 都是这套
  - ==仲裁与重试的边界==：单 Agent 内部出错走 L1-L4 决策，多 Agent 结论冲突走仲裁

  Agent 工程实践.md 从 665 → 773 行（新增 ~108 行）。

  **原因**：用户引用的第五期功能描述==每条都能映射到已有 wiki==，==但"Planner+Worker+Reviewer 三角色经典模式"和"多 Agent 冲突仲裁"==没有专门讲解，分散覆盖让读者难以系统理解。本次补全把"三角色 = Plan-and-Execute + Reflection 组合"这个==关键认知顶到首位==，把"==四种仲裁策略=="作为单 Agent 失败处理的互补维度——读者读完能讲清"==CrewAI 的 Multi-Agent 不是黑魔法,是已有模式的组合=="，这是 Agent 工程师面试时区分概念党与判断派的关键

## 2026-05-25

- ==RAG基础与架构.md 大重构==——回应用户审计后的拆分方案+ "文档解析放哪"的归属讨论。==1165 → 217 行（消减 81%）==，==§三/§四/§五 三大章节内容外迁到 3 个独立文档==：

  **新建/扩充 3 个文档**：
  - ==新建 `wiki/RAG/文档解析综述.md`== (395 行) —— 从 §三 整段迁出。覆盖：两个正交维度（容器 × 内容）/ 格式光谱 / 内容元素通用解析方法（TSR/图表/公式/版面/OCR）/ 各格式独有特点（PDF/Word/Excel/PPT/HTML/音视频）/ 跳过解析（ColPali/VisionRAG）/ 工具全景（Docling/MinerU/Dolphin-v2/MarkItDown/RAG-Anything）/ 结构感知创新架构（BookRAG/ChromaFs）
  - ==扩充 `wiki/文档解析/文本清洗.md`== (248 → 541 行) —— 合并原 §四 算法部分（页眉页脚识别代码 / PDF 水印检测代码 / MinHash+LSH 算法详解）+ 现有方法论（"L1/L2/L3 重复检测三层级"/ "近似去重在正式文档误伤"/ "不去重靠元数据 + 检索时过滤"工程模式）；==新增章节==："语言与句子切分"（多语言 embedding 时代的简化做法）+ "内容增强"（缩写展开/停用词策略/元数据提取）。完成==方法论 + 代码细节==的双层覆盖
  - ==新建 `wiki/RAG/分块策略.md`== (287 行) —— 从 §五 整段迁出。覆盖：分块的三对矛盾 / L1-L4 四种方法学 / chunk 内容增强（Contextual Retrieval / Late Chunking）/ 通用工程参数（chunk_size/Overlap/元数据模板）/ 选型决策树 / 6 个典型组合方案

  **目录归属调整**：
  - ==git mv `wiki/文档解析/分块策略前沿.md` → `wiki/RAG/分块策略前沿.md`== —— 归位修正（之前归类错了，==分块是 RAG 独有概念==，OCR/TSR 那些通用技术留在 wiki/文档解析/）

  **关键决策（与用户讨论）**：
  - ==文档解析/== 保持独立目录（OCR/TSR/版面分析/公式识别/VLM/文本清洗）—— 这些是==Document AI 通用技术==，不只为 RAG 服务（金融/医疗/教育/ERP 都用）
  - ==RAG/== 收纳 RAG 独有概念（==分块策略 / 分块策略前沿 / 文档解析综述==）—— 文档解析综述是"RAG 视角的文档解析工具地图"，归 RAG/ 合理
  - ==关于"尽量不要用文件夹"==：澄清 Obsidian 哲学不是"完全不要目录"而是"==靠 wikilink + tag 而不是 hierarchy 来组织知识=="。==目录代表"研究领域"==（Java基础/JVM/RAG/文档解析），==是健康的用法==

  **改造 RAG基础与架构.md**：
  - 保留 §一 RAG 是什么 / §二 系统架构两阶段 / §六 Prompt 拼接与生成 / §七 相关链接
  - §三/§四/§五 ==压缩为聚合入口==（每个 ~15-25 行）—— 保留核心定义 + "三对矛盾"等关键认知 + callout 链接到独立文档
  - 1165 → ==217 行==

  **更新 14 处 wikilink**：
  - 8 个文件中引用旧 RAG基础与架构 §三/§四/§五 章节锚点的 wikilink 全部修正
  - `[[RAG基础与架构#3.X 文档解析xxx]]` → `[[文档解析综述#X]]`
  - `[[RAG基础与架构#5.X 分块策略xxx]]` → `[[分块策略#X]]`
  - 涉及文件：数据格式/{XML,OOXML,PDF}.md / 文档解析/公式识别.md / RAG/{RAG检索策略,RAG高级技术,RAG评估,分块策略前沿}.md

  ==重构后的层次清晰==：
  ```
  wiki/RAG/                       ← RAG 工程视角（含 RAG 独有概念）
  ├── RAG基础与架构.md             217 行（聚合入口）
  ├── 文档解析综述.md             ★ 新建（395 行）
  ├── 文本清洗.md                  ← 在 wiki/文档解析/（不在这）
  ├── 分块策略.md                  ★ 新建（287 行）
  ├── 分块策略前沿.md              ★ 移入（349 行）
  └── ... 其他 RAG 文档（向量/检索/高级/评估/安全/Code RAG/多轮对话）

  wiki/文档解析/                  ← Document AI 通用技术（不只为 RAG）
  ├── OCR.md / TSR.md / 版面分析.md / 公式识别.md / VLM文档解析.md
  └── 文本清洗.md                  ★ 扩充（541 行）

  wiki/数据格式/                  ← 通用数据格式基础
  └── XML/PDF/OOXML/DOM
  ```

  **原因**：用户审计发现 RAG基础与架构.md 1165 行严重超标（远超 wiki 平均），==其中 §三/§四/§五 共 1005 行属于"文档处理"==——和已有 wiki/文档解析/ 目录==高度重叠==。本次重构把"文档处理"内容外迁，让 RAG基础与架构.md 真正聚焦"==基础与架构==（是什么 + 两阶段管道 + Prompt 拼接 + 链接到 RAG 子主题）"。同时通过用户讨论澄清==目录归属判断标准==（==只为 RAG 服务的概念归 RAG/，跨领域用的通用技术归独立目录==），分块策略前沿.md 顺势从 文档解析/ 移到 RAG/ 完成归位

- ==wiki/LLM + wiki/RAG 目录审计与规范化==——系统审计两个目录（24 个文件）的命名风格、编码质量、文档体量、wikilink 有效性，执行 P0 + P1 两类修复：

  **P0 修复 7 处 U+FFFD 编码损坏字符**（4 个文件）：
  - `Agent Skills 体系.md` L101："行间距 1.5 倍"（原：`��间距`）
  - `Harness Engineering.md` L160："组件样式"（原：`��件样式`）
  - `LLM 基础与训练.md` L72："训练长度硬上限"（原：`���练长度`）
  - `LLM 基础与训练.md` L128："训练成本"（原：`训���成本`）
  - `RAG高级技术.md` L56："瓶颈不是 top-K"（原：`瓶��不是`）
  - `RAG高级技术.md` L104："自动评估相关性"（原：`自动���估`）
  - `RAG高级技术.md` L146："和实际业务"（原：`和���际业务`）
  - 注：`RAG基础与架构.md` L563-564 的 `"�"` 是==合法内容==（讲文本清洗替换 U+FFFD 的代码示例），不修改

  **P1 命名风格统一：6 个文件名中英间加空格**（`git mv` + 全 wiki wikilink 批量替换）：
  - `Agent Skills体系.md` → `Agent Skills 体系.md`
  - `Agent工程实践.md` → `Agent 工程实践.md`
  - `Agent核心概念.md` → `Agent 核心概念.md`
  - `Agent框架.md` → `Agent 框架.md`
  - `AI编程工具.md` → `AI 编程工具.md`
  - `LLM基础与训练.md` → `LLM 基础与训练.md`
  - 更新了 ==21 个文件==中的所有 wikilink（wiki/LLM/ 15 个 + wiki/RAG/ 6 个）
  - 验证：旧文件名全部删除 ✓ / 旧 wikilink 全部替换 ✓ / 最终 U+FFFD 总计 0（合法内容除外）✓

  **审计发现的其他问题（暂不处理，记录供后续参考）**：
  - ==RAG基础与架构.md 1165 行==：内容跨"文档解析/文本清洗/分块策略"三大主题，可考虑拆分（wiki/文档解析/ 已有相关目录）
  - ==文件名 vs H1 标题不一致==：18 处（H1 通常比文件名更精确，建议后续统一向 H1 靠拢）
  - ==last_reviewed 过期==：LLM 基础与训练 / Harness Engineering / Agent Skills 体系 停留在 2026-05-09（本轮未修改，属正常）

  **原因**：用户要求审计两个目录的整体质量——命名风格不一致（中英混合有无空格两种风格并存）和编码损坏字符是最影响使用体验的问题，本次一次性清理。==命名统一后 wiki/LLM/ 所有文件名风格 100% 一致==（中英之间均有空格）。

- `wiki/LLM/Coding Agent 工具集.md` ==新增 §五"Commands / Skills:Tool 的上层封装"==——回应用户疑问"==这些 CLI 命令是怎么做的呢,我想知道=="(针对 `/index` `/search` `/graph` 命令)。原 wiki [[AI编程工具#7.1 Commands]] 只用一句话讲"==命令本质是预定义的 prompt 模板=="——==没讲实现机制==,容易让读者以为命令是黑盒。新增章节(~190 行)系统讲清:

  - **5.1 Tool / Command / Skill 三者的关系**:三层抽象层次清晰对照表(==Tool 是积木 / Command 是用户语法糖 / Skill 是 LLM 经验包==)+ 递进关系图(Tool → Command → Skill → Plugin)

  - **5.2 Command 的实现机制**:三种风格(==纯代码 / 纯 prompt / 混合==)+ Host 拦截斜杠命令的完整代码(BUILTIN_COMMANDS 字典 + .claude/commands/*.md 自定义命令)+ 三个具体例子代码:
    - ==`/index`(纯代码风格)==:遍历文件 → tree-sitter 切块 → embedding → 写 sqlite-vec,==零 LLM 调用==
    - ==`/search`(混合风格)==:确定步骤查 SQLite + 智能步骤交 LLM 整理结果(==Cursor 直接打印 vs Claude Code 让 LLM 解读==的区别)
    - ==`/graph`(纯代码风格)==:SQL 查 call_edges/inherits_edges/imports_edges + 关系图存储三选项(SQLite 多表 / NetworkX 内存图 / Neo4j)
    - ==自定义命令==:`.claude/commands/<name>.md` 直接当 prompt 发给 LLM,==Markdown 即 Prompt==

  - **5.3 Skill 的实现机制**(简述):与 Command 核心差别(用户主动 vs LLM 自动激活)+ SKILL.md 预加载 description + 渐进式披露 + 引用 [[Agent Skills体系]] 完整机制

  - **5.4 ==关键认知:没有魔法==**:Coding Agent = LLM 决策 + 几十个原子 Tool + 各种封装(Command/Skill/Plugin)——==Cursor/Claude Code 强不在工具复杂,强在工具集设计 + 命令体系 + Skill 经验沉淀==

  顺手处理:
  - 原 §五 工具设计原则 → §六(子节 5.1-5.5 → 6.1-6.5)
  - 原 §六 主流产品对比 → §七(子节 6.1-6.2 → 7.1-7.2)
  - 原 §七 工具调用可靠性 → §八
  - 在 [[AI编程工具#7.1 Commands]] 加一句"==实现机制详解见 [[Coding Agent 工具集#五]]=="

  ==文档增长==:492 → 684 行;==逻辑递进==:分类(§二/§三)→ Tool 实现(§四)→ ==Command/Skill 实现==(§五,新)→ 设计原则(§六)→ 产品对比(§七)→ 可靠性(§八)

  **原因**:用户继续追问"==CLI 命令怎么做的=="——是非常本质的工程问题。wiki 之前讲了 Tool 怎么实现(read_file 几十行 Python),==但没讲 Tool 的上层封装==(Command 怎么把斜杠命令转成 LLM 调用)。本次补全把"==Host 拦截斜杠命令 + 三种实现风格 + Markdown 即 Prompt=="讲透——==面试加分点==:候选人能讲清"==`/index` 触发后到底走纯代码还是发 LLM=="比只会答"用 Claude Code 命令"有深度

- `wiki/RAG/多轮对话.md` ==§2.4 大幅修订:把"五种方案"从"==列方案==升级为"==讲清生产实际怎么做=="——回应用户判断"==这里的压缩对话提取重要信息通过什么方法呢,我看了感觉就是 LLM 吧,不然你看说用规则,规则怎么可能提取的出来还有打标签也是不太可能的,你看 Claude Code 是怎么做的呢,或者市面上大家都是怎么做的=="==——判断完全准确==。原 §2.4 平铺列了 5 种方案,==没明确告诉读者方案 2/3/4 在生产中几乎不用==,容易让面试候选人误以为"用规则打分"是合理思路。本次修订:

  - **§2.4 开头加 warning callout**: ==99% 实际是 LLM 在做==——明确告诉读者方案 2/3/4 是 2018-2022 传统 NLP 时代的方案,LLM 时代基本被淘汰

  - **方案 1 标题改为"==生产主流==" + 方案 5 标题改为"==生产实际推荐==" + 方案 2 标题改为"==历史方案,生产几乎不用==" + 方案 3 标题改为"==只能用作否定式过滤,不能识别重要==" + 方案 4 标题改为"==研究前沿,生产几乎不用=="**——所有方案在标题就让读者一眼看出生产价值

  - **方案 2 加 callout 揭穿"谁来打标签"是死穴**:用户标(荒谬)/Agent 自动标(=LLM 在做,绕回方案 1)/规则脚本标(同方案 3 脆弱性)——==方案 2 不是独立方案==,而是方案 1 或方案 3 的变体

  - **方案 3 重写**:从"用规则识别什么重要"改为"==只能用作否定式过滤=="——讲清规则在中文场景的死穴(决策关键词词典爆炸/否定转折难识别/隐式决策无法捕获),给出==正确用法==(预过滤明显寒暄,剩下交给 LLM)

  - **方案 4 加 info callout**:attention 评分需要访问模型内部权重(API 模型不开放),嵌入聚类质量不稳定——==Claude Code/Cursor/ChatGPT/Anthropic/LangChain 都没用==,论文有产品没有

  - ★ **新增子节"==市面主流产品的实际做法=="**:横向对比 6 个主流产品的对话压缩方案
    - Claude Code `/compact`:context 接近上限触发,LLM + 精心 prompt
    - Anthropic Claude Memory tool:LLM 在 ReAct 循环中自主调 save_memory
    - ChatGPT `bio()`:LLM 检测到值得记的事实自主调用
    - Cursor Memory:用户 `/save` 或 LLM 自动写入 Notepads
    - LangChain ConversationSummaryMemory:每 N 轮触发,框架内置 LLM summarize
    - LangGraph Summarizer node:状态图中显式定义,LLM 摘要写入 State
    - 共同点 4 条:全部用 LLM / 给 LLM 工具 / system prompt 引导 / 原始数据保留
    - 给出 Claude Code `/compact` 的 prompt 摘要(开源逆向):"保留约束/已完成/下一步/错误"四要素

  - **"推荐组合"重写**:从"方案 2 入库标注"(已被否定)改为"==预过滤(方案 3 否定式)+ 触发(2.5)+ 主体(方案 5 分层)+ 压缩(方案 1 LLM)+ 持久化保留原文=="==,明确说"==没有方案 2 的入库标注=="(因为"谁来标"没有好答案)

  ==修订后的核心认知==:
  - ==判断重要性 = LLM 调用 + 精心 prompt==,不是规则也不是打标签
  - ==规则只用作否定式过滤==(去明显寒暄垃圾),不用作识别什么重要
  - ==市面主流产品高度趋同==——给 LLM 一个工具(`save_memory` / `bio` / `compact`)+ system prompt 引导

  **原因**:用户判断"==感觉就是 LLM,规则不可能提取出来,Claude Code 怎么做的=="==非常准==——文档原本平铺 5 种方案==没强调 LLM 是绝对主流==,容易让候选人面试时答"用规则打分"。本次修订把"==市面 99% 都是 LLM=="这个事实==顶到首位==,把规则/打标签/注意力评分明确标注为"==历史/学术价值,生产几乎不用=="——==面试加分点==:能讲出 Claude Code `/compact`、ChatGPT `bio()`、Anthropic save_memory tool 的 prompt 套路,远超只会背"五种方案"的候选人

- ==Agent Memory 系统独立成文==——回应用户三个连续判断：(1) "短期/长期记忆讲得不够细"、(2) "==短期到长期转换机制怎么处理的也没有说明==" 、(3) "Memory vs RAG 已经在 RAG 文档讲过没必要重复，但 Agent 视角的 Memory 应该更细"；同时建议"==提取出去新建一个记忆文档,这里引用一下=="——和 Function Calling / MCP / Plan-and-Execute 一样的拆分模式。Agent核心概念.md §五 内容从 60 行缩减至聚合入口(20 行 callout)，完整 Memory 内容拆到独立文档：

  - **新建** `wiki/LLM/Agent Memory 系统.md`(472 行)：
    - §一 三层架构(L1/L2/L3)——保留原表 + 关键认知"==同一份信息在不同生命周期的呈现==,转换是核心"
    - §二 ==L2 短期记忆详解==：JSONL 会话日志格式(为什么是 JSONL 不是 JSON 数组——流式追加 + 崩溃可恢复 + 易于并行) + 同步 vs 异步写入 + 上下文窗口管理(链接 [[多轮对话]] 不重复)
    - §三 ==L3 长期记忆详解==："==Markdown 是本体,SQLite 是加速层=="原则的工程论证(单纯用数据库 vs Markdown+数据库 5 维度对比表) + MEMORY.md 内容结构示例 + SQLite schema(chunks / FTS5 / sqlite-vec) + 索引建立四步 + 写入触发的三种方式
    - **★ §四 短期 → 长期转换机制**(原文档==只有两句话==,本次大幅扩展)：
      - 三种触发条件(用户主动 `/new` / 会话结束 / Context 接近上限)
      - 转换流程三步(重要性判断 → 提炼成事实 → 写入 L3)
      - ==重要性判断==：链接 [[多轮对话#§2.4 五种判断方案]],生产组合"==方案 2 入库标注 + 方案 5 分层 + 方案 1 LLM 摘要=="
      - ==转换 prompt 实战写法==：完整模板(明确"必须保留 vs 必须舍弃"清单 + 严格输出格式 + 允许留空防 LLM 编造)
      - ==两个常见陷阱==：(1) "==把短期对话误当作历史记忆注入==" (对应用户引用功能里"注入只用长期稳定事实"的纪律) (2) "==摘要不可逆,生产必须留原文==" (Compaction 后==绝不删原始 JSONL==)
      - 完整工程实现 6 步代码示例
    - §五 ==Memory 怎么被 Agent 使用==：三个工具(memory_search 粗召回 / memory_get 精读 / save_memory 写入) + ==注入策略== + 完整会话流程示例
    - §六 ==与 RAG 的对比==(简短)：6 维度差异表 + 详细方法论引用 [[RAG基础与架构]] 不重复
    - §七 ==生产实现对比==：Claude Code / Cursor / OpenClaw / Hermes 各自的 L2/L3/转换机制对比 + Anthropic 的 Memory 哲学"==记忆是用户的资产,不是产品的锁定=="

  - **改造** `Agent核心概念.md` §五：从 60 行降到 ==20 行聚合入口==——保留三层架构表 + "Markdown 是本体" 关键认知 + 两个 callout 链接([[Agent Memory 系统]] 详细 + [[多轮对话]] 对话存储)。Agent核心概念.md ==342 → 300 行==

  - **更新** `LLM索引_MOC.md`：Agent 体系新增 [[Agent Memory 系统]] 文档卡；Stage 3 学习路径从 9 步扩为 10 步

  ==归位之后的 Agent 模块==：每个文档单一职责,主入口短而精。Agent核心概念.md 是聚合入口,详细内容拆到 ReAct 与 Harness 实现 / Plan-and-Execute 实现 / Function Calling / MCP 协议 / Coding Agent 工具集 / Agent Skills体系 / ==Agent Memory 系统== / Agent工程实践 / Agent框架 / Harness Engineering。

  **原因**：用户三个判断都对——(1) Memory 这种==有完整工程深度的子系统==不应该塞在 Agent核心概念里("详细 + 浅"两难)；(2) ==短期→长期转换机制==是 Memory 系统的灵魂但原文档只有两句话,本次扩展为完整 §四；(3) Memory vs RAG 的对比应该==简短引用而非展开复述==——RAG 在 wiki/RAG/ 目录已经完整覆盖。本次拆分让 Agent核心概念.md 真正成为"==聚合入口=="(每个子模块一段定义 + 链接),==Agent Memory 系统.md== 则提供"==Agent 视角的记忆完整方法论=="(独立于 RAG 的工程实现细节)

- `wiki/LLM/Plan-and-Execute 实现.md` ==扩展 §3.0 + 新增 §七==——回应用户两个连续疑问：(1) "DAG 是啥意思,干啥用的"——原 §3.2 给了 DAG schema 和并发执行代码,==但没讲 DAG 是什么、为什么需要==；(2) "还有什么别的 plan"——文档只讲了 Plan-and-Execute 一种,没讲 Plan 家族其他模式。==427 → 667 行==:

  - **§三 开头插入 §3.0 "前置认知:DAG 是什么、为什么 Plan 需要它"**(~85 行):
    - DAG 三要素拆解(Directed/Acyclic/Graph)
    - 为什么 Plan 需要 DAG——竞品分析报告例子(==线性 90 秒 vs DAG 30+N 秒,快 3 倍==)
    - DAG 四个工程价值:并行执行 / 循环依赖检测(拓扑排序) / 进度可视化 / 失败影响范围
    - ==DAG vs StateGraph 关系==:StateGraph 是 DAG 超集(允许循环 + 条件分支 + 状态传递),==LangGraph 用 StateGraph==
    - DAG 不是 Plan-and-Execute 独有——Airflow / Make / Git / Spark 都在用,==Plan-and-Execute 只是把数据管道编排搬到 Agent 任务编排==

  - **新增 §七 "其他规划模式(Plan 家族)"**(~155 行):覆盖 5 种 Plan 变体
    - ==7.1 ReWOO==(Reasoning WithOut Observation):==零 LLM 调用的 Executor==,Plan 里用 `#E1` `#E2` 占位符引用前步结果,执行只跑工具。省 5x LLM 调用,但容错差
    - ==7.2 LLMCompiler==(UC Berkeley 2023):LLM 编译 Plan 为 DAG,==自动从变量引用推依赖==(不需要 LLM 显式声明 depends_on),延迟降 35% 成本降 25%。==Bazel 自动推依赖 vs 手写 Makefile== 类比
    - ==7.3 Tree of Thoughts(ToT)==:==每步 N 个候选,树搜索==,适合数学/逻辑推理。成本 O(N^D),生产几乎不用纯 ToT
    - ==7.4 LATS==(UIUC 2024):MCTS + ReAct 混合,类似 AlphaGo 的搜索算法。研究项目用,生产很少
    - ==7.5 Hierarchical / 递归 Plan==:大任务拆子任务再拆子子任务,==Anthropic Multi-Agent Research(2024)就是这种模式==。适合超长任务(写书、重构整个项目)
    - ==7.6 Plan 模式选型表==:9 个场景对应 9 个推荐 + 关键认知"==90% 用 Plan-and-Execute,5% 用 Hierarchical,4% 用 LLMCompiler,1% 用 ToT/LATS=="

  - 原 §七 关键认知 顺移为 §八

  ==文档结构升级后==:
  ```
  §一 核心思想
  §二 80 行 Python 实现
  §三 Plan schema 三种递进
     ★ 3.0 前置认知:DAG 是什么(新增)
     3.1 V1 简单线性
     3.2 V2 加依赖(DAG)
     3.3 V3 加输入输出绑定
  §四 Replan 机制
  §五 与 ReAct 混合
  §六 与 LangGraph 关系
  ★ §七 其他规划模式 Plan 家族(新增 5 种变体 + 选型表)
  §八 关键认知:Plan ≠ 更聪明
  ```

  **原因**:用户连续两个问题揭示了文档的两个缺口——(1) DAG 概念在 §3.2 给了实现代码但==没有前置认知==(读者直接看代码会卡在"DAG 是啥");(2) Plan-and-Execute 只是 Plan 家族的一员,==缺少其他 Plan 模式的横向对比==。本次补全把"==DAG 是什么、为什么需要、与 StateGraph 的关系=="作为==学习曲线的前置铺垫==,把"==ReWOO / LLMCompiler / ToT / LATS / Hierarchical=="作为==Plan 家族的横向广度==——读者读完能讲清==90% 用 Plan-and-Execute,什么场景用什么 Plan==,这是 Agent 工程师面试时区分概念党与判断派的关键

- ==新建 `wiki/LLM/Plan-and-Execute 实现.md`==——回应用户判断"我们有 ReAct 的具体实现文档([[ReAct 与 Harness 实现]]),但没有 Plan-and-Execute 的实现文档"。盘点确认==判断准确==——三种推理框架的实现文档不对称:ReAct 有完整代码(60 行 Python),Plan-and-Execute 只在 [[Agent核心概念#2.1]] 给了 15 行概念描述、[[Agent工程实践]] 提了一两句"两阶段:先调 LLM 生成计划列表,再 for 循环执行"==但没有代码==。新建独立文档(427 行)与 [[ReAct 与 Harness 实现]] 命名对称:

  - **§一 核心思想**：与 ReAct 的对比图 + 为什么分两阶段（全局规划 + 显式可审查产物）+ 适用场景与不适合场景
  - **§二 ==完整 80 行 Python 实现==**：复用 ReAct 的工具定义只换控制流；Planner Prompt（要 LLM 一次输出完整计划列表 JSON）+ make_plan + execute_plan + 主入口；运行示例展示输出
  - **§三 Plan 的 schema 三种递进设计**：V1 简单线性 → V2 加 ==depends_on 的 DAG 形式==（asyncio.gather 并发执行无依赖步骤）→ V3 加���入输出绑定（`{{config.env}}` 引用前步结果，==是 LangGraph State 传递机制的简化版==）
  - **§四 ==Replan 机制==让计划可调整**：触发 Replan 的三种信号（执行失败 / 中间结果异常 / 质量评估不达标）+ 简化版 Replan 代码（带失败历史的 make_plan_with_history）+ 工程要点（上限 2-3 次、不从零开始、Replan vs ReAct 区别）
  - **§五 ==与 ReAct 的混合（生产级架构）==**：顶层 Plan-and-Execute（全局规划）+ 底层每个 Step 内部用 ReAct 完成 + Human-in-the-Loop 审批 Plan ——==这就是 Claude Code 的 Plan Mode==
  - **§六 与 LangGraph 的关系**：当 Plan 复杂到一定程度，从手写 list 升级到 StateGraph；Plan-and-Execute 节点 + Executor 节点 + Replanner 节点的标准图结构
  - **§七 ==关键认知:Plan ≠ 更聪明==**：Plan-and-Execute 的代价（失去灵活性 / Planner 单点故障 / Replan 成本）+ 选 Plan-and-Execute 的真正理由（让人审批整体方案 + 可追溯执行记录）

  顺手处理：
  - 在 [[Agent核心概念#2.1 三种推理框架]] Plan-and-Execute 段末尾加一句"具体实现示例（80 行 Python + Plan schema 三种递进 + Replan 机制 + 与 ReAct 混合）见 [[Plan-and-Execute 实现]]"——与 ReAct 段末"具体实现示例（60 行 Python）见 [[ReAct 与 Harness 实现]]"==格式对称==
  - 更新 `LLM索引_MOC.md`：Agent 体系新增 [[Plan-and-Execute 实现]] 文档卡；Stage 3 学习路径从 8 步扩为 9 步（13. ReAct 实现 → Plan 实现 → Function Calling → ...）

  ==三种推理框架的文档对称性恢复==：
  - ReAct → [[ReAct 与 Harness 实现]]（60 行 Python）
  - Plan-and-Execute → ==[[Plan-and-Execute 实现]]==（80 行 Python + Replan）
  - Reflection → 概念在 [[Agent核心概念#2.1]]，无独立实现文档（==Reflection 较简单且通常叠加在前两种上==，独立成文价值有限）

  **原因**：用户主动指出"==我们好像没有 Plan 的实现文档=="——是真实的对称性缺口。新文档==沿着 ReAct 与 Harness 实现的同一风格==：完整可运行代码 + 关键设计点 + 与 LangGraph 等生产框架的关系；==特别强调"==Plan ≠ 更聪明=="这个常见误区==——避免读者读完产生"Plan 比 ReAct 高级、所有任务都该用 Plan"的错觉，==面试时讲清"Plan-and-Execute 的代价是失去灵活性"==是区分概念党与工程派的关键

- `wiki/LLM/Coding Agent 工具集.md` ==新增 §四"底层实现揭秘：这些工具到底怎么做的"==——回应用户疑问"glob、grep、执行命令、创建项目、RAG 语义检索、联网搜索、MCP 动态工具这些怎么做到的，前面几个简单的（read_file 就 open）我能明白，后面这几个看起来很大"。原文档讲了"工具有哪些 / 怎么分类 / 怎么设计"，==但没讲底层怎么实现==。新增章节==揭穿"魔法"==——9 个子节系统讲清每个工具的底层：
  - **4.1 实现量级总览表**——一眼看见各工具的代码量级和底层依赖
  - **4.2 glob**——Python 内置 `glob` 库，5 行；通配符 `*` `?` `**` 是 Unix 老概念
  - **4.3 grep**——subprocess 包 [ripgrep](https://github.com/BurntSushi/ripgrep)（rg 命令，比 grep 快 10x，自动跳过 .git/node_modules）
  - **4.4 execute_command**——`subprocess.run` 起子进程（10 行）+ 生产版加沙箱（Docker/Bubblewrap/firejail）+ 命令白名单 + Human-in-the-Loop
  - **4.5 create_project ==不是独立工具，是组合==**——LLM 自己调 `execute_command("npx create-react-app")` + `read_file` + `write_file` 完成。==看起来"很大"的功能其实是底层小工具的组合==——这就是 ReAct + 工具组合的力量
  - **4.6 codebase_search ==唯一真"重"的工具==**（~500 行）——完整 RAG 流水线：tree-sitter AST 切块 + Qwen2.5-Coder-Embedding + Qdrant/Milvus 向量库 + Re-rank。Cursor/Continue/Windsurf 是这种实现；==Claude Code 不这么做==，靠 grep + read_file + 1M context 现搜
  - **4.7 web_search**——10 行 `requests.post` 调第三方 API（Tavily/Brave Search/Bing/Google CSE/Perplexity），==不���接调 google.com==（被反爬）
  - **4.8 MCP 动态工具**——启动时 spawn 子进程 + JSON-RPC `list_tools` 发现工具；调用时通过 Client → JSON-RPC 转发到对应 Server 子进程；==对 LLM 透明==（和内置工具长得一样），==对 Host 不透明==（调用路径不同）
  - **4.9 关键认知：Coding Agent 没那么神秘**——本质是 LLM 决策 + 几十个原子工具（每个 5-50 行 Python）+ 一两个"重"工具（codebase_search / MCP）。==Cursor / Claude Code 强不在单个工具复杂，而在工具集设计 + Harness 工程==

  顺手处理：原 §四（工具设计原则）→ §五，原 §五（产品对比）→ §六，原 §六（可靠性）→ §七；子节编号同步顺移（§五下的 5.1-5.5、§六下的 6.1-6.2）

  ==文档增长==：281 → 492 行，==逻辑完整==：分类（§二/§三）→ ==底层实现==（§四）→ 设计原则（§五）→ 产品对比（§六）→ 可靠性（§七）

  **原因**：用户在读完工具分类后追问"这些工具看着很大怎么做到的"——是==非常本质的疑问==。原文档讲的是"分类与设计"，==缺了"底层实现"==这一层。本次补全把"魔法"揭穿——除了 codebase_search 是 RAG 重工程外，其他全是几十行 Python 包装。==这是 Agent 工程师面试时的高质量回答==：候选人能讲"Coding Agent 看似复杂，实际是 LLM 决策 + 原子工具 + RAG/MCP"——比死记产品功能要有深度

- ==MCP 协议 + Coding Agent 工具集独立成文==——回应用户疑问"读文件、写文件、列目录、glob、grep、执行命令、RAG 语义辅助检索、联网搜索、MCP 动态工具——这些工具调用是哪个部分的内容、是 MCP 吗，要不要单独把 MCP 拆出来"。盘点发现两个问题：(1) MCP 在 Agent核心概念.md §四 只有 60 行内容（含 A2A 对比），但==MCP 内容潜在挺多==（架构、写 Server、生态、安全、生产配置）；(2) 用户引用的"工具调用清单"==根本不是 MCP 内容==——是==Coding Agent 产品的工具集==（应用层），但这层概念在 wiki 中==从未独立成文==，常被混淆为 MCP 或 Function Calling。==新建两个独立文档==：

  - **新建** `wiki/LLM/MCP 协议.md`（~410 行）——完整 MCP 参考：
    - §一 是什么 + 解决什么问题（M×N → M+N、USB 类比）
    - §二 ==Host/Client/Server 三层架构==（基于 JSON-RPC 2.0）+ Server 提供的三种能力（Tools / Resources / Prompts）+ 三种 transport（stdio / SSE / WebSocket）
    - §三 ==写一个最简 MCP Server==（Python SDK 完整可运行示例 + Claude Desktop 挂载配置 + 整个链路图解）
    - §四 ==MCP 生态==：文件/数据访问（filesystem/git/postgres/sqlite）、开发协作（github/gitlab/linear）、通信（slack/gmail）、浏览器自动化（puppeteer/playwright）、AI 接入（everart/sequential-thinking/memory）
    - §五 ==安全模型==：Server 进程隔离 + 用户授权 + Server 自身权限设计 + 4 个常见安全坑（第三方 Server 后门 / 权限过宽 / API key 写死 / Prompt Injection 通过工具返回值）
    - §六 ==与 Function Calling 的衔接==（5 步工作流程��list_tools → tools 参数 → tool_calls → forward → tool 消息）
    - §七 与 A2A 协议的区别（保留原 Agent核心概念.md §四的 A2A 内容，扩写）
    - §八 ==生产实战配置==：Claude Code（claude mcp add 命令）/ Cursor（.cursor/mcp.json）/ Claude Desktop（claude_desktop_config.json）配置示例
    - §九 行业现状（Claude/Cursor/Windsurf/VS Code/Continue/OpenAI/Gemini 都已支持）

  - **新建** `wiki/LLM/Coding Agent 工具集.md`（~280 行）——讲清"工具清单到底属于哪一层"：
    - §一 ==工具集的四层归属==：Application（产品决策提供哪些工具）/ MCP（外部工具接入）/ Function Calling（协议）/ Harness（工程实现）—— ==关键澄清==：MCP 是"接入方式"不是"工具种类"
    - §二 ==内置工具五大分类==：文件操作（read_file / edit_file / glob）+ 代码搜索（grep / codebase_search / get_definition）+ 命令执行（execute_command / run_test / git_*）+ 项目操作（create_project / install_dependency）+ 联网（web_search / fetch_url）—— 每类讲设计要点
    - §三 ==MCP 动态工具==：内置 vs MCP 动态的本质区别（编译时 vs 运行时）+ 主流 MCP Server 推荐（github/postgres/slack/puppeteer）
    - §四 ==工具设计原则==：单一职责粒度、description 写法、参数设计、输出格式、Stripe 的"工具不超过 15 个"经验
    - §五 ==主流 Coding Agent 工具集对比==（Claude Code / Cursor / Aider / Continue / Windsurf）+ ==Claude Code 的"无索引模式"==特别说明 + Aider 的极简哲学
    - §六 工具调用可靠性（指向 Function Calling §八 + Agent工程实践 §八-B）

  - **改造** `Agent核心概念.md` §四 MCP 协议：1654 → 797 字符（聚合入口化），保留核心定义 + 与 FC 的关系，详细内容（架构/写 Server/生态/安全/生产配置/A2A 对比）链接到 [[MCP 协议]]；新增 tip callout 链接到 [[Coding Agent 工具集]]。==主文档继续瘦身：380 → 342 行==

  - 更新 `LLM索引_MOC.md`：Agent 体系新增 [[MCP 协议]] 和 [[Coding Agent 工具集]]；Stage 3 学习路径从 6 步扩为 8 步

  ==归位之后的协议/接入层==：
  - `Function Calling.md` → ==LLM ↔ 应用==的协议（怎么表达调用意图）
  - `MCP 协议.md` → ==应用 ↔ 外部工具==的协议（怎么标准化暴露工具）
  - `Coding Agent 工具集.md` → ==应用层==（具体提供哪些工具）
  - `Agent核心概念.md` → 聚合入口（一句话定位 + 链接）

  **原因**：用户的洞察非常准确——"工具清单不是 MCP 内容"。==根本问题==是 wiki 之前没有独立讲"Coding Agent 实际工具集"的文档，导致这层应用层概念无处归属。本次补全两个独立文档：MCP 是==协议==（独立成文有充足内容），Coding Agent 工具集是==产品决策==（讲清"哪些是内置、哪些走 MCP、为什么这么分"）—— 这两个文档加起来回答了"读到 Coding Agent 工具列表时该怎么理解"这个高频疑问

- ==Function Calling.md 内容归位==——回应用户判断"§八 可靠性、§九 失败处理、§十 工具权限控制看着很怪，感觉应该属于 Agent 部分，Function Calling 塞了别的内容"。==判断完全正确==——这三节是上一轮拆分时从原 Agent核心概念.md §三 一起搬过来的，但搬完没识别它们本来就是错放的：是 ==Agent 工程问题==，不是 ==Function Calling ���议问题==。重新归位：

  - **`Function Calling.md` §八 大幅简化**——只保留==协议层==的可靠性内容（与 Function Calling 直接相关）：
    - 8.1 参数 schema 校验（jsonschema 库 + 回注让 LLM 重试）
    - 8.2 JSON 解析失败（方式 A vs 方式 B 对比）
    - 8.3 ==tool_call_id 不匹配==（OpenAI 直接返回 400 错误的常见原因）
    - 8.4 模型选错工具：description 写得不到位（指回 §二.2）
    - 删除原 8.2 执行层（工具超时）+ 8.3 系统层（限流熔断）—— 是 Agent 工程内容，搬到 Agent工程实践.md
    - 删除整个 §九 失败处理四层 + §十 工具权限控制（搬走）
    - 末尾加链接指向 [[Agent工程实践#八-B、Agent 可靠性设计（实战设计题）]]，告诉读者执行层/决策层在哪
    - 文件从 549 行 → 481 行

  - **`Agent工程实践.md` §八-B "Agent 可靠性设计" 新增两节**：
    - ==Agent 工具调用失败的四层决策策略== ——L1 重试 / L2 参数修正 / L3 换工具 / L4 换策略 + 错误信息回注上下文 + 工程实现要点。==与已有的 Fallback 策略互补==：Fallback 讲==架构层降级==（系统能怎么降），新加的讲==LLM 决策层==（在 ReAct 循环里 Agent 怎么选路）
    - ==工具权限控制：白名单 / 参数粒度 / Human-in-the-Loop== ——三个控制维度（工具粒度白名单、参数粒度路径/值域/格式校验、频率粒度）+ 声明式 vs 运行时检查 + Human-in-the-Loop 危险操作人工审批。==与已有的 Prompt Injection 防御互补==：Prompt Injection 讲==输入侧==的对抗，权限控制讲==执行侧==的拦截

  - 顺手修复 Agent工程实践.md 4 处历史遗留 U+FFFD 损坏字符（285/386/429/627 行）+ frontmatter `last_reviewed` 更新到 2026-05-25

  ==归位之后的层次==：
  - `Function Calling.md` → ==协议层==：schema 怎么写、消息怎么拼、解析失败怎么办（==FC 自身的可靠性==）
  - `Agent工程实践.md §八-B` → ==Agent 工程层==：工具超时、Fallback 降级、L1-L4 决策路径、权限控制、Human-in-the-Loop（==Agent 整体的可靠性==）
  - `Harness Engineering.md` → ==方法论层==：六大组件、控制流模式（==为什么这么设计==）

  **原因**：用户判断 Function Calling 文档塞了 Agent 工程的内容——本质是上一轮"Agent 核心概念拆分"做得不够彻底，原 §三 Function Call 章节本来就掺杂了 Agent 工程问题，搬运时整段保留没分类。本次归位让 Function Calling.md 真正聚焦协议本身，==Agent 工程问题统一沉淀在 Agent工程实践.md §八-B==，每个文件单一职责

- `wiki/LLM/AI编程工具.md` ==新增 §二"产品形态：CLI / IDE 插件 / 桌面应用"==——回应用户洞察"目前大部分 AI 应用都趋向使用 CLI 而不是插件或别的"。原 wiki §一只讲了 "AI 工具 vs 传统 IDE"（功能层差异），==没回答=="AI 编程工具自己有几种产品形态、为什么 CLI 流行"。新章节 5 个子节系统讲清形态层差异：
  - **2.1 三种主流形态对照表**：CLI（Claude Code / Codex CLI / Aider / Goose）/ IDE 插件（Copilot / Continue / Cline / Roo Code）/ 桌面应用（Cursor / Windsurf / Zed）
  - **2.2 ==为什么 Agent 时代偏向 CLI==**——六条工程优势叠加：(1) Agent 工作就是执行命令，CLI 零成本；(2) 终端是跨平台跨编辑器最大公约数（Copilot 维护三套插件成本极高）；(3) 远程开发天然支持（SSH/Docker/Codespaces 一行命令）；(4) 容易组合（Unix 哲学，可管道、可 piped 进 CI）；(5) CI/CD 天然（GitHub Actions/Jenkins 都是 shell）；(6) ==Agent 不需要"看屏幕"==（UI 是给人看的，Agent 直接读 AST/文本）
  - **2.3 CLI 的代价**：diff 体验差 / 新手门槛 / 没有富 UI——所以 Cursor/Windsurf 这种桌面应用没死，对非 power user 友好
  - **2.4 趋势判断==两条平行赛道==**：程序员 power user 走 CLI（Claude Code/Codex/Aider），普通开发者走桌面应用（Cursor/Windsurf）；==被夹击的中间态==是 Copilot 这种纯 IDE 插件——上不如 Cursor 原生集成、下不如 Claude Code 灵活，这也解释了 GitHub 推 Copilot Workspace 的动机
  - **2.5 CLI 设计原则**：非交互式 / JSON 输出 / `--dry-run` / 可被管道（合并自原 §五的简短一段，扩展为完整原则）

  顺手解决三个文档结构问题：
  - 现有 §五"这些工具如何使用 MCP 和 CLI" ==重构==为 §六"MCP 接入"——CLI 段已被新 §二.5 覆盖，只保留 MCP 部分并扩写==实际应用==（Claude Code/Cursor/Windsurf/Continue 都已原生支持 MCP，==MCP 是 AI 编程工具生态的事实标准==）
  - 章节编号顺移：原 §二/三/四（Claude Code/Codex/Cursor）→ §三/四/五；原 §六 Claude Code 概念体系 → §七（子节 6.1-6.6 → 7.1-7.6）；原 §七 知识管理方法论 → §八（子节 7.1-7.3 → 8.1-8.3）
  - frontmatter `last_reviewed` 更新为 2026-05-25

  **原因**：用户提出 "CLI 和应用程序端的区别是什么呢，我们这里面有说明吗，我看目前大部分的 AI 应用都趋向于使用 CLI 了" ——这是一个真实的产品形态趋势观察。原 wiki 只讲"AI 工具相对传统 IDE 有什么差异"（功能层），==没讲 AI 工具自身的形态分类与演化==。本次补全把"形态层"差异系统化，特别强调 ==CLI 在 Agent 时代的六大工程优势== 和 ==Copilot 这种纯插件被两边夹击的中间态危机==——这是 Agent 工程师面试时的高质量观点

- ==Agent 核心概念文档大拆分==——回应用户判断"Agent核心概念.md 太长了（822 行），加 Function Calling 详解会更长，需要拆分"。盘点发现两个核心问题：(1) ReAct 在 §一/§二/§3.0 三个不同地方反复讲，没有"权威位置"；(2) §三 标题"Function Call" 但 §3.0 内容是 ReAct 实现，章节标题与内容不匹配；(3) §五 Memory 同一份内容紧挨着讲了两遍（L1/L2/L3 三层 vs 短期+长期 两层）。==822 行 → 380 行主入口 + 两个独立文档==：

  - **新建** `wiki/LLM/Function Calling.md`（548 行）——回应用户连续追问"tools 怎么传"、"结构化对象 vs 字符串"、"独立字段还是嵌入文本"、"四种角色"等协议细节，==零散问答整理成体系==：
    - §一 是什么 + 解决什么（vs 传统 prompt 做法对比表）
    - §二 ==请求结构==：tools schema 三个关键字段（name/description/parameters）+ description 写得好 vs 差对比 + parameters 用 JSON Schema（enum/required/范围）
    - §三 ==四种消息角色==：system/user/assistant/tool 各干什么 + 完整对话角色序列示例 + tool_call_id 关联机制
    - §四 ==响应结构==：完整响应 JSON + content/tool_calls/finish_reason 三个关键字段 + 互斥性（少数模型如 Claude Extended Thinking 两个都有）+ arguments 还是字符串需要 json.loads
    - §五 ==多轮工具调用==：完整 messages 数组演化 + 三个必踩的坑（tools 字段每轮要带、assistant 消息原样塞回、tool_call_id 一一对应）
    - §六 并行调用：asyncio.gather 并发执行 + parallel_tool_calls=False 关闭并行
    - §七 ==厂商差异==：OpenAI（事实标准��/ Anthropic（input_schema 不是 parameters，input 直接是 dict）/ Qwen/DeepSeek（兼容 OpenAI）/ LiteLLM 跨厂商抽象
    - §八 可靠性三层：模型层 / 执行层 / 系统层
    - §九 失败处理四层：L1 重试 / L2 参数修正 / L3 换工具 / L4 换策略 + 错误信息回注上下文（ReAct 天然支持错误处理的原因）
    - §十 工具权限控制：白名单 / 参数粒度 / 频率粒度 / 声明式 vs 运行时 / Human-in-the-Loop

  - **新建** `wiki/LLM/ReAct 与 Harness 实现.md`（387 行）——从 Agent核心概念.md §2.0 + §3.0 剥离的"动手实现"内容：
    - §一 LLM 的本质：无状态纯函数（7 件 LLM 不会做的事）
    - §二 pom.xml 完整例子（无 Harness vs 有 ReAct 的两种执行流）
    - §三 ==14 行完整分工对照表==（LLM 干什么 vs Harness 干什么）
    - §四 ==屋里写纸条 + 屋外秘书类比==
    - §五 ==60 行 Python 极简 ReAct==（定义工具/system prompt/react_loop）
    - §六 工具识别两种方式（方式 A 传统 prompt + 方式 B Function Calling）
    - §七 五个常见疑惑 FAQ
    - §八 完整流程串起来

  - **改造** `Agent核心概念.md`（822 → 380 行）——瘦身为==聚合入口==：
    - §一 Agent 定义与基本架构（保留完整）
    - §二 ==改名=="推理模式与 Harness 控制流"——保留三种推理框架的概念叙述、对比表、分层架构图，删掉 §2.0 LLM/Harness 详细拆解（移到 [[ReAct 与 Harness 实现]]）
    - §三 ==重写==：从详细的 Function Call 改为一段定义 + 链接到 [[Function Calling]]
    - §四 MCP 协议（保留）
    - §五 Memory 系统：==删掉重复的"两层架构"小节==（已被 L1/L2/L3 三层版本覆盖）
    - §六 Skills（已是链接）
    - §七 RAG（已是链接）

  - 更新 `wiki/LLM/LLM索引_MOC.md`：Agent 体系新增 [[ReAct 与 Harness 实现]] 和 [[Function Calling]] 两个独立文档；Stage 3 学习路径从 4 步扩为 6 步

  **拆完之后的逻辑层次**：
  - Agent核心概念.md → ==是什么==（聚合入口）
  - Harness Engineering.md → ==为什么这么做==（工程方法论，已有）
  - ReAct 与 Harness 实现.md → ==怎么写代码==（动手细节）
  - Function Calling.md → ==协议细节==

  ==每个文档只对一个抽象层负责==，不混着讲。

  **原因**：用户在讨论中识别两个连续的结构问题——(1) "ReAct 这个文档前面提一下下面又说了一下，感觉整个文档都有问题"；(2) 询问 Function Calling 的 tools 参数细节、四种角色、响应结构后判断"这个我也需要单独了解一下，需要新建一个章节"。本次重构==把 Agent 核心概念从单文件巨型文档拆为四个职责清晰的文档==，同时把用户追问的协议细节系统化为完整的 Function Calling 协议参考

- `wiki/LLM/Agent核心概念.md` §三 Function Call ==开头新增 §3.0"从零写一个极简 ReAct"==——回应用户疑问"工具是怎么知道的、prompt 是不是手写、怎么识别工具"。原 wiki 讲了 Function Calling 是什么但==没给最底层的实现示例==。新增内容：
  - **60 行完整可运行的 Python ReAct 实现**：定义工具（手写函数）→ 工具注册表（手写 dict）→ system prompt（手写文字）→ ReAct 循环 6 步（拼 prompt / 调 API / 解析 / 检测完成 / 执行工具 / 拼回 history）
  - **工具如何"被识别"的两种方式对比**：
    - 方式 A 传统做法（prompt 里写文字 + 框架 json.loads 解析）—— 早期 LangChain，==解析常失败==
    - 方式 B Function Calling（==生产事实标准==）：tools 参数传结构化 schema + API 直接返回 dict，模型专门训练过结构化输出
    - 完整对比表（5 维度）
  - **5 个常见疑惑 FAQ**：prompt 是不是手写、工具描述要多详细、LLM 怎么知道工具该不该用、调 API 是什么、能不能并行调多工具
  - **完整流程串起来**：你写什么 + 运行时框架做什么 + 没有"魔法"全是代码
  **关键认知补丁**：让读者从"工具调用感觉很神秘"变成"原来全是手写代码 + 几次 HTTP 请求"。配合 §2.0 的"屋里写纸条 + 屋外秘书"类比，==形成完整的从抽象到实现的认知链==
  **原因**：用户问"工具怎么被识别、是不是写代码识别"——这是 Agent 工程的另一个底层认知，缺少最小可运行示例之前永远理解不深；本次给出 60 行完整代码 + 两种识别方式对比，让读者亲眼看见每一行都是手写的

- `wiki/LLM/Agent核心概念.md` §二 ReAct 章节开头新增 §2.0"LLM 是什么？Harness 是什么？"
  - **LLM 的本质：无状态纯函数**——明确列出 LLM ❌不会做的 7 件事（调 API、读文件、执行命令、记忆、循环、等待、真的"做"）
  - **完整拆解例子**："帮我看下 pom.xml 里有什么依赖" 在没框架（LLM 凭空编）vs 有 ReAct（两轮真实交互）的对比
  - **谁干了什么完整对照表**（14 行分工）——LLM 只决定"做什么"，Harness 真的去"做"
  - **"屋里写纸条 + 屋外秘书"类比**——LLM 被关在屋里只能写纸条，Harness 是屋外做实际执行的秘书
  - **为什么这么分**：LLM = 文本输入 → 文本输出仅此而已；所有"做"必须由确定性代码（Harness）去执行
  - 引用 [[Harness Engineering#四、Harness 为什么比模型本身更重要？]] 的 42%→78% 数据强化"Harness 比模型本身更重要"的认知
  **原因**：用户读完 ReAct 仍困惑"思考和行动都是 LLM 干的吗"——这是 Agent 工程的最底层认知，缺少具体例子之前怎么读 ReAct 都讲不通；本次补全在 ReAct 定义之前先讲清"LLM 在屋里只能写文字、Harness 在屋外做实际执行"，下面再读 ReAct 就一目了然

- ==wiki/LLM/ 目录重构==——用户指出"目前文档有点乱了"。盘点发现 4 个问题：(1) Agent工程实践.md 跑偏（§五 Embedding + §六 Agentic RAG 与 wiki/RAG/ 重复）、(2) Hermes/OpenClaw/框架选型 三个小文件过度细分（128+150+63 行讲同一类）、(3) Prompt与Harness.md 文件名 vs 内容矛盾、(4) Agent核心概念.md §六 Skills 与独立文件 Agent Skills体系.md 重复。==11 文件 / 2911 行 → 9 文件 / 2780 行==：
  - **删除并合并** Hermes框架.md / OpenClaw框架.md / 框架选型.md 三个小文件（341 行）→ ==新建 [[Agent框架]].md==（302 行），扩展为：
    - 国际主流：LangChain / ==LangGraph（生产首推）== / CrewAI / AutoGen / LlamaIndex
    - ==Java 生态：Spring AI==（Java 工程师必学）
    - 国产：OpenClaw（飞书/企微）+ Hermes（自进化）
    - 完整选型决策树 + Java 工程师实战建议
  - **重命名** Prompt与Harness.md → ==[[Prompt Engineering]].md==，删 §三"三代演进"（已与 Harness Engineering.md §一 重复），文件从 196 → 142 行，聚焦 Prompt 核心技术 + Fine-tuning vs RAG vs Prompting 决策框架
  - **修改** Agent工程实践.md §五 + §六：删除 Embedding 算法详细内容 + Agentic RAG 详细内容（已在 wiki/RAG/ 完整覆盖），改为==导航表==指向 RAG向量与Embedding / RAG高级技术 / RAG评估 / 幻觉与置信度等具体文件。==629 → 587 行==
  - **修改** Agent核心概念.md §七 RAG 核心要点：修正过期 wikilink（[[检索召回与优化]] / [[RAG管道设计]]）→ 指向 wiki/RAG/ 现有文件
  - **重写** LLM索引_MOC.md：完全重组结构（基础理论 / 工程框架 / Agent 体系 / RAG 体系 / AI 产品形态 五大类），更新所有文件描述，更新 4 阶段学习路径（加 RAG 评估/安全/Code RAG 阶段）

  **重构后清晰分类**：
  ```
  基础理论：LLM基础与训练.md
  工程框架：Prompt Engineering.md / Harness Engineering.md
  Agent 体系：Agent核心概念 / Skills体系 / 工程实践 / 框架
  AI 产品：AI编程工具.md
  RAG 体系：已独立到 wiki/RAG/
  索引：LLM索引_MOC.md
  ```

  **原因**：用户使用 wiki 时感觉"乱"——根因是子主题过度细分（3 个小框架文件）+ 内容跑偏（Agent 工程实践讲了 RAG）+ 文件名与内容不符（Prompt与Harness 实际只讲 Prompt）。本次重构把分类边界划清，==Agent 与 RAG 解耦、Prompt 与 Harness 分离、框架对比集中==

- ==补强"ReAct / Function Calling / Agentic RAG 的分层关系"==——回应用户疑问"ReAct 属于 LLM 哪部分、工具调用是不是 Harness Engineering"。原 wiki 各文件都讲了局部知识但==没明示三个概念的层级关系==，读者容易把 ReAct 和 Function Calling 混为一谈：
  - `wiki/LLM/Agent核心概念.md` §二 末尾新增 §2.4"ReAct/Plan-and-Execute/Reflection 在分层架构中的位置"：
    - ==四层分层架构图==（Application / Harness Engineering / LLM 推理 / 基础设施）
    - 分工表（决定调什么工具 / 输出 JSON / 实际调工具 / 收 Observation / 控制循环 / 决定继续 各属于哪一层）
    - ==ReAct vs Function Calling vs Agentic RAG 的层级对照==（控制流 / 协议 / 应用三层）
  - `wiki/LLM/Harness Engineering.md` §三 六大组件后新增"控制流模式（六大组件之外的第七维）"：
    - 三种控制流（ReAct / Plan-and-Execute / Reflection）对照
    - 关键认知：==这三种都是 Harness 层实现的，LLM 自己不会循环==
    - 三概念关系：Function Calling 协议 → ReAct 控制流 → Agentic RAG/Coding Agent 应用
  **核心认知补丁**：==ReAct 是 Harness Engineering 的控制流模式==，==工具调用横跨 LLM 协议层（Function Calling）和 Harness 标准化工具集成层==，==Claude Code/Cursor 这种产品的核心创新不在 LLM，而在 Harness 设计==
  **原因**：用户读 Coding Agent 实现描述（"ReAct 循环 + 工具调用：读文件/grep/RAG/MCP..."）后疑问"这属于 LLM 哪一层"——本质是分层抽象没有显式映射；本次补强把分层关系从隐含变为明示，避免下个读者再有同样疑惑

- ==RAG 目录新增 4 个生产工程文档==——回应"RAG 还缺什么方向"的盘点分析。当前 wiki 在算法和前沿层面已经超过大多数 RAG 工程师的深度，==短板在生产工程的"评估 / 安全 / 监控 / 失败模式"==。本次补全 P0+P1：

  - **`wiki/RAG/RAG评估.md`（456 行，P0）**：
    - 检索质量指标（Recall@K / Precision@K / MRR / NDCG）+ Python 实现
    - ==RAGAS 框架四指标==（Faithfulness / Answer Relevancy / Context Precision / Context Recall）+ 实操代码 + 四指标联合诊断
    - 评估数据集构建（真实日志采样 / LLM 合成数据 / 难负例挖掘 / Cohen's Kappa 一致性）
    - 人工评估 / A/B 测试 / 生产监控 / Drift 检测
    - 6 个常见错误（测试集污染 / 单一指标盲点等）

  - **`wiki/RAG/幻觉与置信度.md`（497 行，P0）**：
    - RAG 幻觉三类型（事实 / 归因 / 推理）
    - ==四种幻觉检测方法==：引用对齐（生产主选）+ NLI 模型（快）+ 自一致性（高风险）+ LLM-as-Judge（最强）
    - 三层置信度（Token-level / Chunk-level / Answer-level）+ Calibration
    - Verify-Then-Answer 流水线 + Anthropic Constitutional AI
    - 拒绝回答策略 + Citation 强制引用 + 端到端 traceability
    - 三档生产组合（低/中/高风险）+ 实测效果数据

  - **`wiki/RAG/RAG安全.md`（504 行，P1）**：
    - RAG 系统攻击面（vs 普通 LLM）
    - ==Prompt Injection 在 RAG 的攻击模式==（直接 / 间接 / 投毒）+ Bing Chat / Copilot 真实案例 + 4 道防御（Context 隔离 / 输入清洗 / 输出校验 / 来源信任分级）
    - PII 三道关（入库前脱敏 / 检索时权限过滤 / 输出前扫描）+ 检测工具
    - 多租户隔离三方案（逻辑 / 索引 / 物理）+ 选型矩阵
    - 审计合规（GDPR / HIPAA / 等保）+ 被遗忘权实施
    - 四级生产实施（公开/内部/机密/顶级机密）

  - **`wiki/RAG/Code RAG.md`（498 行，P1，==Java + Agent 方向直接相关==）**：
    - Code RAG 与文档 RAG 的差异（结构化 / 显式依赖 / 跨文件上下文）
    - AST-aware 切分（tree-sitter）+ 多粒度索引 + 元数据丰富化（calls / called_by）
    - 代码 Embedding（CodeBERT / Qwen2.5-Coder-Embedding / Voyage-code-3）
    - ==仓库级 RAG==（Cursor 模式）：增量更新 / 5 路多路召回 / 上下文窗口的代码组装
    - ==调用图增强==（LSP / 静态分析 / 图+向量混合）+ GitNexus 实例
    - 主流产品对比（Cursor / Continue / Copilot / Claude Code / sweep-ai）—— Claude Code 的"无索引"模式特别

  - 更新 `wiki/LLM/LLM索引_MOC.md` §2 RAG 体系，新增 4 个文件的索引条目

  **原因**：用户要求盘点 RAG 缺口并补充 P0+P1 必须补的文档；这 4 个文件把 RAG 从"算法懂"提升到"懂落地"——是==实战派面试官重点考察的方向==；Code RAG 与 Cursor/Claude Code 主流产品直接相关，对 Java 工程师做 AI 编程类项目尤其重要

- `wiki/RAG/多轮对话.md` §2.4 方案 5 分层摘要 ==大幅扩展==——回应用户疑问"基础摘要、高度浓缩都是谁干的、怎么干的，没说明白"。原版只给了 `llm_summarize(middle, max_tokens=500)` 这种==空伪代码==。新增 6 个子节：
  - **谁来干？** 三种执行者选择对照表（主 LLM 同步推荐 / 小模型异步 / 主模型异步）
  - **怎么干？** 三种压缩组织方式：方式 1 两步独立压缩（错误示范）、方式 2 递增式压缩（==高度浓缩从基础摘要再压一次而不是从原文==）、方式 3 滚动增量压缩（生产真实做法，按轮次触发只压新增部分）
  - **两个 prompt 怎么写：** 完整列出基础摘要 prompt（逐轮 1-2 句）和高度浓缩 prompt（整段 1 句）的具体内容、输入差异（基础摘要从原文 vs 高度浓缩从基础摘要）、5 维差异表
  - **触发时机：** token 阈值 80% 主触发 + 轮次阈值兜底
  - **完整可执行 Python 示例：** BASIC_PROMPT / HEAVY_PROMPT 常量 + hierarchical_summarize 函数（Step 1 middle → 基础摘要、Step 2a early → 基础摘要、Step 2b → 高度浓缩）
  - **摘要不可逆——生产必须留原文：** 始终保留原文在持久化层、用户追问时绕过摘要从 PG 拉原文、摘要是 prompt 优化不是数据替代
  **原因**：用户读完 §2.4 方案 5 后追问"基础摘要和高度浓缩到底谁干的怎么干的没说清楚"——原文只给了一个空伪代码 `llm_summarize`，没讲执行者选择、组织方式、prompt 差异、触发时机、原文保留等关键工程细节；本次补全把分层摘要从"概念示意"升级为"可工程实施的方案"

- `wiki/RAG/RAG检索策略.md` §一 ==大幅扩展现代查询路由模式==——回应用户洞察"反问澄清/网络检索/工具调用/思维链都属于 Harness Engineering 的内容"：
  - §一开头加 ==info callout== 明点：Function Calling / Agentic RAG / 查询改写 / 反问澄清 / Web Search / 思维链==都不是 RAG 独立发明的概念==，而是 [[Harness Engineering]] 在 RAG 场景下的具体应用；通用架构（六大核心组件、三层上下文压缩）引用过去而非重复
  - §1.1 Function Calling 工具列表==扩展三个新工具==：`web_search`（知识库未命中或时效要求高时）、`ask_clarification`（反问澄清）、`self_reflect`（思维链反思）；加 tip callout"工具不是越多越好"引用 [[Harness Engineering#六、企业级实战经验|Stripe 经验]]
  - 新增 ==§1.4 反问澄清==：四个模糊 query 案例 + 三种实现（LLM 判断模糊度 / Function Calling ask_clarification 工具 / 检索置信度触发）+ 四个设计原则（不要每次问 / 给具体选项 / 历史推断优先 / 有降级路径）+ 学术参考（CLAM / Self-Ask）
  - wikilink 串联：[[多轮对话#3.2 指代消解]]、[[Harness Engineering#三、六大核心组件]]
  **原因**：用户提"LLM 不理解时反向提问澄清，应该是 Agentic RAG 的一部分"——判断完全准确；==更关键的洞察==是用户进一步指出"这些其实是 Harness Engineering 的内容"——这个认知打通了 RAG 和 Agent 框架的统一视角。本次补全反问澄清的具体实现，同时引用 Harness Engineering.md 避免重复

- `wiki/RAG/多轮对话.md` §二 上下文窗口管理 ==大补==——新增 §2.4"怎么知道哪些消息重要？五种判断方案"（插在原 §2.4 触发时机之前），原 §2.5 摘要质量考量被新内容覆盖删除：
  - **方案 1**：让 LLM 自己判断（最常用）—— prompt 明确列"必须保留 vs 可以舍弃"
  - **方案 2**：消息入库时打 is_critical 标签，总结时优先保留
  - **方案 3**：纯规则启发式打分（数字+2、决策关键词+3、寒暄-3、长度<8字-2）
  - **方案 4**：基于注意力/嵌入的重要性评分（MemGPT、attention head、嵌入聚类）—— 研究前沿，生产少用
  - **方案 5**：分层摘要（生产推荐）—— 最近 5 轮原文 / 6-15 轮基础摘要 / 15+ 轮高度浓缩
  - **推荐组合**：方案 2 入库标注 + 方案 5 分层 + 方案 1 LLM 摘要
  - **常被忽略的 4 个细节**：时效性衰减、实体共指（代词替换）、决策反转保留（A→B 两版本都留）、失败/未解决标记
  **原因**：用户读 §2.3 截断策略后追问"摘要压缩怎么知道哪些重要、算法怎么知道的"——这是摘要压缩最关键的工程难点，但原文档只在 §2.5 给了一个表格，==没讲清楚"算法不知道，是人通过 prompt/规则/标注告诉它的"==这个核心认知；本次补全五种判断路径 + 实际推荐组合

- ==RAG 拆分独立目录==：`wiki/LLM/` 下 RAG 4 个文件占了 52%（3037 行 / 总 5846 行），目录过于拥挤。==git mv 到 wiki/RAG/==，与 wiki/数据格式/、wiki/文档解析/ 同级
  - `git mv wiki/LLM/RAG基础与架构.md → wiki/RAG/RAG基础与架构.md`
  - `git mv wiki/LLM/RAG检索策略.md → wiki/RAG/RAG检索策略.md`
  - `git mv wiki/LLM/RAG向量与Embedding.md → wiki/RAG/RAG向量与Embedding.md`
  - `git mv wiki/LLM/RAG高级技术.md → wiki/RAG/RAG高级技术.md`
  - 4 个文件 frontmatter `module: LLM` → `module: RAG`
  - Obsidian wikilink 不依赖路径，移动后引用全部仍有效（无文件名重复）

- ==新建 wiki/RAG/多轮对话.md==：把散落在 6 个文件的多轮对话知识统一起来——RAG检索策略.md §四（去重）、Hermes框架.md（SQLite）、Agent核心概念.md（Memory L1/L2/L3）、LLM基础与训练.md（上下文窗口）、OpenClaw框架.md、Baize项目/库表设计.md。覆盖三大主题：
  - **§一 对话存储设计**：双表 schema（conversations + messages）、消息格式（含 chunk_ids 用于去重）、SQLite/Redis/PostgreSQL 选型对比、写入时机、会话 ID + TTL
  - **§二 上下文窗口管理**：组成（system + history + retrieval + query）、128K 典型预算分配、三种截断策略（滑动窗口 vs 摘要压缩 vs 混合）、80% 触发阈值、摘要质量考量（保留实体/时间/数值/决策）
  - **§三 多轮检索衔接**：consumed_ids 提取算法（最近 5 轮 messages.metadata.chunk_ids）、指代消解（LLM 改写）、查询改写依赖历史、==重复问题缓存快速路径==（hash + Embedding 相似度双重判定，5 分钟 TTL，绕过检索和 LLM）
  - §四 与 Agent Memory L1/L2/L3 的边界（一句话引用）

- `wiki/RAG/RAG检索策略.md` §四 加 callout 引用 [[多轮对话]]，明确"本节讲去重的工程实现，consumed_ids 怎么提取/对话历史如何存储/查询改写如何依赖历史详见多轮对话.md"

- `wiki/LLM/LLM索引_MOC.md` 调整 §2 RAG 技术体系：标注"已独立到 wiki/RAG/"，新增 [[多轮对话]] 引用，更新各文件描述

- `AGENTS.md` 目录结构更新：新增 `wiki/RAG/`，注释 `wiki/LLM/` 中 RAG 已独立

  **原因**：用户提两个紧密相关的问题——(1) RAG 占 wiki/LLM/ 52% 太拥挤，应独立成目录；(2) 多轮对话相关知识（对话存储、上下文窗口、检索去重、重复问题快速回答）散落在 6 个文件没有中心文档。本次把两件事一起做，==独立 RAG 目录的同时新建多轮对话.md 把零散知识统一==

- ==分块章节再次重构：去掉 L5 编号 + SOTA 详细内容拆出独立文件==
  - **问题诊断**（用户指出）：
    1. L1-L4 是"决定 chunk 边界的方式"层层递进，但 L5（chunk 内容增强）实际上和 L1-L4==正交==——可以叠加在任何一层之上。叫 L5 让读者误以为是 L4 的升级版，破坏了层级关系的语义
    2. 5.3 SOTA 章节太长（64 行），主文档不便阅读；且和 L4/L5 已有概要==重复==（命题化切割、Contextual Retrieval、Late Chunking 各讲了两遍）
  - **新建** `wiki/文档解析/分块策略前沿.md`（349 行）—— 详细技术解读：
    - Contextual Retrieval（Anthropic 2024）：算法 + Prompt Caching 优化 + 实测数据
    - Late Chunking（Jina 2024）：先编码再切的机制 + 工程要求
    - 判别式分块（金山办公 2026）：五阶段架构 + VFSC 超长块检索
    - 命题化切割（Dense X Retrieval, EMNLP 2024）：propositioner 模型 + 关联回原段落
    - 四种方法横向对比表 + 组合使用 + 与 L1-L4 基础方法的关系
  - **改造** `wiki/LLM/RAG基础与架构.md` §5：
    - §5.2 ==去掉 L5==：从"五层"改为"四种方法学"，明确说"L1-L4 在同一维度"
    - §5.2 callout 明确点出==与 L1-L4 正交==的两类策略：内容增强（5.3 单独讲）、在线检索增强（[[RAG检索策略]] 单独讲）
    - §5.3 ==重命名+大改==："chunk 内容增强（与切分边界正交）"，只保留 Contextual Retrieval、Late Chunking 概念，详细内容引用 [[分块策略前沿]]
    - 删除原 §5.3 SOTA 详细章节（搬到前沿文档）
    - §5.5 选型决策树和典型组合表==去掉所有 L5 引用==
  **原因**：用户判断"L5 不能这么说，每个层级都可以使用，叫 L5 不合适"==完全正确==——这是命名维度错误；同时"SOTA 方法放在这个里面太多了，单独文档引用过去"==也对==——主文档应该是"概念 + 全局视图"，详细算法应单独成文便于查阅
  - frontmatter last_reviewed 更新为 2026-05-25

## 2026-05-22

- 重新划分分块策略（离线）与检索时上下文扩展（在线）的章节归属：
  - `wiki/LLM/RAG基础与架构.md` §5.2 L5 ==重命名==："检索时增强"→"chunk 内容增强"，仅保留 Contextual Retrieval 和 Late Chunking（纯离线策略）；Overlap 并入 L1（作为标配，不再单列）；==删除==父子分块、句子窗口检索（这两个本质是在线检索逻辑）
  - §5.5 选型决策树相应调整：父子分块/句子窗口移到"在线检索增强"列，wikilink 指向 RAG检索策略.md
  - §5.2 横向对比表更新：去掉父子分块、句子窗口、Overlap 单独行，增加"配套在线检索策略见 [[RAG检索策略#五、检索时的上下文扩展]]"提示
  - `wiki/LLM/RAG检索策略.md` ==新增 §五"检索时的上下文扩展"==：
    - 5.1 父子分块（Small-to-Big）：离线父子结构 + 在线检索逻辑（query→子块→parent_id→父块）+ 代码示例 + 关键参数
    - 5.2 句子窗口检索：单句向量化 + 动态窗口扩展 + 代码示例 + 关键参数
    - 5.3 父子分块 vs 句子窗口对比 + 选型建议
    - 5.4 与离线分块策略的关系（离线 chunk 形态 ↔ 在线检索逻辑配套表）
  **原因**：用户指出"L5 检索时增强"的命名和内容混乱——5 个方法里只有 2 个真正是"检索时"的（父子分块、句子窗口），其他 3 个（Overlap/Contextual Retrieval/Late Chunking）都是纯离线；分类按"离线/在线"边界严格切分后，每个章节只讲一个阶段的事，逻辑清晰
  **关联**：RAG检索策略.md frontmatter 加 ==父子分块、句子窗口== 标签；last_reviewed 更新为 2026-05-22；顺手修复 RAG检索策略.md L326 旧乱码"原��"→"原理"

- `wiki/LLM/RAG基础与架构.md` 第四章"文本清洗"扩写 — 从仅列"做什么"扩展为讲"怎么做"+"为什么"：
  - **4.1 噪声去除**：页眉页脚识别（跨页文本比对+位置过滤+版面分析模型三种方法）、PDF 水印识别（颜色/旋转/关键词/版面模型四种方法）、HTML 导航剥离（DOM选择器+文本密度算法）
  - **4.2 编码与格式规范化**：字符编码（BOM/chardet/替换字符）、空白符规范化（正则）、标点符号上下文判断、Unicode NFKC 规范化
  - **4.3 重复内容检测**：==关键决策"在 chunk 前做、文档/段落粒度做"==；精确去重（哈希）；==MinHash + LSH 算法原理==（k 个哈希函数取最小值得签名向量、LSH 分桶避免 O(n²)）；保留策略（最新/最完整/最权威）
  - **4.4 语言检测与分段**：fastText/langdetect/langid 工具对比；为什么要做语言检测（分词器/Embedding/分句规则）；混合语言按段落检测；逻辑段落识别的多信号判断；中英文句子分割（spaCy 处理缩写/引号边缘情况）
  - **4.5 内容增强**：缩写展开（首次出现替换避免膨胀）、停用词策略（向量检索不要移除停用词、不做词干提取）、元数据提取
  **原因**：用户在阅读第四章时疑问"页眉页脚怎么判断、水印怎么识别、重复检测在 chunk 前还是后、语言检测和分句怎么做"——原文只列了"做什么"没讲"怎么做"，是知识库的明显空白
  **关联**：last_reviewed 更新为 2026-05-22；引用 [[版面分析]]、[[DOM]]、[[RAG向量与Embedding#选型决策树]]、[[RAG高级技术#知识冲突处理]]

## 2026-05-20

- `wiki/LLM/RAG检索策略.md` §一 ==大改：从"传统意图分类"重写为"现代 LLM 路由模式"为主线== — 用户质疑"4 类映射表能 cover 那么多提问方式吗、感觉没用"判断完全准确：传统意图分类（关键词表/BERT 分类器/LLM 零样本分类）在 LLM 时代意义大幅减弱。新结构：
  - ==增加 warning callout== 直接点出"该架构在 LLM 时代意义减弱"
  - ==现代主流模式三大子节==（新主线）：
    - 1.1 Function Calling / Tool Use：把检索/计算/查数据库都暴露为工具，LLM 自己决定调哪个，意图识别隐式发生，==复合意图天然支持==，加新意图无需训练
    - 1.2 Agentic RAG：Agent 自主规划路由，引用 RAG-Gym 的"先反思再检索"
    - 1.3 查询改写：与其分类不如改写为检索友好形式，进阶 Multi-Query / HyDE / Step-back
  - ==传统意图分类的根本局限==独立小节：边界模糊、同义爆炸、粒度难定、维护成本高、复合意图被丢弃 → 解释为什么"映射表 + 4 分类"在生产中常常形同虚设
  - ==什么场景仍需要显式分类==（少数场景）：性能敏感快路径、多租户硬路由、合规分流、高并发兜底；三种方案现代取舍（极简规则 / LLM-as-Classifier / BERT 不推荐）
  - 实体提取保留但简化，重点改为"LLM 抽取生产推荐"，与 [[RAG向量与Embedding#元数据过滤与向量检索的结合]] 对接
  - 查询路由代码：从 if-else 分支换为 Function Calling 主导
  - 容错策略更新：Tool 调用失败回退、Agent 推理超时、空结果兜底、多路径并行
  - 顺手修 RAG高级技术.md §四 标题历史遗留乱码
  **原因**：用户在阅读 wiki 时怀疑"意图识别太少、用对应表分类真有效吗、感觉没啥用"——这个判断准确指向 2018-2022 年传统 NLU 时代的过时架构，需要把现代生产事实标准（Function Calling / Agentic RAG / 查询改写）作为主线

- `wiki/LLM/RAG向量与Embedding.md` ==Embedding 演进与选型大幅更新==——原内容停留在 2024 年初的"BGE-M3 vs GTE-multilingual-base 对决"格局，==落后了约一年==。LLM-backbone embedding 自 2024 年中起主导 MTEB 排行榜，需要把这一代写进 wiki：
  - §四 ==从"三代演进"扩展为"四代演进"==：新增第四代"LLM-backbone Embedding（2024-）——MTEB 排行榜主导"完整子节
    - 把 LLM 改造成 embedding 的四步关键技术：去 causal mask / latent attention pooling / 对比学习 + 难负例 / instruction tuning
    - 为什么效果显著超越 BERT：参数量级（568M vs 8B = 15x）+ LLM 已有的语义理解能力
    - 工程代价：显存（16GB）/ 延迟（10x）/ 向量维度（4096 vs 1024）/ 在线 query 编码吞吐压力
  - §五 选型决策树重写：==精度 vs 速度优先==分流，LLM 系走精度，BGE-M3/Qwen3-0.6B 走平衡，E5-small/MiniLM 走轻量
  - §五 主流模型列表==按 LLM-backbone / BERT-backbone / 轻量 / 多模态 / 商业 API 五类==重新组织：
    - LLM 系：Qwen3-Embedding-8B/4B/0.6B、NV-Embed-v2、GTE-Qwen2-7B、Stella-1.5B、Conan-v2、Linq-Embed-Mistral、Gemini Embedding、Voyage-3-large
    - BERT 系（仍主流）：BGE-M3、BGE-large-zh、mE5-large
    - 轻量：E5-small、MiniLM、BGE-small-zh
  - 强调"==榜首未必生产首选=="——8B 推理慢、向量维度大、向量库存储成本翻倍
  - frontmatter tags 加入 LLM-Embedding / Qwen3-Embedding / NV-Embed
  **原因**：用户指出"现在排行榜都是 Qwen 这种大语言模型的编码模型了"——判断完全准确；外部网络受限无法实时拉榜单，但根据知识库内已有信息（之前列出的 Qwen3-Embedding��Conan-v2、KaLM-Embedding 12B 等线索）可推断 LLM-based 已主导榜单；本次更新把这一架构变革明确写进 wiki

- `wiki/LLM/RAG向量与Embedding.md` §三 新增"元数据过滤与向量检索的结合"子节 — 原对比表只在 Qdrant 那行提了"原生支持过滤"，==容易误导成"只有 Qdrant 支持"==；实际==所有==主流向量库都支持元数据过滤，只是过滤与向量索引的结合方式不同。新增内容：
  - 修正对比表：ES "元数据过滤最强"、pgvector "SQL WHERE 表达力最强"、Qdrant "Hybrid 过滤是产品核心"
  - ==三种策略==：Pre-filter（先筛后向量，HNSW 图破坏导致降级风险）/ Post-filter（先向量后筛，可能返回 < K）/ Hybrid（HNSW 遍历时同步检查，生产首选）
  - 各家默认策略对照表（Milvus/Qdrant/Pinecone/Weaviate/ES/pgvector/Chroma）
  - ==元数据过滤解锁的 RAG 能力==：多租户/时间范围/多版本/权限/路由/多轮去重/多源冲突解决——引用之前已有的[[RAG检索策略#四、多轮对话检索去重]]、[[RAG高级技术#知识冲突处理]]、[[文本清洗#五、不去重而靠元数据 + 检索时过滤]]
  **原因**：用户问"ES 支持元数据过滤，其他向量数据库也支持吗"——这是 RAG 工程的核心能力但当前 wiki 没单独成节，只在选型表里偶尔提及；元数据过滤连接了去重/版本管理/多租户/冲突解决等多个其他主题，需要明确的中心节

- `wiki/LLM/RAG向量与Embedding.md` §一 KNN vs ANN 补充计算细节 — 原章节只有结论性对比表（"KNN 慢、ANN 快"），缺"为什么"。新增三个子节：
  - ==为什么 KNN 慢==：单次距离计算 = D 维点积 = D 次乘 + (D-1) 次加；典型 RAG 场景计算量对照表（1万→1亿 chunk × 768维），亿级 KNN 起步 50s
  - ==为什么 ANN 快==：核心洞察"RAG 不需要绝对最近，只需要足够近"；三大思想（减少候选集图/聚类/哈希/树、压缩向量 SQ8/PQ/RaBitQ、分层精排）
  - 实际加速对照表：N 越大 ANN 优势越大（HNSW 在亿级数据快 10000x）
  - 顺手修复 line 70/299/360 三处历史遗留乱码
  **原因**：用户疑问"为啥 KNN 慢、ANN 快，具体算法细节是啥"——原表只列结论不解释，这是 RAG 工程的核心算法洞察，需要把"单次点积运算量"、"O(N) vs O(log N)"、"牺牲精度换速度"这些机制讲清楚

- `wiki/LLM/RAG基础与架构.md` §二 在线阶段加 tip callout — 明确点出"==chunk 向量预先算好不重算、检索时只对 query 做一次 embedding=="这一 RAG 最核心的工程设计；类比传统数据库"建索引一次、查询多次"；引用 [[RAG向量与Embedding#二、向量检索算法]] 和 [[RAG高级技术#增量索引]]
  **原因**：用户疑问"chunk embedding 是存储时算还是检索时算"——这是 RAG 设计的最关键洞察，但原 §二 章节虽然分了离线/在线两阶段，没有明确点出"chunk 向量是预先算好不重算的"，只在"Embedding 模型在两个阶段必须一致"那句暗含；读者容易误以为 chunk 向量也是在线算的，需要单独 callout 突出

- `wiki/LLM/RAG基础与架构.md` §4.4 重写为现代版 — ==去掉过时的"语言检测 → 选不同分词器 → 中英分别分句"流程==。改为：
  - 多语言 embedding 时代不需要为路由分词做语言检测；==仅元数据打标签和 LLM 路由两个场景==才需要，用字符 Unicode 范围简单判断即可
  - jieba/pkuseg 等中文分词器==不是 RAG 入库流程必需==——embedding 模型自带 tokenizer，分词器现在主要用于混合检索的 BM25 路
  - 段落识别已在文档解析阶段隐式完成
  - 句子级切分按需做：通用正则/spacy 多语言/pysbd 三选一，==一个工具吃所有语言==
  - LangChain `RecursiveCharacterTextSplitter` 是事实标准，字符级递归切分
  **原因**：用户指出"多语言 embedding 已经普及，分句问题文档解析已经处理了，单独找分词器不方便"——三个判断完全准确，原 4.4 节的"先检测语言再选分词器"是 LLM 之前的传统 NLP 思路，到了 BGE-M3/Qwen3-Embedding 时代基本过时

- 新建 `wiki/文档解析/文本清洗.md` — 系统讲解文档清洗的工程方法论：清洗的三大目标、噪声去除（==页眉页脚的多特征联合识别==、PDF 水印的几何/颜色/位置特征检测、HTML 噪声）、编码与格式规范化、==重复内容检测的三个层级（字节级/chunk级/近似级）==、==为什么近似去重对正式文档常常误伤==（财报/合同/法规迭代/SKU 说明书等场景）、不去重而靠元数据 + 检索时过滤的工程模式、版本链管理、清洗的取舍原则
  **原因**：用户在重读 RAG基础与架构.md Section 四 文本清洗时质疑"近似去重对重大文件不合适，因为日期不同的版本可能正是数据本身的特征"——这个判断完全正确，是工业界 RAG 的常见踩坑；同时也疑问页眉页脚识别、PDF 水印检测等具体技术细节；这些都属于 Document AI 文档清洗工程方法论范畴，应单独成文
  **关联修改**：`wiki/LLM/RAG基础与架构.md` Section 四 开头加 callout 引用 [[文本清洗]]

- 新建 `wiki/文档解析/` 目录 + 五个技术领域文件 — Document AI 五大核心技术，每篇侧重"概念 + 输入输出 + 关键模型架构原理"：
  - **TSR.md**（表格结构识别）：TableFormer 双 Decoder 架构、TATR 的 DETR 改造、PubTabNet/FinTabNet 数据集、Pipeline vs 端到端
  - **OCR.md**（光学字符识别）：DBNet 可微分二值化、CRNN+CTC 经典管线、SVTR 纯 Transformer、TrOCR 端到端、PaddleOCR/PP-OCR 演进
  - **版面分析.md**：LayoutLMv3 多模态预训练、DETR/RT-DETR 端到端检测、阅读顺序的规则与学习方法、PubLayNet/DocLayNet 数据集
  - **公式识别.md**：im2markup（CNN+RNN+Attention 奠基）、Pix2Tex（ResNet+Transformer）、Nougat（Donut+mBART 端到端）、为什么是 image-to-sequence
  - **VLM文档解析.md**：三类典型架构（CLIP 双塔/Pali Vision-as-Token/Flamingo Cross-Attention）、Dolphin-v2 异构锚点提示、SmolDocling DocTags 输出语言、ColPali 视觉检索
  **原因**：用户在了解格式（XML/PDF/OOXML）后进入下一层疑问"TSR 模型是什么、各方案技术区别"；这些是 Document AI 子领域的���立技术，不属于"数据格式"也不完全属于 LLM，单独成目录最自然
  **关联修改**：`wiki/LLM/RAG基础与架构.md` 3.3 五个子节末尾各加 `详见 [[技术名]]` 引用；AGENTS.md 目录结构追加 `文档解析/`

- 新建 `wiki/数据格式/PDF.md` — 澄清"PDF 不是图片"的常见误解：PDF 本质是页面描述语言（Page Description Language，源自 PostScript），内部是绘制指令而非位图；数字版 PDF（Word/LaTeX 导出）vs 扫描版 PDF（扫描仪/拍照）的本质区别；PDF vs OOXML vs 图片三者信息密度对比；为什么 PDF 在格式光谱里是"低结构"
  **原因**：用户在重读 RAG基础与架构.md 3.2 格式光谱后疑问"PDF 不就是多个图片的合集吗，为什么比扫描件信息多"；这是常见误解，需要单独成文澄清；归到 wiki/数据格式/ 与 XML/DOM/OOXML 形成完整体系
  **关联修改**：`wiki/LLM/RAG基础与架构.md` 3.4.1 PDF 章节加 callout 简短澄清 + 引用 [[PDF]]

- 新建 `wiki/数据格式/DOM.md` — DOM 核心概念：W3C 标准（1998 年最早为 XML 设计）、DOM 的三层含义（数据结构 / API 标准 / 跨语言实现）、节点类型（Document/Element/Attribute/Text/Comment）、DOM 树结构示例与节点关系 API、==澄清"DOM 不只是浏览器专属"==（Spring/Mybatis/Android/Java JAXP/python xml.dom 都用 DOM）、浏览器 DOM 是核心 DOM 的扩展超集
  **原因**：用户感觉计算机网络.md 里讲 DOM 不合适——DOM 本质是数据结构 + W3C 接口标准，不是网络协议；归到 `wiki/数据格式/` 与 XML.md / OOXML.md 形成完整体系
  **关联修改**：`wiki/数据格式/XML.md` 第七章 DOM 部分简化为概念说明 + 引用 [[DOM]]，相关链接补 DOM；`wiki/计算机网络/计算机网络.md` 浏览器渲染管线里的 DOM 段落保留（作为流程一环必要），追加 ==DOM 不只是浏览器概念，是 W3C 跨语言标准，详见 [[DOM]]==

- 新建 `wiki/数据格式/OOXML.md` — 系统讲解 docx/xlsx/pptx 三种 Office 文档的内部 XML 结构：OOXML 标准（ECMA-376/ISO 29500）背景、共同的 ZIP+OPC 容器结构、docx 关键元素（`<w:p>`/`<w:pPr>`/`<w:tbl>` 等）、==xlsx 单元格 `<c>` 元素的 r/t/s 原生属性 + 共享字符串表 + 合并单元格 + 样式表==、pptx 形状与三层继承、主流解析库（python-docx/openpyxl/python-pptx）的本质
  **原因**：用户在重读 RAG基础与架构.md 3.4.3 Excel 时疑问"Excel 没有像 Word 一样的语义标签，'网格化的单元格 + 单元格属性原生可读'是怎么做到的"；用户明确指出 xlsx 内部结构不属于 XML 本身的概念，应该单独成文；OOXML.md 回答这个问题并扩展讲清三种 Office 格式的统一标准
  **关联修改**：`wiki/数据格式/XML.md` 第五章简化 docx 详细展开（保留概念说明 + 引用 OOXML.md），第八章和相关链接补 OOXML wikilink；`wiki/LLM/RAG基础与架构.md` 3.4.3 Excel 章节加 callout 简短说明 + 引用 [[OOXML#四、xlsx 内部结构]]

- 新建 `wiki/数据格式/` 目录 + `wiki/数据格式/XML.md` — 系统讲解 XML 基础概念：标记语言起源、XML 设计目标、XML 树（三种节点+良构规则）、XML vs HTML vs JSON 对比、原生语义与 docx/xlsx/pptx 内部结构、XML 命名空间、DOM 树本质、XML 在工程中的应用索引（Spring/Mybatis/Maven/SOAP/Claude Prompt 等）
  **原因**：用户重构 RAG基础与架构.md 第三章后产生疑问"什么是 XML、原生语义、XML 树、为什么会有它"，但 wiki 中 XML 只在 Spring/LLM/计算机网络 作为应用场景被提及，从未系统讲过；新建数据格式目录为未来 HTML/JSON/YAML 等基础知识预留位置；AGENTS.md 同步更新目录结构

- 计算机网络与操作系统从汇总文档独立 — 创建 `wiki/计算机网络/`、`wiki/操作系统/` 目录；`git mv wiki/汇总文档/计算机网络篇.md → wiki/计算机网络/计算机网络.md`、`git mv wiki/汇总文档/操作系统篇.md → wiki/操作系统/操作系统.md`；两个文件补 frontmatter（module/tags/difficulty/last_reviewed）；H1 标题去掉"篇"字；更新 `wiki/面试题目.md` 中 16 处 wikilink（`[[计算机网络篇#... → [[计算机网络#...`、`[[操作系统篇#... → [[操作系统#...`）；更新 AGENTS.md 目录结构描述
  **原因**：用户指出其他模块（Java基础/JVM/MySQL 等）都有独立目录，但计算机网络和操作系统只存在于"汇总文档/"下，结构上不一致；独立后可在目录下补充更细分的内容文件

## 2026-05-19

- `wiki/LLM/RAG基础与架构.md` Section 五"分块策略"重构 — 去项目化（删除 V1/V2/V3 版本号、保险条款举例、`is_key_clause` 等项目字段）、去具体指标数字，改为通用知识叙事
  **重构后结构**：5.1 核心矛盾 → 5.2 五层方法学全景（L1规则/L2结构感知/L3语义/L4命题级/L5检索时增强）→ 5.3 当前SOTA（Contextual Retrieval/Late Chunking/金山判别式/命题化）→ 5.4 通用工程参数（chunk_size/Overlap/元数据模板）→ 5.5 选型决策树
  **原因**：原章节读起来像保险项目的实施方案，而非通用知识；用户希望按"有哪些方法 → 最新方法 → 各自优势"组织

- `wiki/LLM/RAG基础与架构.md` Section 三"文档解析"重构 — 从"按文件类型分章"��为"内容元素 × 容器格式"两个正交维度
  **重构前问题**：3.2 PDF / 3.3 Word / 3.4 Excel / 3.5 PPT / 3.7 扫描件 各自重复讨论表格、图片、公式、OCR——同一个内容元素在每种格式下重复讲一遍
  **重构后结构**：
    - 3.1 两个正交维度（容器格式 vs 内容元素）
    - 3.2 格式光谱：按结构化程度排序（XML原生 → DOM → 坐标 → 像素 → 时序）
    - 3.3 内容元素的通用解析方法（核心章节）：表格TSR / 图片图表 / 公式 / 多栏版面 / OCR
    - 3.4 各格式的独有特点（去重后只剩独有难点：PDF坐标还原 / Word的OLE和样式 / Excel合并单元格和NL2SQL / PPT动画和备注 / HTML噪声 / 音视频时序）
    - 3.5 跳过解析（视觉检索）/ 3.6 工具全景 / 3.7 创新架构（保留原内容）
  **原因**：用户指出 PDF 是其他类型文件的"整合版"，文件类型和内容元素本就是正交的两个维度，按文件类型组织导致大量重复

## 2026-05-18

- `wiki/LLM/AI编程工具.md` — 新增 Section 六"Claude Code 概念体系"（Commands/Skills/Rules/Hooks/Subagents/Plugins 六层概念）；新增 Section 七"AI 辅助知识管理方法论"（三层架构/两个大脑分离/知识代谢循环）
  **来源**：Claude Code常用命令指北、Claude Code+Obsidian指南（方法论部分）

- `wiki/LLM/RAG检索策略.md` — 新增 Section 四"多轮对话检索去重"（VimRAG/GGPO 模型层方案 + Milvus `expr not in`/`group_by_field` 工程方案 + Redis 生产细节）；Section 2.4 新增"Embedding + Rerank 搭配原则"（同系列配对原则 + 四组经典搭配）；新增 ColBERT Late Interaction 架构说明（arXiv:2004.12832）
  **来源**：多轮对话重复召回文章（VimRAG arXiv:2602.12735）、BGE/GTE/Rerank选型文章

- `wiki/LLM/RAG高级技术.md` — Section 三新增"RAG-Gym：过程监督与策略优化"（ReSearch 架构 + 三维优化框架 + 实验数据 41%→60% F1，arXiv:2502.13957）；Section 七扩展为完整"多模态 RAG"章节（CLIP/Caption/ColPali 三方案对比 + MultiVectorRetriever 模式 + 表格处理 + 六个生产踩坑，ColPali arXiv:2407.01449）；原 Section 七改为 Section 八；新增"知识冲突处理"子节；Contextual Retrieval 补充 Prompt Caching 具体数据（成本降低 80-90%，失败率降低 49%/67%）
  **来源**：RAG-Gym文章、多模态RAG文章、万字面试题库

- `wiki/LLM/RAG向量与Embedding.md` — Section 五新增"选型决策树"（语种→上下文长度→部署资源三维决策）+ BGE-M3 vs GTE-multilingual-base 详细对比表 + MTEB 阅读方法；Section 六微调阈值补充（Recall@5 < 0.7 需微调 + 500-1000 对数据量指导 + 保险领域实测案例 MRR 0.58→0.82）
  **来源**：BGE/GTE/Rerank选型文章（BGE-M3 arXiv:2402.03216）

- `wiki/LLM/RAG基础与架构.md` — Section 5.6 新增"句子窗口检索"（单句向量化+动态窗口扩展）、"命题化切割"（Dense X Retrieval arXiv:2312.06648，EMNLP 2024）、六方案对比表；Section 六"上下文压缩"新增 Map-Reduce vs Refine 对比
  **来源**：文档切割6方案文章、万字面试题库

- `raws/inbox/Clippings/` 8 篇文档全部移入 `raws/processed/`（6 篇 RAG 文章已整合，2 篇 Claude Code 工具文章已提取方法论）



- `wiki/Baize项目/` 11 个文件整理 — 全部加 frontmatter（module: Baize项目）；修复 6 处断链（`[[检索召回与优化]]` 等旧路径 → LLM/ 下的新文件）；全部加 `## 相关链接`；6 个文件加 callout（Bitmap 追踪、WebSocket vs SSE、JWT 代价、分块表独立、动态上下文、Embedding 选型）
  **原因**：项目文件缺少 OBSIDIAN_STYLE 规范的基础结构

- `wiki/PaiFlow/PaiFlow工作流平台.md` — 加 frontmatter + `## 相关链接`
  **原因**：同上

- `wiki/面试题目.md` — 项目部分（Baize ~80 题）从 Agent与LLM 之前移到文件末尾；删除重复 `## 项目（Baize）` header；新增 `## 项目经验` 总标题；新增 `### PaiFlow（工作流平台）` + 8 题；总计 425 题
  **原因**：用户要求项目放最后，PaiFlow 需要面试题入口

- 全量审计修复 — `wiki/集合框架/集合框架索引_MOC.md` frontmatter 标准化（旧 aliases 格式 → 标准 4 字段）；3 个文件补全 `## 相关链接`（并发编程MOC、集合框架MOC、算法/数组.md）；11 个核心内容文件各加 1 个 callout（锁升级不可逆、线程池参数、happens-before、工作窃取、CountDownLatch vs CyclicBarrier、ArrayList 真实性能、HashSet 本质、JDK/JRE/JVM、双亲委派打破、EXPLAIN 三字段、雪花时钟回拨）
  **原因**：收尾审计，按 OBSIDIAN_STYLE 和 QUALITY_CHECKLIST 标准补齐遗漏

- `wiki/LLM/LLM基础与训练.md` P2 内容补充 — 新增 QLoRA（NF4/Double Quantization + 微调工具对比表）、Flash Attention（三代演进）、推理部署框架（vLLM/SGLang/TensorRT-LLM/Ollama/Triton 对比 + SGLang RadixAttention 详解）
  **原因**：LLM 工程师面试必备知识点

- `wiki/LLM/RAG向量与Embedding.md` P2 内容补充 — 向量数据库对比表扩充至 7 个（+Qdrant/Chroma）；新增生产级 RAG 检索优化（延迟优化 4 策略 + 并发优化）
  **原因**：向量数据库选型是 RAG 面试高频题

## 2026-05-09

- `wiki/LLM/` 全模块重组 — 8 个文件去除数字前缀（`01_LLM基础与训练.md` → `LLM基础与训练.md`）；`wiki/RAG/` 合并进 `wiki/LLM/`（2 文件迁入，占位文件删除，空目录删除）；`检索召回与优化.md`（1923 行）拆分为 `RAG检索策略.md`（871 行）+ `RAG向量与Embedding.md`（385 行）+ `RAG高级技术.md`（697 行）；新建 `LLM索引_MOC.md`；全 wiki 78 处 wikilink 更新（RAG/ 路径 → LLM/，数字前缀 → 简名）
  **原因**：RAG 与 LLM 是同一知识体系，分两个文件夹增加导航成本；数字前缀阻碍 wikilink 可读性；1923 行的单文件违反可维护性原则

- `wiki/面试题目.md` — 完整重写（541 行，410 题），重排模块顺序为 Java基础→集合框架→并发编程→JVM→MySQL→Redis→Spring→网络→OS→项目→Agent，MySQL 从 ~10 题扩充至 59 题（6 子分类），修复所有断链（volatile、锁与事务拆分后的旧链接）
  **原因**：原文件模块顺序与学习路径不匹配，MySQL 题目严重不足，多处 wikilink 指向已拆分/迁移的旧文件

- `wiki/MySQL/` 模块重组 — `锁与事务机制.md` 拆分为 `锁机制.md`（398 行）+ `事务与MVCC.md`（247 行）；`引擎与日志.md` 修正 binlog"物理日志"→"逻辑日志"并加 warning callout，删除 innodb_flush_log_at_trx_commit 重复段落；`MySQL 基础与架构.md` 加 3 个 callout；`索引机制详解.md` 加阿里手册 callout；7 个文件更新旧 wikilink
  **原因**：锁与事务合一文件过长且主题耦合；binlog 分类错误是技术硬伤

- `wiki/Redis/` 全 6 个内容文件 — 各加 1-2 个 callout（多线程、淘汰策略、SETNX 三大坑、everysec、脑裂、跳��� vs 红黑树）；`Redis 基础.md` DRY 去重 ~30 行哨兵/集群部署配置改为 wikilink；frontmatter 标准化
  **原因**：callout 密度为 0，缺少面试关键提醒；部署配置与高可用文件重复

- `wiki/Spring/` 全 5 个内容文件 — 各加 1-2 个 callout（三级缓存局限、@Resource、内部调用失效、CGLIB 默认、spring.factories 迁移、HttpMessageConverter）；frontmatter 标准化
  **原因**：callout 密度为 0，Spring 面试易踩坑点缺少醒目提示

- `wiki/算法/` 14 个文件 — 全部加 frontmatter + 相关链接；`算法_MOC.md` 完整重写（20+ 模式识别信号表、4 阶段学习路径）；`位运算.md` 从 63→240 行、`排序.md` 从 96→350 行；`数组.md`、`动态规划.md` 加���式识别信号表
  **原因**：算法文件缺少 frontmatter 和交叉链接；核心算法（位运算、排序）内容过少；缺少模式识别方法论

- `wiki/并发编程/` 审计修复 — `锁.md` 移除断链 `[[锁与事务机制|乐观锁]]`，volatile 章节精简为一句话 + wikilink；`线程基础与ThreadLocal.md` 新增完整 volatile 章节（60+ 行）；`Java 内存模型.md` 删除 2 条无效 markdown 链接；`并发编程索引_MOC.md` frontmatter 标准化
  **原因**：volatile 归属线程安全（可见性/有序性）更合理；断链和无效链接影响导航

- `wiki/集合框架/` 审计修复 — `集合框架概述.md` 修复 `U+FFFD` 损坏字符；`Set体系与HashSet.md` 去重 ~45 行 putVal 源码改为 3 条 bullet + tip callout；MOC frontmatter 标准化
  **原因**：putVal 源码与 HashMap 文件重复（DRY 违反）

## 2026-05-08

- `OBSIDIAN_STYLE.md` — 新增第 11 节"内容组织"（先概念后应用、单一信息源 DRY、就近原则），并在快速自检清单中补充对应检查项
  **原因**：JVM 文件审计发现内容组织问题，需将原则固化为规范

- `wiki/JVM/JVM 垃圾收集.md` — 重组章节顺序：GC 基础 → 算法 → GC 分类 → 并发 GC 通用机制（三色标记/漏标/写屏障/卡表 RSet/Safepoint）→ 收集器；合并重复的三色标记讲解
  **原因**：原顺序违反"先概念后应用"——收集器章节引用了尚未介绍的三色标记等机制

- `wiki/JVM/JVM JIT与字节码.md` — 调整章节顺序：JIT 基础 → 字节码基础 → JIT 核心优化；将字节码基础移至优化章节之前
  **原因**：invoke 指令等字节码概念在逃逸分析等优化章节中被引用，需先介绍

- `wiki/JVM/JVM 内存管理.md` — 删除 GC 收集器对照表，改为一句话 + wikilink；逃逸分析详解替换为概括 + 链接到 JIT 文件
  **原因**：DRY 去重，详解保留在各自归属文件，避免两处维护

- `wiki/JVM/JVM 类加载机制.md` — Class 文件结构和 invoke 指令详解替换为概括 + 链接到 JIT 文件
  **原因**：DRY 去重，详解归属 JIT 文件

- `AGENTS.md`、`QUALITY_CHECKLIST.md`、`RAW_WORKFLOW_GUIDE.md` — 按业界标准重写：AGENTS.md 改为任务触发式操作指令（Boundaries 三层结构 + 完成标准）；QUALITY_CHECKLIST.md 压缩至 149 行，新增可发现性检查；RAW_WORKFLOW_GUIDE.md 删除与 AGENTS.md 重复内容
  **原因**：原文件写成人类文档而非 agent 操作手册，业界研究显示散文原则会被 agent 忽略

## 2026-05-07

- `wiki/Java基础/` 全部 7 个文件 — 从浅层 bullet 扩充为面试深度内容，补充代码示例、对比表、callout、高亮、wikilink
  **原因**：原内容过于简略，缺乏代码示例和底层原理，不足以应对面试追问

- `wiki/并发编程/线程基础与ThreadLocal.md` — 新增 Condition API 完整讲解（生产者-消费者示例、vs wait/notify 对比表）、CompletableFuture 扩充（链式调用、组合多任务、异常处理）
  **原因**：Condition 和 CompletableFuture 是高频面试题，原文件缺失

- `wiki/集合框架/集合框架概述.md` — 新增 ArrayDeque vs LinkedList 对比表+代码、PriorityQueue 小顶堆/大顶堆+Top-K 应用
  **原因**：队列相关题目在面试中高频出现，原文件只有概述

- `wiki/集合框架/HashMap核心原理.md` — 扩充 LinkedHashMap（两种排序模式+LRU 缓存实现）、TreeMap（自定义 Comparator+范围查询 API+对比表）
  **原因**：LinkedHashMap LRU 和 TreeMap 是面试高频题，原文件缺失

- `wiki/JVM/JVM JIT与字节码.md` — 新建文件（350 行），覆盖分层编译、方法内联、逃逸分析三大优化、OSR、Class 文件结构、5 种 invoke 指令族、invokedynamic 与 Lambda 原理、CodeCache、JVM 预热
  **原因**：JIT 是 JVM 性能调优的核心，原知识库完全缺失

- `wiki/JVM/JVM 垃圾收集.md` — 补充三色标记漏标充要条件、SATB vs Incremental Update、写屏障、卡表/RSet、Safepoint、ZGC 染色指针
  **原因**：并发 GC 核心机制是面试区分度题目，原文件缺失

- `wiki/JVM/JVM 内存管理.md` — 补充直接内存、Mark Word 64 位位分布表、OOM 家族 7 种完整表，修正 ParNew 命名错误
  **原因**：OOM 分类和 Mark Word 结构是面试高频题，原文件缺失

- `wiki/JVM/JVM 调优.md` — 补充 GC 日志逐行解读、三个典型故障案例（CMS Concurrent Mode Failure、G1 Humongous、Metaspace OOM）
  **原因**：GC 日志解读和故障排查是实战面试必考，原文件缺失

- `wiki/JVM/JVM 类加载机制.md` — 补充 Class 文件结构、五大 invoke 指令族
  **原因**：字节码层面知识是 JVM 深度题目的基础

- `wiki/LLM/06_Agent工程实践.md` — 新增 Agent 可靠性设计（死循环检测、Fallback、错误累积防护、Prompt Injection 防御）和幻觉缓解策略
  **原因**：Agent 岗位面试的区分度题目，原文件缺失

- `wiki/LLM/01_LLM基础与训练.md` — 补充 Self-Attention 计算细节（√dk 解释）、Decoder-only 架构、LoRA 微调原理
  **原因**：LLM 底层理解是 Agent 岗位的加分项

- `wiki/面试题目.md` — JVM 板块从 16 题扩充为 37 题（5 个子分类），并发编程新增 Condition，集合框架新增 LinkedHashMap LRU/ArrayDeque/PriorityQueue
  **原因**：与 wiki 内容同步，确保面试题目索引完整

- `OBSIDIAN_STYLE.md` — 新建，定义 wiki 的 Obsidian 格式规范（高亮/callout/frontmatter/wikilink），区分业界共识和本 vault 自定义规则
  **原因**：之前无成文规范，AI 协作时风格不一致

- `QUALITY_CHECKLIST.md` — 新增 Obsidian 风格规范检查节，修正旧文件路径，删除重复 callout 条目，更新自动化工具脚本
  **原因**：与 OBSIDIAN_STYLE.md 保持同步，修正过时路径

- `AGENTS.md` — 更新目录结构（加入 Java基础/、JVM/、OBSIDIAN_STYLE.md），更新题目数量（230 → 325），新增 OBSIDIAN_STYLE.md 引用
  **原因**：目录结构和题目数量已过时

