# 知识库优化记录

## 2026-04-20
- **并发编程/全模块格式重构**：对 `wiki/并发编程/` 下的五个文件进行了系统性排版清洗与内容修正。
  - **标题规范化**：将 `线程基础与ThreadLocal.md`、`并发工具类.md`、`线程池.md`、`并发容器和框架.md` 的顶级标题从 `##` 统一升级为 `#`，符合知识库单文件单主题规范。
  - **内容纠错**：修正 `线程基础与ThreadLocal.md` 中"携程"错别字为"协程"；修正"有序性"定义错误（原文误将死锁/饥饿/活锁归入有序性，已更正为 JVM 指令重排序的正确定义）。
  - **代码输出块包裹**：将 `线程基础与ThreadLocal.md` 中多处裸奔的程序输出文本（线程列表、start/run 对比输出、sleep/wait 对比输出）统一包裹进 ` ```text ` 代码块，消除大行距与误解析。
  - **ThreadLocal 注释内联**：将四处游离在代码块外的 `//创建`、`//设置`、`//获取`、`//删除` 注释行移入对应的 ` ```java ` 代码块内，符合标准代码注释规范。
  - **线程状态表格标准化**：将 `线程基础与ThreadLocal.md` 中非标准的裸文本线程状态表（NEW/RUNNABLE/BLOCKED/WAITING/TIMED_WAITING/TERMINATED）转换为标准 Markdown 表格；同时修正原文 `TIME_WAITING` 拼写错误为 `TIMED_WAITING`。
  - **并发工具类表格标准化**：将 `并发工具类.md` 中两处非标准表格（CyclicBarrier vs CountDownLatch 对比表、BlockingQueue 实现类对比表）转换为标准 Markdown 表格，去除 ✅/❌ emoji 符号。
  - **线程池状态表格标准化**：将 `线程池.md` 中线程池五种状态（RUNNING/SHUTDOWN/STOP/TIDYING/TERMINATED）的非标准表格转换为标准 Markdown 表格。
  - **锁模块修复（上一轮）**：`锁.md` 完成了标题升级、ASCII 图表包裹、synchronized 指令表格标准化、汇编代码块包裹等全面修复。

## 2026-04-19
- **数据库/索引机制**：针对 `wiki/MySQL/索引机制详解.md` 进行了一次精细化人工修正，解决了此前命令行造成的格式错乱问题。
  - **排版清洗与 Markdown 标题化**：找出了因自动重配丢失加粗或标题层级的散乱文本（如”什么是区分度？”、”一句话回复”等游离句段），严格提拔为标准的 `### [问题]` 三级标题，解决了排版混乱的痛点。
  - **代码块包裹**：找出了多处未包裹的 SQL 语句和建表语句（裸奔状态），使用标准的 ````sql```` 包裹整段查询条件及建表逻辑，极大增强了代码高亮显示。
  - **去首行缩进与错位纠正**：修复了批量替换造成的大量首段前置空格、缩进引起的 Markdown 代码块/引用误解析，让整体阅读体验更加丝滑，排版更加标准。
- **数据库/索引机制**：对 `wiki/MySQL/索引机制详解.md` 发起了大规模的**“去题海化格式重构”**操作。
  - **排版清洗**：全面移除了诸如 `🌟41.` 这种生硬的题号标签，以及之前陈旧的 `---- 这部分是帮助大家理解 start，面试中可不背 ----` 这种臃肿的八股文辅导注释。统统将其提取、转化成了优雅标准的 `### [概念问题]` 和 `#### 💡 原理深度剖析与防坑避雷：` 等 Markdown 锚点层级。
  - **模块化改造**：将那些散落在文中的散乱概念（如“什么是区分度？”、“什么样的字段适合加索引？”、“说说优化的思路”）强行提拔为正规的 `###` 级核心知识簇，极大地增强了后续双向链接时的准确定位能力。
- **网络传输底层/TCP 与 UDP**：在 `wiki/计算机网络篇.md` 全新录入了“四、传输层核心对决”大核心，将常考但极难理解其真正使用场景区别的《TCP 与 UDP 传输速度之争》彻底拆解入库。
  - **教学改进（底层隐喻与性格剖析）**：彻底摒弃了死背“它们的首部是不是 20 字节、是否面向字节流”等僵硬八股。独创了**“打电话求回音的稳重挂号信（TCP）”**与**“拿着大喇叭瞎吼根本不管别人听没听见的土匪明信片（UDP）”**的直观性格论！
  - **从原理推导快慢本质**：基于前文讲透的“TCP 极其沉重的前戏（必须三次握手打招呼）”与“重如泰山的包袱（滑动窗口与序列号记录机制）”，让读者通过上帝视角去降维打击这道题，深刻认识到 UDP 是因为“斩断了一切确认回执的牵挂”，才能获得压制性的超高发射速度。
  - **结合实际工程场景的终极升华**：解释了为什么**直播、在线竞技游戏（王者荣耀等）必须且只能用 UDP 即便它会丢包**（宁可接受一两毫秒的画面撕裂，也绝对不能为了纠正一帧的失误而触发 TCP 的死循环重传卡死）。这个降维大白话是目前全网讲的最透彻的游戏抗延迟底层原理。

- **前端知识/基础概念**：在 `wiki/计算机网络篇.md` 的《键入网址到网页显示》一题中，扩写了对于“浏览器解析渲染页面”阶段的名词大白话扫盲。
  - **名词重构**：专门针对“什么是 HTML”、“什么是 HTML 解析”、“什么是 DOM 树”、“什么是 CSS 解析与 CSSOM 树”进行了“扫文盲”级别的物理重构解释。将晦涩的前端对象名词解释为了**“宜家纯文本图纸（HTML）”**被**“逐行翻译（HTML解析）”**的过程；而翻译后的产物，则是在电脑内存里建起了一棵**“像家族大树一样的网页活体骨架（DOM 树）”**，再给这棵骨架做了一套**“量身剪裁的衣服版型（CSSOM 树）”**，最后依靠 JS 让静止的画卷拥有交互和灵魂。彻底讲透了这段过程的实体化变迁。
- **系统架构/内容扩充**：在 `wiki/计算机网络篇.md` 补充了被誉为“计算机八股文之王”的超高频体系题《键入网址到网页显示，期间发生了什么》。
  - **知识精修**：深入剖析了第一步地址栏分析中，浏览器对“关键字搜索”和“合法 URL”的区别处理：明确指出对于搜索关键字，浏览器底层逻辑其实是将关键字 URL 编码并强制拼接到默认搜索引擎（如百度、Google）的查询 URL 末尾，从而将“无地址搜索”统一转换为这道经典名题后续的“去搜索引擎取数据的合法 URL DNS 解析流”。扫除了流程背诵里的常见断点。
  - **教学改进**：没有采用传统的名词干瘪堆叠，而是采用**《全链路宏观视角：去饭店点外卖吃》**的物理隐喻来贯串。从 URL 与 DNS（查黄页找饭店地址）、TCP 建连（打通订餐电话拿号牌）、HTTP 请求与响应（递交外卖单和后厨炒菜装盒）、到最终由于前端解析渲染（像拆宜家家具盲盒一样把 HTML 骨架与 CSS 衣服穿在一起）再到四次挥手（吃完抹嘴走人）。将网络体系极其融洽地具象化了。
  - **内链沉淀**：在该题的详解阶段，针对 TCP 阶段反手向上一节的《TCP 三次握手与四次挥手》做了原生地 Markdown 同文档复用链接 `[[#TCP三次握手与四次挥手]]`，真正做到了“套套相连”。

- **系统架构/双向链接实战**：基于“坚决抵制知识孤岛（Cross-Linking 强制互吹）”的核心操作逻辑，完整跑通了**从单纯的面试题 Checklist 到体系知识库**的双向穿梭模型。
  - **面试驱动体系**：在 `wiki/面试题目.md` 中新增了“计算机网络”大类，并录入经典高频题《TCP三次握手过程，为什么是三次不是两次？》。
  - **内容补充与隔离**：该题目的解答并未直接写在面试题文件中，而是通过 `[[计算机网络篇#TCP三次握手与四次挥手]]` 锚点作为传送门！为此，全新启用了重磅基础学科分类 `wiki/计算机网络篇.md`，并在该正文中利用“服务端单相思（资源耗尽防范）”的物理隐喻，极度生动地拆解了如果只有二次握手会在不可靠网络环境中引发的灾难性时空错乱Bug。

- **系统架构/目录清理**：彻底删除了早期知识库建立时的脚手架与冗余说明文档（`HANDOVER.md`、`OPS-导入进度.md`、主目录下的 `MOC-LLM 知识库.md`）。
  - **精简收益**：这些文件原有的进度记录已无实际意义，其中有价值的协作用法和格式约束已全部提炼并正式固化至 `AGENTS.md` (Workspace Instructions)。拆除外围旧脚手架后，现在的知识库根目录更加清晰清爽，一切日常维护由本文件 (`优化记录.md`) 与导航主干接管。

- **知识库/系统指令**：在根目录的 `AGENTS.md` (Workspace Instructions) 中，提取并正式确立了“算法笔记生成与排版核心规范（Algorithm Notes Conventions）”。
  - **Agent 进化**：通过复盘总结近期的笔记迭代经验，将“标题结构大一统”、“教学循序渐进（严禁起手直接丢出极致空间优化代码，必须先展示直观的二维或常规解法）”、“大白话与物理隐喻解析法”以及“避免孤岛知识的双向链接（Cross-Linking）”等高价值偏好，彻底固化为 Agent 的底层处理潜意识。
  - **长效维护流闭环**：强制确立了后续所有针对算法的重构、改写与优化，均自动触发向 `优化记录.md` 同步变更日志与优化总结的工作流，让知识库的维护形成自我驱动的生命条。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“七、双字符串二维 DP (LCS 模型)”章节下，重装加入了字符串操作难度天花板《编辑距离》。
  - **知识串联**：点破了本题与《最长公共子序列》同宗同源的核心思想（如果相等就不加操作费 `dp[i-1][j-1]`，如果不等就从 `上`、`左`、`左上` 三条路挑最便宜的再 `+1` 操作费）。利用生动的白话“建立叹息之墙”极好地诠释了为什么 `dp[0][j] = j`（白嫖插入）以及 `dp[i][0] = i`（疯狂删除）这极易被遗忘的矩阵四周边界初始化的真正物理意义。
  
- **算法/动态规划**：在 `wiki/算法/动态规划.md` 知识库中单独辟出了“七、双字符串二维 DP (LCS 模型)”，重磅收录面试必考题《最长公共子序列》。
  - **知识串联**：通过第一节“动态规划核心思想”中的概念回扣（为什么数组一定要留出 `n+1` 当作基石而不是 `n` 的原位锁定），打通了与前面“一维状态挂载”的区别。并深入浅出地还原了该类大名鼎鼎的双字符串比对阵列的本质——就是用“网格化记录平行时空的过去（命中牵手就 `dp[i-1][j-1]+1`，错过就抢夺退一格时空中最粗的的大腿 `max`）”。

- **算法/字符串**：在 `wiki/算法/字符串.md` 中，对《最长回文子串》的双指针（中心扩展法）代码进行了精简和代数降维打击。
  - **算法重构**：用极简、统一的 `for i in range(2 * n - 1)` 循环结构替换了原有的繁冗分离式双指针代码。
  - **核心原理解析**：为学习者生动解释了其背后的绝妙数学映射——为什么通过 `l = i // 2` 与 `r = (i + 1) // 2` 的公式，就能一次性天衣无缝地囊括“恰好重叠在字符上的 $N$ 个奇数中心”与“恰巧骑在字符夹缝中央的 $N-1$ 个偶数中心”。消除了原版代码分别调用两次 `expand` 函数的繁琐。

- **算法/动态规划 与 算法/字符串 跨页联动**：在 `wiki/算法/动态规划.md` 及其对应的 `wiki/算法/字符串.md` 中，为《最长回文子串》建立了极其重要的**双向链接**。
  - **知识串联**：既在动态规划页中导流提醒其有更优空间（$O(1)$）的双指针解法，也在字符串的双指针页中补充了指向动态规划“二维区间 DP”解法的锚点，并说明了该方案虽常数稍差但是能覆盖一切相关题目的标准母题。借由相互传送的 markdown 指引，构筑了网状复习知识点，杜绝了孤岛知识的产生。
  - **排版清洗**：顺手将 `wiki/算法/字符串.md` 中的老旧标签格式一起统一成了规范的 `#### 解题思路` 与 `#### 代码实现` 体系。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 知识库末尾全新开启了“六、区间 DP (字符串二维模型)”分类，新增经典神题《最长回文子串》。
  - **教学改进**：针对这道题目，极其深刻地拆解了区间 DP 中初学者最容易踩进去的“**诡异遍历顺序坑**”。通过“大洋葱必须等小洋葱内芯先发育成型”的直观比喻，将这道题中“外层不能无脑循环起点 $i$ 和终点 $j$，而**必须以外层循环枚举子串长度 $L$** 来横向扩充”的依赖关系讲得彻底通透。
  - **遵循教学渐进偏好**：保留了利用空间换取历史记录换取理解最为规范的 $O(N^2)$ 标准二维 DP 打卡矩阵代码。同时在解法的后缀注明了其实也可以使用“中心扩展法（双指针散开）”将空间优化至 $O(1)$ 的发想参考。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的《最小路径和》中补全了基础推演版本代码。
  - **遵循教学渐进偏好**：更正了之前一步到位只给 $O(1)$ 原地覆盖优化版代码的激进排版，找回了专门开辟完整 `dp` 辅助矩阵、$O(M \times N)$ 的规规矩矩的常规主流解法。并在段落与注释中严格注明了从“常规 DP”演进至“原地篡改榨干 $O(1)$”的两步理解路线与复杂度对比。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“五、多维动态规划 (网格 DP)”中，新增了并列的必考题目《最小路径和》。
  - **知识串联**：将其与上方的《不同路径》进行了强相关的物理隐喻挂接。前者是计算“到达该地点的**总路径种数（路口数相加）**”；后者则是“计算到达该地点的最便宜的**总过路费（路口中最便宜的一个加上本身的花费）**”。利用同质化的状态转移推导，让学习者瞬间贯通矩阵 DP 的基本演替思想。
  - **空间降维打击揭秘**：在解析中，讲解了面对这种抛弃型历史网格的情况，甚至可以比《不同路径》一维数组更激进——直接实施原地修改策略覆盖传参的 `grid`，从而将原本理论上 $O(M \times N)$ 的矩阵空间压缩为彻头彻尾的 $O(1)$！并配合给出了这种极致优化的标准最优代码模板。
  
- **算法/动态规划**：在 `wiki/算法/动态规划.md` 知识库末尾全新开启了“五、多维动态规划 (网格 DP)”分类，并录入了该领域的开山母题《不同路径》。
  - **内容补充**：对“只能向右向下”带来的前置依赖模型做了解构（到达某点的路径数必定是上面格子和左边格子相加）。添加了常规的二维打表解法，并特别通过易懂的语言讲透了“如何把二维度面积压缩成一根线”的核心物理意义——循环使用一行数组时，等号右边的旧 `dp[j]` 就是上一行的自己，刚算出来的 `dp[j-1]` 就等价于左边格子。这一降维打击将空间复杂度从 $O(m \times n)$ 彻底拍扁至 $O(n)$。

## 2026-04-18
- **算法/图**：在 `wiki/算法/图.md` 的“网格与岛屿模型”章节下，新增了重磅题型《腐烂的橘子》。
  - **教学改进**：与前文的《岛屿数量》（遇到一个单点开始蔓延）形成了经典的对照组，引入了必须掌握的**多源广度优先搜索（Multi-source BFS）**模板。强调了必须在第 0 分钟时就将所有已经腐烂的“感染源”**同时**放入队列齐头并进，而不能挨个单点扩散。
  - **内容补充**：详细说明了通过提前统计未被感染的 `fresh_count` 以判定“四周全是水，橘子无法被感染只能返回 -1”的边界拦截条件。补充了对应的 $O(M \times N)$ 级时间与空间复杂度解析。

- **算法/图**：在 `wiki/算法/图.md` 中，对《岛屿数量》和《单词搜索》进行了时空复杂度补充与排版清理。
  - **排版清洗**：将《单词搜索》旧存的“【题目】”与“【模板解析与思路归纳】”等硬标签一并升级为符合规范的 `#### 解题思路` 和 `#### 代码实现`。
  - **内容补充**：对《岛屿数量》与《单词搜索》追加了 `#### 复杂度分析` 模块：
    - 明确了解释了**《岛屿数量》为何即便有双重 for 循环加上 DFS，其时间复杂度依然是 $O(M \times N)$**，打破初学者惯性认为有嵌套循环就一定平方次级的盲区（被“沉岛”吃掉的 `'0'` 节点不会再生）。
    - 对《单词搜索》说明了回溯算法的核心指数爆炸来源 $O(M \times N \times 3^L)$ ，以及原地修改棋盘所带来的 $O(L)$ 优秀空间优化。

- **算法/图**：在 `wiki/算法/图.md` 中，对《岛屿数量》题目进行了扩展和规范化重构。
  - **排版清洗**：移除了原有的“【题目】/【模板解析与思路归纳】/【答案代码】”等生硬标签。按照知识库通用标准，使用 `###` 标注大题目，使用 `####` 拆分出题目细节、解题思路与代码实现。
  - **内容补充**：补全了“每座岛屿只能由水平方向和/或竖直方向上相邻的陆地连接形成”以及“网格的四条边均被水包围”等核心约束条件。
  - **教学改进**：除了保留并优化原有如同“病毒蔓延”般直观的 DFS（深度优先搜索）标准解法外，基于“优先展示最标准、最直观常规主流解法”的原则，额外补充了 BFS（广度优先搜索）的实现代码。详细说明了其采用队列逐层推展如“水波扩散”的物理含义，以及在极端网格下防栈溢出的优势。

## 2026-04-16
- **算法/二分查找**：在 `wiki/算法/二分查找.md` 的“二、基础入门”章节中，新增了《搜索二维矩阵》。
  - **教学改进**：没有将其作为一道复杂的二维数据结构题来拆解，而是直击题目的物理本质——利用题干中“每行的第一个整数大于前一行的最后一个整数”的特性，将矩阵看作“首尾相连的长面条（严格单调递增的一维数组）”。
  - **内容补充**：详细讲解了“空间降维打击”的核心防坑点。将问题剥离为**最基础的精确值二分查找模板** + **一维索引 `mid` 到二维坐标 `(row, col)` 的数学推导转换**（即 `row = mid // n` 和 `col = mid % n`），完美契合了该板块“利用模板解决变体”的学习曲线。

- **算法/二分查找**：优化了《在排序数组中查找元素的第一个和最后一个位置》的内容。
  - **排版清洗**：将旧有带有粗体标签（`**题目**`、`**答案代码**`）的样式统一改成了当前知识库标准的 `#### 解题思路` 和 `#### 代码实现`。
  - **细节剖析**：专门针对“如果没找到需要返回 `[-1, -1]`”的边界情况进行了思路补充，点明了函数顶层预设 `res = -1` 所带来的天然兜底效果。

- **算法/二分查找**：完善了《搜索旋转排序数组》的内容结构。
  - **排版清洗**：补全了该题原本缺失的题目描述与解题思路，并将其从纯代码块的状态升级为标准题解格式。
  - **思路升维**：引入了“被中间掰断互换的冰棍”这一通俗的物理隐喻。说明了一刀切下必定有一半是单调递增的“完美区间”。教导学习者在混沌的旋转数组中寻找“那一半完美递增区间”，并用它来作为界定 `target` 嫌疑范围的绝对标尺，从而实现稳定的削半。

- **算法/二分查找**：对全篇的终极 BOSS 题目《寻找两个正序数组的中位数》进行了大重构，使其不再仅仅是一段孤立的代码。
  - **排版清洗**：补齐了题干描述信息与时间复杂度要求，去除了原有的“答案代码”这种僵硬排版，统一为标准的 markdown 锚点格式。
  - **思路升维**：
    1. **降维打击（化整为零）**：首先揭示了突破这道 Hard 题的终极视角——不要试图直接去找缥缈的“中位数”，而是先把它降维并映射出一个找“**第 $K$ 小元素**”的杀手函数 `getKthElement(k)`。中位数只是当总数算出 $K= (m+n)/2$ 时的该函数的特定调用。
    2. **淘汰赛比喻（以武林高手对决解释 $O(\log K)$）**：为了讲清楚 $O(\log K)$ 复杂度的来源（每一轮淘汰一半的嫌疑人），引入了极其生动的“武林两大门派 Kth 淘汰赛”模型。如果我们要找全天下第 10 小的高手，只要让两队派出各自的第 5 名出来对战，谁的第 5 名战力弱，谁家前面所有的那 5 个人加起来充其量也就是全宇宙第 9 弱（无论如何都不可能是我们要找的第 10 弱）。于是这 5 个炮灰被全员斩杀淘汰！这种指数级的淘汰速度（每一轮抹杀 $K/2$ 名炮灰）成了代码能够达到 $O(\log(m+n))$ 复杂度的本源，读完醍醐灌顶！

- **算法/栈与队列**：在 `wiki/算法/栈与队列.md` 的“二、基础栈结构设计”章节中，新增了《字符串解码》这道基础嵌套栈的中等必考题。
  - **教学进阶**：为了摆脱枯燥的“压栈/弹栈”字母拼接推演，特别引入了“洋葱模型 / 盗梦空间”的物理世界观。在解析中，将“遇到左括号”比喻为“由于多层括号的强制优先级，只能把当前上层梦境辛苦打拼出的字符串和倍数先**存档入栈**，只带一张白纸进入下一张深层梦境（层级括号内部）”。将“遇到右括号”比喻为“深层梦境杀青醒来，拿出它内部拼装的字符串结果乘以上一层的遗留倍数，并拼接在存档好的旧层级字符串的后边”。生动形象地具象化了压栈和出栈的实质：保护现场、恢复现场。
  - **排版清洗**：连带将“二、基础栈结构设计”整章中老旧的“有效的括号”与“最小栈”的僵硬题目标签一齐大一统，变更为目前知识库标准的 `#### 解题思路` 和 `#### 代码实现` 锚点组合。

## 2026-04-15
- **算法/贪心算法**：在 `wiki/算法/贪心算法.md` 的“二、不断刷新极限”章节最前端，补充了其入门前置题目《跳跃游戏》。
  - **内容补充**：对“最远能达到的边界（`farthest`）”概念做了具象化拆解，并在解题思路中强调了判断死胡同的核心就在于判断 `i > farthest`。这道仅仅只需要维护一个极限的题目与紧随其后的《跳跃游戏 II》（同时得维护目前极限和下一发子弹极限双边界）形成了绝佳的难度梯度。
  - **核心误区击碎**：在《跳跃游戏 II》中新增了【思维误区：为什么不直接跳到当前步数所能达到的最远位置】。引入了“侦察兵”和“扫地砖”模型，通俗易懂地解释了 `current_end` 辖区的物理意义，打倒了“无脑选最远落脚点”导致后续路难走的反面案例。

- **算法/贪心算法**：在 `wiki/算法/贪心算法.md` 的“二、不断刷新极限”章节中，新增了《划分字母区间》。
  - **内容补充**：该题的破局机制与《跳跃游戏》如出一辙，通过“被迫拉扯极限边界”的方式讲解，解释了核心思维突破口（先统计每个字母最后出现的位置）。将其同样放置于“不断刷新极限”这一章的分类下，使该模型的训练从数学边界延展到了字符串边界。

- **算法/贪心算法**：在 `wiki/算法/贪心算法.md` 的“二、不断刷新极限”章节中，新增了《买卖股票的最佳时机》。
  - **排版清洗**：在添加这一题的同时，也顺手把旧笔记里《跳跃游戏 II》残留的“【题目】/【答案代码】”等僵硬的标签清除了，彻底统一为 `### 题目` -> `#### 解题思路` -> `#### 代码实现` 的标准。
  - **教学改进**：在解析这道股票入门名题时，将重心明确放在了从暴力双重 $O(N^2)$ 循环切换到“线性扫描的同时，动态且贪心地维护一个过往的极低价格基石（基石随着时间前进而被刷新），再利用基石同步计算当日抛售红利”的过程上来。真正体现出由于不需要回头看而带来的 $O(N)$ 思想降维打击。

## 2026-04-14
- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“三、一维状态挂载”章节中，压轴追加了困难级别的《最长有效括号》。
  - **内容补充**：生动拆解了“跳过内部已闭合大括号阵法寻找外层开括号（`pre = i - 1 - dp[i-1]`）”的转移核心逻辑。并通过“情况一层层包 `...()`”与“情况二大乱炖 `...))`”形象化地对比了两种递推场景下的状态叠加公式。
  - **知识串联**：题解中再次回扣了前面两道题（最长递增、乘积最大）里关于“**必须通过打擂台搜集极大值，绝不仅通过 `dp[-1]` 结尾退场**”的一维挂载坑点，通过这三连击彻底巩固了此类 DP 返回值判别的定海神针。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的《分割等和子集》题解中，增加【直击灵魂：为什么要有两层循环？分别在干什么？】以及【逻辑细节：内层循环的终点为什么停在 `num - 1`？】的核心讲解锚点。
  - **内容补充**：对没从二维表格推演而直接面对一维滚动数组从而“一头雾水”的初学者而言，通俗点名了那极其神秘的两层循环究竟意味着什么。外层是“传送带上接连送来的一个个物品/数字”，内层是用手里拿着的这块“新积木”逐个去往“全村希望的账本”里的大大小小各种型号坑位上试填的过程看有没有 `j-num` 的基座能刚好被它借力托举起来。并重点点破了内循环之所以卡在 `num` 位置（借助 Python 前闭后开的 `num - 1`）是因为凡是比新来的物品还要小的背包根本“塞不进”新物品，既然塞不进原样继承就不如借着一维数组原位不变的特性直接跳过不管，豁然开朗。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“四、背包问题”首部，重磅新增了 0-1 背包祖师爷级别题目《分割等和子集》。
  - **内容补充**：在这道题中点破了 0-1 背包（每个物品只限拿一次）和前面《完全平方数》这一类完全背包（每个物品无限拿）的本质差异。
  - **教学改进**：通过【核心易错坑】模块详细图解了“**内层循环为什么要逆向遍历（从 target 递减到 num）**”：因为正序滚动计算大个背包时会用小个子的旧背包，如果小背包这一轮已经受新数字“污染”，就会造成一个数字同个回合被装进包两次（完全背包）。倒着刷漆才能保证借用左边的老数据，从根本上讲清楚了背包问题为什么有正循环和逆循环。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“三、一维状态挂载”章节中，基于教学渐进性原则，为《乘积最大子数组》追加了标准的未优化版解析代码。
  - **内容补充**：在这道题中，深刻映射了刚才前一道题（最长递增子序列 LIS）的“**一维挂载定义**”和“**不能直接返回 dp[-1]**”两大痛点，并且通俗解释了为什么要同时维护一个 `min_dp` 的根本原因在于乘法“负负反转翻盘成极大正数”的特殊性质。
  - **教学改进**：现已将代码重构为**先展示包含 `max_dp` 和 `min_dp` 两个完整 DP 数组的 $O(N)$ 空间解法**，以强化初学者对于“定义与初始化”框架记忆；随后通过平滑衔接，再给出状态只受前一格影响的 $O(1)$ 空间滚动变量优化版。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 中，大幅扩写了《单词拆分》一题的解题思路，专门剖析了初学者在书写双层循环时最容易踩入的两个思维盲区。
  - **内容补充**：详细解释了为何内层切面验证必须使用代表已拼合合规的 `dp[j]` 数组缓存，而绝对不能去生搬硬抄比对超长字串 `s[:j] in word_set` （破除了“被拆解的长字符串不一定作为整体单词存在于字典中”的直觉盲区）。
  - **内容补充**：从循环和状态记录的宏观视角，重申了在状态转移寻解无果时（即没有 `break` 发生时）**不可提前抛出 `return False` 结束程序**的核心逻辑。说明了这仅代表阶段长度 `i` 拼凑失败而已，最终成败全权掌握在终点 `dp[n]` 之上。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 头的“核心做题骨架（DP五部曲）”第一点，追加了【易混辨析：DP 数组初始化长度是 `n` 还是 `n + 1`？】的核心学习经验。
  - **内容补充**：从“物理含义究竟是对应数组下标，还是对应代表容量/数量的总量”的角度，梳理了到底什么时候只建长度 `n` 的数组，什么时候要为了预留“边界合法基石 `dp[0]`”而扩建出 `n+1` 长度的底层逻辑。彻底扫除了初学者做 DP 时建数组报错的雷区。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 的“四、背包问题”章节中，新增了《完全平方数》一题。
  - **优化细节**：同步清洗了《零钱兑换》和《单词拆分》遗留的“题目/解析”等旧标签，全段实现了排版大一统。
  - **内容补充**：通过大白话（自然段）讲解了《完全平方数》为何在本质上与《零钱兑换》构成了“一模一样的完全背包套壳模型”，方便复盘时顺畅理解背包容量与无限物品的高阶映射。在转移方程的注释中详细描述了 $j^2$ 的扣减试探。

- **算法/动态规划**：在 `wiki/算法/动态规划.md` 中，修改了基础一维序列的首两题“爬楼梯”与“打家劫舍”的题解布局，使其作为动态规划前置首题更具教学意义。
  - **优化细节**：去除了旧版的“【题目】/【模板解析与思路归纳】/【答案代码】”标签，采用了全新的 `###` 题目名 -> `####` 解题思路 -> `####` 代码实现 的排版规范。
  - **内容补充**：补全了基于一维 DP 数组的标准模板解法，作为前置教学内容，再给出空间优化滚动变量版，确保“先有框架思维，再谈技巧优化”的逻辑顺利衔接。

## 2026-04-13
- **算法/树**：在 `wiki/算法/树.md` 的“进阶：属性与结构变换”部分，新增了“二叉树的直径”算法题解。
  - **优化细节**：遵循了不使用“题目/答案”标签的约束，以三级与四级标题进行自然段落排版。
  - **内容补充**：优先讲解最长路径与子树深度的逻辑关系，补充了全局最大直径维护的思想，并将代码转为统一的 Python 语言实现。

- **算法/树**：在 `wiki/算法/树.md` 中，新增了一级子版块“六、二叉搜索树 (BST)”，并补充了“验证二叉搜索树”算法题解。
  - **优化细节**：同样遵守去标签化排版规则，采用了大三级、小四级的层级展开。
  - **内容补充**：讲解了为什么必须要在整个分支采用“向下传递 `low` 和 `high` 区间”的方式进行约束，而不能仅判断当前节点比起左右孩子的值大小，并基于你提供的 Python 解法进行了补充注释。

- **算法/树**：在 `wiki/算法/树.md` 的“二叉搜索树 (BST)”下新增了“二叉搜索树中第 k 小的元素”算法题解。
  - **优化细节**：按照规范的 `###` 和 `####` 分层及段落叙述方式添加到末尾。特别注意了前面总结的“Markdown 代码块边界问题感知”，严格校验了插入位置上方代码块闭合标记的完整性。
  - **内容补充**：直接点明二叉搜索树“中序遍历即递增序列”这一核心性质，解释了采用计数器搭配提前返回机制（剪枝）的思路线索，最终提供了带状态维护的中序 Python 模板。

- **算法/树**：在 `wiki/算法/树.md` 的“二叉搜索树 (BST)”章节顶部，补充了核心解题规律说明区。
  - **优化细节**：使用 Markdown 引用块（`> `）以及加粗强调，将“中序遍历必定是一个严格递增的有序序列”这一最关键的“心法”彻底高亮，保证读者在阅读题目之前能够第一时间将这个本质规律印入脑海。

- **算法/树**：在 `wiki/算法/树.md` 的“四、高阶构造与深度搜索”中，新增了“路径总和 III”的算法题解。
  - **优化细节**：遵循之前强调的“优先提供最标准、主流的优秀解法”的偏好原则，直接跳过了 $O(N^2)$ 效率底下的双重 DFS 解法，选择将极其重要的 $O(N)$ 标准主解——**“前缀和 + 哈希表” + DFS** 放在了版面主位。
  - **内容补充**：对“树状前缀和”的概念进行了类比描述（看作一条自上而下的数组）。按执行的推进顺序，分步骤详细解释了“初始字典为何要塞入 `{0: 1}`”、“差值怎么匹配”，最为重点地加粗强调了**DFS状态回溯**（减去当前频次，防止跨树枝污染）这一极容易被忽视的防坑套路。代码实现中均添加了充实的配套步骤注释。

- **算法/树**：执行了全文档级的老旧排版清洗以及知识逻辑重连运动。
  - **优化细节**：将《中序遍历》、《层序遍历》、《最大深度》、《翻转二叉树》、《构造二叉树》、《LCA》、《对称二叉树》和《实现前缀树》这八大历史遗留的老旧条目进行了批量降级格式改造（摒弃“难度/题目/模板解析/答案代码”标签）。
  - **内容维系**：强化了整个树形算法主干上的知识连贯性：通过梳排发现文档是严格遵循“基础遍历（迭代法找路） -> BFS结构变换 -> 分治变种（深度和翻转）-> 子树收集型后序（构树、LCA等高阶问题）-> 前缀字典树特种数据结构 -> 二叉搜索树专场”这条“循序渐进”的成长曲线进行组织排列的。在每个清洗过的题目下的“解题思路”起头，都专门补充了一两句话，通过像“分治”、“后序向上收集”、“双开递归路”这些词汇暗线，将每个看似零散的题目串联到了各自该处的“套路分类”上。
-   e�EQ�N
0�k�e)n�^0US�h�v�V�g�_��VT{�N:N�NHN�_{�HQgbL�w h i l e �Q�Ǐa p p e n d ����]ۏ�S
