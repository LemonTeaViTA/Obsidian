# 前端技术栈详解

> 基于 ai-program-factory 项目  
> 更新时间：2026-07-22

---

## 📦 核心依赖

### 1. UI 框架

#### React 19.1.1
```json
"react": "^19.1.1",
"react-dom": "^19.1.1"
```

**作用**：
- UI 组件库
- 声明式编程
- 组件化开发
- Hooks API

**为什么选择 React 19**：
- 最新的并发特性
- 更好的性能
- Server Components 支持
- 自动批处理更新

---

### 2. 路由管理

#### React Router 7.9.3
```json
"react-router": "^7.9.3",
"@react-router/node": "^7.9.3",
"@react-router/serve": "^7.9.3"
```

**作用**：
- 文件路由（基于目录结构）
- 数据加载（Loader）
- 导航管理
- 嵌套路由

**文件路由示例**：
```
src/routes/
├── home.tsx           → /home
├── tasks.tsx          → /tasks
├── conversation.tsx   → /conversation/:id
└── workflow-manage.tsx → /workflow-manage
```

---

### 3. 数据请求与缓存

#### TanStack Query 5.90.2
```json
"@tanstack/react-query": "^5.90.2"
```

**作用**：
- 数据请求封装
- 自动缓存
- 后台同步
- 乐观更新

**使用示例**：
```typescript
// 查询任务列表
const { data, isLoading } = useQuery({
  queryKey: ['tasks'],
  queryFn: () => fetchTasks()
});

// 创建任务（Mutation）
const mutation = useMutation({
  mutationFn: createTask,
  onSuccess: () => {
    queryClient.invalidateQueries(['tasks']);
  }
});
```

---

### 4. 状态管理

#### Zustand 5.0.8
```json
"zustand": "^5.0.8"
```

**作用**��
- 轻量级状态管理
- 无需 Context Provider
- TypeScript 友好
- 简单直观的 API

**使用示例**：
```typescript
// 定义 Store
const useTaskStore = create((set) => ({
  tasks: [],
  addTask: (task) => set((state) => ({
    tasks: [...state.tasks, task]
  }))
}));

// 使用 Store
const tasks = useTaskStore((state) => state.tasks);
const addTask = useTaskStore((state) => state.addTask);
```

---

### 5. HTTP 请求

#### Axios 1.12.2
```json
"axios": "^1.12.2"
```

**作用**：
- HTTP 客户端
- 请求/响应拦截器
- 自动转换 JSON
- 错误处理

**配置示例**：
```typescript
const axiosInstance = axios.create({
  baseURL: getApiBaseUrl(),
  timeout: 30000,
  withCredentials: true
});

// 请求拦截器
axiosInstance.interceptors.request.use((config) => {
  // 添加 token
  config.headers.Authorization = `Bearer ${token}`;
  return config;
});

// 响应拦截器
axiosInstance.interceptors.response.use(
  (response) => response.data,
  (error) => handleError(error)
);
```

---

### 6. WebSocket 通信

#### Socket.IO Client 4.8.1
```json
"socket.io-client": "^4.8.1"
```

**作用**：
- 实时双向通信
- 自动重连
- 房间和命名空间
- 事件驱动

**使用示例**：
```typescript
import { io } from 'socket.io-client';

const socket = io('wss://ai24.daily.vdian.net/ws/report', {
  transports: ['websocket'],
  reconnection: true,
  reconnectionDelay: 1000
});

// 监听事件
socket.on('message', (data) => {
  console.log('收到消息:', data);
});

// 发送消息
socket.emit('send_message', { text: 'Hello' });
```

---

### 7. 样式方案

#### Tailwind CSS 4.1.8
```json
"tailwindcss": "^4.1.8",
"@tailwindcss/postcss": "^4.1.13",
"@tailwindcss/vite": "^4.1.13"
```

**作用**：
- 原子化 CSS
- 响应式设计
- 深色模式支持
- 高度可定制

**使用示例**：
```tsx
<div className="flex items-center justify-between p-4 bg-white rounded-lg shadow-md">
  <h2 className="text-xl font-bold text-gray-800">任务列表</h2>
  <button className="px-4 py-2 text-white bg-blue-500 rounded hover:bg-blue-600">
    创建任务
  </button>
</div>
```

---

### 8. UI 组件库

#### HeroUI 2.8.4
```json
"@heroui/react": "^2.8.4"
```

**作用**：
- 预制 UI 组件
- 主题定制
- 响应式布局
- 无障碍支持

#### Ant Design 5.27.6
```json
"antd": "^5.27.6"
```

**作用**：
- 企业级组件库
- 丰富的组件
- 中文友好
- 设计规范

---

### 9. 类型安全

#### TypeScript 5.9.2
```json
"typescript": "^5.9.2"
```

**作用**：
- 静态类型检查
- IDE 智能提示
- 重构支持
- 编译时错误检查

**配置**：`tsconfig.json`
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "strict": true,
    "jsx": "react-jsx",
    "moduleResolution": "bundler"
  }
}
```

---

### 10. 构建工具

#### Vite 7.1.7
```json
"vite": "^7.1.7",
"@vitejs/plugin-react": "^5.0.4"
```

**作用**：
- 极速冷启动
- 即时热更新
- 优化构建
- 插件生态

**配置**：`vite.config.ts`
```typescript
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3001,
    proxy: {
      '/api': 'http://localhost:3000'
    }
  }
});
```

---

## 🧪 测试工具

### Vitest 3.0.2
```json
"vitest": "^3.0.2",
"@vitest/coverage-v8": "^3.2.3"
```

**作用**：
- 单元测试
- 组件测试
- 代码覆盖率
- 与 Vite 深度集成

### React Testing Library
```json
"@testing-library/react": "^16.3.0",
"@testing-library/user-event": "^14.6.1"
```

**作用**：
- 组件渲染测试
- 用户交互模拟
- 查询 DOM 元素
- 异步操作测试

---

## 🎨 辅助库

### 日期处理
```json
"date-fns": "^4.1.0"
```

### 图标
```json
"react-icons": "^5.5.0",
"lucide-react": "^0.544.0"
```

### Markdown 渲染
```json
"react-markdown": "^10.1.0",
"remark-gfm": "^4.0.1"
```

### 代码编辑器
```json
"@monaco-editor/react": "^4.7.0-rc.0",
"monaco-editor": "^0.53.0"
```

### 图表
```json
"echarts": "^6.0.0"
```

### Toast 通知
```json
"react-hot-toast": "^2.6.0"
```

---

## 🌐 国际化

### React i18next
```json
"react-i18next": "^16.3.3"
```

**使用示例**：
```typescript
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return <h1>{t('welcome')}</h1>;
}
```

---

## 📊 技术栈总结

| 分类 | 技术 | 版本 | 作用 |
|------|------|------|------|
| **核心框架** | React | 19.1.1 | UI 框架 |
| **类型系统** | TypeScript | 5.9.2 | 类型安全 |
| **路由** | React Router | 7.9.3 | 路由管理 |
| **状态管理** | Zustand | 5.0.8 | 轻量状态管理 |
| **数据请求** | TanStack Query | 5.90.2 | 数据缓存与同步 |
| **HTTP** | Axios | 1.12.2 | HTTP 客户端 |
| **WebSocket** | Socket.IO | 4.8.1 | 实时通信 |
| **样式** | Tailwind CSS | 4.1.8 | 原子化 CSS |
| **UI组件** | HeroUI + Ant Design | 2.8.4 / 5.27.6 | 组件库 |
| **构建** | Vite | 7.1.7 | 构建工具 |
| **测试** | Vitest | 3.0.2 | 测试框架 |
| **国际化** | react-i18next | 16.3.3 | 多语言 |

---

## 🔗 技术栈选择理由

### 为什么选择 React 19？
- ✅ 最新的并发特性
- ✅ 更好的性能
- ✅ Server Components
- ✅ 生态成熟

### 为什么选择 React Router 7？
- ✅ 文件路由，自动生成
- ✅ 数据预加载
- ✅ SPA 模式支持
- ✅ 类型安全

### 为什么选择 Zustand？
- ✅ 比 Redux 更简单
- ✅ 无需 Provider
- ✅ TypeScript 友好
- ✅ 性能优秀

### 为什么选择 TanStack Query？
- ✅ 自动缓存
- ✅ 后台同步
- ✅ 减少样板代码
- ✅ 开发体验好

### 为什么选择 Tailwind CSS？
- ✅ 快速开发
- ✅ 体积小
- ✅ 响应式友好
- ✅ 可定制

---

## 📚 学习资源

| 技术 | 官方文档 |
|------|---------|
| React | https://react.dev |
| TypeScript | https://www.typescriptlang.org |
| React Router | https://reactrouter.com |
| TanStack Query | https://tanstack.com/query |
| Zustand | https://zustand-demo.pmnd.rs |
| Tailwind CSS | https://tailwindcss.com |
| Vite | https://vitejs.dev |

---

**掌握这些技术栈，你就可以独立开发 AI Agent 平台的前端了！** 🚀
