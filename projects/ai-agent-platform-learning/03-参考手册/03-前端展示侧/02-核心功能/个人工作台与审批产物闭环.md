---
module: AI Agent 平台学习
type: project
tags: [ai-program-factory, 个人工作台, 审批, 产物版本, 项目树]
last_reviewed: 2026-08-04
---

# 个人工作台与审批产物闭环

> [!tip] 速览
> 当前首页不再只是任务创建入口，而是“我现在该处理什么”的工作台：待我审批、我参与的需求和新建任务聚合在同一页。任务详情已经接入角色级审批状态和产物 diff，但产物历史抽屉目前只有组件实现、没有可达页面入口；项目筛选和应用拆分内容也已分别升级为 DEP ID 项目树与领域技术方案。

## 一、首页结构已经变化

`src/routes/home.tsx` 当前组合四个模块：

```text
HomeScreen
  |- AIFactoryHeader
  |- PendingApprovalsCard
  |- ParticipatingRequirementsCard
  `- CreateTaskModal
```

旧文把首页描述成 `SimpleTaskForm` 主导的创建页并不完整。`simple-task-form.tsx` 仍是任务创建核心表单，但现在通过右上角“新建任务”打开的 modal 承载；首页主体是个人工作台。

## 二、个人工作台查询

### 2.1 前端调用

```text
GET /api/workbench
  approvalStatus=PENDING|PROCESSED
  requirementStatus=RUNNING|DONE
  pageNum / pageSize
```

`usePersonalWorkbench` 使用 TanStack Query，query key 包含筛选参数，`staleTime` 为 30 秒。审批完成后不能等待长期缓存自然失效，应主动 invalidate 对应查询。

### 2.2 两类卡片

| 卡片 | Tab | 点击行为 |
|------|-----|----------|
| 待我审批 | `PENDING` / `PROCESSED` | 新窗口打开 `#/conversations/{taskId}` |
| 参与中的需求 | `RUNNING` / `DONE` | 有编排任务时打开 `#/orchestration/{orchestrationTaskId}` |

参与需求还展示当前阶段：`PRD_AUDIT`、`APP_SPLIT`、`APP_DEVELOP`。两张卡片各自维护分页状态，服务端分别返回 `approvalTotal` 和 `requirementTotal`。

## 三、审批 UI 读取角色级状态

### 3.1 状态类型

```typescript
type ApprovalRoleStatus =
  | "PENDING"
  | "APPROVED"
  | "REJECTED"
  | "NOT_REQUIRED"
  | "UNKNOWN";
```

后端对开发和测试分别返回状态与描述。`ApprovalRoleStatusBadge` 使用图标、颜色、tooltip 和 `aria-label` 呈现；未知值必须归一化为 `UNKNOWN`，不能误显示为待审批。

### 3.2 审批接口

前端调用 `POST /api/approvalItem/approve`，同一接口通过 `confirmStatus` 表达通过或拒绝：

```typescript
{
  taskId,
  id,
  confirmStatus: "APPROVED" | "REJECTED",
  rejectReason,
  selectedAppList
}
```

操作成功后，`useApproveItem` 同时失效 `approvalItems` 和 `pmReviewItems`。发生特定业务冲突时还会失效 `domainDivideContent`，重新拉取后端事实，避免 UI 继续使用陈旧审批批次。

## 四、产物对比已接入，历史入口仍待接线

### 4.1 三种阅读视图

任务详情面板实际引用的 `TaskArtifactsViewer` 对有新内容的产物提供：

- 旧契约：任务原先引用的内容
- 新契约：上游当前最新内容
- 对比：基于行的新增 / 删除 diff

用户也可以下载当前选中视图。这样“上游产物发生变化”不再只是一个通知，而是可解释、可比较的状态。

仓库里另有 `ArtifactsPanel`，但当前没有任何页面引用它。判断已上线行为时应沿 `TaskDetailPanel -> TaskArtifactsViewer` 的真实调用链，而不是只看相似组件名称。

### 4.2 历史版本

`useArtifactHistory` 调用：

```text
GET /api/taskInfo/artifact/history/{artifactId}
```

`ArtifactHistoryDrawer` 按版本时间线展示内容，默认展开当前版本，并支持按版本下载。查询只在 `artifactId` 有值时启用，5 分钟内视为新鲜数据。

但是抽屉目前只被 `NodeArtifactsViewer` 引用，而 `NodeArtifactsViewer` 自身没有被任何路由或页面引用；`ArtifactsPanel` 也同样不可达。因此当前准确结论是：后端历史接口、查询 hook 和展示抽屉已经实现，面向用户的打开入口尚未完成接线，不能称为完整前端闭环。

## 五、工作流“始终使用最新模板”

工作流表单新增 `alwaysUseLatestTemplate`：

> 开启后，历史任务也会按工作流当前最新版本解析阶段和规范模板。

前端只是采集和展示这个策略，真正的解析发生在 ai24。该开关不能被理解成普通 UI 偏好，它改变历史任务的可复现性；修改时需要同时核对创建、编辑、详情回显和后端解析逻辑。

## 六、实时通信是多协议兼容，不是一条固定 WebSocket

当前仓库同时保留 V0、V1 和 Custom Agent Provider。对 Custom Agent / V3 路径：

- 接收：`CustomAgentWebSocketProvider` 使用 GMS SDK，业务 ID 为 `ai24`，默认连接 `VITE_GMS_WS_URL`。
- 订阅：当前选中的主任务或子任务 ID 参与 GMS userId / 消息过滤。
- 发送：`use-send-message.ts` 通过 HTTP `/api/task/query` 发送，GMS 主要承担下行消息。

所以旧文里的“浏览器直连 `wss://ai24/ws/report` 并双向发送”不是当前 Custom Agent 主路径。阅读 WebSocket 文档时要先确认对话版本，再判断使用 Socket.IO、原生 WebSocket 还是 GMS。

## 七、项目树、领域技术方案与拓扑边界

### 7.1 项目树使用 DEP ID

敏捷看板和批量任务看板都复用 `src/components/shared/project-tree-selector.tsx`。勾选结果不再发送项目名称或路径，而是转换为：

```typescript
type ProjectSelection = {
  projectId: string;
  subprojectId?: string;
};
```

同一项目下多个子项目 ID 用逗号连接。筛选标签仍可显示项目名，但查询身份是稳定的 DEP ID；名称和路径变化不应再破坏过滤条件。

### 7.2 应用拆分展示领域技术方案

`ApprovalItem` 新增 `domainTechnicalDesignContent`。`ApprovalItemsList` 只在应用拆分审批列表增加“领域技术方案”列，点击后用 Markdown 弹窗展示完整内容；领域拆分审批不展示契约协议和该列。这不是所有审批项的通用字段，而是 APP_SPLIT 普通项的内容约束。

### 7.3 拓扑页面仍是外部边界

当前 `master` 的 `/ms-board` 路由实现是：

```text
iframe -> https://ms-board.daily.vdian.net/
```

仓库中没有上一版内置的拓扑画布、force layout 和 topology query state。当前前端 `master` 通过外部治理看板承载页面，而最新 `ai24/master` 也没有 `/api/v1/topology/*` 只读接口；两者不能拼成一条已经打通的主干链路。后续若重新内置拓扑工作台，应把它当成一次新的前后端集成。

## 八、关键源码

- `src/routes/home.tsx`
- `src/api/workbench-service/workbench.api.ts`
- `src/hooks/query/use-personal-workbench.ts`
- `src/components/features/home/pending-approvals-card.tsx`
- `src/components/features/home/participating-requirements-card.tsx`
- `src/components/shared/approval-role-status-badge.tsx`
- `src/hooks/mutation/use-approval-mutations.ts`
- `src/components/features/conversation/task-detail-panel/task-artifacts-viewer.tsx`
- `src/hooks/query/use-artifact-history.ts`
- `src/components/features/conversation/master-task-detail/artifact-history-drawer.tsx`
- `src/components/shared/project-tree-selector.tsx`
- `src/components/features/conversation/task-detail-panel/approval-items-list.tsx`
- `src/components/features/workflow/workflow-latest-template-setting.tsx`
- `src/routes/ms-board.tsx`
