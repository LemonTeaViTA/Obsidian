---
unit: MySQL
fixed_at: 2026-06-01
fixer: 知识库管理专家 + MySQL 领域知识点专家
phase: 1（域内真实修复）
---

# MySQL 单元修复日志

## 一、改了哪些文件 / 各做了什么

### 1. `wiki/MySQL/锁机制.md`
- **[P0 机械乱码]** L36 SQL 注释 `-- 执行���份操作`（"备"字损坏为 3 个 U+FFFD）→ 修复为 `-- 执行备份操作`。用 Python 按字节级精确替换（`e689a7 e8a18c efbfbd efbfbd efbfbd e4bbbd ...` → `执行备份操作`），全文 U+FFFD 计数已归零。
- **[P2 代码块语言标签]** L245 死锁等待图 ASCII 示意图开栏 ``` 无语言标签 → 改为 ```text。
- 注：本篇 `## DDL 与锁`（Online DDL 三算法 / pt-osc / gh-ost）是该概念的**权威单点**，保持全文不动，由「高可用」篇收敛指向它（见下）。

### 2. `wiki/MySQL/高可用与分库分表.md`
- **[P1 域内 SSoT 去重]** `### 千万级大表如何添加字段？`（原 L257-271）与 `锁机制.md ## DDL 与锁` 重复讲"大表加字段/pt-osc/INSTANT"。本篇精简为：MySQL 8.0+ INSTANT 一句话结论 + 一段工具兜底说明 + `[!info]` callout 链接到 `[[锁机制#DDL 与锁]]`，删除了重复的 pt-osc bash 命令示例（工具命令与三算法对照表统一在锁机制篇维护）。
- **[P2 代码块语言标签]** L131 范围分片示意图开栏 ``` 无语言标签 → 改为 ```text。

### 3. `wiki/MySQL/索引机制详解.md`
- **[P1 准确性概念错误]** `### B+树的空间复杂度是多少？`（原 L166-168）答案 "O(logN)" 概念错误 → 改为 **O(N)**（要存全部 N 条记录 + 索引项随 N 线性增长），并澄清树高 $h=\lceil\log_m N\rceil$ 决定的是单次查询 I/O 次数 / 时间复杂度 O(log_m N)，而非空间复杂度。
- **[P2 顶部速览]** 文件 >400 行但开头直接进 `## 索引基础`，无顶部引导 → 在 H1 下补 `> [!info] 速览` callout（四主线 + 锚点跳转）。

### 4. `wiki/MySQL/引擎与日志.md`
- **[P1 准确性]** `#### Buffer Pool 的默认大小是多少？`（原 L89）"1-4GB 取 25%、>4GB 取 50% 不超 4GB" 实为 AWS RDS 等云厂商自动调参规则，非 MySQL 原生默认 → 改为：原生默认 **128MB**（与内存无关），生产常按物理内存 50%~75% 配置，并加 `[!warning]` callout 点明"别把云厂商自动调参当成 MySQL 默认值"。

### 5. `wiki/MySQL/MySQL 基础与架构.md`
- **[P1 准确性-过时]** `### DATETIME 和 TIMESTAMP 有什么区别？`（原 L107）"DATETIME 占 8 个字节" → 更正为 **5 字节**（自 MySQL 5.6.4 起，+0~3 字节小数秒；5.6.4 之前为 8 字节），TIMESTAMP 4 字节并补"+0~3 字节小数秒"。
- **[P1 准确性-概念混淆]** `### varchar 与 char 的区别？`（原 L87）"最多可以容纳 65535 个字符" → 改为 **65535 个字节**（且为整行所有列共享的上限），并补 utf8mb4 下单列实际约 16383 字符、扣长度字节后单列可设到 65533。
- **[P2 顶部速览]** 文件 406 行无顶部引导 → 在 H1 下补 `> [!info] 速览` callout（SQL 基础 / 数据库架构两主线 + 锚点跳转 + 跨篇指引）。

### 6. `wiki/MySQL/事务与MVCC.md`
- **[P1 准确性-表述含糊]** `### 事务的隔离级别是如何实现的？`（原 L132）"读未提交：通过行锁共享锁确保…但不会阻止脏读"把写锁与读行为混叙 → 改为"RU 普通读不加锁、不走 MVCC 快照，直接读最新版本（哪怕未提交），故脏读；写仍加排他锁"，并顺手厘清 RC 那条（写加排他锁、普通读走 MVCC 每次新建 ReadView）。

## 二、拆分清单
无。本单元 8 个文件最大 471 行（索引机制详解），全部 <600 行，按方法论不需要拆分。两篇 >400 行（基础与架构 406、索引机制详解 471）按规则补顶部速览 callout 处理（见上）。

## 三、锚点变更（供阶段2全局改链参考）
- **新增锚点**（不影响旧链）：
  - `MySQL 基础与架构.md`、`索引机制详解.md` 顶部各新增一个 `[!info]` callout，未改任何小节标题。
- **标题/小节改名或移动**：无。所有 H2/H3 标题文本均未改动，原有 `[[文件#小节]]` 链接不受影响。
- 唯一新增的内部链接：`高可用与分库分表.md` 新增指向 `[[锁机制#DDL 与锁]]` 的 callout（目标在本文件夹内，锚点 "DDL 与锁" 真实存在，已核对）。

## 四、暂存清单
无。本单元无跨域内容需要搬走（雪花算法/分布式 ID 虽略偏 Java/分布式域，但作为分库分表自增主键冲突的延伸属合理范围，按报告建议保留，未迁移）。

## 五、留给阶段2的链接问题（跨文件夹坏链 / 锚点漂移）
本次未改动任何跨文件夹链接。以下为本文件夹内**指向其他文件夹**的 wiki 链接，需阶段2全局改链 agent 核验目标文件/锚点是否存在（仅记录，未改）：

- `锁机制.md` L396-397 `相关链接`：`[[Spring 事务]]`、`[[Redis 数据类型与原理]]` —— 需确认 Spring/Redis 域内是否存在同名文件。
- `索引机制详解.md` L471 `[[Redis 基础]]` —— 需确认 Redis 域内文件名。
- `事务与MVCC.md` L245 `[[Spring 事务]]`。
- `引擎与日志.md`：相关链接均为 MySQL 域内（OK）。
- `MySQL 基础与架构.md` L406 `[[Spring 事务]]`。
- `优化与实战.md` L332 `[[Redis 基础]]`。
- `高可用与分库分表.md` L292-293 `[[Spring Boot 与微服务]]`、`[[Redis 基础]]`。

> 说明：上述 `[[Spring 事务]]`、`[[Redis 基础]]`、`[[Redis 数据类型与原理]]`、`[[Spring Boot 与微服务]]` 均为跨文件夹链接，按铁律不在本阶段改动；同一目标名（如 Redis 基础 vs Redis 数据类型与原理）在不同篇里写法不一致，阶段2需统一并核验真实文件名。

## 六、P0/P1 修复计数与遗留

- **P0**：1 条（锁机制 L36 乱码）—— 已修。
- **P1**：6 条 —— 全部已修：
  1. B+树空间复杂度 O(logN)→O(N)（索引机制详解）✅
  2. Online DDL 域内去重 + 链接收敛（高可用 → 锁机制）✅
  3. Buffer Pool 默认值 128MB 更正（引擎与日志）✅
  4. DATETIME 5 字节更正（基础与架构）✅
  5. VARCHAR 字符→字节更正（基础与架构）✅
  6. RU 隔离级别加锁表述更正（事务与MVCC）✅
- **P2**：本阶段顺手做了 4 项 —— 两篇顶部速览 callout（基础与架构、索引机制详解）✅、两处代码块语言标签（锁机制 L245、高可用 L131）✅。

### 本阶段判断不做 / 留待后续：
- **报告 P1-6「优化与实战篇 Diátaxis 混类」**：报告自评"尚不到红旗""记 P1 提示"。本篇 333 行，SQL 实战题 + EXPLAIN 参考 + 慢查询 how-to 三段已用 `---` 与清晰 H2 分区，强行拆分会把 EXPLAIN 参考送进索引篇（跨概念）反而割裂。本阶段保留现状，未拆。EXPLAIN type 排序在篇内 3 处出现（tip callout / type 大表 / 慢查询字段表）属不同语境的快速提示，全表是单一权威源，未做激进合并以保可读性。
- **报告 P2-10「雪花算法项目化细节」**：按报告建议"保留结论与时钟回拨 callout"，且符合用户 Java+Agent 求职方向，未压缩。"延迟降低 20%" 等项目营销表述保留（属个人项目经历，非技术错误）。
- **报告 P2-11「MOC 小节标题缺标记」**：`MySQL索引_MOC.md` 属索引/MOC 文件，按铁律本阶段**不碰**，留阶段2统一处理。
- **redo log "默认 2 个文件 / 每个 48MB / ib_logfile0/1"（引擎与日志 L192）**：报告列为"轻微"。MySQL 8.0.30+ 已用 `innodb_redo_log_capacity` 取代该机制，但原文本身在讲历史固定大小机制语境下不算错误，本阶段未改（可阶段后续补版本提示）。

## 七、校验
- 8 个文件 ```代码围栏全部偶数闭合；全文 U+FFFD = 0；每文件真实 H1（排除代码块内 bash/ini 注释 `#`）均为 1 个；新增 callout 均为单个 info/warning，无嵌套、无连用。
