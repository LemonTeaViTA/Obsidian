# 计算机网络 单元修复日志

- 修复执行日期：2026-06-01
- 负责文件夹：`wiki/计算机网络/`
- 审计报告来源：`wiki审计/领域报告/计算机网络.md`

## 一、改了哪些文件、各做了什么

### 1. `wiki/计算机网络/计算机网络.md`（564 行 → 310 行）

| 改动 | 类型 | 说明 |
|------|------|------|
| 顶层小节重排 一→四→二→三→五 改为 一→二→三→四→五 | P0 结构 | 见下方「锚点变更」 |
| 拆出第 5 章 Agent 传输方式整章 | P0 Diátaxis | 新建 `Agent传输方式.md`，原文留一行 `[!info]` callout 链接 `[[Agent传输方式]]` |
| 顶部新增 `> [!tip] 本文导读` callout | P1 | 列出 5 个二级章节 wikilink + 一句话定位 |
| 移动「键入网址到网页显示」全链路小节 | 结构 | 从原「二、网络模型」移到「四、应用层」末尾作收尾（标题文本不变，外链不受影响） |
| 删除原 296 行紧贴章节的 `---` 分隔线 | P2 | 随第 5 章拆出一并消除 |
| 第 103 行不雅举例「下载苍井空老师的电影」 | P2 | 改为中性举例「下载一个几百 MB 的安装包……文件损坏无法安装」 |
| 「打王者开团」措辞 | P2 | 弱化为「打游戏团战」，保留王者/LOL/Zoom 等通用场景举例 |
| OSI↔TCP/IP 对应关系代码块补语言标签 | 规范 | ` ``` ` → ` ```text ` |
| TLS 握手轮次事实性补正 | P1 知识准确性 | 原文「TLS 四次握手」偏 TLS 1.2 表述，新增 `[!note]` 说明 TLS 1.2=2-RTT、TLS 1.3=1-RTT（0-RTT 恢复），正文「TLS 四次握手」改为中性「TLS 握手」 |
| frontmatter `last_reviewed` | 维护 | 2026-05-20 → 2026-06-01 |
| 修复 5 处写入过程引入的 U+FFFD 乱码 | 数据完整性 | MAC「地址」、产生「数据」、「是否」理解、「开辟」死等、「使用」UDP —— 已全部复原并复查 |

重排后顶层 5 个 H2（≤8 ✓）、单一 H1 ✓、无跳级、所有代码块均有语言标签 ✓。310 行远离 600 黄线 ✓。

### 2. `wiki/计算机网络/Agent传输方式.md`（新建，268 行）

- 装载原「五、Agent 时代的传输方式：Stdio / Streamable HTTP / WebSocket」整章全部内容（5.1~5.6）。
- frontmatter 四字段补全：`module: 计算机网络`（与所在域一致）、`tags: [计算机网络, Agent, MCP, LSP, stdio, WebSocket, RPC]`、`difficulty: medium`、`last_reviewed: 2026-06-01`。
- 单一 H1 `# Agent 传输方式：stdio / Streamable HTTP / WebSocket`；原 `### 5.x` 三级标题升级为 `## `（去掉 5.x 编号，改自然标题）；保持 ≤8 个顶层 H2（实际 7 个）。
- P2 高亮密度治理：原文几乎每段满屏 `==…==`，已降级——仅在「关键认知」保留 2 处真正核心定义高亮（`==stdio 不是网络==`、`==字节流必须自定义消息边界==`），其余一律改为 **bold** 或纯文本。
- 全部代码块补/保留语言标签：ASCII 图与决策树用 ` ```text `，shell 用 ` ```bash `，报文用 ` ```http `，代码用 ` ```python `。
- 顶部加 `> [!info] 本文定位` callout 说明与 [[计算机网络]] 的关系。
- 修复 2 处写入引入的 U+FFFD 乱码（「这个」「双方」），已复查无残留。

## 二、拆分清单

- `wiki/计算机网络/计算机网络.md` 第 5 章
  → 新建 `wiki/计算机网络/Agent传输方式.md`，装载 stdio / Streamable HTTP / WebSocket 三种传输方式的机制、类比、消息边界、对比表、选型决策树、关键认知。
  → 原文件保留 `## 五、Agent 时代的传输方式` 小节标题 + 一行 `[!info]` 占位 callout 指向 `[[Agent传输方式]]`。

## 三、锚点变更（阶段2全局改链需用）

顶层小节文本变化（H2 级，编号重排 + 1 处改名）：

| 旧标题（旧锚点） | 新标题（新锚点） |
|------|------|
| `## 一、传输层协议：TCP 面试痛点核心` | `## 二、传输层协议：TCP 面试痛点核心` |
| `## 四、传输层核心对决：TCP 与 UDP` | `## 三、传输层核心对决：TCP 与 UDP` |
| `## 二、网络模型` | `## 一、网络模型` |
| `## 三、应用层协议：HTTP 与 HTTPS 面试核心` | `## 四、应用层协议：HTTP 与 HTTPS 面试核心` |
| `## 五、Agent 时代的传输方式：Stdio / Streamable HTTP / WebSocket` | 拆出到新文件；原文改为 `## 五、Agent 时代的传输方式` |

重要说明：
- 所有 `###` 子标题（面试问题）的**文本完全未改**，仅其所属 H2 的编号变了。`wiki/面试题目.md` 的 8 条 wikilink 全部指向 `###` 子标题文本（如 `#TCP三次握手与四次挥手`、`#TCP 面向字节流、UDP 面向报文，会导致什么区别？`），**不受本次重排影响，无需改**。
- 「键入网址到网页显示」小节标题文本不变，仅位置从「网络模型」章移到「应用层」章，wikilink 仍可命中。

本文件内部锚点（已自行修复）：
- 第 5 章拆出后，原文内 `[[#TCP三次握手与四次挥手|…]]` 两处引用仍在「四、应用层 / 全链路」小节内，目标 `### TCP三次握手与四次挥手` 仍在本文件「二、传输层」章，**同文件锚点有效**，无需改。
- 新文件 `Agent传输方式.md` 内原 `[[#TCP 面向字节流]]`（旧文件内断链锚点，审计 P1）已改为正确的跨文件链接 `[[计算机网络#TCP 面向字节流、UDP 面向报文，会导致什么区别？|TCP 面向字节流]]`，指向主文件完整标题，断链已修。

## 四、暂存清单

无。Agent 传输方式内容按 `wiki-管理方法论.md:148`「Stdio / WebSocket / Streamable HTTP 只在 [[计算机网络]]，MCP / LSP 链接过去」的定论，仍属本域，拆为同文件夹内新文件 `wiki/计算机网络/Agent传输方式.md`，未跨域、未写入其他文件夹、未写暂存文件。

## 五、留给阶段2的链接问题（跨文件夹坏链/锚点漂移）

第 5 章从 `计算机网络.md` 拆出到 `Agent传输方式.md` 后，以下指向旧位置的跨文件夹链接全部需要改向（旧锚点 `计算机网络#五Agent-时代的传输方式…` 已失效）：

| 源文件:行 | 现状链接 | 应改指向 |
|------|------|------|
| `wiki/Agent/MCP 协议.md:105` | `[[计算机网络#五Agent-时代的传输方式stdio--streamable-http--websocket]]` | `[[Agent传输方式]]`（可加锚点到具体小节） |
| `wiki/Agent/LSP 与代码诊断.md:170` | `[[计算机网络#五Agent-时代的传输方式stdio--streamable-http--websocket]]` | `[[Agent传输方式]]` |
| `思考记录.md:325` | `[[计算机网络]] §五 Agent 时代的传输方式` | `[[Agent传输方式]]` |
| `wiki-管理方法论.md:148` | `... 只在 [[计算机网络]]，MCP / LSP 链接过去` | 文案可保留（仍在本域）；如需精确可改提 `[[Agent传输方式]]` |

其他已核对无需改的跨文件夹链接（保留记录）：
- `wiki/面试题目.md:318-325` 共 8 条 → 指向 `计算机网络.md` 的 `###` 子标题文本，本次未改文本，**仍有效**。
- `wiki/数据格式/DOM.md:137`、`wiki/数据格式/XML.md:254` → `[[计算机网络]]` 整文件链接，有效。
- 主文件「全链路」小节内 `[[DOM]]` → 目标 `wiki/数据格式/DOM.md` 存在，有效（本次把原 `==…详见 [[DOM]]。==` 的高亮去掉，链接本身不变）。
- `wiki/操作系统/操作系统.md:236` → `[[计算机网络篇]]`（带"篇"旧名）疑似历史坏链，但属操作系统域，**不在本单元职责内**，记录供阶段2核对。

## 六、P0 / P1 完成情况

- P0（2 条）：全部修完
  1. 顶层章节编号错乱 一→四→二→三→五 → 重排为 一→二→三→四→五 ✓
  2. Agent 传输章 Diátaxis 混类 → 整章拆出到 `Agent传输方式.md`，原文 callout 链接 ✓
- P1（3 条）：全部修完
  1. 缺顶部渐进式摘要 callout → 已加 `[!tip] 本文导读` ✓
  2. 文档接近 600 行 → 拆分后降至 310 行 ✓
  3. 内部 wikilink 锚点 `[[#TCP 面向字节流]]` 不精确断链 → 改为完整标题跨文件链接 ✓
- P2（额外修，非硬性）：分隔线、不雅举例、高亮密度、代码块语言标签 均已处理 ✓
- 知识准确性：TLS 握手轮次（TLS 1.3 1-RTT）已补正 ✓

## 七、未做（超出本阶段范围）

审计报告「③ 领域知识点准确性评价」中建议「补充」的高频面试点——HTTP/2 多路复用、HTTP/3 与 QUIC 展开、HTTP 状态码/Cookie/Session/JWT、DNS 递归 vs 迭代、流量控制 rwnd、Cookie/CORS/CSRF/XSS——属于**内容扩写**而非结构/数据修复，不在本阶段（拆分/结构/去重/数据完整性/事实订正/frontmatter）范围内，留待后续内容补全阶段处理。
