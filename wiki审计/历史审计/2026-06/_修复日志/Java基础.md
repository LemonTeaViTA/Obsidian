# Java基础 单元 修复日志

> 修复日期：2026-06-01
> 修复范围：`wiki/Java基础/` 文件夹内文件（未触碰 MOC、未触碰 wiki/面试题目.md、未写入其他文件夹）
> 依据：`wiki审计/领域报告/Java基础.md`

---

## 一、改动文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `面向对象.md` | **删除** | 930 行超长红旗，拆为 3 篇（见拆分清单） |
| `面向对象核心.md` | **新建** | 三大特性 + 重载重写 + 抽象类与接口（335 行） |
| `类成员与关键字.md` | **新建** | 访问控制 + this + 成员/局部变量 + static + final + 内部类（385 行） |
| `对象语义.md` | **新建** | ==/equals + equals-hashCode + 值传递 + 深浅拷贝 + 对象创建（268 行） |
| `注解、反射与 Java 8.md` | 修改 | 修复代码块内 8 处中文弯引号；更新 1 处出链 |
| `String.md` | 修改 | 修复 L43 U+FFFD 乱码 |
| `包装类与 Object.md` | 修改 | 修复 1 处断锚 + 1 处出链改向拆分后文件 |
| `基础语法.md` | 修改 | Integer 缓存补 SSoT 回链 callout；更新 1 处出链 |
| `Java 概述.md` | 修改 | LTS 措辞精修（P2-2）；更新 1 处出链 |
| `异常与 IO.md` | 修改 | 更新 1 处出链（因拆分而失效的本地链接） |

---

## 二、拆分清单（P0-1）

旧文件 `面向对象.md`（930 行，顶层 10 小节）→ 拆为 3 篇，均 <400 行、顶层小节 ≤5（含相关链接）：

### 1. `面向对象核心.md`（335 行）
- `## 面向对象基础`（面向对象 vs 面向过程、三大特性：封装/继承/多态）
- `## 方法与重载重写`（重载和重写的区别）
- `## 抽象类与接口`（抽象类和接口有什么区别）
- 顶部新增 `> [!info] 速览` callout（P1-6 同时解决）

### 2. `类成员与关键字.md`（385 行）
- `## 访问控制与 this`（访问修饰符、this）
- `## 成员变量与局部变量`（含 static 存储位置，P2-1 已修正）
- `## static 与 final`（static 关键字、final 关键字、final/finally/finalize）
- `## 内部类`（4 种内部类）
- 顶部新增 `> [!info] 速览` callout

### 3. `对象语义.md`（268 行）
- `## equals 与 hashCode`（== 和 equals、为什么重写 equals 必须重写 hashCode）
- `## 参数传递`（值传递 vs 引用传递）
- `## 深拷贝与浅拷贝`（深浅拷贝区别）
- `## 对象创建方式`（5 种创建方式 + 初始化顺序）
- 顶部新增 `> [!info] 速览` callout

> 拆分后原「相关链接」小节按主题分配到三篇各自的「相关链接」，并新增三篇之间的互链。

---

## 三、锚点变更（阶段2全局改链需用）

原 `面向对象.md` 中的章节迁移，旧锚点 → 新文件#新锚点：

| 旧锚点（在 面向对象.md） | 新位置 |
|------|------|
| `面向对象#面向对象和面向过程的区别？` | `面向对象核心#面向对象和面向过程的区别？` |
| `面向对象#面向对象编程有哪些特性？`（封装/继承/多态） | `面向对象核心#面向对象编程有哪些特性？` |
| `面向对象#重载和重写的区别？` | `面向对象核心#重载和重写的区别？` |
| `面向对象#抽象类和接口有什么区别？` | `面向对象核心#抽象类和接口有什么区别？` |
| `面向对象#访问修饰符...` | `类成员与关键字#访问修饰符 public、private、protected、默认 的区别？` |
| `面向对象#this 关键字有什么作用？` | `类成员与关键字#this 关键字有什么作用？` |
| `面向对象#成员变量与局部变量的区别有哪些？` | `类成员与关键字#成员变量与局部变量的区别有哪些？` |
| `面向对象#static 关键字了解吗？` | `类成员与关键字#static 关键字了解吗？` |
| `面向对象#final 关键字有什么作用？` | `类成员与关键字#final 关键字有什么作用？` |
| `面向对象#final、finally、finalize 的区别？` | `类成员与关键字#final、finally、finalize 的区别？` |
| `面向对象#Java 有哪几种内部类？各有什么特点？` | `类成员与关键字#Java 有哪几种内部类？各有什么特点？` |
| `面向对象#== 和 equals 的区别？` | `对象语义#== 和 equals 的区别？` |
| `面向对象#为什么重写 equals 时必须重写 hashCode？` | `对象语义#为什么重写 equals 时必须重写 hashCode？` |
| `面向对象#Java 是值传递还是引用传递？` | `对象语义#Java 是值传递还是引用传递？` |
| `面向对象#说说深拷贝和浅拷贝的区别？`（旧报告里曾误写 `#深拷贝和浅拷贝`） | `对象语义#说说深拷贝和浅拷贝的区别？` |
| `面向对象#Java 创建对象有哪几种方式？` | `对象语义#Java 创建对象有哪几种方式？` |

本文件夹内部、由我修复的指向 `[[面向对象]]` 的链接已全部改向对应新文件（见下「已修链接」）。

---

## 四、已修链接（本文件夹内、我编辑过的文件）

| 文件:行 | 旧 | 新 |
|------|----|----|
| `包装类与 Object.md` clone 行 | `[[面向对象#深拷贝和浅拷贝]]`（**断锚**，P1-4） | `[[对象语义#说说深拷贝和浅拷贝的区别？]]` |
| `包装类与 Object.md` 相关链接 | `[[面向对象]]` | `[[对象语义]]` |
| `基础语法.md` 相关链接 | `[[面向对象]]` | `[[面向对象核心]]` |
| `Java 概述.md` 相关链接 | `[[面向对象]]` | `[[面向对象核心]]` |
| `注解、反射与 Java 8.md` 相关链接 | `[[面向对象]]` | `[[面向对象核心]]` |
| `异常与 IO.md` 相关链接 | `[[面向对象]]` | `[[面向对象核心]]` |

新增 SSoT 回链：
- `基础语法.md` Integer 缓存处新增 callout → `[[包装类与 Object#Integer 127 与 128 的 `==` 比较结果？各包装类缓存范围是多少？]]`
- `对象语义.md` `==` 处新增 callout → `[[包装类与 Object#Integer 127...]]`；equals-hashCode 处新增 callout → `[[包装类与 Object#为什么重写 equals 必须同时重写 hashCode？]]`（解决 P1-5「面向对象精简版未回链」）

---

## 五、暂存清单

无。本单元 7 篇内容文件全部属于「Java 语言基础」域，无跨域内容需要搬迁，未写任何暂存文件。

---

## 六、留给阶段2的链接问题

### 1. 必须改的 MOC 链接（我不能碰 MOC）
- `Java基础索引_MOC.md:31` `[[面向对象]]` → 旧文件已删除，应改为指向拆分后 3 篇。建议改为分别列出：
  - `[[面向对象核心]]`、`[[类成员与关键字]]`、`[[对象语义]]`
  - 或在 MOC 该节下补三个子条目。

### 2. 跨文件夹坏链 / 锚点漂移
- 本次扫描了 `Java基础/` 所有出链目标，除上面 MOC 的 `面向对象` 外，其余跨文件夹目标文件均存在（`JVM 内存管理`/`JVM 类加载机制`/`AOP 与动态代理`/`Spring 基础与 IoC`/`Spring 事务`/`线程基础与ThreadLocal`/`HashMap核心原理`/`JVM 概述` 等），**未发现跨文件夹坏链**。
- 跨文件夹的入链：全 wiki 范围内（`Java基础/` 之外）当前**没有**任何文件以 `[[面向对象...]]` 形式指向本单元（已 grep 确认），故拆分不产生其他文件夹的坏链。若阶段2新增入链，目标按上「锚点变更」表对应。

---

## 七、P0/P1/P2 完成情况

| 编号 | 问题 | 状态 |
|------|------|------|
| P0-1 | 面向对象.md 930行超长 + 顶层10小节 → 拆 3 篇 | ✅ 已修 |
| P1-1 | 注解篇代码块弯引号 8 处 | ✅ 已修（全角 → 半角直引号） |
| P1-2 | 面向对象篇代码块弯引号 11 处 | ✅ 已修（拆分时一并改为半角直引号；prose 中保留全角引号） |
| P1-3 | String.md L43 U+FFFD 乱码 | ✅ 已修（恢复「等价于」） |
| P1-4 | 包装类断锚 `[[面向对象#深拷贝和浅拷贝]]` | ✅ 已修（改向 `[[对象语义#说说深拷贝和浅拷贝的区别？]]`） |
| P1-5 | 域内 SSoT：Integer 缓存 / equals-hashCode 回链 | ✅ 已修（基础语法 + 对象语义新增 callout 回链至「包装类与 Object」单点；保留各处精简结论） |
| P1-6 | 面向对象缺顶部速览 callout | ✅ 已修（拆分后 3 篇各加 `[!info] 速览`） |
| P2-1 | static 变量存储位置 JDK 7+ 已过时 | ✅ 已修（类成员与关键字篇 callout 补「JDK 7+ static 字段实际在堆中 Class 对象，元空间仅存类元数据」） |
| P2-2 | Java 概述 L99 LTS 措辞 | ✅ 已修（改为「Oracle JDK 商用收费起点，OpenJDK 发行版仍可免费商用」） |

剩余未修：无（P0/P1/P2 全部处理）。唯一遗留是 MOC 的 `[[面向对象]]` 链接，按铁律不碰 MOC，已转交阶段2。

修复过程中自查并修正了我新建文件里因输入引入的 3 处 U+FFFD（类成员与关键字 2 处、对象语义 1 处），当前 3 篇新文件已无乱码。
