# 修复日志：RAG-检索与向量

- 修复日期：2026-06-01
- 负责单元：RAG-检索与向量（6 个原文件，新增 1 个）
- 方法论依据：wiki-管理方法论.md（单文档 <600 行；顶层 ≤8 小节；单 H1；Diátaxis 单一类型；域内 SSoT；frontmatter 四字段；代码块语言标签；标题不内嵌高亮）

---

## 一、改了哪些文件，各做了什么

### 1. wiki/RAG/RAG检索策略.md（P0 拆分主对象）
- **从 904 行降到 596 行**（<600 行达标）。
- §一「查询理解与意图识别」整块（原 12-306 行，约 300 行）**提取**到新文件 [[RAG查询理解]]；原位置改为**导航 stub**（保留 `## 一、查询理解与意图识别` 标题，使既有锚点仍可解析 + 一句话定位 + 跳转）。
- **域内去重（P1 父子分块）**：原 §2.1「方案一 Parent-Child」含完整代码 与 §5.1「父子分块」重复。改为：§2.1 删去父子分块完整代码与「核心矛盾」重复标题，只留一句定位 + `[!note]` callout + 内链到 [[#5.1 父子分块（Small-to-Big Retrieval）]]；§5.1 保留全文（SSoT 单点）。§2.1 仅保留独有的「方案二 摘要索引」「方案三 多粒度分层索引」。
- **P2「L5」命名矛盾**：§5.4 表格原「L5 chunk 内容增强（Contextual Retrieval/Late Chunking）」改为「chunk 内容增强（与 L1-L4 正交，Contextual Retrieval/Late Chunking）」，与本单元自建 L1-L4 框架一致。
- 新增顶部 `[!info] 速览` callout（5 小节导航）。
- frontmatter：去掉已迁出内容相关 tag（查询理解/Function Calling/Agentic RAG），last_reviewed 更新为 2026-06-01。
- 「四层优化组合策略」由无编号 `###` 规范为 `### 2.5 四层优化组合策略`。
- 代码围栏中无语言标签的纯文本框（ASCII 流程图/口诀）补 ` ```text ` 标签。

### 2. wiki/RAG/RAG查询理解.md（新建）
- 装载从 RAG检索策略.md §一 提取的全部内容，重组为 6 个 H2 小节：一 为什么需要查询理解 / 二 现代主流模式（Function Calling、Agentic RAG、查询改写、反问澄清）/ 三 传统意图分类的局限与现代取舍 / 四 实体提取 / 五 查询路由 / 六 容错与回退。
- 顶部加 `[!info] 速览`；frontmatter 四字段补全（module: RAG）。
- 原 `#### 1.1 Function Calling / Tool Use（==生产事实标准==）` 标题去掉内嵌高亮，降为 `### 2.1 Function Calling / Tool Use`，高亮移到正文首句。
- Diátaxis：定位为「解释/指南」单一类型，与 RAG检索策略.md（检索工程指南）分离。

### 3. wiki/RAG/RAG向量与Embedding.md
- **P2 causal-mask 过度概括**：§四「把 LLM 改造成 Embedding 的关键技术」表格原「步骤1 去 causal mask = 通用第一步」改为「注意力/pooling 适配：两条主流路线——①去 mask 改双向（LLM2Vec/NV-Embed）②保留 causal + last-token pooling（e5-mistral/部分 Qwen-Embedding）」，并加 `[!note]` 说明「去 causal mask 不是唯一路线」。
- **P2 KaLM 12B 存疑**：§五前沿趋势原「KaLM-Embedding（12B）登顶 MTEB」删去无法核实的 12B 硬数字，改为「7B/8B 级 LLM-backbone 模型（NV-Embed、Qwen3-Embedding-8B）登顶 MTEB」。
- **P2 标题内嵌高亮**：`#### 3. Hybrid（混合过滤）：HNSW 遍历时==同步检查==元数据` 去掉标题高亮，移到正文。
- 顶部加 `[!info] 速览`（623→635 行，处于 600-800 区间，无干净切缝故按方法论加速览 callout 而非强拆）。

### 4. wiki/RAG/Code RAG.md
- **P2 标题内嵌高亮（5 处）**全部修复，高亮移到正文首句：§2.1 AST-aware 切分（基础）、§2.2 多粒度索引（Cursor 模式）、§四 仓库级别 RAG（核心）、多路召回（Cursor 实战）、§五 调用图增强（Code RAG 杀手锏）。
- **代码 embedding 模型名核实**：§3.1 模型表中无法核实官方专用 embedding 变体的「Qwen2.5-Coder-Embedding」「CodeGemma-Embedding」标注「（待核实）」并加 `[!warning]`；补入已确认的代码检索模型 Salesforce CodeXEmbed（SFR-Embedding-Code）、Nomic-Embed-Code，并把 Jina 标注为 jina-embeddings-v2-base-code。§3.2 示例、§3.3 选型表、§6.3、§7.1 中引用的未核实模型名一并替换为已确认模型（Voyage-code-3 / CodeXEmbed / Jina code embeddings）。

### 5. wiki/RAG/分块策略.md
- **P2 标题内嵌高亮**：`#### 路线二：判别式分块模型（==最新 SOTA==）` 去掉标题高亮，移到正文。
- 坏锚点 `[[分块策略前沿#判别式分块（金山办公-2026）]]`（应为 `#三、判别式分块（金山办公 2026）`）**未改**——按本阶段铁律，跨文件锚点漂移留给阶段2统一改链（见下「留给阶段2」）。

### 6. wiki/RAG/分块策略前沿.md
- **frontmatter module 归类不一致**：`module: 文档解析` 改为 `module: RAG`（文件在 wiki/RAG/ 下，与同域一致）；原「文档解析」保留为 tag。

### 7. wiki/RAG/RAG基础与架构.md
- 本次未做内容改动（审计为 🟢，无 P0/P1）。其指向被迁移内容的链接见「留给阶段2」。

---

## 二、拆分清单

| 旧文件 | 新建文件 | 装载内容 |
|--------|---------|---------|
| RAG检索策略.md（§一 查询理解与意图识别，~300 行） | **RAG查询理解.md** | 为什么需要查询理解 / Function Calling / Agentic RAG / 查询改写 / 反问澄清 / 传统意图分类局限与取舍 / 实体提取 / 查询路由 / 容错回退 |

- 拆后：RAG检索策略.md 596 行、RAG查询理解.md 324 行。
- 命名用自然名（无 00-/01- 前缀），与原文件同放 wiki/RAG/。

---

## 三、锚点变更（阶段2全局改链需用）

迁出 RAG检索策略.md 的小节，其锚点不再存在于原文件，新锚点在 RAG查询理解.md：

| 旧锚点（RAG检索策略.md） | 新锚点 |
|--------------------------|--------|
| `RAG检索策略#一、查询理解与意图识别` | 仍存在（stub 占位，可解析）；内容主体 → `RAG查询理解`（建议重定向） |
| `RAG检索策略#1.1 Function Calling / Tool Use（==生产事实标准==）` | `RAG查询理解#2.1 Function Calling / Tool Use` |
| `RAG检索策略#1.2 Agentic RAG（更彻底的方案）` | `RAG查询理解#2.2 Agentic RAG（更彻底的方案）` |
| `RAG检索策略#1.3 查询改写（Query Rewriting）` | `RAG查询理解#2.3 查询改写（Query Rewriting）` |
| `RAG检索策略#1.4 反问澄清（Clarification）` | `RAG查询理解#2.4 反问澄清（Clarification）` |
| `RAG检索策略#四层优化组合策略`（无编号） | `RAG检索策略#2.5 四层优化组合策略` |

文件内（自编辑文件内部）已自洽的锚点：RAG检索策略.md 内 §2.1 → `[[#5.1 父子分块（Small-to-Big Retrieval）]]`、§5.4 表内引用，均已对齐实际标题，无需阶段2处理。

---

## 四、暂存清单

无。本单元唯一可独立成篇的内容（查询理解）仍属 RAG 域，直接在本文件夹内拆为 RAG查询理解.md，未涉及跨域搬迁，故无暂存文件。

---

## 五、留给阶段2的链接问题（跨文件锚点漂移/坏链）

> 本阶段不改链（铁律4）。以下为本单元发现、需阶段2全局改链 agent 处理的项。

### A. 因 §一 迁出而产生的失效/待重定向链接
1. **wiki/RAG/RAG评估.md:94** → `[[RAG检索策略#1.3 查询改写（Query Rewriting）|查询改写]]`
   - 已失效（1.3 已迁出）。应改为 `[[RAG查询理解#2.3 查询改写（Query Rewriting）|查询改写]]`。
2. **wiki/RAG/幻觉与置信度.md:495** → `[[RAG检索策略#1.4 反问澄清（Clarification）]]`
   - 已失效（1.4 已迁出）。应改为 `[[RAG查询理解#2.4 反问澄清（Clarification）]]`。
3. **wiki/RAG/文档解析综述.md:252** → `[[RAG检索策略#一、查询理解与意图识别]]`
   - stub 仍可解析；建议重定向到 `[[RAG查询理解]]`（更准确）。
4. **wiki/RAG/RAG基础与架构.md:78**（本单元文件）→ `[[RAG检索策略#一、查询理解与意图识别]]`
   - stub 仍可解析；建议重定向到 `[[RAG���询理解#一、为什么需要查询理解]]` 或 `[[RAG查询理解]]`。

### B. 预先存在的坏锚点（本阶段未改，待阶段2修）
5. **wiki/RAG/分块策略.md:110** → `[[分块策略前沿#判别式分块（金山办公-2026）]]`
   - 锚点与实际标题不符。实际标题为 `## 三、判别式分块（金山办公 2026）`（连字符应为空格，且缺 `三、` 前缀）。应改为 `[[分块策略前沿#三、判别式分块（金山办公 2026）]]`。

### C. MOC/索引（本阶段未碰，提示阶段2）
- RAG 域若有 MOC/索引文件，需新增 [[RAG查询理解]] 条目（本阶段按铁律2未碰任何 MOC/索引）。

---

## 六、P0/P1 完成情况

### P0（1 条，已完成）
- ✅ 拆分 RAG检索策略.md（904→596 行）：§一查询理解独立为 RAG查询理解.md，主文件顶层 5 小节（+相关链接），<600 行。

### P1（已完成）
- ✅ 父子分块域内重复消除（§2.1 改指针 + 链接到 §5.1 单点 SSoT）。
- ✅ 坏锚点（分块策略.md:110）——按铁律改为「记录」交阶段2（非本阶段修）。

### P2（顺带完成，因属本阶段「结构/事实性/frontmatter/高亮」范畴）
- ✅ 「L5」命名矛盾修正。
- ✅ Embedding causal-mask 过度概括收紧（两条路线）。
- ✅ KaLM 12B 存疑硬数字移除。
- ✅ 标题内嵌高亮全清（Code RAG 5 处 + 向量Embedding 1 处 + 分块策略 1 处 + 新文件 1 处）。
- ✅ 代码 embedding 模型名核实（未确认者标注 + 补已确认模型）。
- ✅ 分块策略前沿.md module 字段 文档解析→RAG。
- ✅ 两个 600-635 行长文档加 `[!info] 速览` callout。

### 未修（按范围/铁律刻意留下）
- 所有跨文件链接重定向/坏链 → 交阶段2（铁律4），见第五节 5 条。
- RAG向量与Embedding.md 未强拆（635 行，无干净切缝；按方法论加速览 callout 处理）。
- VimRAG（arXiv:2602.12735）、判别式分块（arXiv:2602.23370）等 2026 极新论文无法外部核验，原文保留，记为「待核实」非错误。
- 网络/HuggingFace 在本环境被拦截，KaLM-12B 与代码 embedding 模型名无法在线核验，已采用「移除/标注待核实 + 用已确认模型替换」的保守处理。
