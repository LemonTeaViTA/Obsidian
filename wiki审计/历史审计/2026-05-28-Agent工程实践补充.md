# 2026-05-28 — Agent工程实践补充

## 2026-05-28

- ==第十八-二十一期 + 第六期 HITL 增强，拆 5 处补到 3 个文档==——回应用户对多个期次的"看看这几个部分的内容如何"。5 个知识点按重要性依次补完：

  - **[[Agent 工程实践]] §八-B 末尾**新增"==为什么 Coding Agent 不做沙箱：安全模型的正确认知=="(~80 行):
    - ==本地 CLI Agent 安全模型== = HITL + PathGuard + CommandGuard + AuditLog（不是沙箱）
    - ==4 条不做沙箱的理由==（削弱能力 / 虚假安全感 / 体验差 / 用户已授权）
    - ==真正的 Agent 沙箱是 microVM 级==（Devin/Modal/Anthropic Computer Use 用 Firecracker/gVisor）+ 产品对照表
    - ==CommandGuard 黑名单==（sudo rm -rf / mkfs / dd of=/dev / fork bomb / curl|sh / find / / chmod 777 / / shutdown）+ Python 实现 + `outcome=deny, approver=policy` 审计
    - ==PathGuard 三种拦截==（绝对路径外逃 / `..` 穿越 / 符号链接逃逸）+ Python 实现
    - 安全模型四层流程图（CommandGuard → PathGuard → HITL → 执行+AuditLog）

  - **[[Harness Engineering]] §八 后**新增"==§八-A Prompt 分层架构：System Prompt 的工程化管理=="(~70 行):
    - ==为什么需要分层==（多角色共享 base / 用户项目级定制 / 动态上下文注入 / 审计变更）
    - ==组装顺序==（base → personality → mode → approval → project_context → skills → context_mgmt → handoff）+ ==越靠后越动态==（稳定层进 cache，变化层不进）
    - ==三层文件覆盖==（内置 < 用户级 < 项目级，整文件替换）+ 与 [[Agent Skills 体系]] 三层加载同构
    - ==PromptAssembler 实现==（三层查找 + 动态变量替换）
    - 4 条关键设计原则（base 必须含 `## Language` / 动态内容靠后 / 整文件替换 / 变更审计）

  - **[[Agent 工程实践]] §八-B 回滚机制**扩写"==Side-History Git Snapshot=="(~60 行):
    - ==独立 git 仓库不写用户 `.git`==（不污染项目历史 / 不破坏 index 状态 / 可随时清理）
    - ==三种快照类型==（pre-turn / post-turn / pre-restore）+ 用途对照
    - Python 实现（`create_snapshot` + `restore_snapshot`）
    - ==与 LangGraph checkpoint 的对比==（checkpoint 存 Agent 状态，side-history 存文件系统状态，两者互补）
    - `revert_turn` 纳入 HITL 危险工具链（恢复文件是不可逆操作）

  - **[[Function Calling]] §三 image 章节末尾**新增"==用户输入图片 + 历史裁剪=="(~60 行):
    - ==用户输入图片 vs 工具返回图片==（消息角色 / 触发方式 / 典型场景对比）
    - Anthropic user content 格式示例
    - ==图片预处理三步==（压缩缩放 / alpha 铺白底 / 注入元信息）
    - ==本轮图片优先原则==（不能用历史旧截图替代当前图片）
    - ==历史 image payload 裁剪==（新 turn 省略历史图片只保留文本元信息）+ Python `prune_image_history` 实现
    - `reasoning_content` 也不回传进历史（只写日志）

  - **[[Agent 工程实践]] §七 前**新增"==§七-A 异步后台任务与 Runtime API=="(~70 行):
    - ==Durable Task Queue==（SQLite 持久化 + 任务生命周期 enqueued→running→completed/failed/canceled）+ SQL schema
    - ==Worker Pool==（默认 2 个后台 worker，共享 ToolRegistry/MCP/Skills，各自独立 Memory/HITL）
    - CLI 闭环（`/task` / `/task add` / `/task cancel` / `/task log`）
    - ==Runtime API==（`POST /v1/threads` / `POST /v1/threads/{id}/turns` / `GET /v1/threads/{id}/events`）
    - ==与 OpenAI Assistants API 同构==（Thread/Run/SSE events 对应 Thread/Turn/SSE events）
    - 安全：强制 API Key + 不暴露公网
    - 典型场景（CI/CD 集成 / IDE 插件 / 脚本自动化）

  ==关键认知==（面试题区分度高）:
  - ==本地 CLI Agent 不做沙箱不是偷懒==——沙箱削弱能力 + 虚假安全感 + 体验差；真正隔离用 microVM（Devin/Modal）
  - ==CommandGuard 是 HITL 之前的快速拒绝==——明显危险命令不弹窗直接拒绝，减少 HITL 骚扰
  - ==Prompt 分层架构的组装顺序决定 cache 效率==——稳定层靠前进 cache，动态层靠后每轮变
  - ==Side-History Git 独立仓库==——不污染用户 .git，与 LangGraph checkpoint 互补（状态 vs 文件系统）
  - ==历史 image payload 必须裁剪==——旧截图反复进 context 浪费 token，新 turn 只保留文本元信息
  - ==Durable Task Queue + Runtime API 是 Agent 服务化的关键==——SQLite 持久化 + OpenAI Assistants API 同构设计

  **原因**：用户的 4 个期次 + 1 个补充期次描述了 PaiCli 的多个功能，拆开看有 5 个通用工程模式 wiki 完全空白（沙箱认知 / Prompt 分层 / Side-History / 用户图片输入 / Durable Task Queue）。这些都是 Claude Code / Cursor / Devin / OpenAI Assistants 都有的机制，wiki 0 覆盖是重大缺口。

- ==新建 `wiki/LLM/LSP 与代码诊断.md`==(566 行)——回应用户对"第十七期：LSP 诊断注入"的"这个部分的内容有吗"。判断:6 个 PaiCli 功能聚焦在==两个 wiki 完全空白的话题==(LSP 协议本身 + Post-Edit 诊断回注模式),独立成文:

  - **§一 LSP 是什么 + 为什么会出现**:M×N 困境 → LSP M+N 解法 + Microsoft 2016 提出 + 与 [[MCP 协议]] 同源类比(都是 JSON-RPC 解决 M×N)
  - **§二 LSP 协议核心**:通信机制(JSON-RPC over stdio)+ ==核心方法对照表==(initialize / textDocument/didOpen / textDocument/didSave / ==publishDiagnostics 主动推送== / definition / references / hover / completion / codeAction / rename / workspace/symbol)+ 一次诊断的完整流程(7 步时序)+ 最简 LSP Client Python 代码
  - **§三 主流 LSP Server 生态**:9 个语言 server 对照表(JDT LS / rust-analyzer / pyright / gopls / typescript-language-server / clangd / kotlin-language-server / omnisharp / sourcekit-lsp)+ 2026 趋势(AI 友好化 batch API)
  - **§四 LSP vs MCP:JSON-RPC 双胞胎**:6 维对照表(提出时间 / 连接对 / 协议基础 / 核心方法 / 推送机制 / 生态成熟度)+ ==MCP 明显借鉴 LSP 设计==的认知 + ==LSP 服务通过 MCP 暴露给 LLM== 的桥接模式(langserver-mcp 等)+ 内置 vs MCP 桥接的选型
  - **§五 Coding Agent 的诊断回注模式**:==Post-Edit 触发时序==(write_file → 异步触发 → pending 队列 → 下一轮注入)+ 关键设计 4 点(异步不阻塞 / 缓存 pending / 下一轮注入 / 格式化为人类可读)+ ==三种推理模式都注入==(ReAct 每轮 / PE 每 Step / Multi-Agent Worker+Reviewer)+ 异步 vs 同步等待对比 + 与 [[Reflection 实现#3.3 外部验证器]] 的落地关系
  - **§六 诊断格式化与容量管理**:`[error] file:line:col - message` 标准格式 + ==默认上限 20 条==的理由(token 预算 / 注意力稀释 / cache 稳定性)+ 优先级排序代码(severity > 当前文件 > 行号 > 重复抑制)+ ==注入后清空 pending 队列==
  - **§七 渐进式实现策略**:MVP(JavaParser / tree-sitter,覆盖 60% 语法错)→ v1(1-2 核心 LSP)→ v2(全语言)+ JavaParser 代码示例 + MVP 局限(只查语法,不查类型/跨文件)+ 升级工程成本
  - **§八 与 Reflection / Generator-Evaluator 的关系**:诊断回注 = 外部验证器具体落地 + Generator-Evaluator 架构具象 + ==LSP vs LLM-as-Judge vs 自我反思==三方对比(确定性 / 成本 / 速度)
  - **§九 关键认知与面试要点**:5 条认知 + 高质量答案模板

  ==同时更新==:
  - [[Reflection 实现]] §3.3 外部验证器末尾加 callout 指向 [[LSP 与代码诊断#五]]
  - [[Coding Agent 工具集]] §2.2 代码搜索类末尾加一行 LSP 协议说明 + 诊断回注链接
  - [[LLM索引_MOC]] Stage 3 加第 17 项(原 17-22 顺延为 18-23,Stage 4 顺延为 24-25)

  ==关键认知==(面试题区分度高):
  - ==LSP 与 MCP 是 JSON-RPC 双胞胎==——MCP 明显借鉴 LSP 设计(capability 协商 / notifications),MCP 是 LSP 思想在 Agent 时代的演化
  - ==Coding Agent ≠ IDE,诊断处理方式根本不同==——IDE 给人看,Agent 把诊断回注 LLM 让它自己修
  - ==Post-Edit 诊断必须异步不阻塞==——阻塞等 LSP 分析(1-5s)体验劝退,异步缓存 pending 队列是生产标配
  - ==诊断容量管理是 prompt 工程==——默认 20 条 + 优先级排序,避免 1000 条 flood prompt
  - ==渐进式实现==——MVP 轻量解析器先上,验证模式有价值再接完整 LSP
  - ==LSP 诊断比 LLM-as-Judge 更准更便宜更快==——确定性工具验证是 Reflection 外部验证器的最佳落地

  **原因**:用户的第十七期描述==聚焦在两个 wiki 完全空白的话题==——LSP 协议本身(wiki 仅在 Coding Agent 工具集提了一句)和 Post-Edit 诊断回注模式(Reflection §3.3 只讲了思想没讲落地)。LSP 是 Coding Agent 调用语言能力的核心协议,与 MCP 同源同设计,Cursor / Claude Code / Cline / Aider 都做诊断回注,wiki 0 覆盖是重大缺口。独立成文是因为 LSP 是完整协议主题(与 [[MCP 协议]] / [[Function Calling]] 同级),体量足够(566 行)。

- ==新建 `wiki/LLM/Coding Agent TUI 设计.md`==(550 行)——回应用户对"第十六期：TUI 产品化"的功能盘点。判断:用户提到 v16.1 的双形态 Renderer + inline/lanterna/plain 三实现,==这是 wiki 完全空白的"Coding Agent 的门面"领域==,与现有 [[Agent 工程实践#三、CLI 设计]]==正交但不同==——那个讲"被 AI 调用的 CLI",这个讲"AI 给用户用的 TUI",混在一起会乱:

  - **§一 Coding Agent 的产品形态光谱**:5 种形态(非交互 CLI / inline 流式 TUI / 全屏 TUI / IDE 插件 / 桌面应用)+ 与 [[AI 编程工具]] 的承接关系(本文是终端形态内部的细分)
  - **§二 TUI 三种渲染形态**:
    - inline 流式 TUI(主流,Claude Code / Qoder / Aider 风格)——主屏直出 + 流式 token + 底部 dock + 折叠块 + 行内 diff
    - lanterna 全屏 TUI(三栏 / 模态弹窗)
    - plain 兜底(CI / 单测 / 不支持 ANSI 终端)
    - 三形态对比表(默认推荐 / 终端兼容 / 流式体验 / CI 友好 / 学习曲线 / 实现复杂度)
  - **§三 Renderer 与 Agent 核心解耦**:==关注点分离架构图==(三种 Renderer 共享 Agent / ToolRegistry / MemoryManager / MCP / Skills / HITL 核心)+ Renderer 接口长什么样的 Java 接口示例 + ==共享什么不共享什么==的对照表 + ==测试友好性==(plain Renderer 让单测能跑完整 Agent 循环)
  - **§四 流式渲染的工程细节**:
    - LLM token 流式拼接(代码块缓冲 / markdown 表格缓冲 / 状态栏冲突 / 输入提示符冲突)+ Python 实现示例
    - 工具调用块的折叠展开(`Read 3 files (ctrl+o to expand)`)+ 实现要点
    - 行内 git diff(Myers diff + ANSI 着色 + 行号 + 大 diff 折叠)
    - ==状态栏更新与正文不冲突==(JLine `Status` API 工作原理 + 4 个常见坑)
  - **§五 交互式 HITL 的 UI 设计**:
    - inline 单字符 prompt(`[y/n/a/s/m]` 5 个含义 + 风险等级)
    - lanterna 模态弹窗
    - plain 逐行 prompt(==非交互场景默认拒绝==避免 CI 卡死)
    - 三种 UI 的设计共识(关键信息显眼 / 选项快捷化 / 默认安全 / 高危显示风险)
  - **§六 运行时切换推理模式**:`/plan` / `/team` / `/cancel` 命令 + UX 要点(当前模式可见 / 切换不丢上下文 / 优雅终止 / 模式专属 UI)+ ==`/cancel` 协作式取消实现==(检查点密集 + 输出当前进度 + 清理资源)
  - **§七 终端兼容性与降级**:5 个环境变量降级开关(`NO_COLOR` / `_NO_STATUSBAR` / `_RENDERER` / `TERM=dumb`)+ 自动检测降级代码(非 TTY / CI / dumb terminal → plain)+ ==配置演进 deprecated 处理==(不删旧开关 / stderr 警告 / 文档明确生命周期)
  - **§八 主流产品 TUI 风格对比**:Claude Code / Codex CLI / Qoder / Aider / Goose / Cline / Cursor terminal / PaiCli 8 个产品对照表

  ==同时更新==:
  - [[LLM索引_MOC]] 第 5 节"AI 产品形态"导航加入新文档 + Stage 4 学习路径加第 24 项
  - [[AI 编程工具]] §2.1 形态对比表后追加 callout 指向 [[Coding Agent TUI 设计]]

  ==关键认知==(面试题区分度高):
  - ==两种 CLI 不是一回事==——被 AI 调用的工具型 CLI(JSON 输出) vs 给用户用的交互式 TUI(流式 + 折叠 + 状态栏),不要混
  - ==Renderer 抽象是生产 Coding Agent 的核心==——三形态共享 Agent / Tool / Memory / MCP / Skills / HITL 核心,只是 UI 呈现不同
  - ==HITL 决策在核心层,UI 在 Renderer 层==——同一个决策,inline 单字符,lanterna 模态,plain 回车,==决策结果一致==
  - ==永远不要假设支持高级特性==——降级机制(NO_COLOR / 非 TTY / CI 检测)是 TUI 工程核心
  - ==协作式取消优于强杀==——`/cancel` 设 flag 而非杀线程,在 checkpoint 退出 + 输出当前进度
  - ==inline 流式是事实标准==——Claude Code / Qoder / Aider / Goose / PaiCli 默认都这个形态

  **原因**:用户的第十六期描述==大量是 PaiCli 实现细节==(JLine Status / lanterna 三栏 / `PAICLI_RENDERER` 等),但拆开看 ==Renderer 抽象 / 三形态对比 / 流式渲染细节 / HITL UI 设计 / 终端降级 / 协作式取消== 都是 ==生产级交互式 Coding Agent 通用工程经验==。Claude Code / Qoder / Cursor / Aider 都有同等机制,wiki 完全空白,这是个生态级缺口。独立成文是因为内容体量足够(~550 行)且与现有 [[Agent 工程实践#三、CLI 设计]] 是正交概念——混在一起会让两类 CLI 概念混乱。

- ==第十五期 Skill 系统在 [[Agent Skills 体系]] 增量补 5 处==——回应用户的"这一部分的内容呢"。8 个 PaiCli 功能==聚焦度极高,全在 Skills 一个文档==。174 → 401 行(新增 ~227 行):

  - **§二 渐进式披露 末尾**新增"==索引段预算=="(~25 行):
    - ==启用 skill 上限~20 个==(超过选择困难)、==索引段 ≤ 4KB==(避免吃掉 system prompt 预算)、==单 skill 索引 ≤ 200 字符==
    - 索引段示例 + ==prompt cache 友好==(索引稳定不变和 system prompt 一起进缓存,呼应 [[长上下文工程#prompt-caching]])
    - 上限超出的==正确做法==(按使用频率/最近调用排序取前 N,不是粗暴截断)

  - **§四 SKILL.md 加载机制 末尾**新增"==专用 load_skill vs 通用 view=="(~30 行):
    - 6 维对照表(LLM 输入 / 路径错误率 / 三层位置查找 / 内容截断 / 注入位置 / 缓存友好)
    - ==专用工具核心价值==:把"如何加载"复杂性从 LLM 转移到 Host,LLM 只做语义判断
    - ==SkillContextBuffer 设计==:不直接拼到 tool message,而是缓存到下一轮 user message 前置——既不污染 tool result,又保证"指导位置"

  - **§九 安全机制 末尾**新增"==与 HITL 的协同:Skill 不引入新审批维度=="(~30 行):
    - ==关键设计原则==:Skill 是"使用说明书"不是"权限提升",Skill 内调用工具走该工具既有 HITL 规则
    - 4 条不引入 Skill 维度的理由(避免授权矩阵爆炸 / Skill 是行为模板不是身份 / 审批本质是工具 / 简化心智模型)
    - ==反面教材==:某些产品给 Skill 单独"信任"开关导致预期与实际不符
    - 与 [[Agent 工程实践#hitl-工具读写粒度细化]] 的关系:HITL N 维矩阵 vs Skill 指令模板层正交

  - **§十二 目录位置 改写**==两层 → 三层加载位置 + 覆盖原则==(~25 行):
    - ==新增内置层==(jar 内 / 安装目录 = 产品出厂默认 skill)+ 用户级 + 项目级
    - ==同名 skill 整体覆盖==的理由(字段级合并语义模糊,行为不可预测)+ 派生模式(复制目录再改)
    - 与 [[长上下文工程]] CLAUDE.md 项目级/用户级覆盖思路一致

  - **新增 §十四 "==典型案例:web-access skill=="**(~110 行):
    - ==目录结构==(SKILL.md + 6 个站点经验 + cdp-cheatsheet)
    - ==SKILL.md 内容结构==(浏览哲学四步法 / 工具选择表 / 浏览器优先级 / Jina 兜底)
    - references 子文档示例(mp.weixin.qq.com 经验:防爬墙特征 + 推荐流程 + 已知坑)
    - ==决策手册型 vs 流程型 skill== 对照(web-access / code-review 是手册型,ppt-generation / deploy 是流程型)
    - 这个 skill 体现的 4 个关键设计点(渐进式披露 / 排除条件 / 复用宿主工具 / 沉淀机制)

  - last_reviewed 更新 2026-05-09 → 2026-05-28

  ==关键认知==(面试题区分度高):
  - ==索引段是 prompt cache 友好的==——稳定不变,跟 system prompt 一起进缓存,真实正文懒加载
  - ==专用 load_skill 把复杂性从 LLM 转移到 Host==——LLM 只做语义判断,生产 Coding Agent 都用专用工具
  - ==SkillContextBuffer 注入到下一轮 user 消息前==——而不是 tool result,避免污染又保证指导位置
  - ==Skill 不引入新审批维度==——避免授权矩阵爆炸,Skill 是行为模板不是身份
  - ==同名 skill 整体覆盖不字段级合并==——行为可预测优先于灵活性
  - ==决策手册型 skill== vs ==流程型 skill== 是 Skill 设计的两大分支

  **原因**:用户的第十五期描述==聚焦度极高==,8 个功能基本都在"Skill 系统"这一个领域,前几期"分散在 3-4 个文档"的模式不适用。这次拆开看 wiki 现有 [[Agent Skills 体系]] 在 ==索引预算 / 专用工具 vs view / Skill 与 HITL / 三层加载 / 典型案例== 5 个点都欠覆盖——尤其==索引段大小限制 / SkillContextBuffer / Skill 与 HITL 不引入维度==是生产实践细节,Anthropic / Cursor / Claude Code 都做但很少有人系统讲。本次集中补 [[Agent Skills 体系]],让这个文档==从"概念入门级"升级到"生产级实现=="。

- ==第十四期 CDP 会话复用 + 登录态访问拆 3 处补到 3 个文档==——回应用户的"这又是哪些方面的知识点"。8 个 PaiCli 功能聚焦在==浏览器 MCP 进阶模式==,5 个核心知识点 wiki 完全空白:

  - **[[MCP 协议]] §5.4 浏览器自动化**==大幅扩写==(~150 行):
    - 新增 chrome-devtools-mcp 到 server 列表(Google 官方 2025,2026 事实标准)
    - **==底层协议:Chrome DevTools Protocol (CDP)=="子节**——是所有浏览器 MCP 的共同底层(puppeteer/playwright/chrome-devtools-mcp 都是 CDP 客户端封装)+ macOS/Linux/Windows 启动命令 + 探活 endpoint(`127.0.0.1:9222/json/version`)+ MCP↔CDP 关系图
    - **"==isolated vs shared:两种工作模式=="子节**——核心设计选择对照表(profile / 登录态 / 适用 / 安全风险)+ ==isolated 默认的 fail-safe 理由==+ shared 切换流程(用户开 `chrome://inspect/#remote-debugging` → PaiCli 探活 → 切换 + 清空"全部放行")
    - **"==登录态访问的三种方案=="子节**——CDP 复用(主流) / Cookie injection / OAuth + token 持久化对比表
    - **"==Tab 所有权追踪:共享浏览器时的隔离原则=="子节**——`AGENT_OWNED_TARGETS` 标记代码 + ==不可证明则拒绝==的 fail-safe 原则 + 通用模式扩展(Multi-Agent 共享资源)

  - **[[Coding Agent 工具集]] §2.5 联网类**追加"==浏览器模式自适应=="(~25 行):
    - Agent 遇登录页/权限不足时主动调 `browser_connect` 切 shared 的典型流程(GitHub PR 例子)
    - 关键设计原则:==公开页面不提前切==(避免身份暴露)+ ==只在 Agent 判断需要时切==+ 切换清空"全部放行"

  - **[[Agent 工程实践]] HITL 章节**新增 3 个子节(~120 行):
    - **"==HITL 工具读/写粒度细化=="**:==粒度 2:读型 vs 改写型工具==(take_snapshot 仍 Always Allow / click·fill_form·evaluate_script 回退单步)+ ==敏感页面正则匹配==(`/checkout`、`/payment`、`/admin`、`/transfer` 等)+ `needs_step_approval` 决策代码 + ==HITL 是 N 维矩阵==(粒度/读写/敏感度/可逆性)
    - **"==模式切换 = 会话边界=="**:==上一节"会话级失效"原则的扩展==——浏览器模式切换 / MCP server 重启 / 工作目录切 / 模型切 / `/permissions reset` 都视为会话边界,==身份/权限/信任前提任何一个变化都重置授权==(保守 fail-safe)
    - **"==审计日志 schema 演进与向后兼容=="**:5 条兼容原则(只加不删 / 不改类型 / 新字段可选 / schema 版本号显式 / 枚举只增不减)+ 读取端 `.get()` 兼容代码 + ==踩过的坑案例==(args 从 dict 改 base64 string 导致半年合规数据丢失)+ ==审计日志比 API 演进更严格==(写出去就改不了)

  ==关键认知==(面试题区分度高):
  - ==chrome-devtools-mcp / puppeteer / playwright 共同底层都是 CDP==——本质都是 CDP 客户端的 MCP 封装
  - ==isolated 默认是 fail-safe==——除非用户主动切 shared,Agent 不会拥有用户身份
  - ==登录态主流方案是 CDP 复用==——零迁移成本 + 零站点适配,合规要求高才上 OAuth
  - ==Tab 所有权"不可证明则拒绝"==——是 Multi-Agent 共享资源的通用模式
  - ==HITL 是 N 维矩阵不是单维开关==——粒度 / 读写 / 敏感度 / 可逆性叠加判定
  - ==模式切换=会话边界==——身份/权限范围变就必须重置授权,避免默默升权
  - ==审计日志 schema 比 API 更严格==——写出去就改不了,只能加字段不能改字段

  **原因**:用户的第十四期描述==看似都是 PaiCli 浏览器命令==(`/browser` 命令组、isolated/shared 切换、敏感页面规则),但拆开看 ==CDP 协议层 / isolated-shared 模式 / 登录态访问方案 / Tab 所有权 / HITL 读写粒度 / 模式=会话边界 / 审计 schema 演进== 都是 ==生产级 Agent 通用工程经验==。Cursor / Claude Code / Cline 都有同等机制,wiki 0 覆盖,这是重大缺口。本次拆 3 处补到 3 个文档,==避免新建一个"浏览器模式专题"==(因为内容是已有章节的深化,不是新独立主题)。

- ==第十三期 Chrome DevTools MCP 拆 4 处补到 4 个文档==——回应用户的"这个是哪个部分的内容呢有相关的知识吗"。8 个 PaiCli 功能拆开看分散在 ==4 个领域==,不独立成文,补到对应 wiki:

  - **[[Coding Agent 工具集]] §2.5 联网类**新增"==fetch_url 局限 + 分级 fallback=="(~25 行):
    - fetch_url 处理不了的三类页面(SPA / 防爬墙 / 需交互)
    - ==分级 fallback 流程==(轻量 fetch_url → 失败 → 重量浏览器 MCP)
    - 成本对比表(启动/延迟/内存/输出)+ ==失败站点黑名单==(微信公众号 / 知乎专栏 / 推特 / 小红书命中后直接走浏览器 MCP)
    - 链接到 chrome-devtools-mcp 和 image tool result

  - **[[Function Calling]] §三 四种消息角色**新增"==Image Tool Result:多模态返回=="(~80 行):
    - 三家厂商 image content 格式(==Anthropic content blocks 数组== / ==OpenAI image_url + data:image/png;base64== / Qwen-VL 兼容 OpenAI)
    - ==text fallback 必须保留==的 4 个理由(模型不支持 / 审计日志 / 成本敏感 / 上下文压缩)
    - 生产实现 `normalize_tool_result` 函数(根据 LLM 能力动态选 image / text)
    - 典型 Browser MCP 工具返回结构(image + text + snapshot 三段式)
    - ==Token 计费陷阱==(1024×1024 截图 ~1500 tokens,5-10 张截图就几万 token)+ DOM snapshot 优先策略

  - **[[MCP 协议]] §3.5 新增"==Server 生命周期与异步启动=="**(~110 行):
    - ==initialize 握手 3 步流程==(initialize → 协商 capabilities → notifications/initialized 完成)
    - ==错误做法 vs 异步并发启动==代码对比(同步串行 5 个 server × 2s = 10s 劝退 vs 异步 + 首屏不阻塞)
    - ==两层超时设计==(CLI 首屏 8s vs Server initialize 60s,慢 server 不被错杀)
    - ==Server 状态机==(init → starting → ready / failed)+ `/mcp` 命令显示状态
    - ==健康检查 / 自动重启策略==(心跳 / 指数退避 1s→5s→30s→5min / 5 分钟 5 次上限进 failed / 手动重启重置计数器)

  - **[[Agent 工程实践]] HITL 章节**新增"==HITL 三档授权粒度=="(~60 行):
    - ==三档对照表==(单次确认默认 / Server-工具级"全部放行" / 全局信任模式)+ 失效时机
    - 典型使用流(用户选 [A] 全部放行 → chrome-devtools 后续 N 次调用直接执行)
    - ==5 条设计原则==(粒度可配 / 高危操作绕过 / 会话级失效 / 显式撤销 `/permissions reset` / 审计仍要记录)
    - 安全权衡表(单次 vs Always Allow 的安全/体验/失控代价对比)
    - ==与工具白名单的互补关系==(白名单声明式开发者定 vs HITL 运行时用户定)

  ==关键认知==(面试题区分度高):
  - ==fetch_url 的局限==——主流 Coding Agent 都内置失败站点黑名单 + 分级 fallback
  - ==tool_result 不止 text==——image 类型是 Browser/Computer Use 闭环的关键协议,但 ==text fallback 必须永远保留==
  - ==MCP 异步启动是必做==——同步串行启动 5 个 server 等 10s 用户劝退;两层超时(8s/60s)兼顾体验和容错
  - ==HITL 不是"每次问"==——三档粒度按风险分级,==Always Allow 下高危操作仍单独确认==

  **原因**:用户的第十三期描述==看似都是 PaiCli 产品功能==,但拆开看有 4 个是 ==生产 Coding Agent 通用工程经验==(Cursor / Claude Code 都有同等机制):分级 fallback / image tool result / MCP 异步启动 / HITL 三档粒度。这次拆 4 处补到 4 个文档,==避免新建一个"chrome-devtools 专题"文档(因为本质是分散经验,不是单一主题)==。

- ==新建 `wiki/LLM/长上下文工程.md`==(518 行)——回应用户对"第十二期：长上下文工程"的功能盘点。判断：用户提到 8 个 PaiCli 产品功能，==其中 2 个核心话题 wiki 完全空白==(grep "prompt cache" 全 wiki 0 条结果)，必须独立成文：

  - **§一 Prompt Caching 完整篇**：三家厂商对比表（Anthropic 手动 cache_control / DeepSeek 自动 prefix / OpenAI 自动 / Gemini CachedContent / Qwen-GLM-Kimi 类 DeepSeek）+ ==Anthropic `cache_control` 4 条规则==（最多 4 个缓存点 / 前缀必须完全一致 / 顺序敏感 / 最小 1024 token）+ ==缓存命中率经济学==（一次 20 轮会话 ¥1.8 → ¥0.28 省 84%）+ ==分层缓存策略==（system/tools 最稳 → CLAUDE.md → 历史摘要 → 当前消息）+ 三个常见失效坑（时间戳进 prompt / tools 顺序变 / 消息合并不一致）

  - **§二 模型能力声明与动态预算**：`LlmClient` 接口 5 个能力声明（maxContextWindow / supportsPromptCaching / promptCacheMode / recommendedOutputTokens / supportsParallelToolCalls）+ ==2026 主流模型 window 对照表==（Claude 200K-1M / GPT-5 200K / DeepSeek V4 1M / Gemini 1M+ / GLM-5.1 200K / StepFun 256K / Kimi K2.6 256K / Qwen3-Max 256K）+ ==80% Rule 动态预算==（budget = 0.8 × window - output - safety,允许系统属性覆盖）

  - **§三 Context Mode 切换策略**：==short/balanced/long 三档==(历史压缩 / RAG topK / Resources 索引联动) + 模式驱动的 5 维下游策略矩阵 + ==自动 vs 手动选择==(window<50k 强制 short / window≥500k 命中关键词建议 long)

  - **§四 Long Context 下的 RAG topK 自适应**:Window vs topK 对照（32K→3-5 / 200K→10 / 1M→20-50）+ ==`top_k=None` 默认参数让自适应不被覆盖==的踩坑 + ==实时 grep 优先 + RAG 是补充==的 Coding Agent 主路径

  - **§五 MCP Resources 在 Long Context 下的索引注入**:long mode 下==只注入 URI + description 不注入正文==(token-friendly,50 个 resources × 10 token = 500 token)+ short/balanced 不注入的理由(window 紧 + cache 命中率下降 + 大部分任务用不到)

  - **§六 Token 与成本可观测性**:5 个必显指标(total / cached / hit rate / output / 成本)+ ==状态栏 vs 正文输出区==(底部状态栏不污染正文)+ ==`/context` 命令==暴露 Harness 内部完整状态

  - **§七 关键认知与面试要点**:5 条认知 + 一段高质量答案模板("==你们 Agent 怎么处理长上下文?==")

  ==关键洞察==(面试题区分度高):
  - ==Prompt Caching 是省钱第一杠杆==——Coding Agent 不开 cache 字面意义烧钱(命中率 85%+ 省 80%+)
  - ==模型能力 self-aware 是多模型 Agent 的基础==——Harness 不能写死参数
  - ==Context Mode 是"换一种代价"==——long 不是更高级,80% 任务 balanced 够用
  - ==Long Context 下 RAG topK 必须自适应==——topK=5 跑 1M 模型召回率太低,topK=50 跑 32k 挤爆 prompt
  - ==MCP Resources 索引注入只塞 URI + 描述不塞正文==——token-friendly 优化

  ==同时更新==:
  - `LLM索引_MOC.md` Stage 3 学习路径加入第 22 项(原 22→23 顺延)
  - 第二节"工程框架"导航补"长上下文工程"条目

  **原因**:用户的第十二期描述==大量是 PaiCli 产品自定义==(LlmClient 接口 / `/context` 命令 / 状态栏布局),但拆开看==Prompt Caching、动态预算、Context Mode 切换、topK 自适应、Resources 索引注入==都是 ==2025-2026 生产级 Agent 通用工程方法论==。Prompt Caching 在 wiki 0 覆盖却是 Coding Agent 成本第一杠杆——不补就是 wiki 重大缺口。这次补完后 wiki 才==真正覆盖了"模型选型 → 路由 → 长上下文工程"完整成本/能力优化链路==。

- ==`wiki/LLM/MCP 协议.md` 大补 §四 Host 集成层 + §6.3 凭证脱敏 + §二 transport 升级到 Streamable HTTP==——回应用户对"第十期：MCP 协议核心"的功能盘点。==703 行（新增 ~150 行）==。判断：用户提到的是 PaiCli 产品功能，但拆开看==有 6 点是 MCP 协议本身的工程经验==（不是产品自定义）；产品层（`@`引用 / `/cancel` / `/mcp` 命令）放到 PaiCli 项目目录，不进 MCP 协议 wiki：

  - **§二 transport 章节升级**：==旧 SSE 改为 Streamable HTTP==（2025 MCP 规范变更，单 endpoint POST + chunked，取代 2024 双 endpoint SSE），加 client 代码示例 + Server 配置 `command` vs `url` 区分

  - **§四 全新章节"Host 集成层：MCP → LLM tools 的转换与维护"** ~120 行：
    - ==4.1 工具命名空间== `mcp__{server}__{tool}` 前缀（避免命名冲突 + LLM 看出工具来源 + 审计追踪）
    - ==4.2 Schema 清洗==——三大坑（`$ref` Anthropic 直接 400 / 深嵌 `anyOf` / 长 description）+ ~20 行 `clean_schema` 实现
    - ==4.3 Resources 暴露为虚拟工具==——`list_resources` + `read_resource` 两个虚拟工具（解决 LLM 只懂 tool_calls 不懂 Resource 协议的工程妥协）
    - ==4.4 notifications 处理==——`tools/list_changed` / `resources/list_changed` / `resources/updated` 三种 notification 的实现（实现"运行中加挂工具不重启 Host"）

  - **§6.3 审计日志凭证脱敏**：~50 行 `redact_for_audit` 实现，==key 名集合屏蔽==（token/key/password/secret/authorization）+ ==value 模式正则==（Bearer xxx / sk-... / ghp_... / xox*-...）+ 嵌套 dict 递归 + 真实代价说明（log 一旦落 ELK 就要批量轮换）

  - 后续章节 §五-§十 全部顺延 1（原 §四 → §五 / 原 §九 → §十）

  ==关键认知==（面试题区分度高）：
  - ==Streamable HTTP 取代 SSE== —— 2025 协议本身演进，wiki 不更新就过时
  - ==`mcp__` 前缀 / Schema 清洗 / Resources 虚拟化== 都是 ==Host 端必踩坑==——讲清这些区分"用过 MCP"和"实现过 MCP 集成"
  - ==notifications 让 MCP 工具能热更==——这是 Cursor / Claude Code 体验流畅的隐藏机制
  - ==Anthropic 不支持 `$ref`==——这是早期 GitHub MCP server 集成失败的真实坑

  **原因**：用户的第十期描述==大量是 PaiCli 产品自定义功能==（命名空间 / HITL / 默认配置等已在 wiki 其他位置覆盖），但==其中 6 个点是 MCP 协议本身的工程经验==——把它们抽出来落地到 MCP 协议 wiki，让"实现 MCP Host"的人有完整参考。这次补完后 MCP 协议 wiki 才==真正覆盖了从协议→Host 集成→生态→安全的完整生产视角==，前面只讲了"是什么"和"用什么"，==缺了最值钱的"集成时怎么做"==。

- ==`wiki/LLM/Coding Agent 工具集.md` 补充 §4.8 联网工具的 SSRF 防御==——回应用户对"第九期：联网能力 + Web 工具"的延伸提问，补全 ==Agent 时代联网安全==这个生产必踩点：

  - **三种典型攻击场景**对照表（==`http://169.254.169.254/` 偷云 IAM 凭证== / `file:///etc/passwd` 读敏感文件 / `http://localhost:6379/` 攻击内网服务）+ 攻击链梳理（==Prompt Injection 控制 LLM 输出 URL==→ Agent 用自己的网络身份发请求）
  - **五道防线完整实现代码**（~30 行 Python `safe_fetch`）：协议白名单 / DNS 解析后查 IP（防 ==DNS rebinding==）/ 重定向手动追 / 大小限制 5MB / 频率限制
  - **Agent 场景特殊考量**：URL 来源标记（LLM 输出的 URL 比用户输入多一道检查）/ ==network namespace 沙箱网络==（默认禁内网，纵深防御）/ 凭证隔离（Agent 不持有云元数据访问权限）/ 审计日志
  - 在 §2.5 联网类==开篇加一行安全提示==指向新章节，避免读者读完普通用法就跑

  ==关键认知==（面试题区分度高）：
  - ==Agent 时代 SSRF ≠ 传统 SSRF==——LLM 输出的 URL 可被 Prompt Injection 控制，新攻击面
  - ==DNS rebinding 是隐藏雷点==——只查域名白名单不够,必须解析后再查 IP
  - ==重定向不能让 requests 自动跟==——第一跳合法，第二跳指向内网就完蛋

  **原因**：用户的第九期功能描述涵盖了 web_search/fetch_url 的功能/选型/限流，==但 SSRF 这个 Agent 上线前必过的安全审计项没专门讲==。Anthropic Claude/OpenAI Browse 都内置了这套防护，==是"懂 LLM"和"懂 LLM 安全"的分水岭面试题==。
