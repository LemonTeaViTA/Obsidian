# 修复日志 · 算法-数据结构

> 单元：算法-数据结构（wiki/算法/ 下 数组/链表/栈与队列/树/堆/字符串/图）
> 执行日期：2026-06-01
> 阶段：阶段1（域内真实修复，不碰 MOC/索引，不碰其他文件夹）

---

## 一、改了哪些文件 · 各做了什么

### 1. 树.md
- **P0 修复 maxPathSum 语法错误（原 L327-331）**：`self.max_sum = float('-inf')` 原先裸写在 `class Solution:` 类体内（无 `self` 上下文、无 `__init__`），会直接 `NameError`/`IndentationError`，类无法定义。已将其移入 `maxPathSum` 方法体首行，`maxGain` 内嵌函数随之正确缩进。已用单测验证：`[-10,9,20,null,null,15,7]`→42、单负节点→-3，均正确。
- **P2 修正 sortedArrayToBST 注释/代码错位（原 L590）**：原注意点说“终止条件是 `left > right` 不是 `left >= right`”，但所给代码是切片版（`if not nums: return None`，无 left/right 下标）。已改写注意点：明确本文是切片版（空数组返回 None），并补充说明等价的下标版终止条件 `left > right` 及其省空间优势。
- **结构 · 加顶部速览 callout**：文件 765 行 > 600，无干净切缝（题解按主题强耦合），按方法论加 `> [!tip] 本页速览`，列出五大主题区与代表题。

### 2. 图.md
- **P0 修复 rawnge 拼写（原 L113）**：岛屿数量 DFS 主循环 `for i in rawnge(len(grid)):` → `range`，原先运行即 `NameError`。已用单测验证 numIslands 返回正确（对角三岛网格→2）。

### 3. 链表.md
- **P1 frontmatter difficulty**：`easy` → `medium`（本文含「合并 K 个升序链表」「K 个一组翻转」等困难题，easy 不符）。
- **P1 域内 SSoT 去重「合并 K 个升序链表」**：原本链表.md（三元组解法 ~45 行）与 堆.md（猴子补丁解法 ~45 行）各有一份全量题解。按方法论保留 **堆.md** 为单一事实源（归「多路归并」主题），链表.md 该小节删去完整思路+代码+复杂度，改为保留题目 + `> [!info]` callout 指向 `[[堆#合并 K 个升序链表（难度：困难）]]` + 一句话核心摘要。小节标题保持不变（锚点未漂移）。
- **结构 · 加顶部速览 callout**：去重后 635 行仍 > 600，加 `> [!tip] 本页速览`。

### 4. 堆.md
- **P2 callout 类型越界**：解法二的 `> [!success]` → `> [!tip]`（success 不在 tip/info/note/warning/danger 白名单内）。堆.md 内「合并 K 个升序链表」全文保留为 SSoT。

### 5. 字符串.md
- **P1/P2 补 KMP 章节**：原文通篇无 KMP，而 MOC 承诺「KMP 思维」。新增「## 七、字符串匹配：KMP」，含两道经典题：「找出字符串中第一个匹配项的下标」（strStr，next 数组 + 主串匹配）、「重复的子字符串」（next 数组判周期）。每题含思路 + 带 `python` 标签代码 + 复杂度分析。已用单测验证两份代码全部用例通过。同步在「一、核心思想」补第 4 条「字符串匹配（KMP）」。新增后顶层 H2 = 8（含相关链接），未超 ≤8 上限。
- **本地数据完整性**：修复我新写 KMP 注释里产生的 1 处 U+FFFD 乱码（「的最长相等前后缀长度」）。

### 6. 数组.md
- **本地数据完整性（P0 级乱码）**：L37 模式识别表内「无重复字符最长子串」原本「重」字位置是一个 U+FFFD 替换符，已修复为正确文字。此项审计报告漏记，本次顺手修。
- **结构 · 加顶部速览 callout**：729 行 > 600，加 `> [!tip] 本页速览`。

### 7. 栈与队列.md
- 未做内容改动（健康度 🟢）。其被 MOC 以错误 anchor 链接的问题属 MOC，记入阶段2清单。

---

## 二、拆分清单

无拆分。三篇 > 600 行文件（数组 737 / 链表 635 / 树 773）均在 600-800 区间，且题解按主题强耦合、无干净切缝，按方法论选择「加顶部速览 callout」而非拆分。无文件 > 800 行。

---

## 三、锚点变更（供阶段2全局改链核对）

**本单元未重命名/移动任何既有标题，既有锚点全部不变。** 仅新增内容：

| 文件 | 新增锚点 | 说明 |
|------|---------|------|
| 字符串.md | `字符串匹配：KMP`、`找出字符串中第一个匹配项的下标（难度：简单）`、`重复的子字符串（难度：简单）` | 新增 H2/H3，新内容，无既有链接受影响 |

链表.md「合并 K 个升序链表（难度：困难）」小节标题**保持原样**（只换了正文为 callout），故指向该锚点的任何外部链接不受影响。

---

## 四、暂存清单

无。本单元未发现需要跨域搬迁到别的领域文件夹的内容（KMP 属字符串域，就地补全）。

---

## 五、留给阶段2的链接问题（跨文件夹坏链 / 锚点漂移）

> 以下均**未改动**，按铁律交阶段2全局改链 agent 统一处理。括号内为我已核实的「本单元文件实际标题」。

### A. 字符串.md 内的非法链接格式（坏链）
- **字符串.md（多解法联动 callout，约 L205 附近）**：
  - 现状：`[wiki/算法/动态规划.md](wiki/算法/动态规划.md#六区间-dp-字符串二维模型)` —— markdown 相对路径 + GitHub 风格 anchor，在 Obsidian 内无法跳转。
  - 应改为：`[[动态规划#<动态规划.md 实际标题>]]`，报告建议标题为「六、区间 DP（字符串二维模型）」，需阶段2对照 动态规划.md 实际标题核对。

### B. 堆.md / 链表.md 指向「排序.md」的 anchor 待对齐
- **堆.md（速览 callout 内 + 相关链接，约 L40 / L367）**：`[[排序#快速选择（Top-K 必杀技）]]`。
- **链表.md（相关链接，文末）**：`[[排序#归并排序（必背手写题）]]`。
- 问题：与 MOC 第 27 行 `[[排序#快速选择算法（Top K 必杀技）|快选]]` 措辞不一致（「快速选择」vs「快速选择算法」、「Top-K」vs「Top K」）。至多一处正确，需阶段2对照 排序.md（不在本单元）实际标题统一。

### C. MOC → 本单元文件 的 anchor 断链（MOC 是阶段2职责，此处给出本单元实际标题供改 MOC 用）
| MOC 链接 | 本单元文件实际标题（现状） | 应修方向 |
|---|---|---|
| `[[栈与队列#四、单调栈\|单调栈]]` | 栈与队列.md：`## 三、单调栈：寻找最近的最值` | MOC anchor 改为「三、单调栈：寻找最近的最值」 |
| `[[栈与队列#五、单调双端队列\|单调队列]]` | 栈与队列.md：`## 五、单调队列极限求值` | MOC anchor 改为「五、单调队列极限求值」 |
| `[[数组#三、哈希表加速查找\|哈希]]` | 数组.md：`## 六、哈希表：以空间换时间` | MOC anchor 改为「六、哈希表：以空间换时间」 |

### D. 本单元内新建的跨文件链接（已自验正确，供阶段2知悉）
- 链表.md → `[[堆#合并 K 个升序链表（难度：困难）]]`：已逐字核对 堆.md 实际标题为 `### 合并 K 个升序链表（难度：困难）`，**匹配正确**，无需改。
- 链表.md「模式识别信号」表内 `（见 [[堆]]）`：指向文件本身，正确。

---

## 六、P0 / P1 统计

- **P0（无法运行的代码 / 乱码）：修 4 条**
  1. 树.md maxPathSum class-body 裸写 self.max_sum（NameError）✅ 已验证
  2. 图.md `rawnge`→`range`（NameError）✅ 已验证
  3. 数组.md L37 模式识别表「无重复字符」处的 U+FFFD 乱码 ✅
  4. 字符串.md KMP 注释 U+FFFD 乱码（本次编辑引入并即修）✅
- **P1：修 4 条**
  1. 链表.md difficulty easy→medium ✅
  2. 域内 SSoT「合并 K 个升序链表」去重（链表.md 改 callout，堆.md 留全文）✅
  3. 字符串.md 补 KMP 章节（strStr + 重复子字符串）✅ 已验证代码
  4. 长文加顶部速览 callout（数组 / 链表 / 树）✅
- **P2：修 2 条**
  1. 堆.md `[!success]`→`[!tip]` ✅
  2. 树.md sortedArrayToBST 注释/代码错位 ✅

- **未修（按铁律留阶段2）**：所有跨文件夹/跨文件链接与 anchor 漂移（见第五节 A/B/C），含字符串.md 非法 markdown 链接、堆/链表→排序 anchor 不一致、MOC→栈队列/数组 断链。

---

## 七、验证

- KMP（strStr + repeatedSubstringPattern）：10 条用例全通过。
- maxPathSum / numIslands：P0 修复后用例通过。
- 全 7 文件二次扫描 U+FFFD：已清零。
- 各文件单一 H1（其余 `^#` 均为代码块内 Python 注释）、无标题跳级、代码块均带 `python` 标签。
