# JVM 单元修复日志

修复日期：2026-06-01
负责文件夹：`/home/ubuntu/ADgai/dsq/Obsidian/wiki/JVM/`
阶段：阶段1（域内真实修复，不碰 MOC/索引、不碰跨文件夹链接）
依据报告：`wiki审计/领域报告/JVM.md`

---

## 一、改了哪些文件、各做了什么

### 1. `JVM 内存管理.md`（514 → 531 行）
- **P1-1（事实错误）**：第 298 行"为什么非要进行 8 字节对齐呢？"——删除"一次寻址的指针大小是 8 字节，正好是 L1 缓存行的大小"这一错误等同。改为：8 字节对齐对齐到 CPU 字长/总线宽度/指针宽度；新增一个 `[!warning]` callout 明确区分"8 字节对齐"与"64 字节缓存行（Cache Line）"两个层面，并补充伪共享（False Sharing）+ `@Contended` 知识点。
- **P2-1（结构）**：在 H1 `# JVM 内存管理` 下新增顶部 `[!info]` 速览 callout，列出 5 条脉络（运行时数据区→对象创建与布局→堆分区与晋升→逃逸分析→OOM/泄漏）。
- **P2-3（表述不精确）**：第 372 行"动态年龄判定了解吗？"——原文方向反了。改为正确表述："按年龄从小到大累加，当累计到某年龄的对象总和 > Survivor 一半时，年龄 ≥ 该年龄的对象直接晋升"，并补一句设计动机。
- **P2-8（次要注记）**：`-XX:PretenureSizeThreshold` 补充"默认 0 即不启用""只对 Serial/ParNew 生效，Parallel Scavenge 不支持"。

### 2. `JVM JIT与字节码.md`（350 → 358 行）
- **P1-2（过时）**：第 338 行预热方案——删除原"AOT 编译（JDK 9+，jaotc）"条目。CDS 条目升级为"CDS/AppCDS/动态 CDS（JDK10/13）"；GraalVM Native Image 条目补充峰值吞吐说��。新增 `[!note]` callout 说明 jaotc（JEP 295）已在 JDK 17（JEP 410）移除，并指向 GraalVM Native Image / AppCDS / Project Leyden。
- **P1-3（过时）**：第 90 行"C1 和 C2 编译器的区别？"末尾——改写 Graal 段：明确实验性 Graal JIT 仅 JDK 9-16 在主线、JDK 17（JEP 410）已移除，`-XX:+UseJVMCICompiler` 主线不可用，现仅存在于 GraalVM 发行版。新增 `[!warning]` callout。
- **P2-8（次要注记）**：第 36 行热点阈值——新增 `[!note]` callout 说明 1500/10000 是关闭分层时的 `CompileThreshold`，分层编译下实际用 `Tier3InvocationThreshold≈200`/`Tier4InvocationThreshold≈5000`。

### 3. `JVM 类加载机制.md`（222 → 213 行）
- **P1-4（过时/不完整）**：第 22 行"类加载器有哪些？"——保留 JDK 8 三层模型（面试高频），新增 `[!warning]` callout 说明 JDK 9+ 模块化变化：无 rt.jar/jre/lib/ext、ExtClassLoader→PlatformClassLoader、sun.misc.Launcher 已不存在、AppClassLoader 不再是 URLClassLoader 子类，三层关系变为 Bootstrap→Platform→Application。
- **P2-2（话题错位）**：删除第 198-214 行 `## 解释执行与编译执行` 整节（话题属执行引擎/JIT，与类加载弱相关），内容迁至 `JVM 概述.md`（见下），本篇"相关链接"中保留一句指向概述的说明。

### 4. `JVM 概述.md`（53 → 72 行）
- **P2-2 接收方**：在"说说 JVM 的组织架构"之后新增 H3 `### 解释执行与编译执行的区别是什么？`，承接从类加载机制迁来的内容（略作润色：字节码措辞、"早期被诟病慢"），并新增 `[!info]` callout 说明 HotSpot 是"解释+编译"混合执行，链接到 `JVM JIT与字节码#为什么需要 JIT？解释执行到底慢在哪？`。

### 5. `JVM 调优.md`（350 行，行数不变）
- **P2-4（参数 typo）**：第 62 行 `-XX:+UseParalledlOldGC` → `-XX:+UseParallelOldGC`。

### 6. `JVM 垃圾收集.md`（549 → 559 行）
- **P2-1（结构）**：H1 下新增顶部 `[!info]` 速览 callout，列出 5 条脉络。
- **P2-5（文内冗余）**：合并第 520-534 行两段近重复回答（"你们线上用的什么垃圾收集器？" + "工作中项目使用的什么垃圾回收算法？"）为一问 `#### 你们线上用的什么垃圾收集器？（用的什么回收算法？）`；把原本突兀的 `UseParallelGC = Parallel Scavenge + Parallel Old` 改写成一个 `[!note]` callout 接在查看命令之后；删除重复的第二问。
- **P2-8（次要注记）**：
  - 第 190 行 Major GC——软化"是 CMS 的特有行为"为"术语定义不严格，CMS 语境下常特指其老年代并发回收"。
  - 第 138 行 finalize()——新增 `[!warning]` callout：JDK 9 起 @Deprecated、JDK 18（JEP 421）deprecated for removal，推荐 try-with-resources / Cleaner / PhantomReference。

> 未改动：`JVM索引_MOC.md`（按铁律不碰 MOC/索引，其 P2-7 标题标记问题留给阶段2）。

---

## 二、拆分清单

无。所有文件均 < 600 行（最大 垃圾收集 559、内存管理 531），无 >800 红旗、无 600-800 干净切缝拆分需求。按方法论对两篇 >400（实为 >500）行文档补"顶部速览 callout"代替拆分（P2-1）。

---

## 三、锚点变更（阶段2全局改链需用）

域内已自洽处理（本单元内引用已同步），但仍记录供全局 agent 核对：

1. **`JVM 类加载机制.md`** 删除小节：
   - 旧锚点 `#解释执行与编译执行`（H2）— 已删除
   - 旧锚点 `#说说解释执行和编译执行的区别`（H3）— 已删除
   - → 内容迁至 `JVM 概述.md`，新锚点 `#解释执行与编译执行的区别是什么？`
   - 经全库 grep（`grep -rn "解释执行与编译执行\|说说解释执行"`）：**无任何外部文件引用这两个旧锚点**，故无需改链。

2. **`JVM 垃圾收集.md`** 合并/重命名：
   - 旧锚点 `#你们线上用的什么垃圾收集器？`（H4）→ 新锚点 `#你们线上用的什么垃圾收集器？（用的什么回收算法？）`
   - 旧锚点 `#工作中项目使用的什么垃圾回收算法？`（H4）— 已删除（合并入上一问）
   - 经全库 grep：**无任何外部文件（含 面试题目.md、MOC）引用这两个旧锚点**，故无需改链。

3. **`JVM 概述.md`** 新增锚点：
   - 新增 `#解释执行与编译执行的区别是什么？`（H3）— 阶段2若需可在 MOC/面试题目中补链（可选，非坏链）。

---

## 四、暂存清单

无。本单元所有内容迁移均发生在自己负责的 JVM 文件夹内部（解释执行小节从 类加载机制.md → 概述.md，两者同属本单元），未涉及跨域搬迁，故未写任何暂存文件。

---

## 五、留给阶段2的链接问题（跨文件夹坏链/锚点漂移/MOC）

1. **MOC 标题标记丢失（P2-7，本阶段按铁律未碰）**：`JVM索引_MOC.md` 第 20/24/29/34/38/42 行六个知识聚类小标题（"整体认知与发展史"等）以单个前导空格开头、无 `####`/`-`/emoji 标记，会与下一行合并渲染成普通段落。建议阶段2统一改为 `#### 标题` 或 `**标题**`。功能性 wikilink 均在、无孤儿页。

2. **跨域 wikilink 核验结果**：从 JVM 文件夹出发的所有跨域 `[[...]]` 目标已逐一核验，**无坏链**。其中 `[[Java基础/注解、反射与 Java 8]]`（JIT与字节码.md:358）带文件夹前缀路径，目标文件 `wiki/Java基础/注解、反射与 Java 8.md` 真实存在，Obsidian 可正常解析，非坏链。

3. **跨域锚点链接（未核验锚点是否漂移，留阶段2确认）**：
   - `JVM 内存管理.md:267` → `[[锁#synchronized 锁升级了解吗？]]`（跨到"锁"域，本阶段未核验该锚点是否存在）
   - 其余跨域链接为纯文件名链接（如 `[[锁]]`、`[[线程基础与ThreadLocal]]`、`[[Spring 基础与 IoC]]`、`[[AOP 与动态代理]]`），文件名存在性由全局 agent 统一核验。

---

## 六、P0/P1/P2 修复统计

- **P0**：报告中 0 条，无需修。
- **P1**：共 4 条，**全部修复（4/4）**——P1-1 缓存行、P1-2 jaotc/AOT、P1-3 Graal/JVMCI、P1-4 PlatformClassLoader。
- **P2**：报告 P2-1~P2-8 共 8 组：
  - 已修 6 组：P2-1（两篇顶部速览）、P2-2（解释执行迁移）、P2-3（动态年龄判定）、P2-4（参数 typo）、P2-5（重复回答合并）、P2-8（分层阈值/PretenureSize/Major GC 软化/finalize 弃用，4 个注记全做）。
  - 未做 2 组（合理留置）：
    - **P2-6**（调优篇 Diátaxis Reference+How-to 混排）：报告本身定为 P2 且注明"整体围绕调优高内聚，未达两类强混硬红旗，严格 Diátaxis 化为可选项"。为不破坏现有 Q&A 统一范式、避免过度改造，本阶段不拆分，保持原状。
    - **P2-7**（MOC 标题标记丢失）：位于 `JVM索引_MOC.md`，按本阶段铁律不碰 MOC/索引，已转交阶段2（见上"留给阶段2"第 1 条）。

---

## 七、校验

- 零 U+FFFD 乱码（grep 校验通过）。
- 每文件单一 H1（代码块外校验：每篇恰好 1 个）。
- 顶层 `##` 小节数：JIT 7 / 内存 6 / 垃圾收集 6 / 概述 2 / 类加载 3 / 调优 6，全部 ≤ 8。
- callout 类型仅用 info/note/tip/warning，无非法类型、无嵌套。
- 无标题跳级。
- 改动后各文件仍为合法 Markdown。
