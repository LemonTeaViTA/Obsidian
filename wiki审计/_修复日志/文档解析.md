---
unit: 文档解析
fix_date: 2026-06-01
phase: 1
based_on_report: wiki审计/领域报告/文档解析.md
---

# 文档解析 单元 · 阶段1修复日志

负责文件夹：`/home/ubuntu/ADgai/dsq/Obsidian/wiki/文档解析/`（OCR.md、TSR.md、VLM文档解析.md、公式识别.md、版面分析.md、文本清洗.md，共 6 文件）

报告判定：🟢 整体健康（0 红旗 + 1 黄旗）。本阶段无拆分、无跨域搬迁、无新建 MOC（缺 MOC 留阶段2）。

---

## 一、改了哪些文件 · 各做了什么

### P0 · 代码块语言标签（全域 30 处 ASCII/示意图 fence 补 `text`）
报告未单列，但属方法论铁律「代码块加语言标签」的 P0 项。本域大量 pipeline/架构示意图用裸 ```` ``` ```` 围栏，全部补 `text` 标签（这些是流程图/输入输出示意，非可执行代码，统一用 `text`）。Python 代码块原本已带 `python`、DocTags 块已带 `xml`，未动。

| 文件 | 补 `text` 标签的 fence 数 |
|------|--------------------------|
| OCR.md | 8 |
| TSR.md | 5 |
| VLM文档解析.md | 7 |
| 公式识别.md | 6 |
| 版面分析.md | 4 |
| 文本清洗.md | 1（§5.3 版本链示意图）|
| **合计** | **31** |

验证：全域已 0 个裸开围栏，每文件 fence 数均为偶数（围栏配对正确）。

### 事实性/表述澄清（报告 P2，本阶段一并修）
- **OCR.md L99-103 · CTC 示例**：原文「模型输出 70 个字符」「`H-H-T-T-空-H`」用 `-` 兼作分隔符与 blank，易混淆。改为：明确「每个时间步输出一个字符概率分布，词表含特殊 `[blank]` 空白符」，示例改为 `[H][H][T][T][blank][H]` → 解码（合并相邻重复→去 `[blank]`）→ `HTH`。（报告 §二 P2 第8条、§3.1）
- **VLM文档解析.md L109 · ColPali/SigLIP/PaliGemma 衔接**：在「ColPali 也基于 PaliGemma 改造」后补一句衔接说明——SigLIP 正是 PaliGemma 内部视觉编码器，故「ColPali 用 SigLIP 风格」与「基于 PaliGemma」不矛盾，是同一事物的不同层次。消解 L86 与 L109 的跳跃感。（报告 §3.3 修复建议）

### 代码笔误/可移植性（报告 P2 第9条）
- **文本清洗.md L173 · 不可见字符正则**：原 `re.sub(r'[​-‏­﻿]', '', text)` 在源码里嵌入了真实的零宽字符（U+200B–U+200F 范围 + U+00AD + U+FEFF），复制/跨环境渲染易出错。改为显式转义 `re.sub(r'[​-‏­﻿]', '', text)`，并加 3 行注释说明 codepoint 含义。用字节级替换确保源码内不再含任何不可见字符。

---

## 二、拆分清单
无。文本清洗.md 当前 545 行（< 600 黄线，加注释后仍未超），顶部已有渐进式简介 blockquote，无需拆分、无需「速览」callout。报告建议的「未来可把『四、重复内容检测』独立为去重策略.md」属未来扩展项，本阶段不做。

## 三、锚点变更（供阶段2全局改链）
本单元内 H1/H2/H3 标题文本**全部未改名、未移动**。唯一涉及标题区域的编辑是 OCR.md 段落正文（`**关键技术：CTC...**` 是正文加粗非标题，未变其文本）。
**结论：无锚点变更，阶段2无需为本域调整指向本域的入链锚点。**

## 四、暂存清单
无。本单元无跨域内容需要搬迁（报告 §五 SSoT 检查：单元内 0 红旗，5 篇专题页 + 文本清洗均为单点详讲，文档解析综述在 RAG 目录只做「列名+跳转」）。

---

## 五、留给阶段2的链接问题（跨文件夹，本阶段按铁律未改，仅记录）

> 全部为跨文件夹 wikilink，目标在 `wiki/RAG/` 与 `wiki/数据格式/`，按铁律4由阶段2全局改链 agent 统一修。已逐一核对目标文件真实标题。

### 坏链（确认坏，需改）
| # | 源文件:行 | 现写法 | 问题 | 应指向（已核对目标标题） |
|---|-----------|--------|------|--------------------------|
| 1 | 文本清洗.md L442（修改后行号微调，原报告 L442）| `[[分块策略#5.4 句子窗口检索\|句子窗口检索]]` | `分块策略.md` 无「5.4 句子窗口检索」锚点 | `[[RAG检索策略#5.2 句子窗口检索（Sentence Window Retrieval）\|句子窗口检索]]`（RAG检索策略.md:499 实有此节；亦可指 `#五、检索时的上下文扩展`，但 5.2 更精准）|
| 2 | 文本清洗.md L517 区域（原报告 L517）| `[[分块策略#5.4 通用工程参数]]` | `分块策略.md` 无「5.4 通用工程参数」；实际是 `## 四、通用工程参数` / `### 4.3 chunk 元数据通用模板` | `[[分块策略#4.3 chunk 元数据通用模板]]`（分块策略.md:215 实有此节）|
| 3 | VLM文档解析.md L188 | `[[RAG高级技术#七、多模态-RAG]]` | 连字符 vs 空格：实际标题是 `## 七、多模态 RAG`（空格）| `[[RAG高级技术#七、多模态 RAG]]`（RAG高级技术.md:362）。注：同文件 L240 已正确写「参见七、多模态 RAG」|

### 路径标注偏差（报告 §二 P2 第3条，5 处尾链语义指向偏差，需阶段2统一裁决）
- OCR.md L188、TSR.md L161、公式识别.md L208、版面分析.md L172、VLM文档解析.md L239：尾链写「`[[RAG基础与架构]] …（参见 3.3.x）`」，但 `RAG基础与架构.md` 的文档解析节是 short overview，无 3.3.x 子编号；真正含 3.3.x 子节的是 `RAG/文档解析综述.md`（§3.1–3.5 实际存在：3.1 TSR、3.2 图片图表、3.3 公式识别、3.4 多栏版面、3.5 OCR）。
  - 阶段2建议：把「参见 3.3.x」的语义指向改为 `[[文档解析综述#3.x ...]]`，或保留 `[[RAG基础与架构]]` 作 MOC 入口但删除误导性的「3.3.x」编号。本阶段未改（跨文件夹）。

### 已核验为「有效链接」（无需改，仅备案，防阶段2误改）
- 文本清洗.md：`[[RAG高级技术#知识冲突处理]]`（L313、L381）✅ RAG高级技术.md:306 存在；`[[文档解析综述#4.5 HTML：DOM 噪声与结构语义保留]]` ✅ 文档解析综述.md:265 存在；`[[分块策略#五、选型决策树]]`/`[[RAG向量与Embedding#选型决策树]]`(L392) ✅ 存在；`[[RAG检索策略#三、混合检索原理]]`(L422) ✅ RAG检索策略.md:287 存在；`[[DOM]]`(L126) ✅ 数据格式/DOM.md 存在（无锚点纯文件链）。
- 公式识别.md L148 `[[文档解析综述#六、文档解析工具全景]]` ✅ 文档解析综述.md:316 存在。

---

## 六、计数与剩余

- **P0 修复**：31 处代码块语言标签（铁律「代码块加语言标签」）→ 全部完成。
- **P1 修复**：报告 2 条 P1 均为跨文件夹坏 anchor（文本清洗.md L442/L517）→ 按铁律4**不在本阶段改**，已精确记录给阶段2（上表 #1、#2）。本阶段 P1 在域内可改项 = 0，全部转交阶段2。
- **P2/事实性（域内可改）**：CTC 示例澄清、ColPali 衔接说明、不可见字符正则显式化 → 3 条完成。
- **剩余未修（均为跨文件夹，留阶段2）**：上表坏链 3 条 + 路径标注偏差 5 处。
- **未做项（按铁律）**：未建 文档解析_MOC.md（报告 P2 第4条，缺 MOC 留阶段2新建）；未碰任何 MOC/索引文件；未碰 wiki/面试题目.md；未碰其他文件夹。

修复后本域 markdown 合法、围栏全配对、frontmatter 四字段仍齐全。域内事项已清零，唯余跨文件夹链接待阶段2统一处理。
