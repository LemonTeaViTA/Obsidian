# 修复日志 — 操作系统单元

- 单元：操作系统
- 负责文件夹：`wiki/操作系统/`（仅 1 个文件 `操作系统.md`）
- 修复日期：2026-06-01
- 阶段：阶段1（本域内真实修复，不碰其他文件夹/不碰 MOC/不改跨域链接）

## 一、改了哪些文件、各做了什么

### `wiki/操作系统/操作系统.md`（236 → 254 行）
本域唯一文件。<600 行不需拆分；单 H1、6 个 H2（≤8）、frontmatter 四字段齐全，<400 行不需顶部速览。本次为「结构标注 + 知识缺漏补齐 + 事实性精修 + frontmatter 刷新」，未拆分、未改标题、未动任何 wikilink。

1. **frontmatter**：`last_reviewed` 2026-05-20 → 2026-06-01（因本次大改）。
2. **代码块语言标签**（方法论：代码块须带语言标签）：两处虚拟地址拆分示意图的裸 ``` 围栏 → 改为 ```text（行 45、66）。
3. **知识缺漏补齐（必做 P1）**：
   - §一 TLB 段后新增 `[!tip] 现代 CPU 的 TLB 优化` callout：补 PCID/ASID，纠正「进程切换必然全刷 TLB」在现代硬件已不准确。
   - §一 缺页中断段后新增「次缺页(minor) vs 主缺页(major)」对比表 + `min_flt/maj_flt` 说明（报告点名的高频追问点）。
   - §六 ET 段后新增 `[!warning] ET 模式必须搭配非阻塞 fd` callout：补 `O_NONBLOCK` 必要性（报告点名考点）。
4. **事实性精修**（报告「可优化点」）：
   - §二 协程表「栈大小」：`默认 1-8MB / 通常 2-8KB` → 线程「Linux pthread 默认 8MB，Java HotSpot 约 1MB」、协程「Go goroutine 初始 2KB，按需自动扩展」。
   - §三 CFS 段后新增 `[!note] CFS 已被 EEVDF 取代`：Linux 6.6(2023.10) 默认调度器改为 EEVDF���
   - ET 段正文 `EAGAIN` 加 code 标记。

## 二、拆分清单
无。文件 254 行 < 600，结构干净，无需拆分。

## 三、锚点变更
无。本次未重命名/移动任何标题，所有 H2/H3 锚点保持不变。指向本文件的现有链接（如 `[[操作系统#IO 多路复用]]`、`[[操作系统#进程和线程的区别是什么？]]`）不受影响。

## 四、暂存清单
无。本域是 IO 多路复用 / 进程线程区别 / 死锁四条件 三个跨域概念的 **SSoT 单点保留方**（OS 是更底层归属地），无内容需要搬出。需要剪裁的是【其他域】的重复段落（见下「留给阶段2」）——按铁律本阶段不碰其他文件夹，仅记录。

## 五、留给阶段2的链接问题（跨文件夹坏链 / 锚点漂移 / 跨域 SSoT 剪裁）

### 5.1 本文件内的跨文件夹链接（本阶段未改，待阶段2全局改链 agent 统一修）
| 源文件:行 | 当前链接 | 应指向 | 问题 |
|---|---|---|---|
| 操作系统.md:116 | `[[线程基础与ThreadLocal]]` | 文件存在（`并发编程/线程基础与ThreadLocal.md`） | 链接有效，但建议补精确锚点 `#🌟说说进程和线程的区别？` |
| 操作系统.md:175 | `[[锁#线程死锁了解吗？]]` | `并发编程/锁.md` | 待阶段2核对锚点是否精确存在 |
| 操作系统.md:176 | `[[事务与MVCC]]` | 应改为 `[[锁机制#InnoDB 是如何检测和处理死锁的？]]`（已确认 `MySQL/锁机制.md:241` 存在该标题） | **锚点指向不精准**：死锁检测属锁机制而非事务/MVCC |
| 操作系统.md:254 | `[[计算机网络篇]]` | 应改为 `[[计算机网络]]`（实际文件名 `计算机网络/计算机网络.md`，无「篇」字） | **坏链**：目标文件名不含「篇」 |

### 5.2 跨域 SSoT 剪裁（本阶段不碰其他文件夹，记录给阶段2在对应域执行）
SSoT 单点 = `操作系统.md`，以下他域重复段建议剪为「一句话提要 + callout 链接回 OS」：
- **IO 多路复用（P1，报告 Y1）**：`Redis/Redis 基础.md`（约 167–206 行 select/poll/epoll 对比）→ 保留「Redis 为何选 epoll + 单线程事件循环」应用视角(≤20行)，三者对比剪为提要 + `[[操作系统#IO 多路复用]]`。同步把 `面试题目.md:248`「IO 多路复用了解吗？」跳转目标由 `[[Redis 基础#…]]` 改为 `[[操作系统#IO 多路复用]]`。
- **进程/线程区别（报告 Y2）**：`并发编程/线程基础与ThreadLocal.md`（约 97–114 行）→ 剪为「面试一句话 + callout `[[操作系统#进程和线程的区别是什么？]]`」。
- **死锁四条件（报告 Y3）**：`并发编程/锁.md`(540–599) 与 `MySQL/锁机制.md`(202–233) 三方视角差异清晰，未越红线，保持现状即可。

### 5.3 缺 MOC（报告 Y4，留给阶段2新建）
`wiki/操作系统/` 无 `操作系统索引_MOC.md`。按铁律本阶段不建 MOC。阶段2新建：列 6 大子模块锚点 `[[操作系统#一、内存管理]]` … `[[操作系统#六、I/O 模型]]`，并为未来拆分预留路由位。

## 六、P0 / P1 统计
- **P0**：报告判定本单元 0 条 P0（无 ≥50 行两处重复硬红线）。本阶段 P0 修复 0 条。
- **P1**：
  - Y1 IO 多路复用 SSoT — 本域为 SSoT 保留方，已确认 OS 单点内容完整（含 select/poll/epoll 对比 + 内核实现 + LT/ET），无需改动；跨域剪裁记录给阶段2。✅（本域侧完成）
  - knowledge gaps：major/minor page fault ✅、ET 需 O_NONBLOCK ✅，均已补。
- **附带事实性精修**（报告「可优化点」，非硬性 P0/P1）：PCID/ASID、EEVDF、协程栈大小 — 已补 3 条。

### 还剩什么没修（按铁律不在本阶段做）
- 跨域 SSoT 剪裁（Redis 基础 / 线程基础）— 属其他文件夹，记录给阶段2。
- 跨文件夹坏链/锚点（5.1 表）— 记录给阶段2全局改链 agent。
- 新建 MOC（5.3）— 留阶段2。
- 报告建议的「补零拷贝 / COW / 用户态切换代价 / 信号与僵尸进程 / Huge Page」属【内容扩充】而非问题修复，超出本阶段「修复」范畴，未做（仅 minor page fault 表中提及 COW 触发场景）。
