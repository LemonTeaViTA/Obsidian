---
unit: 数据格式
phase: 1
fixed_at: 2026-06-01
fixer: 知识库管理专家 + 数据格式领域专家
---

# 数据格式 单元 — 阶段1修复日志

负责文件夹：`wiki/数据格式/`（XML.md / OOXML.md / DOM.md / PDF.md）。
本阶段只改本文件夹内文件，未碰 MOC/索引、未碰 wiki/面试题目.md、未写入其他文件夹。

## 一、改了哪些文件、各做了什么

### XML.md（255 → 241 行）
1. **P1 域内 SSoT 去重（§五 原生语义与"XML 原生"格式 → "为什么 docx/xlsx/pptx 是 XML 原生"小节）**
   - 删除了与 OOXML.md §三 重复的第二个 `<w:p><w:pPr><w:pStyle w:val="Heading1"/>...` 完整代码块（XML.md 原 176–183 行）。
   - 删除了"docx 是 ZIP 包 / 三种文件共享容器规范 / 主体内容在 word|xl|ppt 目录"这段与 OOXML.md §二、§三双写的论述。
   - 改为：1 句"OOXML 本质是 ZIP+XML"摘要 + 1 句"结构信息用标签显式标注所以易解析"摘要 + 保留并增强原有 `> [!info] ... 详见 [[OOXML]]` callout（补了"ZIP 容器与 OPC 关系寻址"一项）。
   - 注意：§五开头"什么叫原生语义"小节里的**第一个** `<w:p>` 示例代码块**保留**（这是 XML 篇讲"原生语义"概念的本职示例，非重复）。OOXML.md §三 作为完整结构的 SSoT 未改其代码。
2. **P2 代码块语言标签**
   - 第 23 行 `<h1>/<p>` 示例块：补 ` ```html `，并把行尾 `←` 注释改为合法 `<!-- -->` HTML 注释（避免破坏高亮）。
   - 第 93 行订单 XML 树形 ASCII 图：补 ` ```text `。
   - （第 48、154、191 行的 XML 片段原已有 ` ```xml `，未动。）
3. **P2 字节估算去具体数字**
   - §四 callout "XML 约 300 字节、JSON 约 150 字节" → 改为"XML 大约是等价 JSON 的 1.5–2 倍体积（开闭标签翻倍所致）"，可复现、无误导。
4. 修复了一次自身编辑引入的 U+FFFD 替换字符（"标题层级、单元格类型"间），已校验 0 残留。

### OOXML.md（364 行，行数不变）
1. **P2 代码块语言标签**：4 处目录树骨架块（共有目录树 / word/ / xl/ / ppt/，原 60、105、163、291 行附近）由裸 ` ``` ` 补为 ` ```text `。
2. **P2 r: 命名空间严谨化（§4.6 多 Sheet 与命名区域）**：把"`r:id="rId1"` 通过 `_rels/workbook.xml.rels` 映射到具体文件"扩写为——点明 `r:` 是 Office Relationships 命名空间（URI `.../officeDocument/2006/relationships`），并说明主文档 `r:id="rId1"` 与 rels 文件里 `<Relationship Id="rId1" Target=.../>` 通过同名 `rId1` 匹配。
3. §三 docx 的 `<w:p>` 完整示例与"原生语义"工程论述作为 SSoT 单点保留，未删减。

### DOM.md（137 行，行数不变）
1. **P2 代码块语言标签**：DOM 树 ASCII 图（原 79 行）由裸 ` ``` ` 补为 ` ```text `。（HTML 源码块原已是 ` ```html `，未动。）

### PDF.md（139 → 142 行）
1. **P2 扫描版栅格编码纠错（§三 数字版 vs 扫描版 表格）**：把"每页就是一张 JPEG/PNG 塞进 PDF 容器"改为"每页是一张栅格图像塞进 PDF 容器（黑白多用 CCITT G4 / JBIG2，彩色多用 JPEG / JPEG2000）"。
2. 在该表格下方新增 `> [!info] 扫描版 PDF 的图像编码与 OCR` callout：说明 CCITT G4（OCR 难度低）/ JBIG2（字典误配可能整体替换错字符）/ JPEG2000（兼容性差），以及 OCR 前需先识别编码解码为位图。该 callout 与后文 `> [!warning] 混合 PDF` 之间隔着"怎么一眼判断/占比"两小节，不连用、不嵌套。
3. **P2 代码块语言标签**：§二 PDF 内部对象示例（原 42 行，含 `BT/Tf/Td/Tj` 与 `<-- -->` 箭头注释）由裸 ` ``` ` 补为 ` ```text `（含箭头注释，非合法 PostScript，故用 text 而非 postscript）。

## 二、拆分清单
无。4 文件均 <600 行（最大 OOXML.md 364 行），无超长、无干净切缝需求，未拆分、未新建文件、未加速览 callout（未达 400 行警戒线）。

## 三、锚点变更（供阶段2全局改链参考）
- 无标题改名/移动。所有 H2/H3 标题文本保持不变。
- XML.md §五标题"五、原生语义与\"XML 原生\"格式"未改；其下子标题"为什么 docx/xlsx/pptx 是\"XML 原生\""未改。指向这些锚点的入站链接无需调整。

## 四、暂存清单
无。本单元 4 文件内容全部属于"数据格式"域，无跨域内容需搬走，未写任何暂存文件。

## 五、留给阶段2的链接问题
### 跨文件夹出站链接（已核验目标文件与锚点均存在，标注以便阶段2统一形态校验）
- XML.md:195 → `[[文档解析综述#二、格式光谱：结构化程度决定解析起点]]` — 目标 `RAG/文档解析综述.md` 存在，H2"二、格式光谱：结构化程度决定解析起点"存在（该文件 33 行）。✅可达
- XML.md 相关链接 → `[[RAG基础与架构]]`(RAG/存在)、`[[计算机网络]]`(计算机网络/存在)、`[[Spring 基础与 IoC]]`(Spring/存在)。✅可达
- OOXML.md:246 → `[[文���解析综述#3.1 表格结构识别（TSR）]]` — 目标存在，"### 3.1 表格结构识别（TSR）"存在（62 行）。✅可达
- OOXML.md:341 → `[[文档解析综述#4.4 PPT（pptx）：信息分散与隐藏]]` — 目标存在，"### 4.4 PPT（pptx）：信息分散与隐藏"存在（254 行）。✅可达
- OOXML.md 相关链接 → `[[RAG基础与架构]]`(参见 3.4.2/3.4.3/3.4.4)。✅目标存在；阶段2可核验这些子锚点是否仍准确。
- PDF.md:131 → `[[文档解析综述#4.1 PDF：坐标到逻辑结构的还原]]`（210 行存在）+ `[[文档解析综述#3.4 多栏版面分析]]`（144 行存在）。✅可达
- DOM.md 相关链接 → `[[计算机网络]]`(存在)。✅可达

### MOC 缺失（P1，阶段2处理，不在本阶段职责）
- `wiki/数据格式/` 缺 `数据格式_MOC.md`。审计报告 ②P1 项已记录。本阶段未新建（铁律：不碰/不建 MOC，留阶段2）。建议阶段2新建并串讲 XML→OOXML→DOM→PDF 四篇。

### 无坏链 / 无锚点漂移
本文件夹内所有 wikilink 的目标文件与锚点均经 grep 核验存在，未发现坏链或锚点漂移。

## 六、修复计数
- **P1**：报告列 2 条。
  - XML↔OOXML SSoT 去重：✅已修。
  - 缺 MOC：⏳未修（按铁律留阶段2，已在上方记录）。
- **P2**：报告列 5 条，全部已修。
  - 代码块语言标签（XML/OOXML/DOM/PDF 共 8 处裸 fence 全部补标签）：✅
  - PDF 扫描版 JPEG/PNG 表述纠错 + OCR callout：✅
  - OOXML r: 命名空间严谨化：✅
  - XML 字节估算去具体数字：✅
- **领域进阶建议（XXE / SAX·StAX 对比 / PDF·A 等）**：审计列为"可补不算缺陷"的扩展项，非 P0/P1 缺陷，本阶段未新增（属内容增写，超出"修复"范畴）。

## 七、剩余未修
- 缺 MOC（P1）：交阶段2新建。
- 领域扩展内容（XXE 小节、SAX/StAX 对比表等推荐项）：非缺陷，未做。
- 全部 fence 标签、SSoT 去重、事实性纠错、frontmatter（原已完整四字段，无需补）已完成。
